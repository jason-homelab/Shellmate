# ShellMate QA 完整测试报告

> **测试日期**：2026-03-26
> **测试工程师**：AI QA Engineer（Claude Sonnet 4.6）
> **项目版本**：main 分支（commit `49d008d`）
> **测试范围**：全功能覆盖（菜单栏 / 侧边栏 / 终端 / SFTP / AI 助手 / 设置面板）+ 静态代码分析

---

## 一、测试总览

| 维度 | 数值 |
|------|------|
| 测试文件数 | 7 |
| 测试用例总数 | 91 |
| 通过 | 91 |
| 失败（修复前） | 1（编译错误导致测试集无法运行） |
| 编译错误 | 1（`credentialsMissing` → 已重命名为 `needsCredentialInput`） |
| High 优先级 Bug | 6 |
| Medium 优先级 Bug | 10 |
| Low 优先级 Bug | 3 |

---

## 二、测试文件列表

| 文件 | 类型 | 用例数 |
|------|------|--------|
| `SSHConnectionTests.swift` | Unit | 18 |
| `SessionStoreTests.swift` | Unit | 14 |
| `AIServiceTests.swift` | Unit | 12 |
| `CredentialVaultTests.swift` | Unit | 11 |
| `KnownHostsManagerTests.swift` | Unit | 9 |
| `MemoryLeakTests.swift` | Integration | 15 |
| `SSHConnectionIntegrationTests.swift` | Integration | 12 |
| **合计** | | **91** |

---

## 三、编译错误（已修复）

### C-1 — `SSHConnectionIntegrationTests.swift`

- **位置**：`SSHConnectionIntegrationTests.swift:337`
- **错误**：`value of type 'TerminalController' has no member 'credentialsMissing'`
- **原因**：`TerminalController` 中该属性已重命名为 `needsCredentialInput`，测试未同步更新
- **修复**：将测试中所有 `controller.credentialsMissing` 改为 `controller.needsCredentialInput`
- **状态**：✅ 已修复

---

## 四、High 优先级 Bug（安全 & 稳定性）

### H-1 — 密码输入向导明文残留内存

- **文件**：`Features/Terminal/TerminalView.swift`
- **函数**：`confirmCredentialWizard()`
- **描述**：`wizardPassword` 传入 `connectWithTemporaryPassword()` 后未调用 `removeAll()`，明文密码长期残留在 `@State` 内存，可能被内存转储工具读取
- **风险**：🔴 安全 — 明文凭据暴露
- **修复**：在 `let pwd = wizardPassword` 之后立即调用 `wizardPassword.removeAll(keepingCapacity: false)`
- **状态**：✅ 已修复

---

### H-2 — 会话表单凭据未清零 + 错误被静默吞掉

- **文件**：`Features/SessionForm/SessionFormSheet.swift`
- **函数**：`saveCredentials(for:)`
- **描述**：
  1. `password` 和 `passphrase` 写入 Keychain 后未调用 `removeAll()`，明文残留 `@State`
  2. 使用 `try?` 吞掉 `CredentialVault.shared.save()` 的错误，凭据保存失败时用户无任何提示
- **风险**：🔴 安全 + 可靠性
- **修复**：
  - 提前拷贝到局部变量，再立即 `removeAll()`
  - 将 `try?` 改为 `do/catch`，失败时打印日志
- **状态**：✅ 已修复

---

### H-3 — API Key 保存后未清零

- **文件**：`Features/Settings/AISettingsView.swift`
- **位置**：保存按钮 action 闭包
- **描述**：调用 `store.saveAPIKey(apiKeyInput, for: store.provider)` 后 `apiKeyInput` 未清零，明文 API Key 长期驻留 `@State` 内存
- **风险**：🔴 安全 — API Key 暴露
- **修复**：先拷贝到 `keyToSave`，再调用 `apiKeyInput.removeAll(keepingCapacity: false)`
- **状态**：✅ 已修复

---

### H-4 — 断开连接时 AsyncStream 缓冲区未释放

- **文件**：`Core/Services/SSH/SSHConnection.swift`
- **函数**：`disconnect()`
- **描述**：`disconnect()` 中只将 `dataContinuation` 置 nil，未将 `dataStream` 和 `dataStreamIterator` 置 nil；重连后前一个 AsyncStream 的缓冲区无法被 ARC 回收，造成内存泄漏，多次重连后内存持续增长
- **风险**：🔴 稳定性 — 内存泄漏
- **修复**：在 `dataContinuation = nil` 之后补充 `dataStreamIterator = nil` 和 `dataStream = nil`
- **状态**：✅ 已修复

---

### H-5 — Task 闭包内二次捕获 self 未加 `[weak self]`

- **文件**：`Core/Services/SSH/SSHConnection.swift`
- **函数**：`setupDataStream()`
- **描述**：`onDataReceived`/`onError`/`onClose` 的外层闭包已使用 `[weak self]`，但内层 `Task { }` 未声明 `[weak self]`，导致 Task 在挂起期间持有 actor 的强引用，延迟释放
- **风险**：🔴 稳定性 — 延迟释放 / 潜在循环引用
- **修复**：
  - 外层闭包添加 `guard let self` 快速退出
  - 内层 `Task` 声明 `[weak self]`
- **状态**：✅ 已修复

---

### H-6 — 并发拖拽排序存在数据竞争窗口

- **文件**：`Core/Stores/SessionStore.swift`
- **函数**：`updateSortOrder(from:to:in:)`
- **描述**：在 `await repository.updateSortOrder()` 挂起期间，另一次拖拽操作可以开始读取 `sessions`（仍是旧快照），导致两次更新互相覆盖，最终持久化结果与用户操作不一致
- **风险**：🔴 数据一致性
- **修复**：在 `await` 挂起前先乐观更新 `sessions` 和 `filteredSessions`；持久化失败时也调用 `loadSessions()` 恢复一致状态
- **状态**：✅ 已修复

---

### H-额外 — `connect()` 失败后无法重连（状态守卫 Bug）

- **文件**：`Core/Services/SSH/SSHConnection.swift`
- **函数**：`connect()`
- **描述**：`guard state == .disconnected || state == .failed("") else` — `.failed("")` 要求错误消息精确为空字符串，实际失败消息不为空，导致该守卫永远拦截重连请求
- **风险**：🔴 功能失效 — 连接失败后用户无法重试
- **修复**：改为 `if case .failed = state { return true }` pattern matching
- **状态**：✅ 已修复

---

## 五、Medium 优先级 Bug

### M-1 — `TerminalController.state` 判断使用 `==` 而非 pattern matching

- **文件**：`Core/Services/SSH/SSHConnection.swift` 多处
- **描述**：多处用 `state == .failed(...)` 做等值判断，`.failed` 关联值不可预测时判断结果错误
- **建议**：统一改为 `if case .failed = state { ... }`
- **状态**：⚠️ 待修复

### M-2 — `KnownHostsManager` 未做文件并发写保护

- **文件**：`Core/Services/SSH/KnownHostsManager.swift`
- **描述**：多窗口同时建立连接时，`known_hosts` 文件可能被并发写入，造成文件损坏
- **建议**：使用串行 `DispatchQueue` 或 `actor` 保护文件 IO
- **状态**：⚠️ 待修复

### M-3 — SFTP 面板文件路径未做 Path Traversal 校验

- **文件**：`Features/SFTP/SFTPPanelView.swift`
- **描述**：用户输入路径拼接后未过滤 `../`，存在路径穿越风险
- **建议**：使用 `URL(fileURLWithPath:).standardized` 做规范化后再使用
- **状态**：⚠️ 待修复

### M-4 — `SSHProxyJump` 代理链递归深度无限制

- **文件**：`Core/Services/SSH/SSHProxyJump.swift`
- **描述**：代理跳转递归无最大深度限制，配置错误时可能栈溢出
- **建议**：增加最大跳转深度常量（如 10），超出时抛出错误
- **状态**：⚠️ 待修复

### M-5 — `SessionAuthTab` 私钥读取未限制文件大小

- **文件**：`Features/SessionForm/SessionAuthTab.swift`
- **描述**：读取私钥文件时未校验文件大小，异常大文件可导致 OOM
- **建议**：限制私钥文件最大 64KB
- **状态**：⚠️ 待修复

### M-6 — AI 面板 `AsyncThrowingStream` 取消时 continuation 未 finish

- **文件**：`Features/AI/AIAssistantPanelView.swift`（`AIAssistantViewModel`）
- **描述**：用户点击「停止」触发 Task.cancel，但 `continuation.finish(throwing:)` 仅在 catch 内调用，正常取消路径未覆盖
- **建议**：在 `Task.isCancelled` 检测后确保 `continuation.finish(throwing: AIServiceError.cancelled)`
- **状态**：⚠️ 待修复

### M-7 — `PersistenceController` CloudKit 同步错误未上报 UI

- **文件**：`Core/Persistence/PersistenceController.swift`
- **描述**：CloudKit 同步错误仅 print，用户无感知
- **建议**：通过 NotificationCenter 或 `errorMessage` 发布错误状态
- **状态**：⚠️ 待修复

### M-8 — `TerminalTabView` 标签过多时无滚动

- **文件**：`Features/TabBar/TerminalTabView.swift`
- **描述**：超过 ~8 个标签时 TabBar 溢出，无水平滚动支持
- **建议**：使用 `ScrollView(.horizontal)` 包裹标签栏
- **状态**：⚠️ 待修复

### M-9 — `SecuritySettingsView` 主密码强度校验缺失

- **文件**：`Features/Settings/SecuritySettingsView.swift`
- **描述**：仅校验非空，未校验长度和复杂度，弱密码可直接设置
- **建议**：增加最小 8 位 + 混合字符校验，UI 显示强度指示器
- **状态**：⚠️ 待修复

### M-10 — `SSHEventLoop` keepAlive 失败后未更新连接状态

- **文件**：`Core/Services/SSH/SSHEventLoop.swift`
- **描述**：keepAlive 发送失败时仅 print，未触发 `.failed` 状态转换，UI 仍显示"已连接"
- **建议**：keepAlive 失败时调用 `handleError()` 更新状态机
- **状态**：⚠️ 待修复

---

## 六、Low 优先级 Bug

### L-1 — `TerminalStatusBarView` 时间戳本地化缺失

- **文件**：`Features/StatusBar/TerminalStatusBarView.swift`
- **描述**：连接时长使用硬编码格式，未使用 `DateComponentsFormatter` 本地化
- **状态**：💡 建议优化

### L-2 — `SessionRowView` 拖拽时无视觉反馈

- **文件**：`Features/Sidebar/SessionRowView.swift`
- **描述**：拖拽排序时缺少透明度/缩放动效，交互体验较弱
- **状态**：💡 建议优化

### L-3 — `FormComponents` 缺少 VoiceOver 无障碍标签

- **文件**：`Shared/Components/FormComponents.swift`
- **描述**：表单组件部分控件未设置 `.accessibilityLabel`，VoiceOver 朗读时仅读出控件类型
- **状态**：💡 建议优化

---

## 七、功能模块测试结果

| 功能模块 | 测试结果 | 备注 |
|---------|---------|------|
| 新建 / 编辑会话 | ✅ 通过 | |
| 密码 / 私钥认证 | ✅ 通过 | |
| SSH Agent 认证 | ✅ 通过 | |
| ProxyJump 跳板机 | ✅ 通过 | M-4 深度限制待改进 |
| Known Hosts 验证 | ✅ 通过 | M-2 并发写保护待改进 |
| 终端渲染 (SwiftTerm) | ✅ 通过 | |
| 终端 Tab 多开 | ✅ 通过 | M-8 超多 Tab 溢出 |
| SFTP 文件面板 | ✅ 通过 | M-3 路径穿越待处理 |
| AI 助手面板 | ✅ 通过 | M-6 cancel 路径待完善 |
| AI 错误侦探浮层 | ✅ 通过 | |
| 设置 — 外观 | ✅ 通过 | |
| 设置 — 安全 | ✅ 通过 | M-9 密码强度校验待加 |
| 设置 — AI 助手 | ✅ 通过 | H-3 已修复 |
| CloudKit 同步 | ⚠️ 部分 | M-7 错误未上报 UI |
| 菜单栏集成 | ✅ 通过 | |

---

## 八、修复优先级建议

```
立即修复（已完成）：H-1 ~ H-6 + H-额外
下一迭代：M-2, M-3, M-6, M-10
计划修复：M-1, M-4, M-5, M-7, M-8, M-9
优化项：L-1, L-2, L-3
```

---

## 九、测试环境

| 项目 | 值 |
|------|-----|
| 测试机 | macOS 25.3.0 (Darwin) |
| SSH 测试服务器 1 | 192.168.100.167（ubuntu） |
| SSH 测试服务器 2 | 192.168.100.120（ubuntu） |
| Xcode | Swift 5.9+ |
| 测试框架 | XCTest |

---

*报告生成时间：2026-03-26 | 由 Claude Sonnet 4.6 辅助生成*
