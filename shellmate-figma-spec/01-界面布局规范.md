# ShellMate — Figma 界面布局规范

> **文档版本：** v1.1
> **创建日期：** 2026-03-18
> **更新日期：** 2026-03-20（补全 D01 Tab 2/3/4 内容规格；新增 SFTP 右键菜单与拖拽 Drop Zone；D02 Randomart 渲染标注；D01 Tab 颜色合规修订；Settings 导航项完整列表）
> **用途：** 每个界面的精确布局参数，供 Figma 设计师 1:1 搭建

---

## 画布设置

所有界面 Frame 使用以下基准尺寸：

| 设备 | 尺寸 | 缩放 | 用途 |
|------|------|------|------|
| MacBook Pro 16" | 1728 × 1117 pt | @2x | 主展示 |
| MacBook Pro 14" | 1512 × 982 pt | @2x | 备选 |
| MacBook Air 13" | 1280 × 832 pt | @2x | 最小布局验证 |

**主设计稿使用 1512 × 982 pt（MacBook Pro 14" 等效）**

---

## Screen 01 — 主窗口（默认状态）

### 01-A：窗口整体框架

```
Frame 尺寸: 1200 × 760 pt（App 窗口，居中于 1512×982 画布）
窗口圆角: 10pt（macOS 窗口标准圆角）
窗口阴影: 0 24pt 64pt rgba(0,0,0,0.8)

窗口内容布局（从上到下，Vertical Auto Layout，间距 0）:
  ① TitleBar         高 52pt   ← Unified Title + Toolbar 合并
  ② TabBar           高 36pt
  ③ MainArea         flex-1    ← 水平 Auto Layout
  ④ StatusBar        高 24pt
```

### 01-B：TitleBar（Unified 样式）

```
尺寸: 1200 × 52pt
背景: Surface/Toolbar + Vibrancy titlebar 标注
边框: 下边 1pt Border/Faint

内部布局（水平，间距自适应）:
  左区域（Auto Layout，间距 8pt，左内边距 14pt）:
    ├── Traffic Lights Group（宽 56pt，高 52pt，垂直居中）
    │   ├── Close Dot    12×12pt  #FF5F57  box-shadow: 0 0 4pt rgba(255,95,87,0.5)
    │   ├── Minimize Dot 12×12pt  #FEBC2E  box-shadow: 0 0 4pt rgba(254,188,46,0.4)
    │   └── Maximize Dot 12×12pt  #28C840  box-shadow: 0 0 4pt rgba(40,200,64,0.4)
    └── Separator 1pt × 14pt Border/Subtle

  中央标题（水平居中，绝对定位于 TitleBar 中心）:
    └── Text: "ShellMate — prod-web-01"
        样式: UI/Caption/1，Text/Tertiary，不可选中

  右区域（Auto Layout，间距 2pt，右内边距 14pt）:
    ├── IconBtn/Tunnel  22×22pt  SF Symbol "arrow.left.arrow.right"
    ├── IconBtn/Command 22×22pt  SF Symbol "list.bullet.rectangle"
    ├── IconBtn/Sync    22×22pt  SF Symbol "square.grid.2x2"
    ├── Separator 1pt × 14pt Border/Subtle
    └── IconBtn/Settings 22×22pt  SF Symbol "gear"
```

### 01-C：TabBar

```
尺寸: 1200 × 36pt
背景: Surface/Toolbar（同标题栏颜色，与 Unified 合并时视觉连续）
边框: 下边 1pt Border/Faint

内部布局（水平 Auto Layout，左内边距 8pt，间距 1pt，底部对齐）:
  ├── Tab/Active: prod-web-01    约 128pt 宽
  ├── Tab/Default: prod-db-01   约 120pt 宽
  ├── Tab/Default: dev-staging  约 128pt 宽（含脉冲黄点）
  ├── Tab/New Button            28×28pt
  └── Spacer（flex-1）

Tab 组件规格（以激活态为例）:
  高度: 28pt（在 36pt TabBar 中底部对齐）
  最小宽度: 80pt，最大宽度: 160pt
  圆角: 6pt 6pt 0pt 0pt（只有顶部两角）
  背景: Surface/App（激活），Surface/Toolbar 透明度 0%（非激活）
  边框: Border/Subtle（激活时左/右/上三边），底边与 MainArea 背景色相同
  顶部指示线: 2pt，Accent/Default，只在激活态显示，圆角 1pt

  内部 Auto Layout（水平，间距 6pt，内边距 0pt 10pt）:
    ├── StatusDot    5×5pt
    ├── Label Text   flex-1，11.5pt SF Pro Regular，Text/Primary（激活）/ Text/Secondary（非激活）
    └── CloseButton  14×14pt，× 图标，默认 opacity 0，Hover 时 opacity 1
```

### 01-D：MainArea

```
尺寸: 1200 × (760 - 52 - 36 - 24) = 648pt 高
布局: 水平 Auto Layout，间距 0

├── Sidebar          220 × 648pt
├── TerminalArea     flex-1 × 648pt
└── SFTPPanel        [默认隐藏，展开时 290pt]
```

### 01-E：Sidebar 侧边栏

```
尺寸: 220 × 648pt
背景: [VIBRANCY] Surface/Sidebar ← Figma 近似，开发以系统为准
右边框: 1pt Border/Faint
布局: Vertical Auto Layout，间距 0

子区域（从上到下）:
  SearchArea      220 × 44pt
  ToolbarStrip    220 × 26pt
  SessionList     220 × flex-1（可滚动）
  FooterArea      220 × 52pt
```

**SearchArea 规格：**
```
背景: 透明（融入 Sidebar Vibrancy）
内边距: 上 7pt 下 7pt 左 10pt 右 10pt

搜索框（单行 Auto Layout）:
  尺寸: 200 × 30pt，圆角 6pt
  背景: Surface/Elevated，透明度 60%（半透明，融入 Vibrancy）
  边框: Border/Subtle，1pt
  内容:
    ├── SF Symbol "magnifyingglass" 12pt Text/Disabled，左 8pt
    ├── Placeholder Text "搜索会话…" 11.5pt Text/Disabled
    └── 清除按钮 × 14pt Text/Tertiary（有内容时显示）
```

**ToolbarStrip 规格：**
```
背景: Surface/Window 透明度 40%
下边框: 1pt Border/Faint
内边距: 左右各 8pt，垂直居中
布局: 水平 Auto Layout

按钮组（左对齐，间距 2pt）:
  IconBtn 20×20pt × 2 个（折叠全部、展开全部）

Spacer flex-1

按钮组（右对齐，间距 2pt）:
  IconBtn 20×20pt × 2 个（导入、导出）
```

**分组标题行（Section Header）规格：**
```
尺寸: 200 × 28pt（宽度 = 侧边栏宽 - 左边距 0）
背景: 透明
左内边距: 10pt，右内边距: 10pt

布局 Auto Layout 水平，间距 5pt，垂直居中:
  ├── ChevronIcon  8×8pt  SF Symbol "chevron.down"  Text/Disabled
  │   （折叠时旋转 -90°）
  ├── GroupName   flex-1  10pt SF Pro Medium  Text/Disabled  Uppercase  LS 0.08em
  ├── CountBadge  16pt 高  10pt SF Mono  Text/Disabled
  │   背景 Surface/Elevated，圆角 8pt，内边距 0pt 5pt
  └── AddButton   16×16pt  +  14pt  Text/Disabled
      （默认 opacity 0，鼠标悬停 GroupHeader 时 opacity 1）
```

**会话行（Session Row）规格：**
```
尺寸: 220 × 44pt（包含左侧选中指示线的完整宽度）
背景: 透明（Default），Surface/Overlay（Hover），Accent/Glow（Selected）

左侧激活指示线（Selected 状态独有）:
  宽 2pt，高 36pt（上下各留 4pt），圆角 0pt 2pt 2pt 0pt
  颜色: Accent/Default，阴影 0 0 6pt Accent/Default

内容区域 Auto Layout（水平，间距 8pt）:
  左内边距: 18pt（= 标准 10pt + 8pt 缩进），右内边距: 10pt

内容元素:
  ├── StatusDot    6×6pt（见状态规格）
  ├── InfoGroup    flex-1（Vertical Auto Layout，间距 1pt）
  │   ├── SessionName  12pt SF Pro Regular  Text/Primary（Selected）/ Text/Secondary（Default）
  │   └── HostLabel    10pt SF Mono  Text/Tertiary（Default）/ Text/Secondary（Hover）
  └── TagBadge     [可选，见 Tag 规格]
```

**FooterArea 规格：**
```
尺寸: 220 × 52pt
背景: 透明（Sidebar Vibrancy 延伸）
上边框: 1pt Border/Faint
内边距: 10pt 四周

新建会话按钮（New Session Button）:
  尺寸: 200 × 32pt，圆角 7pt
  背景: Accent/Dim
  边框: Accent/Border，1pt，虚线（dasharray: 4 3）
  文字: "＋ 新建会话"  12pt SF Pro Medium  Accent/Default

  Hover:
    背景: rgba(Accent, 0.22)
    边框: Accent/Default，1pt 实线
    阴影: 0 0 12pt Accent/Glow
```

### 01-F：TerminalArea 终端区域

```
尺寸: flex-1 × 648pt
布局: Vertical Auto Layout，间距 0

子区域:
  ToolbarBar      flex-w × 28pt
  TerminalPane    flex-1（含 PaneBar 28pt + Terminal content）
  QuickCmdBar     flex-w × 40pt
```

**ToolbarBar 规格：**
```
高度: 28pt
背景: Surface/Toolbar
下边框: 1pt Border/Faint
左右内边距: 10pt，垂直居中

左区域 Auto Layout（间距 0）:
  路径显示:
    ubuntu    12pt SF Mono  Terminal/Green
    @         12pt SF Mono  Text/Disabled
    prod-web-01  12pt SF Mono  Terminal/Cyan
    :         12pt SF Mono  Text/Disabled
    ~/services/api  12pt SF Mono  Terminal/Yellow

右区域 Auto Layout（间距 2pt）:
  ├── Separator 1pt × 14pt
  ├── IconBtn 分屏水平    SF Symbol "rectangle.split.2x1"
  ├── IconBtn 分屏垂直    SF Symbol "rectangle.split.1x2"
  ├── Separator 1pt × 14pt
  ├── IconBtn 搜索       SF Symbol "magnifyingglass"
  ├── IconBtn Compose    SF Symbol "text.alignleft"
  ├── IconBtn SFTP       SF Symbol "folder.fill"
  ├── Separator 1pt × 14pt
  └── RecordButton       7×7pt 红色圆点（带发光）
```

**PaneBar 规格：**
```
高度: 28pt
背景: Surface/Panel
下边框: 1pt Border/Faint

路径文字（同 ToolbarBar 路径样式，更小一号: 11pt SF Mono）

右侧图标（小型 18×18pt）:
  ⊟ 上下分屏  ⊞ 左右分屏  ⊗ 关闭窗格（多窗格时才显示）
```

**Terminal Content 规格：**
```
背景: Terminal/Background（纯黑，用户可自定义）
内边距: 12pt 上下，14pt 左右
字体: Terminal/Default（SF Mono 13pt，行高 22pt）

键盘输入模式:
  用户直接在终端区域输入命令（无独立输入框）
  点击终端区域获取焦点，焦点时显示块状光标
  所有按键通过 SSH 通道发送至服务器
  详见 07-终端覆层规范 §15

终端内容排版:
  Prompt 格式:
    ubuntu  SF Mono 500  Terminal/Green
    @       SF Mono 400  Text/Disabled
    prod-web-01  SF Mono 400  Terminal/Cyan
    :       SF Mono 400  Text/Disabled
    ~/path  SF Mono 400  Terminal/Yellow
    $[空格] SF Mono 400  Text/Secondary

  Command 文字: SF Mono 400  Terminal/White

  Output 文字: SF Mono 400  Terminal/Dim（普通）
  Output 错误行:
    背景: rgba(Status/Error, 0.07)
    左边框: 2pt  Status/Error
    左内边距: 8pt（额外）
    文字: Status/Error 透明度 85%

  关键词高亮（ERROR）:
    背景: rgba(Status/Error, 0.2)
    圆角: 3pt
    文字: Status/Error  SF Mono 600

  闪烁光标:
    7×14pt 矩形，圆角 1pt
    背景: Terminal/Cursor
    动画标注: opacity 0↔1，1.1s step-end infinite

垂直滚动条:
  宽: 4pt
  圆角: 2pt
  颜色: Border/Default（hover 时 Border/Strong）
  右边距: 4pt
  仅 hover 内容区时显示
```

**QuickCmdBar 规格：**
```
高度: 40pt（展开），4pt（折叠）
背景: Surface/Toolbar
上边框: 1pt Border/Faint
左右内边距: 8pt，垂直居中

命令按钮（Quick Command Chip）:
  高度: 28pt，左右内边距: 11pt，圆角 5pt
  背景: Surface/Elevated
  边框: Border/Subtle，1pt
  文字: 11pt SF Pro Regular  Text/Secondary

  Hover:
    背景: Accent/Dim
    边框: Accent/Border
    文字: Accent/Default

按钮间距: 5pt
右侧:
  折叠箭头 ⌃ 14pt  Text/Disabled
  编辑按钮  SF Symbol "pencil"  11pt  Text/Tertiary + 边框
```

### 01-G：StatusBar 状态栏

```
尺寸: 1200 × 24pt
背景: Surface/Toolbar + bar material 标注
上边框: 1pt Border/Faint
左右内边距: 12pt

布局: 水平 Auto Layout，间距 0，垂直居中

左侧区域（间距 1pt）:
  ConnStatus 组（Auto Layout 水平，间距 5pt，左内边距 6pt，右内边距 6pt）:
    ├── StatusDot 5×5pt  Status/Connected
    └── "已连接" 10.5pt SF Mono 500  Status/Connected
  Divider 1pt × 12pt  Border/Subtle
  Item "SSH-2.0" 10.5pt SF Mono  Text/Tertiary，内边距 6pt
  Divider
  Item "AES256-CTR" 10.5pt SF Mono  Text/Tertiary，内边距 6pt
    [标注: 悬停展示 Tooltip 显示完整算法套件]
  Divider
  Item "ubuntu" 10.5pt SF Mono  Text/Tertiary，内边距 6pt
  Divider
  Item "UTF-8" 10.5pt SF Mono  Text/Tertiary，内边距 6pt  [可点击]
  Divider
  Item ":22" 10.5pt SF Mono  Text/Tertiary，内边距 6pt
  Divider
  Item "6h 14m" 10.5pt SF Mono  Text/Tertiary，内边距 6pt

Spacer flex-1

右侧区域:
  Item "⌁ 11ms" 10.5pt SF Mono  Terminal/Cyan，内边距 6pt
  Divider
  Item "120×38" 10.5pt SF Mono  Text/Tertiary，内边距 6pt
  Divider
  Item "✓" 11pt SF Mono  Status/Connected，内边距 6pt
    [标注: 悬停展示 iCloud 同步 Tooltip]
```

---

## Screen 02 — 主窗口（分屏状态）

### 02-A：左右分屏

```
TerminalArea 内的 PaneSplit 变为:
  布局: 水平 Auto Layout，间距 0

  ├── Pane1   flex-1 × 100%（左侧）
  ├── PaneDivider   4pt 宽  背景 Surface/Toolbar  cursor: col-resize
  │   Hover: 背景 Accent/Default 透明度 30%
  └── Pane2   flex-1 × 100%（右侧）
      PaneBar 路径显示不同服务器
```

---

## Screen 03 — 主窗口（SFTP 打开状态）

### 03-A：SFTP Panel

```
尺寸: 290 × 648pt
背景: Surface/Window（略区别于 Sidebar，用 Vibrancy 标注）
左边框: 1pt Border/Faint
布局: Vertical Auto Layout，间距 0

子区域:
  SFTPHeader      290 × 28pt
  SFTPNav         290 × 30pt
  SFTPColumnHeader 290 × 24pt
  SFTPFileList    290 × flex-1
  SFTPActions     290 × 36pt
  TransferQueue   290 × [折叠 36pt / 展开 auto]
```

**SFTPHeader：**
```
背景: Surface/Toolbar
下边框: 1pt Border/Faint
左内边距: 8pt，右内边距: 8pt

Left: SFTP 图标 + "SFTP — prod-web-01"  11pt SF Pro Medium  Text/Tertiary
Right: 关闭按钮 × 18×18pt
```

**SFTPNav（路径导航）：**
```
背景: Surface/Panel
下边框: 1pt Border/Faint
内边距: 左右 6pt，垂直居中

布局（水平，间距 2pt）:
  ‹ 按钮 20×20pt（无历史时 Text/Disabled 不可点击）
  › 按钮 20×20pt
  ↑ 按钮 20×20pt
  
  面包屑路径（水平，间距 1pt，flex-1，overflow hidden）:
    "home" 10.5pt SF Mono  Text/Tertiary  cursor pointer，hover 背景 Surface/Elevated
    "/"    10.5pt SF Mono  Text/Disabled
    "ubuntu" 10.5pt SF Mono  Text/Tertiary  同上
    "/"    10.5pt SF Mono  Text/Disabled
    "api"  10.5pt SF Mono  Text/Primary  ← 当前目录高亮
  
  刷新按钮 20×20pt  SF Symbol "arrow.clockwise"  Text/Disabled
```

**SFTPColumnHeader：**
```
背景: Surface/Panel
下边框: 1pt Border/Faint
内边距: 左 8pt 右 8pt

布局（水平）:
  "名称" flex-1  10pt SF Pro Medium  Text/Disabled  Uppercase  LS 0.04em  cursor pointer
  "大小" 52pt    10pt SF Pro Medium  Text/Disabled  Uppercase  右对齐   cursor pointer
  "修改时间" 70pt 10pt SF Pro Medium  Text/Disabled  Uppercase  右对齐   cursor pointer
```

**SFTP 文件行（SFTPFileRow）：**
```
尺寸: 290 × 30pt
下边框: 1pt rgba(255,255,255,0.015)
内边距: 左 8pt 右 8pt

布局（水平，间距 7pt，垂直居中）:
  FileIcon  14×14pt  SVG 图标（类型颜色见下方）
  FileName  flex-1  11.5pt SF Mono  Text/Secondary（普通）
                                    Text/Primary（选中）
                                    Text/Disabled + italic（隐藏文件）
  FileSize  52pt   10.5pt SF Mono  Text/Disabled  右对齐
  FileDate  70pt   10.5pt          Text/Disabled  右对齐

状态:
  Default:  背景透明
  Hover:    背景 Surface/Toolbar
  Selected: 背景 Accent/Glow

文件图标颜色（与 SF Symbol 图标类型对应）:
  目录:  Terminal/Cyan
  .js .ts .jsx:  Terminal/Yellow
  .json .yaml:   Terminal/Green
  .md .txt:      Terminal/Blue
  .zip .gz .tar: Terminal/Amber（Amber/400）
  .png .jpg .svg: Terminal/Purple
  .env（隐藏）: Text/Disabled
  其他:  Text/Tertiary
```

**SFTP 文件右键菜单（ContextMenu）：**
```
触发方式: 在 SFTPFileRow 上右键单击 / Control + 单击

菜单尺寸: 宽 200pt，圆角 8pt
背景: Surface/Elevated + Vibrancy（NSMenu 系统材质）
阴影: 0 8pt 24pt rgba(0,0,0,0.5)

菜单项（按顺序）:
  ↓ 下载到本地          图标 arrow.down.to.line  11pt  Text/Primary
  ─────────────────── Divider ───────────────────
  重命名…               图标 pencil               11pt  Text/Primary
  复制路径              图标 doc.on.clipboard      11pt  Text/Primary
  新建文件夹…           图标 folder.badge.plus     11pt  Text/Primary
  ─────────────────── Divider ───────────────────
  属性                  图标 info.circle           11pt  Text/Primary
  ─────────────────── Divider ───────────────────
  删除                  图标 trash                 11pt  Status/Error（Destructive）

菜单项规格:
  高度: 26pt
  内边距: 左 12pt 右 12pt
  图标尺寸: 12pt  右间距 8pt
  字体: 12pt SF Pro Regular

  Hover 态: 背景 Accent/Default  文字 White（系统默认 NSMenu 高亮）
  Disabled 态: Text/Disabled（如目录不支持重命名时）

多选状态（选中多个文件时）:
  "下载 3 个文件"（显示数量）
  重命名… → 禁用（Disabled）
  删除 3 个文件（Destructive）

⚠️ Figma 标注: 使用 SwiftUI .contextMenu {} 修饰符实现
```

**SFTP 拖拽上传 Drop Zone 状态：**
```
触发条件: 用户从本地 Finder 拖入文件/目录至 SFTPFileList 区域

Drop Zone 覆盖层（覆盖整个 SFTPFileList 区域）:
  背景: rgba(Accent/Default, 0.12)
  边框: 2pt Accent/Default 虚线（dasharray: 6 4）圆角 8pt
  中央提示区（垂直居中）:
    图标: SF Symbol「arrow.up.doc.fill」36pt  Accent/Default
    文字: 「拖放文件上传到此目录」  14pt SF Pro Medium  Accent/Default
    副文字: 「当前路径：/home/ubuntu/api」  11pt SF Mono  Text/Secondary

无效 Drop 状态（拖入不支持的内容，如应用程序）:
  背景: rgba(Status/Error, 0.08)
  边框: 2pt Status/Error 虚线
  图标: SF Symbol「xmark.circle.fill」36pt  Status/Error
  文字: 「不支持上传此类型文件」  14pt SF Pro Medium  Status/Error

Drop 完成（文件松开后 0.3s 内）:
  覆盖层以 opacity 0 淡出（100ms ease-out）
  文件出现在列表中并高亮选中（背景 Accent/Glow）
  同时在 TransferQueue 添加上传任务项

⚠️ Figma 标注: NSDropOperation，SwiftUI .onDrop(of:delegate:)
  Drop Zone 使用 .dropDestination(for: URL.self) 接收 URL 数组
```

**SFTPActions（操作工具栏）：**
```
高度: 36pt
背景: Surface/Toolbar
上边框: 1pt Border/Faint
内边距: 左右 8pt

按钮组（Auto Layout，间距 4pt）:
  每个按钮: 高 26pt，左右内边距 9pt，圆角 5pt
  背景: Surface/Elevated，边框: Border/Subtle
  内容: SF Symbol 11pt + 文字 11pt，间距 4pt
  颜色: Text/Secondary

  Hover: 背景 Accent/Dim，边框 Accent/Border，颜色 Accent/Default
  Active（隐藏文件开关）: 同 Hover
```

**TransferQueue：**
```
折叠态（36pt 高）:
  背景: Surface/Window
  上边框: 1pt Border/Faint
  标题行: "↔ 传输队列" 10.5pt Uppercase  Text/Disabled + 数量徽章 + 展开箭头

展开态（自动高度）:
  每个传输项高度约 52pt:
    行信息（水平）: 上下箭头指示 + 文件名（12pt SF Mono）+ 速度（10pt Text/Disabled）+ 暂停按钮
    进度条（高 3pt）: 背景 Surface/Overlay，填充色 Accent/Default，圆角 2pt
    填充动画: shimmer（亮光从左到右扫过），参见原型 CSS

  已完成项: ✓ 绿色 + 文件名 + 大小/时间  Text/Disabled
```

---

## Screen 04 — 弹窗：新建/编辑会话（D01）

```
位置: 从窗口标题栏顶部滑下（Sheet 动画）
尺寸: 540 × auto（最小 480pt）
圆角: 10pt（四角）
背景: Surface/Elevated
边框: Border/Default，1pt
阴影: 0 12pt 40pt rgba(0,0,0,0.65)

距窗口顶部: 52pt（紧接 Unified TitleBar 下方）
水平居中于窗口
```

**Modal Header：**
```
高度: 52pt
内边距: 左 18pt 右 14pt
背景: Surface/Overlay
下边框: 1pt Border/Faint

标题: "新建 SSH 会话"  13pt SF Pro Medium  Text/Primary

关闭按钮（右对齐）: ×  14pt  22×22pt  圆角 4pt
  Default: Text/Disabled
  Hover:   背景 Surface/Overlay，Text/Secondary
```

**Modal Tab Bar：**
```
高度: 36pt
背景: Surface/Panel
下边框: 1pt Border/Faint
内边距: 左右 18pt

Tab 项（Segmented 控制，自动均分宽度）:
  "基本连接" / "认证" / "高级" / "外观"
  激活: 12pt SF Pro Medium  Text/Primary，底部 2pt Accent/Default
  非激活: 12pt SF Pro Regular  Text/Secondary（⚠️ 可点击元素不得使用 Text/Tertiary，对比度不足）
  Hover: Text/Primary
```

**Form Body（Tab 1: 基本连接）：**
```
内边距: 18pt 四周
间距: 14pt（表单组之间）

会话名称 FormGroup:
  Label: "会话名称（可选）"  11pt SF Pro Medium  Text/Tertiary  Uppercase LS 0.04em
  Input: 高 28pt，宽 504pt（满宽）

主机地址 + 端口（水平分割，3:1 比例）:
  主机地址 Input: 宽 380pt
  端口 Input: 宽 108pt
  间距: 10pt

用户名 + 分组（水平分割，各半）:
  各 247pt，间距 10pt

备注 Input: 高 28pt，满宽

错误状态说明文字: 11pt  Status/Error  紧随对应 Input 下方 4pt
```

**Form Body（Tab 2: 认证）：**
```
内边距: 18pt 四周
间距: 14pt

认证方式选择（AuthMethodCard × 3，水平排列，见 08-缺失组件补充.md §三）:
  SSH Key / 密码 / SSH Agent
  各卡片宽约 154pt × 70pt，间距 10pt

─── 认证详情区（根据选中卡片动态切换内容）─────────────────────────

【SSH Key 模式】:
  私钥文件 FormGroup:
    Label: "私钥文件路径"  11pt SF Pro Medium  Text/Tertiary  Uppercase LS 0.04em
    输入行（水平，间距 8pt）:
      Input: flex-1  高 28pt  占位符 "~/.ssh/id_ed25519"
      [浏览…]: Button/Bordered 高 28pt  11pt
    副说明: 9.5pt  Text/Disabled  "支持 Ed25519、RSA、ECDSA 格式"

  Passphrase FormGroup:
    Label: "私钥密码（Passphrase）"
    Input: 高 28pt  满宽  密码类型  占位符 "留空表示无 Passphrase"
    Toggle + Label: "☑ 使用 macOS Keychain 记住密码"  11pt  Text/Secondary

【密码模式】:
  密码 FormGroup:
    Label: "SSH 密码"
    Input: 高 28pt  满宽  密码类型
    Toggle + Label: "☑ 使用 macOS Keychain 记住密码"  11pt  Text/Secondary

【SSH Agent 模式】:
  状态行（水平，垂直居中，间距 8pt）:
    状态图标: SF Symbol "checkmark.circle.fill" 14pt
      Agent 可用: Status/Connected
      Agent 不可用: Status/Error
    状态文字: 11pt  Text/Secondary
      可用: "SSH Agent 已连接（SSH_AUTH_SOCK）"
      不可用: "未检测到 SSH Agent，请确认 ssh-agent 正在运行"
  提示条（Agent 不可用时显示）:
    背景: rgba(Amber/400, 0.08)  边框: rgba(Amber/400, 0.2)  圆角 6pt  内边距 10pt 12pt
    文字: "Direct 版支持 SSH Agent，App Store 版受沙盒限制"  11pt  Amber/400

⚠️ Figma 标注: SwiftUI 中用 @State var authMethod: AuthMethod 驱动条件渲染
```

**Form Body（Tab 3: 高级）：**
```
内边距: 18pt 四周
间距: 14pt

─── 跳板机（ProxyJump）───────────────────────────────────────────────
Section 标题: "跳板机（ProxyJump）"  11pt SF Pro Medium  Text/Tertiary  Uppercase LS 0.04em

Input（满宽，高 28pt）:
  占位符: "user@jump-host.example.com:22"
副说明: 9.5pt  Text/Disabled  "多跳板机用英文逗号分隔，如 user@host1,user@host2"

─── 自动重连 ─────────────────────────────────────────────────────────
Toggle + Label: "☑ 断连后自动重连"  12pt  Text/Secondary

子选项容器（Toggle 开启时激活，未开启时 opacity 0.4 禁用）:
  内边距: 左 14pt（缩进）  上 6pt  间距 8pt

  重试次数行（水平，间距 10pt，垂直居中）:
    Label "最大重试次数": 120pt  11pt  Text/Secondary
    NumberInput: 高 26pt  宽 52pt  范围 1–20  默认值 3
    Unit "次": 9.5pt  Text/Disabled

  重试间隔行（水平，间距 10pt，垂直居中）:
    Label "重试间隔": 120pt  11pt  Text/Secondary
    NumberInput: 高 26pt  宽 52pt  范围 1–60  默认值 5
    Unit "秒": 9.5pt  Text/Disabled

─── Keep-Alive ────────────────────────────────────────────────────────
Toggle + Label: "☑ 发送 Keep-Alive 心跳包"  12pt  Text/Secondary

子选项（缩进，间距 8pt）:
  间隔行（水平，间距 10pt，垂直居中）:
    Label "心跳间隔": 120pt  11pt  Text/Secondary
    NumberInput: 高 26pt  宽 52pt  范围 10–300  默认值 60
    Unit "秒": 9.5pt  Text/Disabled

─── 连接超时 ──────────────────────────────────────────────────────────
连接超时行（水平，间距 10pt，垂直居中）:
  Label "连接超时": 120pt  11pt  Text/Secondary
  NumberInput: 高 26pt  宽 52pt  范围 5–120  默认值 30
  Unit "秒": 9.5pt  Text/Disabled

─── 环境变量（EnvVars）──────────────────────────────────────────────
Section 标题行（水平）:
  标题: "环境变量"  11pt SF Pro Medium  Text/Tertiary  Uppercase LS 0.04em
  Spacer flex-1
  [+ 添加]: Button/Bordered 高 22pt  10pt SF Pro Regular

变量列表（Vertical Auto Layout，间距 4pt，margin-top 8pt）:
  每行（EnvVarRow，水平，间距 6pt，垂直居中）:
    Name Input: flex-1  高 26pt  占位符 "KEY"  SF Mono
    Label "=": 10pt  Text/Disabled
    Value Input: flex-1 × 2  高 26pt  占位符 "value"  SF Mono
    [×] 删除按钮: IconBtn 22×22pt  SF Symbol "xmark" 10pt  Text/Disabled  Hover: Status/Error

  空状态: 11pt  Text/Disabled  居中  "暂无环境变量"

⚠️ Figma 标注:
  自动重连开关 → SessionAdvancedConfig.autoReconnect: Bool
  Keep-Alive 间隔 → SSHSessionConfig.keepAliveInterval: Int
  ProxyJump → SSHSessionConfig.proxyJump: String?
```

**Form Body（Tab 4: 外观）：**
```
内边距: 18pt 四周
间距: 14pt

─── 颜色主题覆盖 ─────────────────────────────────────────────────────
Toggle + Label: "☐ 覆盖全局颜色主题"  12pt  Text/Secondary
子选项（缩进 14pt，Toggle 开启时激活）:
  Picker "跟随全局设置 ▼"  高 28pt  满宽（减去缩进）
    选项: 跟随全局设置 / ShellMate Dark / ShellMate Light / Solarized Dark / ...（同 S02 主题列表）

─── 字号覆盖 ──────────────────────────────────────────────────────────
Toggle + Label: "☐ 覆盖全局字号"  12pt  Text/Secondary
子选项（缩进 14pt，Toggle 开启时激活，水平，间距 10pt）:
  NumberInput: 高 26pt  宽 52pt  范围 8–32  默认 13
  Unit "pt": 9.5pt  Text/Disabled

─── 启动命令 ──────────────────────────────────────────────────────────
Label: "启动命令（可选）"  11pt SF Pro Medium  Text/Tertiary  Uppercase LS 0.04em
Input: 高 28pt  满宽
  占位符: "如: screen -r main 或 tmux attach -t main"
副说明: 9.5pt  Text/Disabled  "连接成功后自动发送此命令"

─── 环境标签 ──────────────────────────────────────────────────────────
Label: "环境标签"  11pt SF Pro Medium  Text/Tertiary  Uppercase LS 0.04em
预设徽章选择行（水平，间距 6pt，margin-top 6pt）:
  每个徽章（SelectableBadge）: 高 22pt  左右内边距 8pt  圆角 4pt
    "无标签"  背景 Surface/Elevated   边框 Border/Subtle
    "PROD"    背景 rgba(红, 0.12)     边框 rgba(红, 0.3)    文字 #FF6B6B
    "DEV"     背景 rgba(蓝, 0.12)     边框 rgba(蓝, 0.3)    文字 #7AB4F5
    "STAGING" 背景 rgba(橙, 0.12)     边框 rgba(橙, 0.3)    文字 #FFB347
    "TEST"    背景 rgba(紫, 0.12)     边框 rgba(紫, 0.3)    文字 #C88AF0
    "自定义"  背景 Surface/Elevated   边框 Border/Subtle  + ColorSwatch 16×16pt

  选中态: 外侧发光环 0 0 0 2pt Accent/Default 透明度 40%

⚠️ Figma 标注: 外观覆盖选项存储于 Session Core Data Entity 的 appearance 相关字段
```

**Modal Footer：**
```
高度: 56pt
背景: Surface/Panel
上边框: 1pt Border/Faint
内边距: 左右 18pt

布局（水平，右对齐，间距 8pt）:
  取消按钮: Bordered  高 28pt
  测试连接按钮: Bordered  高 28pt  [3 种状态：Default / Loading / Success / Error]
  保存并连接按钮: Bordered Prominent  高 28pt
```

---

## Screen 05 — 弹窗：首次连接指纹（D02）

```
类型: Alert（模态，居中于窗口）
尺寸: 420 × auto
背景: Surface/Elevated
标题栏背景: rgba(Amber/400, 0.1)
标题栏边框: rgba(Amber/400, 0.2)
```

**关键元素规格：**

```
警告图标标题:
  SF Symbol "exclamationmark.triangle.fill" 16pt  Amber/400
  "验证服务器身份" 13pt SF Pro Medium  Amber/400

指纹显示框:
  背景: Surface/Panel，边框: Border/Default，圆角 5pt，内边距 8pt 12pt
  内容: 指纹 SHA256 字符串  11.5pt SF Mono  Terminal/Cyan

指纹艺术图（Random Art）:
  尺寸: 136 × 64pt（17列 × 8行，每格 8×8pt，间距 2pt）
  每格: 圆角 1pt，颜色从调色板随机选取
  在 Figma 中绘制示例图，标注"由代码生成，每台服务器唯一"

  ⚠️ SwiftUI 渲染实现说明（供开发参考，非 Figma 绘制范围）:
    使用 Canvas / SwiftUI Grid 渲染，不使用 Image
    字符映射调色板（8 色，对应 SSH Randomart 字符集 .o+=*BOX@#%）:
      ' '（空）→ Terminal/Background 透明
      '.'     → rgba(Terminal/Dim, 0.3)
      'o'     → Terminal/Cyan 透明度 50%
      '+'     → Terminal/Blue
      '='     → Terminal/Green
      '*'     → Terminal/Yellow
      'B'     → Terminal/Amber
      'O'     → Terminal/Red
      'X'     → Terminal/Purple
      '@'     → Text/Primary（高亮峰值）
    'S'（起点）→ Accent/Default 带 2pt 白色描边
    'E'（终点）→ Status/Connected 带 2pt 白色描边

警告条:
  背景: rgba(Amber/400, 0.1)，边框: rgba(Amber/400, 0.2)，圆角 6pt
  图标: SF Symbol "exclamationmark.triangle.fill" 12pt  Amber/400
  文字: 11.5pt  Amber/400

Footer 按钮（右对齐，间距 8pt）:
  "拒绝"         Bordered
  "仅本次接受"    Bordered
  "接受并记住"    Bordered Prominent
```

---

## Screen 06 — 弹窗：密钥变更警告（D03）

```
类型: Alert（强制模态，居中，无背景点击关闭）
尺寸: 420 × auto
边框: 2pt  rgba(Status/Error, 0.5)  外轮廓（非 inner border）
阴影: 0 24pt 60pt rgba(0,0,0,0.8)
      + 0 0 0 1pt rgba(Status/Error, 0.2) 内外轮廓

标题栏背景: rgba(Status/Error, 0.1)
标题栏边框: rgba(Status/Error, 0.2)

标题图标: SF Symbol "exclamationmark.octagon.fill" 16pt  Status/Error
标题文字: "安全警告：服务器指纹已更改" 13pt SF Pro Medium  Status/Error

指纹对比框:
  背景 Surface/Panel，内边距 10pt 12pt
  "已记录的指纹:" 11pt  Text/Tertiary
  SHA256 旧值   11pt SF Mono  Status/Connected（绿色，表示「曾经信任」）
  "当前服务器指纹:" 11pt  Text/Tertiary  margin-top 10pt
  SHA256 新值   11pt SF Mono  Status/Error（红色，表示「不受信任」）

建议警告条:
  背景: rgba(Status/Error, 0.07)，边框: rgba(Status/Error, 0.2)
  文字: 11.5pt  Status/Error

Footer 按钮:
  "取消连接"      Destructive（红色）← 左对齐（强调这是推荐操作）
  "更新并继续"    Bordered            ← 右对齐
```

---

## Screen 07 — 设置界面

```
类型: 独立 Settings 窗口（⌘, 打开）
尺寸: 640 × 520pt
圆角: 10pt
不可调整大小
```

**Settings 布局：**
```
布局（水平，间距 0）:
  ├── SettingsNav  160 × 520pt
  └── SettingsContent  480 × 520pt

SettingsNav:
  背景: Surface/Sidebar + Vibrancy 标注
  右边框: 1pt Border/Faint
  内边距: 10pt 0

  导航项列表（从上到下，共 5 项）:
    外观    图标 paintbrush.fill     → 详见 06-设置面板规范.md §S02
    关键词高亮  图标 highlighter     → 详见 06-设置面板规范.md §S01
    安全    图标 lock.shield.fill   → 详见 06-设置面板规范.md §S03
    终端    图标 terminal.fill      → 详见 06-设置面板规范.md §S04（新增）
    iCloud 同步  图标 icloud.fill   → 详见 06-设置面板规范.md §S05（新增）

  导航项 (160 × 32pt):
    未激活: 12pt SF Pro Regular  Text/Secondary，背景透明
    激活:   12pt SF Pro Regular  Text/Primary，背景 Accent/Dim，
             左侧 2pt Accent/Default 指示线，圆角 0pt 4pt 4pt 0pt（右侧两角）
    图标:   SF Symbol 14pt，左内边距 14pt，间距 8pt

SettingsContent:
  滚动区域（不限高度）
  内边距: 16pt 18pt
  间距: 16pt（各 Section 之间）

Section 结构:
  Section 标题: 12pt SF Pro Medium  Text/Primary  margin-bottom 10pt
  Section 内容: 各组件按规范排列
  Section 分割线: 1pt Border/Faint  margin 14pt 0
```

**主题预览缩略图（Appearance 设置中）：**
```
尺寸: 72 × 56pt，圆角 6pt，边框 1pt Border/Subtle
激活/选中态: 边框 2pt Accent/Default，外轮廓阴影 0 0 0 1pt Accent/Default

预览内容（缩略图内，8pt SF Mono）:
  背景色 = 对应主题终端背景
  显示 2 行 prompt + 输出（用对应主题颜色）

主题名标签: 9.5pt  Text/Tertiary  底部，背景 Surface/Panel，内边距 4pt，圆角 0 0 6pt 6pt
```

---

## Screen 02-B — 主窗口（上下分屏）

### 02-B：上下分屏

```
TerminalArea 内的 PaneSplit 变为:
  布局: 垂直 Auto Layout，间距 0

  ├── Pane1   100% × flex-1（上方）
  │     PaneBar：高 28pt（同默认状态）
  │     TerminalContent：flex-1
  ├── PaneDivider（水平）  4pt 高  背景 Surface/Toolbar  cursor: row-resize
  │   Hover: 背景 Accent/Default 透明度 30%
  │
  └── Pane2   100% × flex-1（下方）
        PaneBar：高 28pt，路径显示不同服务器
        TerminalContent：flex-1

注: 上不设 QuickCmdBar，QuickCmdBar 仅属于最下方 Pane
    PaneBar 右侧「⊗ 关闭窗格」按钮在多窗格时显示
```

---

## Screen 02-C — 主窗口（四格分屏）

### 02-C：四格分屏（2×2）

```
TerminalArea 内容替换为:
  布局: 垂直 Auto Layout，间距 0

  ├── TopRow（水平 Auto Layout，间距 0）  100% × flex-1
  │     ├── Pane1   flex-1 × 100%（左上）
  │     ├── PaneDivider-V   4pt × 100%  col-resize
  │     └── Pane2   flex-1 × 100%（右上）
  │
  ├── PaneDivider-H   100% × 4pt  row-resize
  │
  └── BottomRow（水平 Auto Layout，间距 0）  100% × flex-1
        ├── Pane3   flex-1 × 100%（左下）
        ├── PaneDivider-V   4pt × 100%  col-resize
        └── Pane4   flex-1 × 100%（右下）

备注:
  四格分屏时每个 Pane 的 PaneBar 保留
  QuickCmdBar 不显示（空间不足）
  各 Pane 均显示 ⊗ 关闭按钮
  Figma 中此 Frame 命名: Main Window / Split 2x2
```

---

## Screen 05-A — 主窗口（侧边栏折叠）

### 05-A：Sidebar 折叠状态

```
触发: ⌘⌥S，或拖拽 Sidebar 右边缘至 0
动画: 见 03-动效规范（Sidebar 滑出退场）

折叠后 MainArea 布局:
  ├── SidebarCollapsed（16pt 宽，替代 220pt Sidebar）
  │     背景: Surface/Sidebar + Vibrancy 标注
  │     右边框: 1pt Border/Faint
  │
  │     内容（垂直居中）:
  │       展开按钮（IconBtn 22×22pt）:
  │         SF Symbol「sidebar.left」14pt  Text/Tertiary
  │         Tooltip: 「显示侧边栏 ⌘⌥S」
  │
  ├── TerminalArea  flex-1（占据原 Sidebar 的空间）
  └── SFTPPanel  [可选，290pt]

⚠️ Figma:
  创建 Frame: Main Window / Sidebar Collapsed（1200×760pt）
  SidebarCollapsed 宽 16pt（替代 220pt，MainArea 水平分布重新计算）
  可拖拽 Sidebar 右边缘展开（cursor: col-resize）

全屏模式下的 Sidebar（额外状态）:
  侧边栏默认收起（宽 0，不占位），鼠标移到屏幕左边缘时：
    Sidebar 以 Overlay 形式滑出（不压缩终端区，而是覆盖）
    背景: Surface/Sidebar + Vibrancy，加一层 Elevation 2 阴影
    Figma 中用绝对定位 Frame 覆盖在 TerminalArea 左侧
```

---

## Screen 10 — 主窗口（Compose Pane 展开）

### 10-A：主窗口集成 Compose Pane 状态

```
触发: 工具栏 SF Symbol「text.alignleft」按钮

TerminalArea 内 TerminalPane 结构变为（Vertical Auto Layout）:
  ToolbarBar     flex-w × 28pt（不变）
  TerminalContent  flex-1（高度缩减，为 Compose Pane 让出空间）
  ComposePaneDivider  flex-w × 4pt（拖拽调整线，同 07-终端覆层规范）
  ComposePane    flex-w × 120pt（默认高度，可拖拽调整）
  QuickCmdBar    flex-w × 40pt（不变，仍在最底部）

Figma 创建步骤:
  1. 以 Screen 01 为基础 Duplicate
  2. 在 TerminalPane 的 Vertical Auto Layout 中，
     于 TerminalContent 和 QuickCmdBar 之间插入:
       - ResizeHandle Frame（4pt 高）
       - ComposePane Frame（参考 07-终端覆层规范 §13-B 至 §13-E）
  3. TerminalContent 高度调整（删除 fixed height，改为 flex-1）
  4. Frame 命名: Main Window / Compose Pane Open
```

---

## Screen 11 — 主窗口（同步输入激活）

### 11-A：同步输入激活主窗口

```
触发: O03 同步输入确认弹窗 → 点击「确认开启同步」

基于 Screen 01 或 Screen 02（分屏态）创建：

视觉变化（详细规格见 07-终端覆层规范 §14-C）:

① 参与窗格橙色边框:
   TerminalPane 外叠加 2pt Amber/400 内轮廓 Frame
   Figma: 每个参与 Pane 叠加一个 Overlay Frame:
     尺寸与 TerminalPane 相同，无填充
     Stroke: 2pt  Amber/400  内轮廓（Inside）

② 标签指示图标:
   激活同步的 Tab 内，SessionName 左侧插入:
     SF Symbol「bolt.fill」7pt  Amber/400

③ 工具栏图标激活:
   「square.grid.2x2」→「square.grid.2x2.fill」
   颜色: Amber/400（非 Accent）
   背景: rgba(Amber/400, 0.14)  圆角 4pt

④ 状态栏添加同步提示（状态栏最左侧，连接状态之前）:
   「⚡ 同步 (2)」  10.5pt SF Mono  Amber/400
   背景: rgba(Amber/400, 0.08)  左右 6pt 内边距  圆角 3pt
   右侧 1pt Border/Subtle 分割线

Frame 命名: Main Window / Sync Input Active (2 sessions)
```

---

## 注释规范

在每个 Figma Frame 右侧或底部添加以下注释块：

```
┌─────────────────────────────────────────────┐
│  📌 开发标注                                 │
│  SwiftUI 组件: NavigationSplitView          │
│  关键修饰符: .listStyle(.sidebar)           │
│  颜色映射: Surface/Sidebar → Vibrancy       │
│  动画: 折叠展开 0.2s ease-in-out            │
│  Keychain: 是/否                            │
│  VoiceOver: accessibilityLabel 已标注        │
└─────────────────────────────────────────────┘
```

---

*文档版本：v1.0 · 2026-03-18*
