# ShellMate 项目 Review 报告 — M2 阶段

> **阶段：** M2 — SSH 核心（W6–W9）
> **生成时间：** 2026-03-20 17:35
> **审查范围：** W6–W9（2026-04-21 → 2026-05-16）
> **总体评级：** 部分通过 ⚠️

---

## 1. 执行摘要

M2 阶段目标为完成 SSH 连接核心（libssh2 桥接、认证、终端渲染、自动重连、跳板机），是 ShellMate 最核心的技术里程碑。从代码层面看，W6–W9 的绝大多数任务已实现：`LibSSH2Bridge` + `SSHAuthService` + `SSHConnection` 三层架构完整；密码/Ed25519/Agent 三种认证均有实现；SwiftTerm 终端渲染管线（PTY → SSH poll loop → feed）已串联；自动重连指数退避状态机（3次，1/2/4s）已实现；跳板机单跳与多跳架构已落地（2026-03-19）。

然而存在一个关键 P0 缺口：**W7.7（D02/D03 弹窗与 SSH 连接流程的实际串联）未完成**，Host Key 指纹确认和密钥变更警告弹窗仅以独立视图存在，不会在真实连接过程中被触发，直接导致 TC-009（密钥变更阻止自动连接）无法通过。此外，TC-001/002/004/005 均停留在"代码已生成，待真机验收"阶段，M2 里程碑演示（W9.5）因 Docker 测试环境未搭建而未进行。综合评估，**整体完成度约 70%**，代码基础扎实，核心阻塞为真机验收环境缺失和 D02/D03 流程串联缺口。

---

## 2. 验收条件核查

| # | 验收条件 | 状态 | 评估说明 |
|---|---------|------|---------|
| 1 | TC-001：密码认证 SSH 连接成功，终端显示 shell 提示符 | ⚠️ | `SSH2Connection.connect()` 含密码认证（`libssh2_userauth_password`）；SwiftTerm 数据管线已串联（`onDataReceived → terminalView.feed()`）；待 Docker sshd / 192.168.100.167 真机端到端验收 |
| 2 | TC-002：SSH Key（Ed25519）认证无需输入密码连接成功 | ⚠️ | `SSH2Connection.connectWithKey()` 实现，从 Keychain 读取私钥；Ed25519 算法通过 libssh2 支持；待真实服务器验收 |
| 3 | TC-003：退出重启后所有会话配置完整恢复 | ✅ | Core Data 持久化逻辑已验证；`SessionStore.fetchSessions()` 启动时加载；TC-003 逻辑已通过（W9.3 注明） |
| 4 | TC-004：3 个标签页并发独立工作 | ⚠️ | ZStack+opacity 多标签架构已修正（2026-03-19，W9.6：TC-004 多标签架构修正）；每个标签持有独立 `TerminalController` 实例；待 3 会话并发真机验收 |
| 5 | TC-005：断网后网络恢复自动重连 | ⚠️ | `TerminalController` 指数退避重连状态机（3次，间隔 1/2/4s，上限后进入 `.disconnected`）已实现；待网络断开模拟 / NWPathMonitor 集成验收 |
| 6 | Known Hosts 首次确认（D02）和密钥变更警告（D03）弹窗正常 | ⚠️ | `HostKeyConfirmationView`（D02）和 `HostKeyChangedWarningView`（D03）UI 已实现并加入编译目标；**W7.7（与 SSH 握手流程的实际对接）🔲 未完成**，弹窗不会在真实连接中自动弹出 |
| 7 | Keychain 凭据安全存取验证 | ✅ | `KeychainService` 以 `ShellMate_<UUID>` 为 Service，`kSecAttrAccessibleWhenUnlockedThisDeviceOnly` 访问策略；SSH 密码与私钥原文只存 Keychain，Core Data 仅存 `keychainRef` UUID |

**本阶段验收通过率：2/7（约 29%）**
（TC-003 和 Keychain 完全通过；其余 5 条受制于真机验收环境或 D02/D03 流程缺口）

---

## 3. PRD P0 用例对齐检查

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| TC-001 | 密码认证 SSH 连接，终端显示 shell 提示符，延迟 < 3s | ⚠️ | 代码完整（三层架构：Bridge → Connection → TerminalController）；已知测试服务器：192.168.100.167 / ubuntu / Int3l@123；建议立即以此真机验收 |
| TC-002 | SSH Key（Ed25519）认证无需输入密码 | ⚠️ | `connectWithKey()` 实现；Keychain 存取逻辑已实现；需在测试服务器配置 authorized_keys 后验收 |
| TC-003 | 退出重启后会话配置和分组结构完整恢复 | ✅ | Core Data 持久化已验证通过 |
| TC-004 | 3 个独立标签页各自独立工作，相互不干扰 | ⚠️ | 架构已修正，每个 Tab 有独立 Controller；需 3 个并发 SSH 连接真机压测 |
| TC-005 | 断网后自动重试，网络恢复后自动重连 | ⚠️ | 重连状态机已实现；需配合 NWPathMonitor 网络状态监听（当前仅定时重试，未感知网络恢复事件）— 建议核查是否接入 `NWPathMonitor` |

---

## 4. UI/Figma 规范符合度

### 4.1 设计令牌使用

- 终端字体/颜色：`DesignTokens.swift` 定义了终端相关颜色令牌；SwiftTerm 主题颜色是否与令牌联动**需人工验证**
- 工具栏按钮使用 SF Symbols（字号/清屏/搜索图标）：符合 02-SF Symbols 规范方向

### 4.2 组件实现符合度

| 组件 | Figma 规范要求 | 实现状态 | 符合度 |
|------|--------------|---------|-------|
| 终端内容区 | 等宽字体，xterm-256color，背景透明度支持 | SwiftTerm 集成，支持 VT100/xterm-256color/True Color | ✅ 基础符合 |
| 终端工具栏 | 字号+/-、清屏、搜索按钮，28px 高 | `TerminalToolbarView` 已修正（API 对齐 SwiftTerm），含相应按钮 | ⚠️ 高度/图标需验证 |
| TabBar 标签 | 36pt，状态点+名称+关闭按钮 | `TerminalTabBarView` 已串联 `TabBarStore` | ⚠️ 精确规格需验证 |
| D02 首次指纹确认弹窗 | Alert 样式，含指纹显示+确认/拒绝 | `HostKeyConfirmationView` 已实现 | ⚠️ 触发时机未串联 |
| D03 密钥变更警告弹窗 | 高优先级 Alert，含详细说明+确认/断开 | `HostKeyChangedWarningView` 已实现 | ⚠️ 触发时机未串联 |
| StatusBar | 连接状态/SSH版本/用户名/编码/端口/网络延迟/终端尺寸 | `TerminalStatusBarView` 存在，字段完整性**需人工验证** | 需人工验证 |

### 4.3 动效与交互

- 连接中状态：`StatusDotView` 脉冲黄点动画 ✅
- 终端内容滚动：SwiftTerm 原生处理 ✅
- 标签页切换过渡：ZStack+opacity 切换，无显式动画；07-终端覆层规范中覆层动画（搜索栏滑入等）**需人工验证**
- 重连状态 UI 提示（如 overlay 文字 "重连中..."）：**需人工验证**是否符合 PRD 4.1 的连接状态视觉规范

### 4.4 可访问性

- M2 阶段未进行系统性可访问性注解（M4 W14.4 才专项处理）
- 终端视图 VoiceOver：SwiftTerm NSView 的 VoiceOver 支持依赖 AppKit 层，**需人工验证**
- D02/D03 弹窗的 `accessibilityLabel`：**需人工验证**

**UI 规范符合度：中**
（终端渲染核心已实现，D02/D03 视图已建；D02/D03 触发流程缺失是核心问题；工具栏/StatusBar 精确规格需对齐 Figma）

---

## 5. 技术架构符合度

### 5.1 架构层次遵循

- SSH 连接层分层清晰：
  - `LibSSH2Bridge`：libssh2 C 函数的 Swift 封装层（最底层）
  - `SSHConnection` / `SSH2Connection`：连接状态机（握手/认证/PTY/读写）
  - `SSHAuthService`：认证策略分发（密码/Key/Agent）
  - `TerminalController`：@MainActor，衔接 SSH 层与 SwiftTerm 渲染层
  - `ShellMateTerminalView`：NSViewRepresentable，将 SwiftTerm 嵌入 SwiftUI
- 符合技术方案 3.1 节的 SSH 连接层设计 ✅

### 5.2 Swift Concurrency 使用

- SSH IO 读取循环（read loop）使用 `Task` + `await`，避免阻塞主线程 ✅
- `TerminalController` 标注 `@MainActor`，UI 更新在主线程 ✅
- **潜在问题**：W6.2 注明「非阻塞模式 + 读取循环」实现为「SSH2Connection 含 readLoop + `DispatchQueue`」——存在 GCD 残留风险，与技术方案「全面弃用 GCD」要求不符；建议执行 `grep -r "DispatchQueue" ShellMate/Core/Services/SSH/` 核查
- libssh2 为 C 库，其回调和阻塞调用需正确隔离在非主线程（actor 或 Task background）**需人工验证**

### 5.3 安全规范

- 密码/私钥原文不落 Core Data ✅（仅存 `keychainRef` UUID）
- Keychain 访问控制 `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` ✅
- Host Key 校验：`KnownHostsManager.swift` 持久化到 `ApplicationSupportDirectory/ShellMate/known_hosts.json` ✅（M4 P0-2 已确认有效）
- **缺口**：W7.7 未完成 → SSH 握手时未调用 KnownHostsManager 做指纹比对 → 首次连接未知主机时不弹 D02，密钥变更时不弹 D03，存在安全合规 P0 缺口
- 日志中密码泄漏风险：**需人工验证** SSH 认证失败日志是否包含密码明文

### 5.4 数据模型符合度

- `Session.keychainRef`（String? UUID）：设计与实现一致 ✅
- `Session.authMethod`（AuthMethod 枚举）：`AuthMethod.swift` 已定义，枚举值（password/sshKey/agent/gssapi）与 PRD 3.10 一致 ✅
- `Session.jumpHosts`（[JumpHost]）：`Session.swift` 含 `jumpHosts` 数组，从 `CDJumpHost` Core Data 实体加载 ✅
- `ConnectionState` 枚举（`.connecting/.connected/.disconnected/.reconnecting`）：`ConnectionState.swift` 已定义，与技术方案状态机一致 ✅
- **待核查**：`CDJumpHost` Core Data 实体的字段是否完整（host/port/username/authMethod）

**技术规范符合度：良**
（SSH 分层架构清晰，Keychain 安全正确，Swift Concurrency 方向正确；GCD 残留和 W7.7 流程缺口是主要风险）

---

## 6. 风险与问题清单

| 优先级 | 问题描述 | 影响范围 | 建议措施 |
|-------|---------|---------|---------|
| P0 | W7.7 D02/D03 与 SSH 连接流程串联完全缺失：握手阶段未调用 `KnownHostsManager` 校验，弹窗不会自动触发 | TC-009 直接失败；安全核心功能缺口（允许中间人攻击） | 在 `SSH2Connection` 握手成功后，调用 `KnownHostsManager.check(host:fingerprint:)`；结果为未知时弹出 D02，结果为冲突时弹出 D03 并阻断连接 |
| P0 | 全部 TC-001/002/004/005 待真机验收，M2 演示（W9.5）未进行 | M2 里程碑官方未通过 | 立即使用已知测试服务器（192.168.100.167）验收 TC-001；完成后依次验收 TC-002/004/005 |
| P0 | 自动重连（TC-005）可能未接入 `NWPathMonitor`：当前仅定时重试，无法感知「网络已恢复」事件，可能在网络恢复后延迟重连或超时放弃 | TC-005 验收标准「网络恢复后自动重连」 | 核查 `TerminalController` 是否使用 `NWPathMonitor` 监听路径状态；若无，添加 `.satisfied` 回调触发重连 |
| P1 | GCD 残留：W6.2 注明 readLoop 使用 `DispatchQueue`，与技术方案 Swift Concurrency 规范冲突 | 线程模型一致性；潜在并发问题 | 将 `DispatchQueue.async` 替换为 `Task { }` / `actor`；对 libssh2 阻塞调用使用 `Task.detached(priority: .utility)` |
| P1 | 跳板机（W9.1/9.2）代码已实现但待真实服务器穿通验证：`ProxyJumpManager` 的 `direct-tcpip` libssh2 调用在多跳场景的正确性未验证 | 跳板机核心功能可用性 | 在测试环境搭建跳板机链路（或使用 SSH 本地隧道模拟），验收 1 跳和 2 跳场景 |
| P1 | 内存泄漏测试（W9.4）仅有测试文件，未实际执行 Instruments Leaks 分析 | 3 并发连接内存控制目标（<150MB RSS）无量化验证 | 在 M3 启动前运行 Instruments Leaks，确认单连接 <5MB、并发 <150MB RSS 指标 |
| P2 | 终端尺寸变化（窗口 resize）的 PTY resize 事件是否在多标签场景下正确路由到当前活跃 Tab：**需人工验证** | 多标签并发场景下终端宽度/高度不一致 | 验收 TC-004 时同时测试窗口拖拽 resize 时 3 个 Tab 各自的终端列数是否正确更新 |
| P2 | W6.4 TCP 建连错误处理：DNS 解析失败 / 连接超时的 `SSHError` 分支和 UI 提示是否完整（标注🔄进行中） | 用户体验（错误提示） | 核查 `SSHError` 枚举覆盖的错误类型，并确保每种错误在 UI 上有明确提示（Alert 或 StatusBar） |

---

## 7. 下一阶段建议

进入 M3（高级功能，W10–W13）前，必须先解决以下 M2 遗留项：

1. **最高优先级：补全 W7.7**（D02/D03 SSH 流程串联）— 这是 TC-009 的前提，也是 SSH 工具安全合规的底线；预计 0.5 天工作量
2. **搭建 Docker sshd 测试环境**（或直接使用 192.168.100.167）逐一完成 TC-001 → TC-002 → TC-004 → TC-005 真机验收；建议按序进行，TC-001 通过后其余难度大幅降低
3. **NWPathMonitor 接入确认**：在开始 SFTP 开发前，确认自动重连能正确感知网络恢复事件
4. **M3 阶段注意**：SFTP 使用独立 SSH 连接（`SFTPSession actor`），与主终端连接完全分离，技术方案已明确；端口转发（LocalPortForwarder）需要访问本地 socket，注意 App Sandbox 的 `com.apple.security.network.server` 权限已在 entitlements 配置

---

## 8. 附录：核查依据

- MRD 版本：v1.0（2026-03-18）
- PRD 版本：v1.0（2026-03-18）
- 技术方案版本：v1.0（2026-03-18）
- 数据库设计文档版本：v1.0（2026-03-18）
- Figma 规范版本：v1.0（shellmate-figma-spec 00–08）
- 开发进度文档版本：v1.3（最后更新 2026-03-20）
- 审查 Git 范围：最近 20 次提交（最新：0a86a31 perf(terminal): W15 全面性能优化）
- 关键 Git 提交：
  - `66b93da feat(ssh): 完成 W6 libssh2 桥接层完整实现`
  - `c212a3e feat(ssh): 完成 W7 认证 + 指纹校验功能`
  - `976389a feat(terminal): 完成 W8 终端渲染功能`
  - `73f145b feat(ssh): 完成 W9 阶段收尾功能`
  - `1cb9860 fix(ssh): 实现真正的 SSH 连接功能`
  - `d20a64d fix(terminal): 修复编译错误并串联多标签页架构`
- 审查时间：2026-03-20 17:35
