# ShellMate 阶段性项目 Review 报告

> **Review 日期：** 2026-03-19
> **Review 类型：** 阶段性差异评审（Baseline vs Actual）
> **评审人角色：** 项目经理
> **参考文档：** MRD v1.0 · PRD v1.0 · 开发进度 v1.0 · Figma Spec v1.0 · shellmate-ui

---

## 一、总体评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 进度推进速度 | ⭐⭐⭐⭐⭐ | 2天内完成W1-W9的代码提交，效率极高 |
| 功能完整性 | ⭐⭐⭐ | 核心SSH通路跑通，但大量PRD功能缺失 |
| 代码质量 | ⭐⭐⭐ | 架构分层清晰，但关键技术选型偏离规格 |
| UI/设计还原度 | ⭐⭐ | 框架已搭，但与Figma规范差距显著 |
| 稳定性/可编译性 | ⭐⭐ | 多个文件无法编译，核心架构存在断链 |

---

## 二、进度 vs 计划对比

### 2.1 时间线异常

| 计划节点 | 计划完成日 | 实际状态 | 差异 |
|---------|-----------|---------|------|
| W1 工程初始化 | 2026-03-22 | ✅ 完成（03-18/19） | 提前 |
| W2 Repository层 + 侧边栏骨架 | 2026-03-29 | ✅ 完成（单次提交） | 提前 ~10天 |
| W3-W5 会话CRUD + 组件 + TabBar | 2026-04-18 | ✅ 完成（单次提交） | 提前 ~30天 |
| W6 libssh2桥接层 | 2026-04-25 | ✅ 标称完成 | 提前 ~37天 |
| W7 认证+指纹校验 | 2026-05-02 | ✅ 标称完成 | 提前 ~44天 |
| W8 终端渲染 | 2026-05-09 | ✅ 标称完成 | 提前 ~51天 |
| W9 阶段收尾+M2验收 | 2026-05-16 | ✅ 标称完成 | 提前 ~58天 |

**⚠️ 风险判断：** 上述提交极有可能是框架骨架快速生成，而非高质量的功能实现。需要对每个"完成"项做质量穿透测试，防止出现"提交了代码 ≠ 功能可用"的虚假进度。

### 2.2 开发进度文档未更新

`开发进度.md` 仍显示：
- 项目状态：`🟡 进行中（第 1 周）`
- 功能模块进度：所有模块均为 `🔲 未开始`
- W1周报停留在初始模板

**行动项 #1：** 立即更新开发进度文档，反映真实代码状态。

---

## 三、功能差异（PRD vs 实际代码）

### 3.1 已完成（代码存在且基本可用）

| 功能 | PRD优先级 | 实际状态 | 备注 |
|------|----------|---------|------|
| Session CRUD + Core Data持久化 | P0 | ✅ 已实现 | Repository/Store层完整 |
| 分组管理 | P0 | ✅ 已实现 | 颜色/折叠状态均有 |
| 会话表单（4个Tab） | P0 | ✅ 已实现 | Basic/Auth/Advanced/Appearance均有 |
| Keychain凭据存储 | P0 | ✅ 已实现 | KeychainService完整 |
| libssh2桥接层 | P0 | ✅ 基本实现 | LibSSH2BridgeReal + LibSSH2Bridge |
| SSH密码/Key/Agent认证 | P0 | ✅ 基本实现 | SSH2Connection包含三种认证方式 |
| Known Hosts管理 | P0 | ✅ 已实现 | KnownHostsManager.swift存在 |
| 主机密钥确认弹窗（D02/D03） | P0 | ✅ 已实现 | HostKeyConfirmationView/HostKeyChangedWarningView |
| NavigationSplitView骨架 | P0 | ✅ 已实现 | |
| 侧边栏搜索框 | P0 | ✅ 已实现 | SidebarSearchView |
| TabBar骨架 | P0 | ⚠️ 存在但未串联 | TerminalTabBarView存在，未与SSH连接对接 |

### 3.2 缺失或严重不足（PRD要求但未实现）

| 功能 | PRD优先级 | 实际状态 | 影响 |
|------|----------|---------|------|
| **SwiftTerm集成** | P0 | ❌ 未集成 | 终端仿真是自研简化版，不满足规格 |
| **SFTP文件管理器** | P0/P1 | ❌ 完全缺失 | W10计划，当前无任何SFTP代码 |
| **端口转发GUI** | P0/P1 | ❌ 完全缺失 | W11计划，无UI无逻辑 |
| **跳板机多跳支持** | P0/P1 | ⚠️ 有SSHProxyJump.swift但未验证 | 文件存在，功能未穿通 |
| **自动重连状态机** | P0 | ❌ 缺失 | W8任务，TerminalController不可编译 |
| **多标签页实际功能** | P0 | ⚠️ UI骨架有，SSH会话未绑定 | TabBar UI与SSH Session无关联 |
| **分屏布局** | P0 | ❌ 缺失 | 无左右/上下分屏实现 |
| **关键词高亮引擎** | P1 | ❌ 缺失 | W12任务，无代码 |
| **终端内搜索（O01）** | P1 | ❌ 缺失 | 覆层搜索栏未实现 |
| **快捷命令管理器** | P1 | ❌ 缺失 | W11任务，无代码 |
| **Compose Pane（O02）** | P2 | ❌ 缺失 | |
| **同步输入模式（O03）** | P2 | ❌ 缺失 | |
| **iCloud配置同步** | P0 | ⚠️ 基础初始化有，双设备未验证 | PersistenceController有NSPersistentCloudKitContainer |
| **设置面板（S01/S02/S03）** | P0/P1 | ❌ 缺失 | 外观/高亮/安全设置面板均无 |
| **会话导入/导出** | P1 | ❌ 缺失 | OpenSSH config导入/JSON导出均无 |
| **侧边栏拖拽排序** | P0 | ❌ 缺失 | DragGesture未实现 |
| **会话录制/回放** | P2 | ❌ 缺失 | W13计划，无代码 |
| **侧边栏右键菜单** | P0 | ⚠️ 存在但不完整 | SessionRowView有contextMenu，操作不全 |

---

## 四、关键技术债务

### 4.1 🔴 严重：SwiftTerm 未集成

**规格要求：** MRD §7.1、PRD §3.3 均明确要求使用 SwiftTerm 进行终端渲染。

**实际情况：**
- `ShellMateTerminalView.swift` 是团队自研的简化终端仿真器
- 注释中写明"注意：实际项目中需要 import SwiftTerm 并使用真实的类型"
- 当前自研实现的缺陷：
  - 无 True Color（24-bit）支持
  - 无完整 xterm-256color 支持（仅处理部分 CSI 序列）
  - 无 `?` 前缀 CSI 参数（如 `?2004h` 的 bracketed paste mode 处理不完整）
  - 无 Unicode/CJK 全宽字符支持
  - 无鼠标事件（选中、滚动报告）
  - 无回滚缓冲区（scrollback buffer）
  - 无真正的 80×24 grid 字符缓冲区（当前是简单数组）

**影响：** 终端 60fps 渲染要求（PRD §5）无法达标，多数终端程序（vim/tmux/htop）显示异常风险极高。

**行动项 #2：** W8 阶段必须完成真正的 SwiftTerm 集成，当前 ShellMateTerminalView 只能作为临时占位。

---

### 4.2 🔴 严重：核心文件无法编译

以下文件不在 Xcode 项目中或存在未解析依赖，导致无法编译：

| 文件 | 状态 | 原因 |
|------|------|------|
| `TerminalController.swift` | ❌ 无法编译 | 依赖 `SSHProcessConnection`、`SSHConnection`、`SSHChannelManager`、`SSHSessionConfig` 等未定义类型 |
| `TerminalView.swift` | ❌ 无法编译 | 依赖 `TerminalController` |
| `TerminalToolbarView.swift` | ❌ 未加入项目 | 项目文件中无注册 |
| `SSHProxyJump.swift` | ⚠️ 状态待查 | 未确认是否在项目中 |

**影响：** `TerminalView.swift` 是规格意图中的"正式终端视图"，无法使用意味着整个终端架构上层是断链的。应用实际运行依赖 `TerminalPlaceholderView`，这是临时方案。

**行动项 #3：** 修复 TerminalController 的类型依赖，或明确采用新的架构方案。

---

### 4.3 🟡 中等：双轨并存导致架构混乱

当前存在两套 SSH 连接实现：
1. **SSH2Connection** (libssh2 原生)
2. **SSHProcessBridge** (调用系统 `/usr/bin/ssh` 命令)

同时存在两套终端视图：
1. **TerminalPlaceholderView** (生产中使用，临时方案)
2. **TerminalView + TerminalController** (规格意图，但无法编译)

**风险：** 代码维护成本翻倍，行为差异难以追踪，测试覆盖困难。

**行动项 #4：** 明确一条主线，移除或归档备用路径。

---

### 4.4 🟡 中等：`TerminalPlaceholderView` 中的 `[self]` 捕获

在 `performConnectWithSystemSSH` 中，`bridge.onDataReceived = { [self] data in ...}` 将 SwiftUI 结构体 `self` 捕获进闭包。SwiftUI 结构体是值类型，捕获 `self` 后回调中修改 `@State` 属性会产生潜在竞态问题。

---

## 五、UI/设计规范差异

### 5.1 与 shellmate-ui 的差异

| 设计项 | shellmate-ui 规格 | 当前实现 | 差异 |
|--------|-----------------|---------|------|
| UI字体 | Geist（Vercel出品） | SF Pro（系统默认） | 字体不一致 |
| 终端字体 | Geist Mono | SF Mono | 字体不一致 |
| 背景主色 | `#0c0c0e` | `#0C0C0E`（0.047,0.047,0.055） | ✅ 基本一致 |
| 侧边栏背景 | `#111115` | DesignTokens（需核查） | 待验证 |
| 强调色 | `#3d8ef0` | DesignTokens.accentPrimary | ✅ 规格对齐 |
| 标题栏样式 | Unified Title+Toolbar 52pt | NavigationSplitView默认 | ⚠️ 未对齐 |
| 标签栏高度 | 36pt | TerminalTabBarView（待测） | 待验证 |
| 状态栏高度 | 24pt | TerminalStatusBarView（待测） | 待验证 |
| TitleBar右侧图标 | Tunnel/Command/Sync/Settings 4个按钮 | toolbar仅有 新建会话/连接 按钮 | ❌ 缺失 |
| 侧边栏宽度 | 220pt（最小180，最大320） | DesignTokens定义有 | ✅ 规格存在 |

### 5.2 与 Figma Spec 的差异

| Figma规范项 | Figma要求 | 当前实现差异 |
|------------|----------|------------|
| 字体系统 | SF Pro（UI）+ SF Mono（代码） | 存在 DesignTokens 但字体名硬编码情况待检查 |
| 设计令牌 | Variable Collection 三层体系 | Swift DesignTokens.swift 有映射，但不完整 |
| 会话行高度 | 44pt（双行：名称+主机） | SessionRowView高度待确认 |
| 分组行高度 | 28pt | GroupHeaderView高度待确认 |
| 搜索框高度 | 28pt | SidebarSearchView高度待确认 |
| Tag徽章高度 | 16pt，圆角3pt | TagBadgeView需核查 |
| StatusDot大小 | 6×6pt，有外发光 | StatusDotView存在，发光效果待确认 |
| Session表单弹窗（D01） | 4个Tab，精确布局 | 4个Tab均有文件，质量待Handoff确认 |
| 主机密钥弹窗（D02/D03） | 精确的 Alert 样式 | 有对应视图文件，样式待确认 |
| 设置面板（S01/S02/S03） | 三个完整面板 | ❌ 无实现 |
| 隧道管理器（D04） | 完整列表+编辑 | ❌ 无实现 |
| 快捷命令管理器（D05）| 双栏+拖拽 | ❌ 无实现 |
| 终端搜索覆层（O01） | 右上角 320×40pt 覆层 | ❌ 无实现 |
| Compose Pane（O02） | 底部命令编辑区 | ❌ 无实现 |
| 同步输入模式（O03） | 橙色边框+状态栏提示 | ❌ 无实现 |

---

## 六、M2 验收标准差距

按开发进度文档 §3.2，M2 验收条件（2026-05-16 目标）如下，评估当前状态：

| 验收条件 | 当前状态 | 可验收？ |
|---------|---------|---------|
| TC-001: 密码认证SSH连接<3s | ⚠️ 功能有，今日发现乱码Bug（已修复） | 部分 |
| TC-002: SSH Key（Ed25519）免密连接 | ⚠️ 代码有，真实环境未验证 | 待验 |
| TC-003: 退出重启后会话配置完整恢复 | ✅ Core Data持久化存在 | 可测 |
| TC-004: 3个标签页并发独立工作 | ❌ TabBar UI与SSH会话未绑定 | ❌ |
| TC-005: 断网自动重连 | ❌ 重连状态机未实现 | ❌ |
| Known Hosts D02/D03弹窗流程正常 | ⚠️ 视图存在，流程串联待验 | 待验 |
| Keychain凭据安全存取验证 | ✅ KeychainService实现完整 | ✅ |

**结论：** 当前代码距离真正的 M2 验收通过还有 2-3 项关键差距。

---

## 七、风险登记（新增）

| 风险 | 等级 | 说明 | 行动项 |
|------|------|------|--------|
| SwiftTerm缺失导致终端体验不达标 | 🔴 高 | 自研终端仿真器功能大幅受限，vim/tmux等常用工具可能显示异常 | 行动项#2 |
| 架构双轨导致维护困难 | 🟡 中 | TerminalPlaceholderView vs TerminalView两套路径 | 行动项#4 |
| 进度数据虚报风险 | 🟡 中 | 2天提交W1-W9代码，实际功能完成度需穿透验证 | 行动项#1 |
| SFTP缺失影响M3里程碑 | 🟡 中 | 无任何SFTP代码，W10只剩5周（实际按计划） | 跟踪 |
| 设置面板缺失阻塞M3 | 🟡 中 | S01/S02/S03均为空，W13到期 | 跟踪 |

---

## 八、优先行动项汇总

| # | 行动项 | 负责人 | 优先级 | 截止 |
|---|--------|--------|--------|------|
| 1 | 更新`开发进度.md`：真实反映各模块完成状态，区分"代码已生成"与"功能已验收" | PM | P0 | 立即 |
| 2 | 集成真正的SwiftTerm：替换ShellMateTerminalView为SwiftTerm的TerminalView | Lead | P0 | 本周 |
| 3 | 修复TerminalController编译错误：解决SSHProcessConnection等类型缺失 | Lead | P0 | 本周 |
| 4 | 明确技术架构主线：废弃SSHProcessBridge，统一为libssh2方案 | Lead | P1 | 本周 |
| 5 | 多标签页与SSH会话绑定：TabBar点击能实际创建/切换SSH连接 | 工程师 | P0 | 下周 |
| 6 | 自动重连状态机实现：指数退避，3次上限（TC-005） | 工程师 | P0 | 下周 |
| 7 | 完成真实SSH连接端到端验证：TC-001~TC-004在Docker环境跑通 | QA | P0 | 下周 |
| 8 | UI对齐Figma：侧边栏行高/字体/间距/TitleBar工具按钮 | 工程师 | P1 | W3 |
| 9 | 字体统一：确认 shellmate-ui 中Geist字体 vs Figma中SF Pro的最终选型 | PM+设计 | P1 | W2 |
| 10 | 启动SFTP开发：SFTPSession actor基础框架 | Lead | P1 | W10按计划 |

---

## 九、正面评价

尽管存在以上差距，以下方面值得肯定：

1. **代码架构分层清晰**：Repository → Store → View的单向数据流执行到位，符合CLAUDE.md规范
2. **Keychain安全实现**：凭据存储安全，符合核心设计原则
3. **Core Data模型完整**：Session/Group数据模型已完整实现持久化
4. **错误处理有结构**：SSHError枚举设计完整，涵盖主要错误场景
5. **乱码问题快速响应**：今日发现ANSI乱码bug后即时修复，替换为ShellMateTerminalView的feed机制
6. **已建立CI基础结构**：项目文件、SwiftLint、gitignore均已就位

---

## 十、下周重点（W2，按计划）

基于以上Review，建议下周聚焦：

1. **SwiftTerm真实集成**（最高优先级，解决终端体验根本问题）
2. **TC-001/TC-002 真实服务器验证**（有了真终端后进行）
3. **多标签页串联**（TabBar与SSH Session绑定）
4. **进度文档更新**（反映真实状态）

---

*报告生成时间：2026-03-19*
*下次Review计划：2026-03-26（W2末）*
