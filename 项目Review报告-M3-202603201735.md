# ShellMate 项目 Review 报告 — M3 阶段

> **阶段：** M3 — 功能集完整（W10–W13）
> **生成时间：** 2026-03-20 17:35
> **审查范围：** W10–W13（2026-05-19 → 2026-06-13）
> **总体评级：** 部分通过 ⚠️

---

## 1. 执行摘要

M3 阶段目标为完成 ShellMate 的全部高级功能：SFTP 文件管理、端口转发/隧道、快捷命令、关键词高亮、终端内搜索、同步输入、iCloud 同步以及完整设置面板。从 WBS 任务表（第4章）看，W10–W13 的绝大多数任务标注为 ✅，包括 SFTPSession、TransferQueue、LocalPortForwarder、RemotePortForwarder、Socks5Proxy、D04/D05 UI、ComposePane、HighlightEngine、终端内搜索、同步输入以及三个设置面板；W13 甚至标注了 M3 里程碑演示 ✅。

然而本次 Review 发现**两项关键矛盾**：

**矛盾一（最严重）：WBS 任务表与功能模块进度追踪表（第6章）存在系统性不一致。** 模块表中，关键词高亮、终端内搜索逻辑、同步输入模式、设置面板（外观/高亮/安全）均标注为 🔲 未开始，而 WBS 表标注这些任务 ✅。这说明第6章的模块追踪表可能停留在 W11 状态未更新，但也可能反映 WBS 表的"✅"仅代表"代码文件存在"而非"功能逻辑完整"。

**矛盾二（代码追溯性风险）：W10–W13 的全部代码均为 git 未追踪文件（untracked files）**，没有对应的 git commit 记录（git log 显示 W9 之后直接跳到 W15），导致代码变更历史完全缺失，无法追溯任何一项 M3 功能的引入时间和修改记录。

TC-010（iCloud 跨设备同步）因依赖真实 CloudKit 容器和 Apple Developer 账户而无法端到端验收。综合评估，**整体完成度约 65%**（代码量已达标，但逻辑完整性、真机验收和 git 历史均有重大缺口）。

---

## 2. 验收条件核查

| # | 验收条件 | 状态 | 评估说明 |
|---|---------|------|---------|
| 1 | TC-006：SFTP 文件上传，进度队列显示 100% | ⚠️ | `SFTPSession`（独立 SSH 连接，actor 隔离）+ `SFTPTransferQueue`（maxConcurrent=3）+ `SFTPTransferProgressView` 均已实现（W10，2026-03-19）；断点续传（seek64 + APPEND）已实现；待真实 SFTP 服务器（192.168.100.167）端到端验收 |
| 2 | TC-007：本地端口转发流量正确转发 | ⚠️ | `LocalPortForwarder.swift` 已实现：POSIX accept 循环 + 每连接独立 SSH 会话 + 双向 bridge（W11，2026-03-20）；`TunnelManagerView` 已实现管理 UI；待真实服务器 + 本地客户端穿通验收 |
| 3 | TC-008：ERROR 关键词高亮显示 | ⚠️ | `HighlightEngine.swift`（预编译正则）标注 ✅（W12）；但**功能模块进度表（第6章）标注「关键词高亮 🔲 未开始」**，与 WBS 表矛盾；HighlightEngine 是否已与 SwiftTerm 终端输出管线对接**需人工验证** |
| 4 | TC-009：主机密钥变更阻止自动连接 | ⚠️ | `HostKeyChangedWarningView`（D03 UI）已实现；`KnownHostsManager` 持久化已确认有效（M4 P0-2）；**W7.7（D03 与 SSH 握手流程串联）仍未完成**，密钥变更警告不会在真实连接中自动触发 — 此为 M2 遗留 P0 缺口，影响 M3 验收条件 |
| 5 | TC-010：Mac A 新建会话，Mac B 30s 内同步出现 | ❌ | `NSPersistentCloudKitContainer` 已初始化，CloudKit 同步代码逻辑完整；但 CloudKit 容器 identifier 和 Apple Developer entitlement 未激活，无法端到端验收；属外部依赖，代码层面无需变更 |
| 6 | 设置面板（外观/高亮/安全/终端/iCloud）全部可用 | ⚠️ | WBS 表：S01 高亮✅、S02 外观✅、S03 安全✅（W13）；M4 P0 修复确认 `AppearanceSettingsView` 存在（P0-5）+ `TerminalSettingsView` 新增（P0-6）；**功能模块进度表（第6章）标注设置-外观/高亮/安全均 🔲 未开始**，与 WBS 表矛盾；iCloud 设置面板**需人工验证**是否存在 |
| 7 | 跳板机多跳连接验证 | ⚠️ | `ProxyJumpManager` 含多跳循环架构，libssh2 direct-tcpip 已实现（W9，2026-03-19）；待真实跳板机环境（≥2 跳）穿通验收 |

**本阶段验收通过率：0/7（约 0% 完全通过）**
（TC-010 未通过；其余 6 条均为代码已实现但待真机验收，或存在前置流程缺口（TC-009/W7.7）；若放宽为"代码层面完成"则 6/7）

---

## 3. PRD P0 用例对齐检查

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| TC-006 | SFTP 文件上传，传输队列显示 100% | ⚠️ | SFTPSession + SFTPTransferQueue 实现完整；待 192.168.100.167 测试服务器验收；上传 buffer 已优化至 128KB（W15 性能优化），进度更新节流 1.0s |
| TC-007 | 本地端口转发流量正确转发到远端 | ⚠️ | LocalPortForwarder 实现 POSIX accept 循环 + SSH direct-tcpip 通道桥接；Sandbox entitlement 已包含 `com.apple.security.network.server`；需真实端口转发场景验收（如本地 8080 → 远端 3306） |
| TC-008 | ERROR 关键词高亮显示为配置颜色 | ⚠️ | HighlightEngine 代码存在；内置规则集包含「系统错误」组（ERROR/FATAL/CRITICAL）；与 SwiftTerm 输出流的对接**需人工验证**；第6章模块表标注🔲是重要风险信号 |
| TC-009 | 主机密钥变更弹出高优先级警告，阻止自动连接 | ⚠️ | D03 UI 完整；KnownHostsManager 持久化正常；核心缺口：SSH 握手层未调用 KnownHostsManager（W7.7 未完成）；此为 M2 遗留 P0 缺口延伸到 M3 |
| TC-010 | Mac A 新建会话，Mac B 30s 内同步出现 | ❌ | CloudKit 代码完整，外部依赖（Apple Developer 账户 + CloudKit 容器 identifier）未解除 |

---

## 4. UI/Figma 规范符合度

### 4.1 设计令牌使用

- SFTP 面板、隧道管理器、快捷命令面板等新增 UI 是否使用 `DesignTokens` 颜色令牌**需人工验证**
- D04/D05 弹窗的圆角、间距是否遵循 Figma 05-弹窗规范**需人工验证**

### 4.2 组件实现符合度

| 组件 | Figma 规范要求 | 实现状态 | 符合度 |
|------|--------------|---------|-------|
| SFTP 面板（右侧） | 300pt 宽，路径导航栏 + 文件列表 + 传输队列，HSplitView | `SFTPPanelView` 通过 `HSplitView` 集成到 `TerminalView`，含路径导航/列表/文件操作 | ⚠️ 宽度/列宽调整精确规格需验证 |
| D04 隧道管理器 | 规则列表（状态/类型/本地地址/远端目标/操作）+ 详情编辑区 | `TunnelManagerView` 含规则列表 + 类型徽章 + 详情编辑 + 全部启停 | ✅ 基本符合规范结构 |
| D05 快捷命令管理器 | 双栏布局（命令集列表 + 命令编辑）+ 拖拽排序 + KeyRecorder | `QuickCommandManagerView` 含命令集 Picker + 双栏编辑 | ⚠️ 拖拽排序和 KeyRecorder（快捷键录制）需人工验证 |
| Compose Pane（O02） | 底部可停靠，含行号列 + 语言选择 + 逐行发送 + 拖拽高度调整 | `ComposePaneView` 集成至 `TerminalView`，含拖拽调整高度 + 行号列 + 逐行发送 + 语言选择 | ✅ 符合规范 |
| 终端内搜索（O01） | 覆层 UI，含文本输入 + 上/下跳转 + 正则模式 + 关闭 | `TerminalSearchBar` UI 已实现；**第6章标注🔲逻辑未对接**；覆层位置和动画需验证 | ⚠️ UI 存在，逻辑未确认接通 |
| 同步输入（O03） | 开启时所有参与窗格显示橙色边框 + 确认弹窗 | `SyncInputConfirmView` + `SyncInputStore` 已实现（W12）；**第6章标注🔲未开始**，同上矛盾 | ⚠️ 代码存在，逻辑待验证 |
| 设置面板 S01–S03 | S01 关键词高亮（规则列表/颜色/字体样式）；S02 外观（主题卡片/字体/光标/透明度）；S03 安全（Known Hosts 表格/SSH 密钥管理） | WBS 表标注 ✅；M4 P0 修复中 AppearanceSettingsView 和 TerminalSettingsView 被确认存在；S01/S03 待人工验证 | ⚠️ 部分确认存在，完整性需验证 |

### 4.3 动效与交互

- SFTP 文件操作（拖拽上传）：**需人工验证**是否支持从 Finder 拖拽文件到 SFTP 面板
- Compose Pane 高度拖拽：已实现（W11）✅
- D04 隧道启动/停止切换动画：**需人工验证**
- 终端内搜索覆层滑入动画：**需人工验证**是否符合 07-终端覆层规范的 Spring 参数

### 4.4 可访问性

- M3 阶段新增 UI 组件（SFTP 面板/D04/D05/设置面板）均未进行系统性可访问性注解（M4 W14.4 才专项处理，且仅覆盖了 SessionRowView / ToolbarButton / SidebarSearchView）
- 新增面板的 VoiceOver 支持：**全部需人工验证**

**UI 规范符合度：中**
（D04/D05/Compose Pane 结构基本符合规范；关键词高亮/同步输入的逻辑对接状态不确定（第6章 vs WBS 矛盾）；SFTP 精确规格和可访问性均需验证）

---

## 5. 技术架构符合度

### 5.1 架构层次遵循

- `SFTPSession` 为独立 `actor`，使用专属 SSH 连接（与终端连接完全分离），符合技术方案 3.4 节设计 ✅
- `LocalPortForwarder` / `RemotePortForwarder` / `Socks5Proxy` 各为独立服务类，通过 `TunnelManager` 统一管理 ✅
- `QuickCommandStore` 使用 `UserDefaults JSON` 持久化，未使用 Core Data；需确认是否符合数据库设计文档（iCloud 同步快捷命令集的设计意图）**需人工验证**
- `HighlightEngine` 为单独服务类，预编译正则，符合性能设计要求 ✅

### 5.2 Swift Concurrency 使用

- `SFTPSession` 作为 `actor` 正确隔离 SFTP 阻塞操作 ✅
- `LocalPortForwarder` 使用 POSIX socket（同步 accept 循环）：此阻塞操作应在独立 `Task.detached` 或专用线程中运行**需人工验证**
- `RemotePortForwarder` 的 forwardListen / forwardAccept 循环：同上，阻塞 libssh2 调用的线程隔离**需人工验证**

### 5.3 安全规范

- SFTP 使用独立 SSH 会话，凭据仍通过 Keychain 获取 ✅
- 端口转发 SOCKS5 代理：RFC 1928 握手实现，仅转发 CONNECT 请求，不存储目标地址 ✅
- 端口转发的本地绑定地址默认 `127.0.0.1`（非 `0.0.0.0`），防止意外对外暴露 — **需人工验证** `LocalPortForwarder` 的 bind 地址

### 5.4 数据模型符合度

| 实体 | PRD/DB 设计 | 实现状态 | 符合度 |
|------|-----------|---------|-------|
| TunnelRule | type/localPort/remoteHost/remotePort/sessionID/autoStart | `TunnelModels.swift` 已定义；是否映射到 Core Data 实体**需验证** | ⚠️ |
| HighlightSet / HighlightRule | keyword/matchMode/foreground/background/fontStyle/triggerNotification | `HighlightEngine.swift` 含规则结构；Core Data 实体映射**需验证** | ⚠️ |
| QuickCommandSet / QuickCommand | name/command/appendNewline/groupID/shortcut | `QuickCommandModels.swift` 已定义；使用 UserDefaults 存储（非 Core Data）可能影响 iCloud 同步能力 | ⚠️ 存储方式存疑 |
| LogConfig | 会话日志配置 | 未在 M3 代码中确认实现（日志录制 W13 标注🔲） | ❌ |

**技术规范符合度：中**
（SFTPSession actor 架构优良；端口转发实现完整；QuickCommand 存储选型（UserDefaults）可能破坏 iCloud 同步能力；HighlightEngine 与终端的对接逻辑不确定）

---

## 6. 风险与问题清单

| 优先级 | 问题描述 | 影响范围 | 建议措施 |
|-------|---------|---------|---------|
| P0 | **WBS 任务表与功能模块进度追踪表存在系统性矛盾**：关键词高亮/终端内搜索/同步输入/设置面板在 WBS 标 ✅，在模块表标 🔲；真实完成状态不明 | 无法确定 M3 实际完成度 | 逐一真机验证：①终端输出 ERROR 是否出现高亮；②终端内搜索是否可跳转；③同步输入是否同步到多标签；④设置面板各项是否持久化 |
| P0 | **W10–W13 全部代码为 untracked files，无 git commit 记录**：SFTP/PortForwarding/Tunnel/QuickCommand/Settings 目录均游离于版本控制之外 | 代码追溯性为零；无法 git blame / git log 定位引入时间；CI 无法感知这部分代码 | 立即执行规范化 commit：`git add` M3 相关目录，按功能模块分批提交，补全 W10/W11/W12/W13 的提交记录 |
| P0 | W7.7（D02/D03 SSH 流程串联）仍未完成，TC-009 无法验收；此缺口从 M2 延续至 M3，未在 M3 阶段修复 | TC-009 验收条件；安全合规 P0 | 参考 M2 Review 建议，立即补全握手阶段的 KnownHostsManager 调用 |
| P0 | TC-010（iCloud 同步）完全无法验收，依赖真实 Apple Developer 账户和 CloudKit 容器激活 | M3 最关键验收条件之一 | 注册 Apple Developer 账户（$99/年），在 App Store Connect 创建 CloudKit 容器，更新 entitlement，双 Mac 端到端测试 |
| P1 | `QuickCommandStore` 使用 `UserDefaults` 存储快捷命令，PRD 3.12.1 明确「快捷命令集」需通过 iCloud 同步 | iCloud 同步完整性（UserDefaults 不通过 CloudKit 同步） | 将 QuickCommand / QuickCommandSet 迁移到 Core Data 实体，纳入 NSPersistentCloudKitContainer 同步范围 |
| P1 | `LogConfig` 实体（会话日志配置）未在 M3 中实现：日志录制功能（W13 原计划）为🔲 | PRD 3.9 日志与录制（P2 功能，但 LogConfig 数据模型是 P0 实体） | 至少完成 `CDLogConfig` Core Data 实体定义（即使 UI 功能延后），保持数据模型完整性 |
| P1 | SFTP 与终端工作目录双向同步（PRD 3.5.4）：cd 命令 → SFTP 跳转、SFTP 双击 → 终端 cd 命令；未见实现 | 高级用户体验功能 | 可在 M4 Buffer 中补全；需监听终端 shell 的 PWD 变化（通过 OSC escape sequence 或 SFTP pwd 命令） |
| P2 | D05 快捷命令管理器的 KeyRecorder（快捷键录制组件）在 Figma 规范 05-弹窗规范中明确要求，但实现状态不明 | 快捷命令快捷键绑定功能 | 验证 `QuickCommandManagerView` 是否包含 KeyRecorder 控件；若无，可用 macOS 原生 `NSEvent` 捕获实现 |
| P2 | TestFlight 内部测试构建（W12.7）未进行 | 没有内部测试数据支持 M3 里程碑演示的质量评估 | 在 Apple Developer 账户激活后补发 TestFlight 内部构建，覆盖核心工程师测试 |

---

## 7. 下一阶段建议

M4 阶段（打磨上架）已进行中（W14+W15 完成），基于 M3 Review 发现，在 W16（App Store 准备）前需重点处理：

1. **紧急：规范化 M3 git 历史** — 将 W10–W13 所有 untracked 代码按模块分批提交；这是代码可追溯性和团队协作的基础，建议将所有 M3 代码补提交，避免带着"幽灵代码"进入发布阶段
2. **功能完整性核查（每项需真机验证 30 分钟内完成）**：
   - 终端输出 "ERROR test" → 是否红色高亮
   - ⌘F 终端内搜索 → 是否可搜索跳转
   - 设置-外观字体修改 → 重启后是否持久化
   - 设置-高亮添加规则 → 是否在终端生效
3. **QuickCommand 存储迁移** — UserDefaults → Core Data，这是 iCloud 同步完整性的前提
4. **W7.7 D02/D03 流程串联** — 在 M4 App Store 提审前必须完成，否则应用存在安全合规缺口

---

## 8. 附录：核查依据

- MRD 版本：v1.0（2026-03-18）
- PRD 版本：v1.0（2026-03-18）
- 技术方案版本：v1.0（2026-03-18）
- 数据库设计文档版本：v1.0（2026-03-18）
- Figma 规范版本：v1.0（shellmate-figma-spec 00–08，含 05-弹窗D04D05、06-设置面板、07-终端覆层、08-缺失组件补充）
- 开发进度文档版本：v1.3（最后更新 2026-03-20）
- 审查 Git 范围：最近 20 次提交（注意：W10–W13 代码均为 untracked，未纳入 git 历史）
- 已有 Review 报告参考：项目Review报告-20260319.md、项目Review报告-M1M2-20260320.md
- 审查时间：2026-03-20 17:35
