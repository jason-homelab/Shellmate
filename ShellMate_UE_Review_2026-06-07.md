# ShellMate UE Review 报告

> **Review 类型**：用户体验全面评审（User Experience Audit）
> **评审视角**：资深 UE 工程师 + 真实用户场景代入
> **评审日期**：2026-06-07
> **评审对象**：ShellMate macOS 原生 SSH 客户端
> **基准设计**：Figma Desktop `OBPyCWFtlCx5OEIXwrckZm`
> **代码版本**：main 分支 @ 16052f0（动态标签页菜单）

---

## 0. 评审方法论

本 Review 不是技术 Review，也不是产品 Review，而是站在**真实用户的椅子上**，按用户进入产品的顺序，逐个动作问三个问题：

1. **我看到了什么？**（信息层）—— 视觉是否清晰、能否一眼读懂
2. **我下一步该做什么？**（决策层）—— 路径是否唯一、CTA 是否明确
3. **我做了之后发生了什么？**（反馈层）—— 是否给我即时反馈、错了能否纠错

评分采用 5 星制，并标注 P0/P1/P2 改进优先级。**P0 = 影响留存的硬伤；P1 = 影响满意度的体验缺陷；P2 = 锦上添花的精致度**。

---

## 1. 总体印象（TL;DR）

| 维度 | 评分 | 简评 |
|---|---|---|
| 视觉品质 | ⭐⭐⭐⭐⭐ | 与 macOS HIG 高度一致，Design Token 系统化，亮/深模式完备 |
| 信息架构 | ⭐⭐⭐⭐ | 主导航清晰，但高级功能"图标墙"问题开始显现 |
| 关键路径流畅度 | ⭐⭐⭐⭐ | 新建会话→连接 3 步触达，但错误恢复路径需优化 |
| 反馈与容错 | ⭐⭐⭐⭐ | Loading/Toast/Alert 覆盖完整，缺骨架屏 |
| 可发现性 | ⭐⭐⭐ | **本次评审最大短板**：AI/Tmux/隧道等高级能力埋藏过深 |
| 无障碍 | ⭐⭐ | **必须改善**：VoiceOver 标签大面积缺失 |
| 性能感知 | ⭐⭐⭐⭐ | 启动预热到位，但大数据列表无虚拟化 |
| 国际化 | ⭐⭐⭐⭐⭐ | 中英对齐近 100%，文案得体 |

**整体得分：8.3 / 10**

**一句话总结**：ShellMate 已经具备了 iTerm2 + Termius 的视觉品质，但在"让用户知道你拥有这些能力"上还差最后一步。

---

## 2. 用户旅程 #1：首次打开应用

### 2.1 场景代入

> *"我刚下载了 ShellMate.dmg，双击图标。我之前用 iTerm2 / Termius，想看看这个国产新品是不是更顺手。"*

### 2.2 体验切片

**✅ 优点**
- **欢迎引导设计精致**：3 步骤 + 径向渐变背景 + 弹性步骤指示器（`WelcomeScreenView.swift:139-154`），第一印象拉满
- **"跳过引导"始终可见**：尊重老用户，不强制走完流程
- **功能特性网格预览**：📁文件传输 / 🤖AI助手 / 📜脚本自动化 / 🔐安全连接 —— 让用户提前知道"这不是只是终端"

**⚠️ 问题**

#### 【P1】欢迎屏只是"看"，没有"做"
当前 3 步骤都是文字说明，**用户走完引导后被空空地丢在主界面**。
- iTerm2/Termius 的做法：引导最后一步直接让用户**新建一个会话**（甚至预填测试服务器）
- 建议：第 3 步增加 "立即新建一个 SSH 会话" 主按钮，点击后直接打开 SessionFormSheet，让用户在引导内就完成"首次连接成功"的高光时刻

#### 【P1】没有"示例会话"或"演示模式"
新用户走完引导后看到的是**完全空的侧边栏**。`EmptyStateView` 虽然漂亮，但只是"请新建会话"的静态提示。
- 建议：检测到首次启动时，自动注入一个 `localhost:22` 的示例会话，并附带说明气泡："这是一个本地示例，你可以编辑或删除"

#### 【P2】Welcome 退场时机不可控
当前 Welcome 用 `@AppStorage("hasLaunchedBefore")` 触发，意味着**一旦看过就再也看不到**。
- 建议：在「设置 → 通用」中提供 "再次显示欢迎引导" 按钮，方便用户回看或推荐给同事

---

## 3. 用户旅程 #2：新建并连接第一个 SSH 会话

### 3.1 场景代入

> *"我要连公司测试机 192.168.100.90，用户名 ubuntu，密码登录。"*

### 3.2 体验切片

**✅ 优点**
- **表单字段顺序符合认知梯度**（`SessionFormSheet.swift:67-162`）：协议 → 主机/端口 → 用户名 → 密码 → 高级设置（折叠） → 分组 → 名称
- **端口智能默认**：选 SSH 自动 22、选 Telnet 自动 23 —— 这是 Termius 都没做到的细节
- **密码 Keychain 存储默认勾选**：符合 macOS 用户预期
- **会话名称可留空**：自动用主机名兜底，**减少新手的"必须填什么"焦虑**
- **校验错误整列显示**（`L218-230`）：不是"修一个错才看下一个"的反人类逐条 Alert

**⚠️ 问题**

#### 【P0】缺少"测试连接"按钮
表单提交前**没有任何途径验证配置是否正确**。用户填完一长串字段，点"创建会话"→ 返回主界面 → 双击连接 → 连接失败 → 重新打开编辑 → 改字段 —— 整个回路 5 步以上。

竞品对比：
- **Termius**：表单底部有 "Test Connection" 按钮
- **iTerm2 Profile**：有 "Test" 调用
- **SecureCRT**：连接前自动 ping

**建议**：在「创建会话」按钮**左侧**加 "测试连接"（次按钮样式），点击后：
- 仅做 TCP 握手 + 认证测试（不打开终端）
- 在按钮下方显示绿色 ✅ "连接成功" 或红色 ⚠ 错误诊断（复用现有 `ConnectionErrorAnalysis`）

#### 【P1】"高级设置"折叠藏得太深
跳板机（Jump Host）、SSH Agent、Passphrase 等**专业用户的必备功能**被折叠隐藏。
- 当前折叠的展开图标过小（推测<16pt），缺少"展开后有什么"的预览
- 建议：把折叠区改为"高级（4 项）"，括号内显示已配置项数；首次悬停时显示 Tooltip："包含 跳板机 / 私钥 / Agent / 心跳"

#### 【P1】密码字段没有"显示密码"切换
当前是纯 `SecureField`，**用户无法核对自己有没有打错密码**。
- 建议：右侧加 👁 图标，按住时显示明文（不是切换，避免误锁屏被同事看到）

#### 【P2】"保存密码到 Keychain" Toggle 缺少说明
对小白用户，"Keychain" 是个黑盒。
- 建议：Toggle 右侧加 ⓘ Hover Tooltip："密码加密存储到 macOS 钥匙串，仅本机可用"

#### 【P2】分组下拉无"新建分组"快捷入口
用户在表单里想给新会话分到"生产环境"，发现没有这个分组，必须**关闭表单 → 去侧边栏新建分组 → 重新打开表单**。
- 建议：分组下拉最底部加 "+ 新建分组..." 选项，触发简化版分组创建 Inline Sheet

---

## 4. 用户旅程 #3：日常使用终端

### 4.1 场景代入

> *"我每天打开 ShellMate，连接 5~8 台服务器，每台开 2~3 个 Tab，时不时复制粘贴日志、搜命令历史。"*

### 4.2 体验切片

**✅ 优点**
- **多 Tab + 拖拽重排**（`TerminalTabBarView.swift:69-77`）：选中 Tab 自动滚入可视区，幽灵效果(opacity 0.45)直观
- **状态栏信息密度高且不杂乱**：连接状态点 / hostname / CPU sparkline / 内存 / 延迟 / 终端尺寸 / Tmux 状态
- **Tmux 窗口快切 Popover**（`TerminalStatusBarView.swift:28-30`）：从状态栏直接切窗口，少了一次 ⌘B 操作
- **字号会话级覆盖**（`TerminalView.swift:111-112`）：临时调大字号看日志，不污染全局设置

**⚠️ 问题**

#### 【P0】Tab 关闭无二次确认 / 无误关闭恢复
日常工作中**最高频的误操作**：连着多个 Tab 时点错位置关掉了正在执行任务的会话。
- 当前行为推测：直接关闭（基于 `TerminalTabBarView.swift` 模式）
- 建议：
  - 检测到 Tab 内有未完成命令（终端最近 5s 有输出）时，关闭前 Alert "该会话正在活动，确认关闭？"
  - 提供 ⌘⇧T 重新打开最近关闭的 Tab（Browser 通用习惯）

#### 【P1】终端内"查找"快捷键发现性差
`TerminalView.swift:124-128` 已经实现搜索覆层（正则 + 大小写 + 计数），但**用户怎么知道有这个功能？**
- 检查：工具栏是否有 🔍 按钮直接对应？Edit 菜单是否有"在终端中查找 ⌘F"？
- 建议：
  - 工具栏 🔍 图标在终端聚焦时高亮（暗示"现在按它会查找终端内容"）
  - 首次启动 Tooltip 引导一次："试试 ⌘F 搜索终端内容"

#### 【P1】复制/粘贴默认行为是否符合 Terminal 用户习惯？
专业用户期待：
- 选中即复制（iTerm2 默认）
- ⌘V 粘贴时检测多行警告（防止粘进 `rm -rf` 等危险命令）
- 粘贴大量内容（>1000 行）时 Alert 提醒

需验证当前 SwiftTerm 配置是否覆盖这些场景。

#### 【P1】无"恢复连接"按钮
SSH 连接因网络问题断开后，状态栏变红 ⚠ "Not Connected"，但**没有显眼的「重连」按钮**。
- 建议：连接失败时，TerminalView 中央显示大号 "重新连接" 按钮（带倒计时自动重连 5s）
- 配合现有 `ConnectionErrorView` 显示具体原因

#### 【P2】TabBar 标签宽度无最小约束
推测在 10+ Tab 时标签会被压缩到只剩图标。
- 建议：达到压缩阈值时显示 "..." Overflow 菜单，避免误点

---

## 5. 用户旅程 #4：发现并使用高级功能（SFTP / Tmux / 隧道 / AI）

### 5.1 场景代入

> *"我用了一周 ShellMate，朋友告诉我'它内置了 AI 和 SFTP'，但我没用过，不知道在哪。"*

### 5.2 体验切片

**这是本次 Review 最关键的章节，也是 ShellMate 与竞品拉开差距的关键战场。**

**⚠️ 核心问题**

#### 【P0】工具栏"图标墙"问题严重
当前 `ContentViewToolbar.swift` 左侧排列 8+ 个 Unicode 图标：⏻ ✦ </> ⇅ ⊡ 日志 命令 隧道

- 用户痛点：
  - **图标语义不直观**：⏻ 是连接？✦ 是 AI？</> 是脚本？普通用户需要逐个 hover Tooltip
  - **没有视觉层级**：所有按钮等权重，看不出哪个常用哪个偶尔用
  - **AI 这种核心卖点被埋没**：✦ 图标在 7 个图标中排第二，但没有任何视觉强调

- 建议：
  - **三段式重组**：
    - 左：连接控制（⏻ 连接 / 断开）— 唯一会话级操作
    - 中：内容增强（✦ AI 助手 — **配色突出**为 accentAI 紫蓝渐变，是核心卖点）
    - 右：会话工具（SFTP / Tmux / 隧道 / 脚本 / 日志 / 命令） — 收纳为「工具」下拉菜单或独立 Sidebar Drawer
  - **首次连接成功后弹出 Onboarding Tooltip**：依次高亮 AI 按钮 → "试试 ✦ AI 助手，让 AI 帮你写命令"

#### 【P1】AI 助手隐私同意流程"打断式"
`AIAssistantPanelView.swift:54-62` 首次启用 AI 时弹同意框。
- 用户路径：打开 AI 面板 → 弹同意 → 阅读 → 同意 → 才能用
- 问题：用户的兴趣在"试试 AI"，被法律文本拦截后流失率高
- 建议：
  - 同意框改为"协议入面板内嵌"：面板打开即显示 AI 介绍 + 输入框（禁用）+ 底部「同意并开始」蓝色 CTA
  - 用户看到示例 prompt + UI 后再同意，转化率显著提高

#### 【P1】SFTP 入口在哪里？
即便看了 Figma 对齐文档我都需要反复确认。代码注释中 SFTP 是 ⇅ 图标。
- 用户问题：⇅ 在我脑中是"排序"或"上下"，不是"文件传输"
- 建议：
  - 改图标为 📁 或更直白的 "Files" 文字按钮
  - 或者把 SFTP 做成**侧边栏可拖拽的 Inspector Drawer**（类似 Finder 边栏），始终可见

#### 【P2】隧道（Tunnel）功能缺少"已启用"指示
用户配置了 3 条端口转发，**关闭应用后是否还运行？重启后状态如何？**
- 建议：状态栏新增"🔗 2 条隧道运行中"小指示器，点击直达 TunnelManagerView

#### 【P2】快捷命令 vs 脚本自动化的区别不明
- **快捷命令**（QuickCommand）：参数化命令片段
- **脚本自动化**（Scripts）：多步流程
- 用户问：我要存一段"清理日志"代码，放哪个？

建议：在两个面板顶部加一句对比说明 + "互转"按钮（命令 → 脚本）。

---

## 6. 用户旅程 #5：错误处理与恢复

### 6.1 场景代入

> *"我连接公司 VPN 内服务器，VPN 偶尔会断。或者我密码改了忘了同步。"*

### 6.2 体验切片

**✅ 优点**
- **错误分类完备**（`ConnectionErrorAnalysis`）：认证失败 / 网络不可达 / Host Key 变更 / 端口被拒
- **针对性建议**：不是"连接失败"四个字，而是"请检查私钥权限是否为 600"等可操作指引
- **Host Key 变更警告**：明确提示 MITM 风险，符合安全心智

**⚠️ 问题**

#### 【P1】错误恢复路径过长
当前流程：连接失败 → 看到 ConnectionErrorView → 关闭 → 侧边栏右键会话 → 编辑 → 修改密码 → 重新连接

建议：ConnectionErrorView 上直接提供操作按钮：
- "重新输入密码"（认证失败时）→ 弹出仅密码字段的 Inline Sheet
- "清除并接受新 Host Key"（Host Key 变更时）
- "检查网络"→ 自动 ping 主机 + 显示结果

#### 【P1】网络断开无主动通知
用户开着 ShellMate 在另一个 App 工作，SSH 连接因网络断开。
- 当前推测：用户切回来才发现状态栏变红
- 建议：连接断开时通过 macOS Notification Center 通知（"会话 prod-01 已断开"），可点击直达会话

#### 【P2】SSH 重试策略不透明
心跳超时多久判定为断开？自动重连次数？
- 建议：设置 → 终端 中暴露这些参数（默认值合理但允许专业用户调整）

---

## 7. 视觉与一致性

### 7.1 评分细则

| 子项 | 评分 | 说明 |
|---|---|---|
| 色彩系统 | ⭐⭐⭐⭐⭐ | DesignTokens 完备，#077aff 品牌色对齐 Figma |
| 字体节奏 | ⭐⭐⭐⭐ | 等宽体 + 系统字结合得当，但缺少"等宽字推荐列表" |
| 间距韵律 | ⭐⭐⭐⭐⭐ | Spacing Token (nano→xxl) 覆盖完整 |
| 圆角语言 | ⭐⭐⭐⭐⭐ | 4/6/8/12/16pt 分级清晰 |
| 动效手感 | ⭐⭐⭐⭐ | 200ms 标准 + Spring 弹性，但部分时长仍硬编码 |
| 亮/深模式 | ⭐⭐⭐⭐⭐ | 自动切换，色板对应 |
| 图标体系 | ⭐⭐⭐ | **Unicode 文本图标在多语言环境下字宽不一致**，建议统一改 SF Symbols |

### 7.2 关键问题

#### 【P1】Unicode 文本图标的字宽风险
`ContentViewToolbar.swift` 用 ⏻ ✦ </> ⇅ ⊡ 等 Unicode 字符 + 中文标签。
- 在英文系统下 ⏻ 和 "Connect" 之间间距正常
- 在中文系统下 ⏻ 和 "连接" 之间可能出现宽度跳变（CJK 全宽字符与英文符号字宽差异）
- 在 macOS 13 vs 14 vs 15 渲染可能不同

建议：迁移至 SF Symbols（`Image(systemName:)`），统一字宽 + 自动适配权重/着色 + Accessibility 自带。

#### 【P2】少量硬编码色值
TunnelModels.swift 中：
- localForward `#7ab4f5`
- remoteForward `#f5c842`
- dynamicSocks `#c88af0`

WelcomeScreenView 中 `#4299fd / #7eb7fb / #bad6f9`。

建议：迁入 `DesignTokens.Colors.semantic.*`，命名为 `tunnelLocal / tunnelRemote / tunnelSocks` 等。

---

## 8. 无障碍与包容性（Accessibility）

### 8.1 现状

⚠️ **本节是 ShellMate 最大短板，建议作为下个迭代 P0 专项。**

调研发现：
- 仅 GroupHeaderView 有 `.accessibilityAction`（展开/折叠）
- 大量功能性 UI（连接状态点、CPU sparkline、AI 消息气泡、Tab 选中态、SFTP 文件行）**完全无 VoiceOver 标签**

### 8.2 具体清单

| 元素 | 当前状态 | 必需标签 |
|---|---|---|
| SessionRowView 连接状态圆点 | 仅视觉颜色 | `.accessibilityLabel("已连接" / "连接中" / "已断开")` |
| TerminalStatusBarView CPU 柱状图 | 无语义 | `.accessibilityLabel("CPU 使用率 45%，过去 8 秒")` |
| TerminalTabBarView 选中 Tab | 仅蓝色背景区分 | `.accessibilityAddTraits(.isSelected)` |
| AI 消息气泡（用户/助手） | 无角色区分 | `.accessibilityLabel("助手回复: ...")` |
| SFTPFileRowViews 文件类型 | 仅图标 | `.accessibilityLabel("文件夹 logs，2026-06-01 修改")` |
| 工具栏按钮 | 仅 `.help()` Tooltip | 加 `.accessibilityLabel()` 以支持 VoiceOver |

### 8.3 建议

- **P0**：为所有交互元素补全 `.accessibilityLabel`，估算工作量 1-2 周（40~50 处）
- **P1**：键盘焦点环（Focus Ring）检查 —— 确保所有按钮 Tab 键可达且有视觉指示
- **P1**：对比度审计 —— `textSecondary` 在深色模式 52% 透明度可能不达 WCAG AA 标准
- **P2**：支持 Dynamic Type（系统字号调整跟随）

---

## 9. 性能感知

### 9.1 良好实践

- ✅ HighlightEngine 启动预热（`ShellMateApp.swift:36-39`）
- ✅ 异步 Core Data 查询
- ✅ ScrollViewReader + LazyVStack 模式

### 9.2 待优化

#### 【P1】缺骨架屏 / Shimmer
所有 Loading 都是 `ProgressView()` 旋转圈，**用户感知不到加载进度**。
建议：
- 侧边栏初次加载：显示 4-5 行灰色占位行（带微动效）
- SFTP 远程文件列表：显示文件行骨架
- AI 流式响应：已实现打字机？验证

#### 【P1】大数据场景压力测试
- SFTP 目录 >1000 文件：是否分页 / 虚拟化？
- 终端历史 >10000 行：滚动是否流畅？
- 日志面板 >5000 条：过滤性能？

建议：用 XCTest 性能测试覆盖这些场景，发现回归。

#### 【P2】启动后台预热范围扩展
HighlightEngine 已预热，可考虑：
- Core Data 持久化容器预初始化
- Keychain 读取常用凭证缓存

---

## 10. 国际化（i18n / l10n）

### 10.1 现状

✅ 中英双语对齐度近 100%（782/783 行），值得称赞。

### 10.2 待优化

#### 【P2】文案"译味"残留
推测部分英文文案是从中文直译而来。建议邀请英文母语者 review，例如：
- "Not Connected" vs "Disconnected" 的微妙差别
- 错误诊断建议中的"please" 使用频率

#### 【P2】日期/时间格式本地化
录制文件、日志时间戳 —— 是否使用 `DateFormatter` 而非硬编码 `yyyy-MM-dd`？

#### 【P2】支持 RTL（阿拉伯语等）的 Layout 预案
当前未支持，但代码中是否大量使用 `.leading / .trailing`（自动镜像）而非 `.left / .right`？

---

## 11. 设置面板深度审视

### 11.1 优点

- ✅ 8 个分类清晰：通用 / 外观 / 终端 / AI / 自动化 / 关键词高亮 / 安全 / iCloud 同步
- ✅ 固定窗口尺寸（600×584pt）避免内容跳动
- ✅ 保存/取消按钮明确

### 11.2 问题

#### 【P1】缺设置搜索框
8 个 Tab 内有 50+ 设置项，"光标闪烁频率" 在哪？"心跳间隔" 在哪？**没有搜索就只能逐 Tab 翻**。
- 参考：macOS System Settings 搜索框
- 建议：Tab 栏右侧加 🔍 输入框，键入关键词高亮所有匹配项

#### 【P1】设置"修改后未保存"无提示
用户切换 Tab 时如果改了未保存的内容，**直接被丢弃**。
- 建议：未保存时切换 Tab 弹 Alert "保存当前更改？"
- 或：取消"保存"按钮，改为即时保存（macOS Settings 现代风格）

#### 【P2】iCloud 同步范围多选无"互斥提示"
若用户只勾选"脚本"不勾选"会话"，但脚本引用了某会话 —— 同步后是否一致？需要 inline 提示。

---

## 12. 已知技术债与遗留问题

### 12.1 代码注释中的明确标记

| 文件 | 标记 | 影响 |
|---|---|---|
| `SessionListView.swift:33` | "BUG-002：双击会触发父级" | 双击交互可能存在事件冒泡，需测试验证 |
| `ScriptLibraryView.swift:12` | "TODO: 录制会话功能（后续迭代实现）" | 录制与脚本整合规划中 |

### 12.2 架构观察

- `TerminalView.swift` 接近 1200 行，建议进一步拆分（已有 TerminalToolbar / TerminalPlaceholder 拆出，但 Panel 逻辑仍混合）
- 部分状态跨越 ContentView → TerminalView → TerminalController 三层，可考虑 Reducer 模式简化

---

## 13. 与竞品对标（关键差距）

| 能力 | ShellMate | iTerm2 | Termius | Tabby |
|---|---|---|---|---|
| 多 Tab 拖拽 | ✅ | ✅ | ✅ | ✅ |
| 分屏（Split） | ✅(Compose Pane) | ✅ | ✅ | ✅ |
| SFTP 集成 | ✅ | ❌ | ✅ | 部分 |
| 内置 AI 助手 | ✅ **领先** | ❌ | ✅(Beta) | ❌ |
| 端口转发 GUI | ✅ | ❌（命令行） | ✅ | ✅ |
| 录制/回放 | ✅ **领先** | ❌ | ❌ | ❌ |
| **测试连接按钮** | ❌ | N/A | ✅ | ✅ |
| **iCloud/云同步** | ✅ | ❌ | ✅(收费) | ❌ |
| 选中即复制 | ? | ✅ | ✅ | ✅ |
| 双击单词选中 | ? | ✅ | ✅ | ✅ |
| 右键拖拽矩形选区 | ? | ✅ | ❌ | ❌ |
| 触发器（关键词高亮） | ✅ | ✅ | 部分 | ❌ |
| 主题导入 (.itermcolors) | ✅ | ✅ | ❌ | 部分 |

**ShellMate 的护城河**：
1. 原生 SwiftUI + AppKit 性能优势
2. AI 助手深度集成
3. 录制回放为审计场景准备
4. iCloud 免费同步

**差距弥补优先级**：
- P0: 测试连接 → 立刻补
- P1: 选中即复制 / 双击单词选中 → 影响日常体验

---

## 14. 改进路线图（优先级排序）

### 🔴 P0 - 阻断/严重缺陷（建议 2 周内启动）

1. **VoiceOver 无障碍标签全面补齐**（40~50 处，1-2 周）
2. **新建会话表单加 "测试连接" 按钮**（1 周）
3. **Tab 误关闭保护 + ⌘⇧T 恢复**（3 天）
4. **BUG-002 双击事件冒泡调查与修复**（2 天）
5. **工具栏图标墙重组**：AI 突出 / 工具收纳（1 周）

### 🟡 P1 - 体验提升（建议 1 个月内）

6. **欢迎引导接入新建会话流程**（3 天）
7. **示例会话/演示模式**（2 天）
8. **错误诊断 Inline 操作按钮**（"重输密码"/"接受新 Host Key"）（1 周）
9. **网络断开 macOS 系统通知**（3 天）
10. **密码字段"按住显示明文"切换**（1 天）
11. **分组下拉内联"+ 新建分组"**（2 天）
12. **AI 隐私同意改 Inline 引导**（3 天）
13. **设置面板加搜索框**（3 天）
14. **设置面板未保存提醒**（2 天）
15. **骨架屏组件库**（侧边栏 + SFTP 列表）（1 周）
16. **终端"重连"显眼按钮**（2 天）
17. **Unicode 图标迁移 SF Symbols**（1 周，配合 VoiceOver 一起做）
18. **多行粘贴危险检测**（3 天）

### 🟢 P2 - 精致度（建议 季度内）

19. Tunnel/Welcome 硬编码色值 Token 化（1 天）
20. 动画时长 Token 化（2 天）
21. 隧道运行中状态栏指示器（2 天）
22. 快捷命令 vs 脚本说明 + 互转（2 天）
23. Tooltip 加 ⓘ 解释（Keychain / Agent 等术语）（1 周）
24. 英文文案母语者 review（外协）
25. Dynamic Type 支持（1 周）
26. 大数据场景性能基准测试（持续）
27. 欢迎引导"再次显示"入口（半天）

---

## 15. 一句话给 PM / 设计 / 研发的建议

- **给 PM**：ShellMate 的核心体验已超越 80% 商业 SSH 客户端，下一步**不是堆功能而是放大已有功能的可发现性**。AI 助手值得做成首屏第一卖点，不要让它和 SFTP / Tmux 平起平坐。
- **给设计**：Figma 对齐工作做得极好，下一阶段建议从"对齐"走向"主导"——把 VoiceOver/Accessibility 也纳入 Figma 设计规范，让设计本身成为无障碍的第一道屏障。
- **给研发**：警惕 `TerminalView.swift` 的膨胀，考虑用 SwiftUI Reducer / TCA 模式进一步解耦；同时为 P0 改进腾出"专题周"，避免持续性 Feature 工作淹没体验债务。

---

## 16. 最后的话

ShellMate 给我留下的印象是：**一款被工程师认真打磨的、面向工程师的工具**。它的视觉品质、Token 系统、Figma 对齐方法论，已经达到了 Anthropic / Linear / Raycast 这一梯队的水准。

但**优秀工具产品的最后一公里，从不在视觉，而在"用户是否知道它有这些能力"以及"在错的瞬间是否还能回到对的路上"**。本次 Review 的 P0/P1 建议，本质上都在补这两个维度的功课。

希望它在 v1.0 正式发布时，能成为我们这一代 macOS 用户的默认 SSH 客户端。

---

**评审人**：Claude (代理资深 UE 工程师视角)
**评审基线代码**：`/Users/jason/shellmate-app` @ main 16052f0
**关联设计文件**：Figma `OBPyCWFtlCx5OEIXwrckZm`
