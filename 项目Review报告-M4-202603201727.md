# ShellMate 项目 Review 报告 — M4 阶段

> **阶段：** M4 — 打磨上架（W14–W17）
> **生成时间：** 2026-03-20 17:27
> **审查范围：** W14–W17（2026-06-16 → 2026-07-11）
> **当前进度：** W14 + W15 已完成，W16 + W17 尚未开始
> **总体评级：** 部分通过 ⚠️

---

## 1. 执行摘要

M4 阶段（打磨上架）目标为将功能完整的产品推向 App Store 正式发布。截至 2026-03-20，W14（Sandbox 审计 + 多版本差异）与 W15（性能优化）的全部 10 项任务均已标记完成，代码层面已完成打包/公证脚本、AppVariant Feature Flag、可访问性注解、60fps 渲染优化及内存控制。同时本阶段还修复了来自 M3 Review 的 5 项 P0 问题（分屏布局、StatusBar 计时、字体设置同步、scrollbackLines 动态配置等）。

然而，W16（App Store 元数据 / 正式提审）与 W17（正式发布 / 运营启动）6 项核心任务全部处于未开始状态，M4 核心验收条件中的 TestFlight 公开测试（≥20 位 Beta 用户）与 Direct DMG 发布均尚未达成。此外，全部 10 个 P0 TC 用例均停留在"代码已生成，待真机/真实环境验收"阶段，TC-010（iCloud 跨设备同步）仍依赖真实 CloudKit 容器。

**整体完成度估算：约 45%**（W14+W15 的 10 项任务完成，W16+W17 的 9 项任务未启动，M4 验收条件 6 项中仅 3 项有技术基础）。

---

## 2. 验收条件核查

| # | 验收条件 | 状态 | 评估说明 |
|---|---------|------|---------|
| 1 | 所有 P0 Bug 清零（P1/P2 Bug < 5 个） | ⚠️ | M3 Review P0 问题 6 项中 5 项已修复（P0-3 iCloud TC-010 待端到端验收）；W7.7（D02/D03 SSH 流程串联）仍未完成，存在已知 P0 级功能缺口；侧边栏拖拽排序（PRD P1）仍未实现 |
| 2 | 冷启动 < 2s，终端渲染 ≥ 60fps | ✅ | W15.1：Task{@MainActor} 预热 HighlightEngine；W15.2：TerminalDataCoalescer actor 合并 16ms 批量数据喂给 SwiftTerm，read buffer 4KB→32KB；均已代码实现，需 Instruments 真机验证 |
| 3 | App Store 代码签名 + 公证（notarytool）通过 | ⚠️ | W14.3：Scripts/archive_appstore.sh + archive_direct.sh + ExportOptions plist 已创建（代码层面完成），但未在真实 Apple Developer 账户上执行完整 notarytool 公证流程，Provisioning Profile 中 iCloud/Keychain Access Group entitlement 尚需正式账户激活 |
| 4 | App Store 元数据（截图×6、描述、关键词）准备完成 | ❌ | W16.1–W16.3 全部未开始：App Store Connect 描述/关键词/分类/隐私政策均未撰写；6 张 MacBook 截图未制作；App 预览视频（可选）未准备 |
| 5 | TestFlight 公开测试 ≥ 20 位 Beta 用户完成反馈 | ❌ | W14.6 TestFlight 公开测试邀请未发出（需真实 App Store Connect 账户操作）；Beta 用户反馈收集 W15.5 仅指代码层面 buffer 调整，非真实 Beta 反馈 |
| 6 | Direct（官网 DMG）版本同步发布 | ❌ | W16.6 官网 DMG 发布页 / 更新日志准备均未开始 |

**本阶段验收通过率：1/6（约 17%）**（仅性能指标具备代码基础，其余均未满足或部分满足）

---

## 3. PRD P0 用例对齐检查

以下为 PRD 第 9 章 10 个 P0 级验收用例的当前状态。M4 阶段的目标是所有 TC 通过真机验收。

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| TC-001 | 密码认证 SSH 连接，终端显示 shell 提示符，延迟 < 3s | ⚠️ | 代码完整（SSH2Connection.connect + SwiftTerm 管线），待 Docker sshd / 真实服务器端到端验收 |
| TC-002 | SSH Key（Ed25519）认证无需输入密码 | ⚠️ | connectWithKey() 已实现，Keychain 存取逻辑已实现，待真机验收 |
| TC-003 | 退出重启后会话配置完整恢复 | ✅ | Core Data 持久化逻辑已验证，SessionStore 启动时 fetchSessions，TC-003 逻辑通过 |
| TC-004 | 3 个标签页并发独立工作 | ⚠️ | ZStack+opacity 多标签架构已修正（2026-03-19），待 3 会话并发真机验收 |
| TC-005 | 断网后网络恢复自动重连 | ⚠️ | TerminalController 指数退避重连状态机（3次，1/2/4s）已实现，待网络模拟验收 |
| TC-006 | SFTP 文件上传，进度队列显示 100% | ⚠️ | SFTPTransferQueue（maxConcurrent=3）+ SFTPTransferProgressView 已实现，待真实 SFTP 服务器验收 |
| TC-007 | 本地端口转发流量正确转发 | ⚠️ | LocalPortForwarder + TunnelManager 已实现，POSIX accept 循环 + bidirectional bridge，待真实服务器验收 |
| TC-008 | ERROR 关键词高亮显示 | ⚠️ | HighlightEngine（预编译正则）+ TerminalDataCoalescer 已实现，待终端实际输出验收 |
| TC-009 | 主机密钥变更阻止自动连接 | ⚠️ | HostKeyChangedWarningView（D03）已实现，但 W7.7（D02/D03 与 SSH 流程对接）仍未完成，存在流程缺口 |
| TC-010 | Mac A 新建会话，Mac B 30s 内同步出现 | ❌ | NSPersistentCloudKitContainer 已初始化，但 CloudKit 容器 identifier 与 Apple Developer entitlement 未激活，无法端到端验收 |

**P0 用例通过情况：1/10 真正完成（TC-003），其余 8 个待真机验收，TC-010 待外部依赖解决**

---

## 4. UI/Figma 规范符合度

### 4.1 设计令牌使用

- `DesignTokens.swift` 已定义颜色令牌体系，基于语义命名（符合 00-总纲原则 A：语义优先于字面值）
- 深色模式强制开启（`ContentView` 添加 `.preferredColorScheme(.dark)`），对应终端工具设计定位，符合 00-总纲的差异化设计边界定义
- 强调色：需人工验证是否使用 `NSColor.controlAccentColor` 而非硬编码色值（00-总纲原则 C）

### 4.2 组件实现符合度

**标题栏（TitleBar）：**
- Figma 规范要求 52pt Unified TitleBar，含 Traffic Lights + 中央标题 + 右侧 4 个 IconBtn
- 当前实现：`TerminalToolbarView` 提供工具栏按钮，分屏 Menu 按钮（W14 P0-1 修复）已加入，需人工验证高度与右侧按钮图标是否与规范一致（`arrow.left.arrow.right` / `list.bullet.rectangle` / `square.grid.2x2` / `gear`）

**标签栏（TabBar）：**
- Figma 规范要求 36pt 高度，Tab 组件包含状态点 + 名称 + 关闭按钮
- `TerminalTabBarView` 已实现，串联 `TabBarStore`，需人工验证 Tab 宽度（约 120–128pt）与激活态视觉样式

**状态栏（StatusBar）：**
- Figma 规范要求 24pt，展示连接状态/SSH版本/用户名/编码/端口/连接时长/网络延迟/终端尺寸/iCloud 同步
- `TerminalStatusBarView` 已添加连接时长（TimelineView 实时更新，M4 P0-4 修复），其余字段如网络延迟（每 30s 更新）、iCloud 同步状态指示器需人工验证

**分屏布局（SplitView）：**
- Figma 规范 Screen 01–D 包含 HSplitView（左右）和 VSplitView（上下）
- `ContentView` 已通过 `SplitLayout` 枚举实现分屏（M4 P0-1 修复），`SplitSessionPickerView` 用于选择第二窗格会话 ✅

**SFTP 面板：**
- `SFTPPanelView` 已通过 `HSplitView` 集成到 `TerminalView`，需人工验证 300pt 宽度、路径导航、列宽调整等细节

### 4.3 动效与交互

- 侧边栏分组折叠 chevron 旋转动画已实现（W2）
- StatusDotView 脉冲动画已实现（W4）
- 标签页切换过渡：需人工验证是否符合 03-动效规范的 Spring 参数
- 连接/断开状态变化过渡：需人工验证

### 4.4 可访问性

- W14.4 专项完成：`SessionRowView`（accessibilityLabel/Hint/Value/Traits）、`ToolbarButton`（accessibilityLabel/Traits）、`SidebarSearchView`（label/hint/清除按钮）均已添加可访问性注解 ✅
- VoiceOver 完整导航路径需真机验证（需配合屏幕阅读器测试）
- Dynamic Type 字体缩放：终端字体通过 `@AppStorage("appearance.fontSize")` 配置，但 UI 文字是否跟随系统 Dynamic Type 需人工验证

**UI 规范符合度：中**
（分屏、StatusBar 连接时长、可访问性注解为本阶段重要推进；多个细节尺寸与图标对应需真机视觉回归验证）

---

## 5. 技术架构符合度

### 5.1 架构层次遵循

- 4 层架构（展示层→应用层→领域服务层→数据层）整体结构清晰：
  - 展示层：SwiftUI Views（ContentView / TerminalView / SFTPPanelView 等）
  - 应用层：SessionStore / TerminalController（@MainActor ObservableObject）
  - 服务层：SSH2Connection / SFTPSession（actor）/ LocalPortForwarder / HighlightEngine
  - 数据层：Core Data（NSPersistentCloudKitContainer）+ Keychain
- AppVariant.swift 实现编译时 `APP_STORE_BUILD` 标志，App Store 版与 Direct 版功能隔离清晰 ✅

### 5.2 Swift Concurrency 使用

- SSH 连接核心使用 async/await，SFTPSession 为独立 `actor`（符合技术方案单独 actor 要求）
- TerminalDataCoalescer 新增为 actor，将 SSH 数据包合并到 16ms 批量处理（W15 性能优化的核心架构改进）✅
- W15.1 冷启动优化使用 `Task { @MainActor }` 预热正则，正确区分主线程与后台上下文
- GCD 残留：需人工 grep 检查是否存在 `DispatchQueue.main.async` 调用，应全部替换为 `@MainActor` 或 `MainActor.run`

### 5.3 安全规范

- Keychain 凭据存取：`KeychainService` 以 `ShellMate_<UUID>` 为 Service 名称，`kSecAttrAccessibleWhenUnlockedThisDeviceOnly` 访问策略 ✅
- App Store 版 Entitlements：`ShellMate.entitlements` 已配置 sandbox/network.client/network.server/files（W14.1 审计完成），生产环境 iCloud+Keychain 组项已注释留档 ✅
- Direct 版：`ShellMate-Direct.entitlements` 含 SSH_AUTH_SOCK 临时异常，允许访问本地 SSH Agent ✅
- 安全日志审计：凭据是否有可能泄漏到控制台/日志需人工验证（CLAUDE.md 安全第一原则）

### 5.4 数据模型符合度

- PRD 定义的 `Session` 模型含 23 个字段（jumpHosts / tunnelRules / highlightSetIDs / quickCommandSetID / logConfig 等）
- Core Data `.xcdatamodeld` 实体完整性：W1.4 状态标注"API 模型已建，需核对 .xcdatamodeld 实体完整性"，该核查工作在开发进度中未被明确标记为已完成
- CloudKit 兼容性：Core Data ordered 关系已移除（W9.6 修复，CloudKit 不支持 ordered relationship）✅
- `TunnelModels.swift` / `QuickCommandModels.swift` / `SFTPModels.swift` 均已实现，与 PRD 数据模型结构基本对齐

**技术规范符合度：良**
（架构分层清晰，Swift Concurrency 使用规范，安全存储合理；GCD 残留待核查，xcdatamodeld 实体完整性待确认）

---

## 6. 风险与问题清单

| 优先级 | 问题描述 | 影响范围 | 建议措施 |
|-------|---------|---------|---------|
| P0 | TC-001/002/004/005/006/007/008/009 共 8 个 P0 用例仅代码实现，未完成真机端到端验收 | M4 发布目标 | 立即搭建 Docker sshd 测试环境（192.168.100.167 已有真机），按 TC 顺序逐一验收 |
| P0 | TC-009（密钥变更警告）：W7.7 D02/D03 与 SSH 连接流程串联仍未完成，指纹弹窗不会在真实连接中触发 | 安全核心功能 | 补全 SSHConnection 握手阶段对 KnownHostsManager 的调用，串联 HostKeyChangedWarningView 弹出逻辑 |
| P0 | W16/W17 全部 9 项任务未开始：App Store 元数据、截图、正式提审、DMG 发布均为空白 | 发布计划（M4 核心目标） | 按计划时间线（W16: 2026-06-30 → 2026-07-04）启动；提前准备 App Store Connect 账户；截图制作需设计师配合 |
| P0 | TestFlight 公开测试（≥20 位 Beta 用户）未启动，W14.6 因需真实 App Store Connect 账户而搁置 | Beta 反馈质量 | 注册 Apple Developer 账户（$99/年），上传 TestFlight 构建，发放邀请码给目标用户群 |
| P1 | TC-010（iCloud 同步）依赖真实 CloudKit 容器 identifier + Apple Developer Entitlement，无法在当前环境验收 | 跨设备同步核心功能 | 账户激活后，在 App Store Connect 创建 CloudKit 容器，更新 entitlement，双 Mac 端到端测试 |
| P1 | xcdatamodeld 实体完整性未明确验收（W1.4 状态为"需核对"，后续无确认记录） | 数据层基础稳定性 | 对比 PRD 数据模型 6.1-6.10 与 `ShellMate.xcdatamodeld` 实体定义，逐字段核查 |
| P1 | 侧边栏拖拽排序（W3.3）仍未实现，PRD 3.1.2 明确要求 DragGesture + sortOrder 更新 | PRD P1 功能缺口 | 实现 DragGesture + Core Data sortOrder 更新，可在 W16 Bug 修复 Buffer 中补全 |
| P1 | 部分快捷键未实现：⌘T（新建空标签）/ ⌘⇧]⌘⇧[（标签切换）/ ⌘⌥→↓（分屏）/ ⌃Return（Compose Pane 发送）等 PRD 8 章定义的快捷键需逐一验证 | 效率操作体验 | 在 ContentView 和 TerminalView 中补充 `.keyboardShortcut()` 修饰符，对照 PRD 8.1-8.3 全表核查 |
| P2 | GCD 残留风险：部分早期代码可能存在 `DispatchQueue.main.async` 调用，与 Swift Concurrency 规范不符 | 线程安全 / 代码规范 | 执行 `grep -r "DispatchQueue" ShellMate/` 全量检查，逐一替换为 async/await |
| P2 | W10-W13（M3 阶段）代码均为未提交的 untracked 文件或直接修改，git 历史缺失 W10/W11/W12/W13 对应提交 | 代码审计与追溯 | 对 M3 代码进行 git add + commit，补全每周提交记录，便于日后问题追溯 |
| P2 | 本地化（i18n）：PRD 5.5 要求首发简体中文+英文双语，strings 文件外部化；当前代码中 UI 文字多为硬编码中文字符串 | 发布合规性 | 在提审前完成关键 UI 字符串外部化，至少建立 zh-Hans.lproj/en.lproj 双语支持 |

---

## 7. 下一阶段建议

M4 仍是当前阶段，距离发布目标（2026-07-11）尚有 W16+W17 两周。基于本次 Review 发现，建议按以下优先级推进：

**紧急（本周内）：**
1. **搭建真实测试环境** — 利用已知测试服务器（192.168.100.167，ubuntu/Int3l@123）立即验收 TC-001 密码认证，以此为突破口依次验收 TC-002 至 TC-009
2. **补全 W7.7**（D02/D03 SSH 流程串联）— 这是 TC-009 的前提，也是安全合规的 P0 要求
3. **补全 M3 git 提交记录** — 将 untracked 的 W10-W13 代码进行规范化提交

**W16 阶段（2026-06-30 起）：**
1. 注册 Apple Developer 账户，激活 App Store Connect 与 CloudKit 容器
2. 执行正式 Archive + notarytool 公证（基于已有脚本 Scripts/archive_appstore.sh）
3. 撰写 App Store 元数据（描述文案建议强调"零系统依赖/Keychain 本地存储/iCloud 同步"三大差异化卖点）
4. 制作 6 张截图（主窗口默认态 / 多标签 / SFTP 面板 / 端口转发 / 快捷命令 / 设置面板）
5. 补全侧边栏拖拽排序（W3.3）与快捷键全表

**W17 阶段（2026-07-07 起）：**
1. 监控 App Store 审核状态，准备加急审核预案
2. 同步发布官网 DMG Direct 版本
3. 向目标社区（V2EX / 少数派 / Product Hunt / GitHub）发布发现公告

---

## 8. 附录：核查依据

- MRD 版本：v1.0（2026-03-18）
- PRD 版本：v1.0（2026-03-18）
- 技术方案版本：v1.0（2026-03-18）
- Figma 规范版本：v1.0（2026-03-18，01-界面布局规范 已更新至 v1.1）
- 开发进度文档版本：v1.3（最后更新 2026-03-20）
- 审查 Git 范围：最近 20 次提交（最新：0a86a31 perf(terminal): W15 全面性能优化）
- 审查时间：2026-03-20 17:27
- 参考已有 Review 报告：项目Review报告-M1M2-20260320.md、项目Review报告-M3-20260320.md
