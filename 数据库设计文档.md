# ShellMate for macOS — 数据库设计文档

> **文档版本：** v1.0
> **创建日期：** 2026-03-18
> **文档类型：** 数据库设计文档（Database Design Document）
> **基于：** 技术方案.md v1.0 · ShellMate PRD v1.0
> **存储引擎：** Core Data（SQLite 后端）+ CloudKit Private Database + macOS Keychain

---

## 目录

1. [数据存储架构总览](#1-数据存储架构总览)
2. [Core Data 实体设计](#2-core-data-实体设计)
3. [实体关系图（ER Diagram）](#3-实体关系图)
4. [字段详细说明](#4-字段详细说明)
5. [索引设计](#5-索引设计)
6. [Keychain 存储设计](#6-keychain-存储设计)
7. [文件系统存储](#7-文件系统存储)
8. [CloudKit 同步设计](#8-cloudkit-同步设计)
9. [数据迁移策略](#9-数据迁移策略)
10. [数据安全设计](#10-数据安全设计)

---

## 1. 数据存储架构总览

### 1.1 三层存储策略

ShellMate 根据数据敏感度和同步需求，将数据分配到三个存储层：

```
┌─────────────────────────────────────────────────────────────────┐
│           第一层：Core Data（SQLite）                             │
│   跨设备共享的业务配置数据（通过 CloudKit Private DB 同步）        │
│   Session / SessionGroup / HighlightSet / TunnelRule            │
│   QuickCommandSet / TerminalProfile / AppSettings              │
└─────────────────────────────────────────────────────────────────┘
           ↕ NSPersistentCloudKitContainer 自动同步

┌─────────────────────────────────────────────────────────────────┐
│           第二层：macOS Keychain                                  │
│   本设备专属的安全凭据（不跨设备，不进入 CloudKit）               │
│   SSH 密码 / 私钥 Passphrase / 主密码摘要 / 私钥原文             │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│           第三层：文件系统（~/Library/）                          │
│   本地非结构化数据（不同步）                                      │
│   known_hosts / 日志文件 / 终端录制文件 / 私钥文件               │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 存储选型依据

| 数据类型 | 存储位置 | 原因 |
|---------|---------|------|
| 会话配置 | Core Data | 结构化、需要 iCloud 同步 |
| SSH 密码 | Keychain | 安全飞地，系统级加密保护 |
| 私钥文件 | Keychain（Data）| 不落磁盘明文，App Sandbox 限制文件系统路径 |
| known_hosts | 文件系统 | 兼容 OpenSSH 格式，不含敏感数据 |
| 日志/录制 | 文件系统 | 体积大，不适合数据库存储 |

---

## 2. Core Data 实体设计

### 2.1 实体总览

| 实体名 | 中文说明 | 记录量估算 | CloudKit 同步 |
|--------|---------|-----------|--------------|
| `CDSession` | SSH 会话配置 | ≤ 500 | ✅ |
| `CDSessionGroup` | 会话分组 | ≤ 50 | ✅ |
| `CDJumpHost` | 跳板机节点 | ≤ 200 | ✅ |
| `CDTunnelRule` | 端口转发规则 | ≤ 200 | ✅ |
| `CDHighlightSet` | 关键词高亮规则集 | ≤ 20 | ✅ |
| `CDHighlightRule` | 单条高亮规则 | ≤ 500 | ✅ |
| `CDQuickCommandSet` | 快捷命令集 | ≤ 20 | ✅ |
| `CDQuickCommand` | 单条快捷命令 | ≤ 200 | ✅ |
| `CDTerminalProfile` | 终端外观配置 | ≤ 20 | ✅ |
| `CDAppSettings` | 全局应用设置 | 1（单例） | ✅ |

### 2.2 CDSession（会话配置）

```
实体名:    CDSession
对应模型:  Session（Swift struct）
```

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键，全局唯一 |
| `name` | String | String | 否 | — | 显示名称，如「prod-web-01」 |
| `host` | String | String | 否 | — | 主机名或 IP |
| `port` | Integer 32 | Int32 | 否 | 22 | SSH 端口 |
| `username` | String | String | 否 | — | 登录用户名 |
| `authMethodRaw` | Integer 16 | Int16 | 否 | 0 | 认证方式枚举值（见枚举表）|
| `keychainRef` | String | String? | 是 | nil | Keychain 条目的 UUID 引用 |
| `keepAliveInterval` | Integer 32 | Int32 | 否 | 60 | Keepalive 心跳间隔（秒）|
| `autoReconnect` | Boolean | Bool | 否 | true | 断连后自动重连 |
| `encoding` | String | String | 否 | "UTF-8" | 字符编码，可选 GBK 等 |
| `notesEncrypted` | Binary Data | Data? | 是 | nil | AES-GCM 加密的备注（主密码保护）|
| `tagsJSON` | String | String? | 是 | nil | 标签 JSON 数组，如 `["prod","web"]` |
| `iconName` | String | String? | 是 | nil | 自定义 SF Symbol 图标名 |
| `sortOrder` | Integer 32 | Int32 | 否 | 0 | 同组内排序序号 |
| `colorHex` | String | String? | 是 | nil | 行标记颜色（如「#E53935」）|
| `lastConnectedAt` | Date | Date? | 是 | nil | 最后一次成功连接时间 |
| `createdAt` | Date | Date | 否 | now() | 创建时间 |
| `modifiedAt` | Date | Date | 否 | now() | 最后修改时间（冲突仲裁用）|
| `isDeleted` | Boolean | Bool | 否 | false | 软删除标记 |

**关系：**

| 关系名 | 目标实体 | 类型 | 删除规则 | 说明 |
|--------|---------|------|---------|------|
| `group` | CDSessionGroup | 多对一 | Nullify | 所属分组，可为 nil（顶层）|
| `jumpHosts` | CDJumpHost | 一对多 | Cascade | 跳板机链路，有序数组 |
| `tunnelRules` | CDTunnelRule | 一对多 | Cascade | 关联的端口转发规则 |
| `terminalProfile` | CDTerminalProfile | 多对一 | Nullify | 外观配置，nil 则继承分组配置 |
| `highlightSets` | CDHighlightSet | 多对多 | Nullify | 关联高亮规则集 |
| `quickCommandSet` | CDQuickCommandSet | 多对一 | Nullify | 关联快捷命令集 |

**authMethodRaw 枚举值：**

| 值 | 含义 |
|----|------|
| 0 | password（密码认证）|
| 1 | publicKey（SSH Key 认证）|
| 2 | sshAgent（SSH Agent 认证）|
| 3 | keyboard（键盘交互）|

---

### 2.3 CDSessionGroup（会话分组）

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键 |
| `name` | String | String | 否 | — | 分组显示名称 |
| `colorHex` | String | String | 否 | "#888888" | 左侧色条颜色 |
| `sortOrder` | Integer 32 | Int32 | 否 | 0 | 排序序号 |
| `isExpanded` | Boolean | Bool | 否 | true | 侧边栏折叠态（本地状态，也持久化）|
| `modifiedAt` | Date | Date | 否 | now() | 修改时间 |

**关系：**

| 关系名 | 目标实体 | 类型 | 删除规则 | 说明 |
|--------|---------|------|---------|------|
| `parent` | CDSessionGroup | 多对一（自引用）| Nullify | 父分组，nil 为顶层 |
| `children` | CDSessionGroup | 一对多（自引用）| Cascade | 子分组列表 |
| `sessions` | CDSession | 一对多 | Nullify | 该分组下的会话 |
| `inheritedProfile` | CDTerminalProfile | 多对一 | Nullify | 子会话继承的外观配置 |

---

### 2.4 CDJumpHost（跳板机节点）

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键 |
| `host` | String | String | 否 | — | 跳板机主机名或 IP |
| `port` | Integer 32 | Int32 | 否 | 22 | SSH 端口 |
| `username` | String | String | 否 | — | 登录用户名 |
| `authMethodRaw` | Integer 16 | Int16 | 否 | 0 | 认证方式（同 CDSession）|
| `keychainRef` | String | String? | 是 | nil | Keychain 密码/密钥引用 |
| `sortOrder` | Integer 32 | Int32 | 否 | 0 | 跳板链路中的顺序（从 0 起）|

**关系：**

| 关系名 | 目标实体 | 类型 | 删除规则 | 说明 |
|--------|---------|------|---------|------|
| `session` | CDSession | 多对一 | Cascade | 所属会话 |

---

### 2.5 CDTunnelRule（端口转发规则）

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键 |
| `tunnelTypeRaw` | Integer 16 | Int16 | 否 | 0 | 隧道类型枚举（见枚举表）|
| `localBindAddress` | String | String | 否 | "127.0.0.1" | 本地绑定地址 |
| `localPort` | Integer 32 | Int32 | 否 | — | 本地监听端口 |
| `remoteHost` | String | String | 否 | — | 远端目标主机 |
| `remotePort` | Integer 32 | Int32 | 否 | — | 远端目标端口 |
| `autoStart` | Boolean | Bool | 否 | false | 会话建立后自动启动 |
| `notes` | String | String | 否 | "" | 备注说明 |
| `modifiedAt` | Date | Date | 否 | now() | 修改时间 |

**tunnelTypeRaw 枚举值：**

| 值 | 含义 | 说明 |
|----|------|------|
| 0 | localForward | 本地端口转发（-L）|
| 1 | remoteForward | 远程端口转发（-R）|
| 2 | dynamicSocks | SOCKS5 动态代理（-D）|

**关系：**

| 关系名 | 目标实体 | 类型 | 删除规则 |
|--------|---------|------|---------|
| `session` | CDSession | 多对一 | Cascade |

---

### 2.6 CDHighlightSet / CDHighlightRule（关键词高亮）

**CDHighlightSet：**

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键 |
| `name` | String | String | 否 | — | 规则集名称，如「系统错误」 |
| `isGlobal` | Boolean | Bool | 否 | false | true：应用于所有会话 |
| `sortOrder` | Integer 32 | Int32 | 否 | 0 | 排列顺序 |
| `modifiedAt` | Date | Date | 否 | now() | 修改时间 |

**关系：**

| 关系名 | 目标实体 | 类型 | 删除规则 | 说明 |
|--------|---------|------|---------|------|
| `rules` | CDHighlightRule | 一对多（有序）| Cascade | 规则列表，NSOrderedSet |
| `sessions` | CDSession | 多对多 | Nullify | 关联的会话 |

**CDHighlightRule：**

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键 |
| `keyword` | String | String | 否 | — | 关键词或正则表达式 |
| `isRegex` | Boolean | Bool | 否 | false | 是否为正则模式 |
| `caseSensitive` | Boolean | Bool | 否 | false | 是否大小写敏感 |
| `foregroundColorHex` | String | String? | 是 | nil | 前景色（如「#FFFFFF」）|
| `backgroundColorHex` | String | String? | 是 | nil | 背景色，nil 表示透明 |
| `isBold` | Boolean | Bool | 否 | false | 加粗 |
| `isItalic` | Boolean | Bool | 否 | false | 斜体 |
| `isUnderline` | Boolean | Bool | 否 | false | 下划线 |
| `triggerNotification` | Boolean | Bool | 否 | false | 匹配时推送系统通知 |
| `triggerSound` | Boolean | Bool | 否 | false | 匹配时播放提示音 |
| `sortOrder` | Integer 32 | Int32 | 否 | 0 | 规则集内的顺序 |

---

### 2.7 CDQuickCommandSet / CDQuickCommand（快捷命令）

**CDQuickCommandSet：**

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键 |
| `name` | String | String | 否 | — | 命令集名称，如「运维常用」 |
| `isDefault` | Boolean | Bool | 否 | false | 是否为默认命令集 |
| `sortOrder` | Integer 32 | Int32 | 否 | 0 | 排列顺序 |
| `modifiedAt` | Date | Date | 否 | now() | 修改时间 |

**CDQuickCommand：**

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键 |
| `title` | String | String | 否 | — | 命令显示名称 |
| `content` | String | String | 否 | — | 命令内容（支持多行）|
| `appendNewline` | Boolean | Bool | 否 | true | 末尾自动追加回车 |
| `lineByLine` | Boolean | Bool | 否 | false | 多行内容是否逐行发送 |
| `lineDelay` | Integer 32 | Int32 | 否 | 50 | 逐行发送的间隔（毫秒）|
| `shortcutKeyJSON` | String | String? | 是 | nil | 快捷键 JSON，如 `{"key":"1","modifiers":["cmd","shift"]}` |
| `sortOrder` | Integer 32 | Int32 | 否 | 0 | 命令集内的顺序 |

---

### 2.8 CDTerminalProfile（终端外观配置）

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 自动生成 | 主键 |
| `name` | String | String | 否 | — | 配置名称，如「默认 Dark」 |
| `themeJSON` | String | String | 否 | — | 颜色主题 JSON（16色+背景色等完整配色）|
| `fontFamily` | String | String | 否 | "JetBrains Mono" | 等宽字体名 |
| `fontSize` | Float | Float | 否 | 13.0 | 字号（pt）|
| `lineHeight` | Float | Float | 否 | 1.4 | 行间距倍数 |
| `ligatures` | Boolean | Bool | 否 | true | 字体连字 |
| `nonAsciiFontFamily` | String | String? | 是 | nil | 非 ASCII 字体（CJK）|
| `cursorShapeRaw` | Integer 16 | Int16 | 否 | 0 | 光标形状：0=块状 1=竖线 2=下划线 |
| `cursorBlink` | Boolean | Bool | 否 | true | 光标闪烁 |
| `cursorColorHex` | String | String? | 是 | nil | 光标自定义颜色 |
| `backgroundOpacity` | Float | Float | 否 | 1.0 | 背景透明度 0.0–1.0 |
| `vibrancyEnabled` | Boolean | Bool | 否 | false | 启用 macOS Vibrancy 模糊 |
| `paddingTop` | Integer 16 | Int16 | 否 | 12 | 终端内边距（pt）|
| `paddingBottom` | Integer 16 | Int16 | 否 | 12 | 同上 |
| `paddingLeft` | Integer 16 | Int16 | 否 | 14 | 同上 |
| `paddingRight` | Integer 16 | Int16 | 否 | 14 | 同上 |
| `scrollbackLines` | Integer 32 | Int32 | 否 | 10000 | 回滚缓冲行数 |
| `isBuiltIn` | Boolean | Bool | 否 | false | 内置主题不可删除 |
| `modifiedAt` | Date | Date | 否 | now() | 修改时间 |

**`themeJSON` 字段结构示例：**

```json
{
  "background": "#0C0C0E",
  "foreground": "#EEEDF5",
  "cursor":     "#EEEDF5",
  "black":      "#3B3852",  "brightBlack":   "#6B6A74",
  "red":        "#F04060",  "brightRed":     "#F07080",
  "green":      "#2DCE7A",  "brightGreen":   "#5DEEAA",
  "yellow":     "#F5C842",  "brightYellow":  "#F5D882",
  "blue":       "#3D8EF0",  "brightBlue":    "#6DAEFC",
  "magenta":    "#C77DFF",  "brightMagenta": "#D79DFF",
  "cyan":       "#2EC9D0",  "brightCyan":    "#5EE9F0",
  "white":      "#EEEDF5",  "brightWhite":   "#FFFFFF"
}
```

---

### 2.9 CDAppSettings（全局应用设置，单例）

| 字段名 | Core Data 类型 | Swift 类型 | 可空 | 默认值 | 说明 |
|--------|--------------|-----------|------|--------|------|
| `id` | UUID | UUID | 否 | 固定值 | 使用固定 UUID 作为单例标识 |
| `masterPasswordEnabled` | Boolean | Bool | 否 | false | 是否启用主密码 |
| `autoLockAfterMinutes` | Integer 32 | Int32 | 否 | 0 | 0=不自动锁定 |
| `defaultProfileID` | UUID | UUID? | 是 | nil | 默认终端外观配置 |
| `sidebarWidth` | Float | Float | 否 | 220 | 侧边栏宽度（pt）|
| `iCloudSyncEnabled` | Boolean | Bool | 否 | true | iCloud 同步开关 |
| `syncSessions` | Boolean | Bool | 否 | true | 同步会话配置 |
| `syncHighlights` | Boolean | Bool | 否 | true | 同步高亮规则 |
| `syncCommands` | Boolean | Bool | 否 | true | 同步快捷命令 |
| `syncAppearance` | Boolean | Bool | 否 | true | 同步外观配置 |
| `showStatusBar` | Boolean | Bool | 否 | true | 显示底部状态栏 |
| `modifiedAt` | Date | Date | 否 | now() | 修改时间 |

---

## 3. 实体关系图

```
CDSessionGroup ◄──── CDSessionGroup（父子自引用，支持无限嵌套）
      │  1
      │
      │ N
CDSession ──────────────┬──── CDJumpHost（有序，sortOrder）
      │                 │
      │                 └──── CDTunnelRule
      │
      ├──── CDTerminalProfile（N:1，可继承自分组）
      │
      ├──── CDHighlightSet（N:M，关联表）
      │
      └──── CDQuickCommandSet（N:1）


CDHighlightSet ──1:N(ordered)──► CDHighlightRule

CDQuickCommandSet ──1:N(ordered)──► CDQuickCommand

CDAppSettings（单例，不与其他实体关联）
CDTerminalProfile（被 CDSession / CDSessionGroup 引用）
```

**关系核心约束：**

| 约束 | 说明 |
|------|------|
| CDSession.group 可为 nil | 会话可在顶层，不强制归属分组 |
| CDSessionGroup.parent 可为 nil | 顶层分组无父节点 |
| CDJumpHost.sortOrder 决定连接顺序 | 0 = 第一跳，递增 |
| CDHighlightSet.rules 使用 NSOrderedSet | 规则顺序影响匹配优先级 |
| CDTerminalProfile.isBuiltIn = true | 内置主题的 `name` 字段在界面显示为「系统」前缀，不可删除 |

---

## 4. 字段详细说明

### 4.1 枚举字段映射

所有枚举以 `Integer 16` 存储，在 Swift 层通过 Enum 转换：

```swift
// AuthMethod
enum AuthMethod: Int16 {
    case password      = 0
    case publicKey     = 1
    case sshAgent      = 2
    case keyboardInteractive = 3
}

// TunnelType
enum TunnelType: Int16 {
    case localForward  = 0
    case remoteForward = 1
    case dynamicSocks  = 2
}

// CursorShape
enum CursorShape: Int16 {
    case block     = 0
    case iBeam     = 1
    case underline = 2
}
```

### 4.2 JSON 字段规范

使用 JSON 字段存储非关系型动态数据，避免过度设计实体。

**tagsJSON（CDSession）：**
```json
["prod", "web", "nginx"]
```

**shortcutKeyJSON（CDQuickCommand）：**
```json
{
  "key": "1",
  "modifiers": ["command", "shift"]
}
```

**themeJSON（CDTerminalProfile）：** 见 §2.8，固定 16 色键名。

> ⚠️ JSON 字段变更需要应用层迁移（在新版本 App 中读取旧格式时做兼容处理），不触发 Core Data 版本迁移。

### 4.3 modifiedAt 字段规范

- 所有参与 CloudKit 同步的实体均包含 `modifiedAt: Date` 字段
- 每次 Save 前在 Repository 层自动更新：

```swift
func save(_ session: Session) async throws {
    let cd = try fetchOrCreate(id: session.id)
    cd.modifiedAt = Date()  // 自动更新
    // ... 其他字段赋值
    try context.save()
}
```

- CloudKit 冲突仲裁依据：`modifiedAt` 更大的版本胜出

---

## 5. 索引设计

Core Data 在 `.xcdatamodeld` 中配置复合索引，对应 SQLite 的 `CREATE INDEX`：

| 实体 | 索引字段 | 类型 | 用途 |
|------|---------|------|------|
| CDSession | `id` | Unique | 主键查找 |
| CDSession | `name` | Non-unique | 侧边栏搜索（模糊匹配 name/host/username）|
| CDSession | `host` | Non-unique | IP 搜索 |
| CDSession | `isDeleted` | Non-unique | 过滤软删除记录 |
| CDSession | `(group, sortOrder)` | 复合 | 按分组+排序加载 |
| CDSession | `lastConnectedAt` | Non-unique | 最近连接排序 |
| CDSessionGroup | `id` | Unique | 主键查找 |
| CDSessionGroup | `parent` | Non-unique | 拉取子分组 |
| CDTunnelRule | `session` | Non-unique | 按会话加载所有隧道规则 |
| CDHighlightRule | `highlightSet` | Non-unique | 按规则集加载所有规则 |
| CDQuickCommand | `commandSet` | Non-unique | 按命令集加载 |

---

## 6. Keychain 存储设计

### 6.1 条目类型与 Key 命名

| 存储内容 | kSecClass | kSecAttrAccount | 说明 |
|---------|----------|----------------|------|
| SSH 密码 | GenericPassword | `pwd_{sessionID}` | Session 的密码凭据 |
| 私钥 Passphrase | GenericPassword | `pp_{keyID}` | SSH 私钥的解锁密语 |
| 私钥文件内容 | GenericPassword | `key_{keyID}` | 私钥原文（Binary Data）|
| 跳板机密码 | GenericPassword | `pwd_{jumpHostID}` | 跳板机密码 |
| 主密码摘要 | GenericPassword | `masterPasswordHash` | PBKDF2 摘要，不存明文 |

> `sessionID`、`keyID`、`jumpHostID` 均为 UUID 字符串

### 6.2 访问控制（kSecAttrAccessible）

| 数据 | 访问控制 | 说明 |
|------|---------|------|
| SSH 密码 | `WhenUnlockedThisDeviceOnly` | 设备锁屏后不可读，不迁移至新设备 |
| 私钥 Passphrase | `WhenUnlockedThisDeviceOnly` | 同上 |
| 私钥文件内容 | `WhenUnlockedThisDeviceOnly` | 同上 |
| 主密码摘要 | `AfterFirstUnlockThisDeviceOnly` | 允许后台重启后首次解锁时验证，不迁移 |

### 6.3 Keychain 与 Core Data 的关联桥接

```
CDSession.keychainRef = "abc123-..." （UUID 字符串）
         ↓
Keychain 查询:
  kSecAttrAccount = "pwd_abc123-..."
  kSecAttrService = "app.shellmate"
```

**设计原则：** Core Data 中只存 `keychainRef`（UUID），不存任何凭据内容本身。新设备同步 Session 后，`keychainRef` 存在但对应的 Keychain 条目不存在，App 检测到后提示用户重新输入密码。

---

## 7. 文件系统存储

### 7.1 目录结构

```
~/Library/Application Support/ShellMate/
├── known_hosts                    ← 兼容 OpenSSH 格式的已信任服务器指纹
├── logs/
│   ├── {sessionID}/
│   │   └── {YYYYMMDD}.log         ← 按天分片的会话日志（UTF-8 纯文本）
│   └── ...
└── recordings/
    └── {sessionID}/
        └── {timestamp}.cast       ← asciinema .cast 格式的终端录制文件

~/Library/Preferences/
└── app.shellmate.plist            ← 非敏感 NSUserDefaults（UI 状态等）
```

### 7.2 known_hosts 格式

兼容 OpenSSH `~/.ssh/known_hosts` 格式：

```
# 格式: hostname[,ip] key-type base64-encoded-key
prod-web-01,10.0.1.10 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5...
[bastion.example.com]:2222 ssh-rsa AAAAB3NzaC1yc2E...
```

**程序内解析结构（内存中）：**

```swift
struct KnownHostEntry {
    let patterns: [String]   // 主机名模式列表（含 IP）
    let keyType: String      // "ssh-ed25519" / "ssh-rsa" 等
    let keyData: Data        // Base64 解码后的原始密钥数据
    let fingerprint: String  // SHA256 指纹（用于展示）
    let addedAt: Date        // 从文件 mtime 推断或内部记录
}
```

### 7.3 日志文件规范

| 属性 | 规范 |
|------|------|
| 格式 | UTF-8 纯文本，含时间戳前缀 |
| 文件名 | `YYYYMMDD.log` |
| 轮转策略 | 超过 10MB 新建文件，`YYYYMMDD_part2.log` |
| 保留策略 | 默认保留 30 天，可在设置中调整 |
| 写入方式 | 异步写入（ioQueue），不阻塞终端渲染 |

### 7.4 录制文件规范（asciinema .cast v2）

```json
{"version": 2, "width": 220, "height": 50, "timestamp": 1710748800, "title": "prod-web-01"}
[0.123, "o", "ubuntu@server:~$ "]
[0.456, "o", "ls -la\r\n"]
[1.234, "o", "total 48\r\n"]
```

---

## 8. CloudKit 同步设计

### 8.1 CloudKit Container 配置

```
Container ID:   iCloud.app.shellmate.sessions
Database Type:  Private Database（用户私有，不与他人共享）
Zone:           _defaultZone（使用默认区域，简化冲突处理）
```

### 8.2 同步范围

| 实体 | 同步 | 说明 |
|------|------|------|
| CDSession | ✅ | 核心业务数据 |
| CDSessionGroup | ✅ | 分组结构 |
| CDJumpHost | ✅ | 跳板机配置 |
| CDTunnelRule | ✅ | 端口转发规则 |
| CDHighlightSet / Rule | ✅ | 高亮规则 |
| CDQuickCommandSet / Command | ✅ | 快捷命令 |
| CDTerminalProfile | ✅ | 外观配置 |
| CDAppSettings | ✅ | 全局设置 |
| Keychain 凭据 | ❌ | 安全原因，本机专属 |
| 日志文件 | ❌ | 体积大，无同步价值 |
| 录制文件 | ❌ | 体积大，可选用 iCloud Drive 代替 |

### 8.3 冲突解决策略

```swift
// 配置在 NSPersistentCloudKitContainer 初始化时
container.viewContext.mergePolicy = NSMergeByPropertyObjectTrumpMergePolicy
```

**冲突仲裁规则：**

| 冲突场景 | 解决方式 |
|---------|---------|
| 同一 Session 在两台设备同时修改 | `modifiedAt` 更晚的版本覆盖 |
| Mac A 删除 / Mac B 修改同一 Session | Mac B 修改优先（软删除 + modifiedAt 比较）|
| 新设备第一次拉取（全量同步）| 全量覆盖本地（本地无数据）|
| 网络断开期间的本地修改 | 上线后自动推送，按 modifiedAt 仲裁 |

### 8.4 同步状态机

```
SyncState:
  idle        ← App 启动初始态
  syncing     ← NSPersistentStoreRemoteChange 通知触发
  synced      ← 成功完成
  error(msg)  ← 网络不可达 / iCloud 账户问题
  disabled    ← 用户关闭 iCloud 同步
```

---

## 9. 数据迁移策略

### 9.1 Core Data 版本管理

每次增删改 Core Data 实体或字段必须：
1. 在 `ShellMate.xcdatamodeld` 中创建新版本（如 `ShellMate v2.xcdatamodel`）
2. 设置为当前模型版本
3. 提供迁移 Mapping 或轻量级迁移配置

```swift
// 优先使用轻量级迁移（Lightweight Migration）
// 满足：添加可空字段、重命名字段、添加关系（默认值为空）
let options: [String: Any] = [
    NSMigratePersistentStoresAutomaticallyOption: true,
    NSInferMappingModelAutomaticallyOption: true
]
```

**轻量级迁移支持的变更：**

| 变更类型 | 是否支持轻量级迁移 |
|---------|-----------------|
| 添加可空属性 | ✅ |
| 添加可空关系 | ✅ |
| 删除属性 | ✅ |
| 属性重命名（用 `renamingIdentifier`）| ✅ |
| 修改属性类型（如 Int16 → Int32）| ❌ 需 Mapping Model |
| 拆分实体 / 合并实体 | ❌ 需自定义迁移 |

### 9.2 版本迭代计划

| 版本 | 主要变更 | 迁移类型 |
|------|---------|---------|
| v1（初始）| 基础 10 个实体 | — |
| v2 | CDSession 增加 `colorHex`/`tags` 字段 | 轻量级 |
| v3（留存）| 若新增录制配置实体 | 评估后决定 |

---

## 10. 数据安全设计

### 10.1 敏感字段处理原则

| 字段 | 存储位置 | 加密方式 |
|------|---------|---------|
| SSH 密码 | Keychain | Keychain 硬件加密（Secure Enclave 支持机型）|
| 私钥文件 | Keychain | 同上 |
| 私钥 Passphrase | Keychain | 同上 |
| 备注（注释）| Core Data `notesEncrypted` | AES-256-GCM，密钥由主密码 PBKDF2 派生 |
| 会话 Host/Username | Core Data | 明文（非敏感，需要 iCloud 同步）|

### 10.2 notesEncrypted 加密流程

```
用户输入 Notes 文本
    │
    ▼
MasterPasswordService.encrypt(data)
    ├── 获取内存中的 AES-GCM 密钥（解锁后缓存，从不持久化）
    ├── 生成随机 Nonce（12 bytes）
    ├── AES-256-GCM 加密
    └── 输出: Nonce(12B) + CipherText + Tag(16B)
    │
    ▼
存入 CDSession.notesEncrypted（Binary Data）
```

### 10.3 主密码 PBKDF2 流程

```
用户设置主密码
    │
    ▼
生成随机 Salt（32 bytes）
    │
    ▼
PBKDF2-HMAC-SHA256(password, salt, iterations=310000)
    │             ↑
    │     OWASP 2023 推荐最低迭代次数
    ▼
Hash（32 bytes）
    │
    ▼
将 Salt + PBKDF2 Hash 存入 Keychain
  key: "masterPasswordHash"
  encoded: Base64(salt) + ":" + Base64(hash)
```

### 10.4 数据清除规范

| 操作 | 清除范围 |
|------|---------|
| 用户退出登录（iCloud）| 清空 Core Data 所有记录（保留 Keychain）|
| 用户重置应用 | 清空 Core Data + Keychain + 文件系统数据 |
| 删除会话 | 软删除（`isDeleted=true`）→ 同步到所有设备 → 7 天后物理删除 |
| 删除私钥 | 立即删除 Keychain 条目 + 清空关联 Session 的 `keychainRef` |

```swift
// 删除会话（软删除）
func delete(sessionID: UUID) async throws {
    let cd = try fetch(id: sessionID)
    cd.isDeleted = true
    cd.modifiedAt = Date()
    try context.save()
    // 7 天后由后台任务物理删除
}
```

---

## 附录 A：Core Data 实体字段速查

| 实体 | 字段数 | 关系数 | 参与同步 |
|------|--------|--------|---------|
| CDSession | 19 | 6 | ✅ |
| CDSessionGroup | 6 | 4 | ✅ |
| CDJumpHost | 7 | 1 | ✅ |
| CDTunnelRule | 10 | 1 | ✅ |
| CDHighlightSet | 5 | 2 | ✅ |
| CDHighlightRule | 11 | 1 | ✅ |
| CDQuickCommandSet | 5 | 2 | ✅ |
| CDQuickCommand | 9 | 1 | ✅ |
| CDTerminalProfile | 18 | 0 | ✅ |
| CDAppSettings | 13 | 0 | ✅ |

---

## 附录 B：Repository 接口定义

```swift
// 会话仓库
protocol SessionRepository {
    func fetchAll() async throws -> [Session]
    func fetchGroup(_ groupID: UUID?) async throws -> [Session]
    func fetch(id: UUID) async throws -> Session?
    func save(_ session: Session) async throws
    func delete(id: UUID) async throws  // 软删除
    func move(_ sessionID: UUID, toGroup groupID: UUID?) async throws
    func search(query: String) async throws -> [Session]
}

// 高亮规则仓库
protocol HighlightRepository {
    func fetchAllSets() async throws -> [HighlightSet]
    func saveSet(_ set: HighlightSet) async throws
    func deleteSet(id: UUID) async throws
    func saveRule(_ rule: HighlightRule, inSet setID: UUID) async throws
    func deleteRule(id: UUID) async throws
    func reorderRules(_ ruleIDs: [UUID], inSet setID: UUID) async throws
}

// 外观配置仓库
protocol TerminalProfileRepository {
    func fetchAll() async throws -> [TerminalProfile]
    func fetchBuiltIn() async throws -> [TerminalProfile]
    func save(_ profile: TerminalProfile) async throws
    func delete(id: UUID) async throws
}
```

---

*文档版本：v1.0 · 2026-03-18*
*作者：解决方案架构师*
*关联文档：技术方案.md v1.0 · ShellMate_PRD.md v1.0*
