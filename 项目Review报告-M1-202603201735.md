# ShellMate 项目 Review 报告 — M1 阶段

> **阶段：** M1 — 基础框架（W1–W5）
> **生成时间：** 2026-03-20 17:35
> **审查范围：** W1–W5（2026-03-18 → 2026-04-18）
> **总体评级：** 部分通过 ⚠️

---

## 1. 执行摘要

M1 阶段目标为搭建 ShellMate 的基础工程框架，涵盖 Core Data 模型、Repository 层、侧边栏 UI、会话 CRUD 弹窗及共享组件。从代码层面看，W1–W4 的大多数核心任务已超前完成（含侧边栏渲染、D01 四 Tab 弹窗、TabBar、FormComponents 等），整体代码交付度约 75%。然而三项关键缺口导致无法宣告 M1 验收通过：①W3.3 拖拽排序功能完全缺失；②W5 全部 6 个单元测试 / 性能测试 / UI 回归任务均未开始；③Figma Handoff（设计稿交付）任务均标注为未开始。此外，Core Data 的 `.xcdatamodeld` 实体完整性（W1.4）仅标注"API 模型已建，需核对"，10 个实体是否全部正确定义至今未确认。综合评估，M1 代码骨架已就位，但测试覆盖与设计交付存在显著缺口，**整体完成度约 60%**。

---

## 2. 验收条件核查

| # | 验收条件 | 状态 | 评估说明 |
|---|---------|------|---------|
| 1 | Xcode 工程结构搭建完成，SPM 依赖可正常编译 | ✅ | 编译错误已于 2026-03-19 全部修复：删除 `TerminalContainerView` 引用、修正字号 API、修正清屏 API、补全 `FormComponents.swift` / `HostKeyConfirmationView.swift` 等文件至 `project.pbxproj` 编译目标；SPM（SwiftTerm）依赖完整 |
| 2 | Core Data 10 个实体定义完成，基础 CRUD 通过测试 | ⚠️ | W1.4 状态：「API 模型已建，需核对 .xcdatamodeld 实体完整性」；W5.1/5.2 Repository 单元测试与 SessionStore 状态机测试均 🔲 未开始；实体总数与 PRD/数据库设计文档的对应关系未完成验收 |
| 3 | 侧边栏 UI：会话列表渲染、分组折叠/展开、拖拽排序 | ⚠️ | 列表渲染 ✅（SessionRow + NavigationSplitView）；分组折叠/展开 ✅（chevron 旋转动画）；**拖拽排序 🔲 完全未实现**（W3.3 DragGesture 未实现，sortOrder 未接入 Core Data） |
| 4 | 新建/编辑会话弹窗（D01）所有 Tab 可交互 | ✅ | `SessionFormSheet` + 4 个 Tab（基本/认证/高级/隧道）已全部实现并加入编译目标（2026-03-19 修正） |
| 5 | 搜索功能：500 条会话 < 100ms 响应 | ⚠️ | `SidebarSearchView` 实时过滤已实现（姓名/主机/标签过滤），但 W5.3 性能测试（500 会话搜索延迟 < 100ms）🔲 未执行，无量化验证数据 |
| 6 | Figma 设计稿：所有组件交付 Handoff | ❌ | W1.6 / W2.6 / W3.6 / W4.6 设计师 Handoff 任务全部 🔲 未开始；仅 Figma 规范文档（shellmate-figma-spec）已整理，无实际设计稿交付记录 |

**本阶段验收通过率：1.5/6（约 25%）**
（条件1全通过；条件4全通过；条件2/3/5部分通过；条件6未通过）

---

## 3. PRD P0 用例对齐检查

M1 阶段涉及的 PRD P0 用例主要为 TC-003（会话持久化）和 TC-004 的前置架构准备（多标签页基础设施）。

| 用例 | 描述 | 状态 | 备注 |
|------|------|------|------|
| TC-003 | 退出重启后所有会话配置和分组结构完整恢复 | ✅ | `SessionRepository` Core Data CRUD 已实现；`NSPersistentCloudKitContainer` 已初始化；`SessionStore.fetchSessions()` 启动时加载；逻辑层面通过，需真机重启验证 |
| TC-004（前置） | 多标签页架构基础设施 | ✅ | `TerminalTabBarView` + `TabBarStore` 已实现并串联 `ContentView`，`connectToSession()` 驱动标签页新建/切换；多标签 SSH 链路架构完整（2026-03-19 修正） |
| TC-001/002/005（前置基础） | SSH 认证 / 自动重连的凭据存储基础 | ✅ | `KeychainService` 完整实现（CRUD + kSecAttrAccessibleWhenUnlockedThisDeviceOnly）；SessionForm 认证 Tab 包含密码/密钥/Agent 认证方式选择 |

---

## 4. UI/Figma 规范符合度

### 4.1 设计令牌使用

- `DesignTokens.swift` 已定义颜色令牌体系，使用语义命名（符合 00-总纲原则 A）
- 深色/浅色模式适配：`DesignTokens` 颜色基于语义 Token，理论上支持模式切换；M4 阶段补充了 `.preferredColorScheme(.dark)` 强制深色
- 强调色：**需人工验证**是否统一使用 `NSColor.controlAccentColor`（00-总纲原则 C 禁止硬编码强调色）
- 间距/圆角/阴影：**需人工验证**是否遵循 Figma 01-界面布局规范中定义的 8pt 基准网格

### 4.2 组件实现符合度

| 组件 | Figma 规范要求 | 实现状态 | 符合度 |
|------|--------------|---------|-------|
| 主窗口框架 | 1200×760pt，TitleBar 52pt + TabBar 36pt + MainArea + StatusBar 24pt | ContentView 实现 NavigationSplitView 分区，具体尺寸需人工验证 | 需人工验证 |
| SessionRow | 状态点 + 名称 + 主机副标题 + 彩色徽章 | `SessionRowView` 已实现，含 StatusDot + 名称 + host 副标题 | ⚠️ 彩色分组徽章需验证 |
| D01 弹窗 | Sheet 样式，4 Tab（基本/认证/高级/隧道） | `SessionFormSheet` 完整实现 4 Tab | ✅ |
| TabBar | 36pt，Tab 含状态点+名称+关闭按钮 | `TerminalTabBarView` 已实现 | ⚠️ 精确高度/宽度需验证 |
| StatusDot | 脉冲动画（connected=绿/connecting=黄/disconnected=灰） | `StatusDotView` 已实现含脉冲动画 | ✅ |
| 侧边栏搜索框 | 顶部实时过滤，`⌘F` 聚焦 | `SidebarSearchView` 已实现 | ⚠️ ⌘F 快捷键需验证 |

### 4.3 动效与交互

- 分组折叠 chevron 旋转动画：✅ 已实现
- StatusDot 脉冲动画：✅ 已实现
- D01 弹窗 Sheet 滑出动画：**需人工验证**是否符合 03-动效规范的 Spring 参数（`response: 0.3, dampingFraction: 0.8`）
- 侧边栏 SessionRow Hover 状态：**需人工验证**

### 4.4 可访问性

- M1 阶段 VoiceOver 标签：**未系统性添加**（M4.4 W14.4 才进行可访问性专项）
- 侧边栏元素的 `accessibilityLabel` / `accessibilityHint`：**需人工验证**
- 键盘导航（Tab 键焦点环）：**需人工验证**

**UI 规范符合度：中**
（核心组件结构已实现，设计令牌体系建立；精确尺寸/动效参数/可访问性均需人工对齐验证，Figma Handoff 未交付导致无法精确核查）

---

## 5. 技术架构符合度

### 5.1 架构层次遵循

- 4 层架构整体清晰：
  - 展示层：SwiftUI Views（ContentView / SessionFormSheet / SidebarSearchView 等）
  - 应用层：SessionStore / GroupStore（`@MainActor ObservableObject`）
  - 领域层：SessionRepository / GroupRepository（Core Data CRUD 封装）
  - 数据层：Core Data（`NSPersistentCloudKitContainer`）+ Keychain
- 单向数据流：Store 暴露 `@Published` 属性，UI 通过 `@ObservedObject` 订阅，符合技术方案规范 ✅

### 5.2 Swift Concurrency 使用

- Repository 层使用 `async/await` 封装 Core Data 操作，符合技术方案要求 ✅
- GCD 残留：**需人工执行** `grep -r "DispatchQueue" ShellMate/` 检查是否存在旧式回调
- `@MainActor` 标注：Store 类需确认是否全部标注（技术方案强制要求）

### 5.3 安全规范

- Keychain 存储：`kSecAttrAccessibleWhenUnlockedThisDeviceOnly` 访问策略 ✅
- 密码/私钥不落 Core Data：架构设计上通过 `keychainRef`（UUID）关联，原文不存 ✅
- 日志安全：**需人工验证**控制台输出中是否存在凭据泄漏风险

### 5.4 数据模型符合度

- PRD 定义 Session 实体含 23 个字段，数据库设计文档（`ShellMate.xcdatamodeld`）应包含 10 个 Core Data 实体
- **关键风险**：W1.4 至今标注为「需核对 .xcdatamodeld 实体完整性」，未有任何后续核查记录
- CloudKit 兼容：W9.6 已移除 ordered 关系（CloudKit 不支持），但该修复在 M1 范围外；M1 的 Core Data 设计是否预先考虑 CloudKit 兼容性**需人工验证**
- `SessionGroup.isExpanded` / `sortOrder` 字段：是否正确映射到 Core Data 实体**需人工验证**

**技术规范符合度：良**
（架构分层清晰，Swift Concurrency 方向正确，安全设计合理；GCD 残留和 xcdatamodeld 完整性是主要待验证项）

---

## 6. 风险与问题清单

| 优先级 | 问题描述 | 影响范围 | 建议措施 |
|-------|---------|---------|---------|
| P0 | 拖拽排序（W3.3）完全未实现：DragGesture + sortOrder 均缺失 | PRD P1 功能缺口，侧边栏 M1 验收条件 #3 | 在 M2 阶段 Buffer 周（W9.6 已有）补全；使用 `onDrag` / `.onInsert` + Core Data `sortOrder` 整数字段实现 |
| P0 | W5 全部单元测试未开始：Repository CRUD、SessionStore 状态机、500 会话性能测试均 🔲 | M1 验收条件 #2/#5 直接关联；无测试保障后续回归 | 立即补全 W5.1/5.2/5.3；优先覆盖 Repository CRUD（增删改查）和 SessionStore 状态变更 |
| P0 | Core Data xcdatamodeld 实体完整性未验收（W1.4 悬而未决） | 数据层基础稳定性，影响全部功能 | 对照数据库设计文档逐一核查 10 个实体（Session/SessionGroup/JumpHost/TunnelRule/HighlightSet/HighlightRule/QuickCommandSet/QuickCommand/LogConfig/TerminalProfile）是否全部定义且字段类型正确 |
| P1 | Figma Handoff 全部缺失（W1.6/W2.6/W3.6/W4.6 均 🔲） | UI 实现与设计规范对齐缺少基准，后续 UI 回归无参照 | 将 shellmate-figma-spec 文档转化为可导出的 Figma 文件，完成 Screen 01/D01/组件库的 Handoff；如无设计师资源，至少完成标注文档导出 |
| P1 | 侧边栏右键菜单（W3.5）仅部分实现：contextMenu 存在但操作不全 | PRD 3.1.2 要求：编辑/复制/重命名/删除/在新窗口打开等完整操作 | 核查 `SessionRowView.contextMenu` 已实现的操作项，补全缺失的「复制会话」「在新窗口打开」等操作 |
| P1 | StatusBar（TerminalStatusBarView）未集成到主界面（W4.4 🔄） | PRD 4.4 状态栏信息密度要求；M1 视觉完整性 | 将 StatusBar 集成到 ContentView 底部，验证连接状态/终端尺寸等基础字段显示 |
| P2 | ⌘T（新建标签）/ ⌘⇧N（快速连接）等快捷键仅部分实现（W4.5 🔄） | PRD 8.1–8.2 快捷键表完整性 | 系统性对照 PRD 第 8 章快捷键表逐一补全 `.keyboardShortcut()` 修饰符 |
| P2 | 无 CI/CD 流水线（W1.3 仅配置 SwiftLint + .gitignore） | 代码质量门禁缺失 | 配置 GitHub Actions 或 Xcode Cloud 自动构建 + SwiftLint 检查 |

---

## 7. 下一阶段建议

进入 M2（SSH 核心，W6–W9）前，建议先完成以下遗留项，避免技术债务积累：

1. **立即补全 xcdatamodeld 实体核查**：这是所有数据功能的基础，拖延会导致后续 SSH 会话持久化 / CloudKit 同步出现难以排查的数据层 Bug
2. **补全 Repository 单元测试**（W5.1/5.2）：SSH 层开发期间需要稳定的 Store 状态机，单元测试是回归保障
3. **实现拖拽排序**（W3.3）：PRD P1 功能，技术成本低，在 M2 启动前一并完成可避免里程碑验收时被降级
4. **M2 阶段重点关注**：libssh2 的非阻塞模式（W6.2）和 SSH 认证流程（W7）是技术难点，需在 W6 第一天确认 XCFramework 链接正常；Keychain 存取应在 W7 阶段和 SSH 认证流程做端到端集成测试

---

## 8. 附录：核查依据

- MRD 版本：v1.0（2026-03-18）
- PRD 版本：v1.0（2026-03-18）
- 技术方案版本：v1.0（2026-03-18）
- 数据库设计文档版本：v1.0（2026-03-18）
- Figma 规范版本：v1.0（shellmate-figma-spec 00–08）
- 开发进度文档版本：v1.3（最后更新 2026-03-20）
- 审查 Git 范围：最近 20 次提交（最新：0a86a31 perf(terminal): W15 全面性能优化）
- 审查时间：2026-03-20 17:35
