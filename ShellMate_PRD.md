# ShellMate for macOS — 产品需求文档（PRD）

> **文档版本：** v2.2
> **创建日期：** 2026-03-18
> **最后更新：** 2026-04-14
> **产品名称：** ShellMate
> **平台：** macOS 13 Ventura 及以上
> **文档状态：** 已批准

---

## 目录

1. [产品概述](#1-产品概述)
2. [整体功能架构](#2-整体功能架构)
3. [功能模块详细需求](#3-功能模块详细需求)
   - 3.1 会话管理
   - 3.2 SSH 连接与认证
   - 3.3 终端仿真
   - 3.4 标签页与分屏
   - 3.5 SFTP 文件管理器
   - 3.6 端口转发与隧道管理
   - 3.7 快捷命令与效率工具
   - 3.8 关键词高亮
   - 3.9 日志与录制
   - 3.10 安全与凭据管理
   - 3.11 外观与个性化
   - 3.12 iCloud 同步
   - 3.13 AI 智能助手
   - 3.14 服务器实时监控
   - 3.15 Hotkey Window（全局浮动终端）
   - 3.16 tmux 集成管理
   - 3.17 FTUE 首次使用引导
   - 3.18 自动化触发器
4. [全局交互规范](#4-全局交互规范)
5. [非功能性需求](#5-非功能性需求)
6. [数据模型](#6-数据模型)
7. [错误处理规范](#7-错误处理规范)
8. [快捷键一览](#8-快捷键一览)
9. [验收标准](#9-验收标准)
10. [版本历史](#10-版本历史)

---

## 1. 产品概述

### 1.1 产品简介

ShellMate 是一款面向 macOS 平台的原生 SSH 会话管理工具，提供会话管理、终端仿真、SFTP 文件传输、端口转发等一站式功能。所有用户凭据存储于本地 macOS Keychain，不依赖任何云服务器。

### 1.2 核心设计原则

| 原则 | 说明 |
|------|------|
| **原生优先** | 遵循 macOS HIG 规范，使用 SwiftUI + AppKit 构建，非 Electron 或 Web 技术 |
| **安全本地化** | 凭据仅存于设备 Keychain，传输数据端到端加密，不设遥测上报 |
| **效率导向** | 减少连接操作步骤，支持键盘驱动工作流，功能触手可及 |
| **信息密度** | 侧边栏 + 多标签 + 状态栏设计，单窗口承载完整工作上下文 |

### 1.3 竞品功能参考基准

在制定以下需求时，参考了以下主流 SSH 工具的设计：

- **Xshell 8**（NetSarang）：Session Manager、Highlight Sets、Compose Pane、Quick Commands、即时隧道、Tab Manager、脚本录制
- **MobaXterm**：**启动即展示本地 Shell（零摩擦冷启动）**、自动弹出 SFTP 侧边栏、多标签 + 分屏、MultiExec 同步输入、内嵌 X11 服务器
- **SecureCRT**（VanDyke）：可继承 Session Properties、凭据管理器、Python 脚本自动化、关键词高亮（含正则）、FIPS 140-2 加密合规
- **Termius**（跨平台）：Keychain 跨设备同步、Snippets 共享、团队协作、SFTP 并发传输、Vault 密码管理——移动端体验优先，但 macOS 深度集成弱
- **iTerm2**（macOS 专属开源）：Profiles 强大继承、Split Pane 精细控制、Triggers（正则自动化）、Shell Integration（命令历史/标记/路径感知）、Python API 脚本化——macOS 原生集成标杆
- **Tabby**（开源跨平台）：插件生态丰富、Zmodem 文件传输、WSL 集成、SSH 配置文件直接编辑、主题市场——工程师群体口碑好，但 Electron 架构性能受限
- **Royal TSX**（企业级）：跨协议支持（SSH/RDP/VNC/Web）、团队文档共享、凭据对象化管理、Dynamic Folder（API 动态生成会话）——大型 IT 团队首选，功能复杂度高

**战略差异化方向（ShellMate v2.0）：**

| 方向 | 核心主张 | 竞品盲区 |
|------|----------|----------|
| **macOS 原生集成护城河** | Touch ID 解锁、iCloud 跨设备同步、Shortcuts.app 自动化、Spotlight 快速搜索 | Termius/Tabby 为跨平台，无法深度集成 |
| **SSH 场景专属 AI** | 错误诊断、命令智能补全，上下文来自终端输出而非代码仓库 | Warp 聚焦通用 AI 编码，对运维/SSH 场景支持弱 |
| **迁移零摩擦** | 支持从 Xshell/SecureCRT/`.ssh/config`/Termius 导出格式一键导入 | 大多数工具只支持 OpenSSH config 导入 |

---

## 2. 整体功能架构

```
ShellMate
│
├── 【会话层】
│   ├── 会话管理器（Session Manager）
│   ├── 会话配置（连接参数 / 认证 / 隧道 / 外观）
│   └── iCloud 同步
│
├── 【连接层】
│   ├── SSH 连接引擎（libssh2）
│   ├── 认证模块（密码 / SSH Key / Touch ID / Agent / GSSAPI）
│   └── 跳板机 / 代理模块
│
├── 【终端层】
│   ├── 终端仿真器（SwiftTerm / VT100 / xterm-256color）
│   ├── 标签页管理（多 SSH 会话标签）
│   ├── 分屏布局（单/水平/垂直/四格）
│   ├── 关键词高亮引擎
│   └── Hotkey Window（NSPanel 全局浮动终端，⌥Space）
│
├── 【功能扩展层】
│   ├── SFTP 文件管理器
│   ├── 端口转发 / 隧道管理
│   ├── 快捷命令库（Quick Commands，含参数化变量）
│   ├── Compose Pane（命令编辑区）
│   ├── tmux 集成管理器
│   ├── 日志记录
│   ├── 会话录制 / 回放
│   └── 自动化触发器（Triggers，正则匹配 → 执行动作）
│
├── 【AI 智能层】（v2.0 新增）
│   ├── AIService（Claude API / OpenAI API 可配置）
│   ├── 错误诊断（Error Detective，终端输出自动检测）
│   ├── 命令智能补全（上下文 = 最近 50 行终端输出）
│   └── 会话摘要（操作历史 → Markdown 报告）
│
├── 【监控层】（v2.0 新增）
│   ├── 服务器实时监控 Actor（CPU / 内存 / 磁盘 / 网络，2s 轮询）
│   └── 告警通知（阈值超限 → macOS UserNotifications）
│
└── 【系统集成层】
    ├── macOS Keychain（凭据 + AI API Key）
    ├── CloudKit / iCloud（会话配置 / 主题 / 触发器规则）
    ├── Notification Center（断连 / 监控告警 / 关键词命中）
    ├── Shortcuts.app（自动化动作暴露）
    └── LocalAuthentication（Touch ID / 主密码解锁）
```

---

## 3. 功能模块详细需求

---

### 3.1 会话管理

#### 3.1.1 功能概述

会话管理器（Session Manager）是左侧固定侧边栏，集中呈现所有已保存的 SSH 会话，支持分组、搜索、快速操作。参考 Xshell 的 Dockable Session Manager 和 MobaXterm 的 Sessions 侧边栏设计。

#### 3.1.2 会话列表

**显示内容（每个会话条目）：**

- 连接状态指示灯（绿色=已连接 / 黄色=连接中 / 灰色=未连接）
- 会话名称（自定义，默认为 `用户名@主机名`）
- 主机地址与端口（小字副标题）
- 分组标签（彩色徽章）
- 自定义会话图标（可选）

**列表操作：**

| 操作 | 触发方式 | 行为 |
|------|----------|------|
| 连接 | 双击 / 回车 | 在新标签页中建立连接 |
| 在新窗口打开 | 右键菜单 | 新建独立窗口并连接 |
| 编辑配置 | 右键 → 编辑 / `⌘E` | 打开会话配置弹窗 |
| 复制会话 | 右键 → 复制 | 创建同配置的新会话 |
| 重命名 | 双击名称 | 行内编辑 |
| 删除 | `⌫` / 右键 → 删除 | 二次确认弹窗后删除 |
| 拖拽排序 | 拖拽 | 在分组内或跨分组移动 |
| 拖拽新建分组 | 拖入空白区域 | 自动创建新分组 |

#### 3.1.3 分组管理

- 支持无限层级分组树（建议 UI 最多展示 3 层，避免视觉混乱）
- 分组可设置颜色（10 个预设色），用于标签徽章
- 分组可折叠 / 展开，状态在应用重启后持久化
- 分组右键菜单：新建会话、重命名、删除分组（保留或迁移会话）、展开/折叠所有子项

#### 3.1.4 搜索与过滤

- 顶部搜索框，快捷键 `⌘F`（或 `⌘/`）聚焦
- 实时过滤：输入时即时缩小结果，响应延迟 < 100ms
- 匹配范围：会话名称、主机名/IP、用户名、备注字段
- 支持模糊匹配（`web01` 可匹配 `prod-web-01`）
- 过滤结果高亮匹配字符

#### 3.1.5 会话导入 / 导出

| 格式 | 导入 | 导出 | 说明 |
|------|------|------|------|
| ShellMate JSON | ✅ | ✅ | 含分组结构，不含密码 |
| OpenSSH `~/.ssh/config` | ✅ | ❌ | 支持 Include / ProxyJump 语法 |
| CSV（主机/端口/用户名） | ✅ | ✅ | 标准三列格式 |
| Termius JSON 导出 | ✅ | ❌ | 从 Termius 迁移（v2.0 新增） |
| Xshell `.xsh` 文件 | ✅ | ❌ | 单会话配置文件（v2.0 新增） |

导入时若存在重名会话，提示用户选择：跳过 / 覆盖 / 重命名导入。

#### 3.1.6 会话属性继承

参考 SecureCRT 的 Inheritable Session Properties 机制：

- 分组可设置默认属性（字体、颜色方案、Keepalive 间隔等）
- 子会话默认继承父分组属性，亦可单独覆盖
- 覆盖后的属性条目有视觉标记（如蓝色圆点）

---

### 3.2 SSH 连接与认证

#### 3.2.1 连接参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 主机名 / IP | 文本 | — | 支持域名与 IPv4/IPv6 |
| 端口 | 数字 | 22 | 1–65535 |
| 用户名 | 文本 | — | — |
| 连接超时 | 数字（秒） | 15 | 0 为不限制 |
| Keepalive 间隔 | 数字（秒） | 30 | 0 为关闭，防止 NAT 超时断连 |
| 自动重连 | 开关 | 开 | 断连后自动重试，最多 3 次 |
| 编码 | 下拉 | UTF-8 | UTF-8 / GBK / GB2312 / Shift-JIS 等 |
| 代理设置 | 下拉 | 无 | SOCKS4 / SOCKS5 / HTTP 代理 |

#### 3.2.2 认证方式

支持以下认证方式（可叠加配置，按顺序尝试）：

**a) 密码认证**
- 用户名 + 密码，密码存入 macOS Keychain
- 支持首次连接后提示保存
- 支持键盘交互认证（Keyboard-Interactive，如 2FA 验证码提示）

**b) SSH 公钥认证**
- 支持密钥类型：RSA（2048/4096）、Ed25519（推荐）、ECDSA（nistp256/384/521）、DSA（兼容）
- 私钥文件路径选择 或 私钥内容直接粘贴
- 私钥 Passphrase 存入 Keychain（可选）
- 内置密钥生成向导：选择算法 → 设置位数 → 设置注释 → 保存

**c) SSH Agent 认证**
- 对接系统 `ssh-agent` / `ssh-add` 管理的密钥
- 支持 SSH Agent 转发（`-A` 参数），用于跳板机场景
- App Store 版本注明：受 Sandbox 限制，Agent socket 路径可能需手动配置

**d) Touch ID 生物认证解锁（v2.0 新增）**
- 适用场景：应用启动锁定解锁 / 主密码替代验证（§3.10.2）
- 技术方案：`LocalAuthentication.LAContext.evaluatePolicy(.deviceOwnerAuthenticationWithBiometrics, ...)`
- 仅适用于 App Store 版本（需 `NSFaceIDUsageDescription` 或 Touch ID capability entitlement）
- Direct 版本同样支持，无沙盒限制
- 降级处理：Touch ID 失败超过 3 次 → 自动切换为主密码输入
- 隐私声明：生物特征数据不离开设备，仅用于本地解锁验证

**e) GSSAPI / Kerberos 认证**
- 企业内网域账号认证（Kerberos v5）
- 需系统已配置 Kerberos realm

**e) 跳板机（Jump Host）**
- 支持最多 3 跳链路：本机 → 跳板机 A → 跳板机 B → 目标主机
- 每一跳可独立配置认证方式
- 配置 UI：可视化链路图，逐跳添加

#### 3.2.3 服务器指纹验证

- 首次连接未知主机时，弹窗展示：
  - 服务器地址、算法类型（ED25519 / RSA 等）
  - 完整指纹（SHA256 哈希）
  - 指纹可视化（随机艺术图）
- 用户选择：接受并记住 / 仅本次接受 / 拒绝
- 已记录的 Known Hosts 在「设置 → 安全 → Known Hosts」中可查看和删除
- 检测到指纹变更时（可能为中间人攻击），弹出**高优先级警告弹窗**，阻止连接直到用户明确确认

#### 3.2.4 连接状态管理

连接生命周期状态机：

```
空闲(Idle) → 连接中(Connecting) → 已连接(Connected) → 断开中(Disconnecting) → 已断开(Disconnected)
                    ↓ 失败                                       ↑
               连接失败(Failed) ←── 网络中断(Interrupted) ──→ 自动重连(Reconnecting)
```

断连处理：
- 检测到断连后，状态栏显示断连原因（网络中断 / 超时 / 服务器关闭连接）
- 若「自动重连」开关开启，5 秒后自动重试，最多 3 次
- 3 次重试失败后，弹出通知并提供「手动重连」按钮
- 重连期间终端输出区域置灰，显示"正在重连…"覆盖层

---

### 3.3 终端仿真

#### 3.3.1 协议与编码支持

- 终端类型：xterm-256color（默认）/ xterm / VT100 / VT220 / Linux console
- 颜色：256色 + True Color（24-bit RGB）
- Unicode：全宽字符（CJK）正确渲染，Emoji 支持
- 字符编码：UTF-8（默认）/ GBK / GB2312（可按会话配置）

#### 3.3.2 文本选择与复制

| 操作 | 快捷键 / 手势 |
|------|--------------|
| 选择文本 | 鼠标拖拽 |
| 选择单词 | 双击（可自定义分隔符） |
| 选择行 | 三击 |
| 矩形选择 | `⌥` + 鼠标拖拽 |
| 复制选中 | `⌘C` |
| 粘贴 | `⌘V` |
| 粘贴时带括号包裹（Bracketed Paste） | 自动，防止多行粘贴触发误操作 |

双击选词的分隔符用户可自定义（Settings → Terminal → Word Delimiters）。

#### 3.3.3 滚动与回滚缓冲

- 默认回滚行数：10,000 行（可在「设置 → 终端」调整，范围 1,000–100,000）
- 鼠标滚轮 / 触控板双指滑动：滚动
- `⌘↑` / `⌘↓`：翻页
- `⌘Home` / `⌘End`：跳到顶部 / 底部
- 内容更新时自动滚动到底部（可关闭）

#### 3.3.4 终端内搜索

- `⌘F` 唤出搜索栏（浮层于终端顶部）
- 支持大小写敏感开关
- 支持正则表达式模式
- 高亮所有匹配项，`Enter` / `⇧Enter` 前后跳转
- `Esc` 关闭搜索栏

#### 3.3.5 URL 与路径识别

- 终端输出中的 HTTP/HTTPS URL 自动转为可点击超链接
- 文件路径（如 `/var/log/nginx/error.log`）`⌘` + 点击尝试通过 SFTP 打开
- `⌘` + 鼠标悬停：显示下划线指示可点击

#### 3.3.6 铃声与通知

| 触发条件 | 响应行为（可配置） |
|----------|-------------------|
| 终端响铃（BEL 字符） | Dock 图标闪烁 / 系统通知 / 声音 / 无响应 |
| 会话断连 | 系统通知（含会话名称、断连原因） |
| 关键词触发（见 3.8） | 系统通知 + 标签页徽章 |
| CPU 使用率超阈值（> 80%） | 系统通知：「{主机名} CPU 使用率 {value}%，已超警戒线」（见 §3.14） |
| 内存使用率超阈值（> 85%） | 系统通知：「{主机名} 内存使用率 {value}%」 |
| 磁盘使用率超阈值（> 90%） | 系统通知：「{主机名} 磁盘 {mount} 使用率 {value}%」 |
| 自动化触发器命中（见 §3.18） | 根据动作类型：通知 / 执行脚本 / 打开 URL |

通知实现框架：`UserNotifications.framework`（`UNUserNotificationCenter`）；首次触发前请求授权。

---

### 3.4 标签页与分屏

#### 3.4.1 标签页管理

**标签页外观：**
- 标签显示：状态指示灯 + 会话名称 + 关闭按钮
- 活跃标签与非活跃标签视觉区分（背景色深浅）
- 标签支持自定义颜色（右键 → 设置颜色），10 个预设色，对应会话分组颜色
- 鼠标悬停 2 秒后显示 Tooltip（完整主机地址、用户名、连接时长）

**标签操作：**

| 操作 | 快捷键 |
|------|--------|
| 新建标签（空连接） | `⌘T` |
| 新建标签（重复当前会话） | `⌘⇧T` |
| 关闭当前标签 | `⌘W` |
| 切换到下一标签 | `⌘⇧]` 或 `⌃Tab` |
| 切换到上一标签 | `⌘⇧[` 或 `⌃⇧Tab` |
| 直接跳转第 N 个标签 | `⌘1` – `⌘9` |
| 拖拽重排标签顺序 | 拖拽标签 |
| 分离标签为独立窗口 | 拖出标签栏 / 右键 → 移至新窗口 |
| 合并窗口标签 | 拖入另一窗口的标签栏 |
| 重命名标签 | 双击标签文字 |

**标签组（Tab Groups）：**
- 参考 Xshell 的 Tab Group 功能
- 可将多个标签划入同一组，组内同一时刻只显示一个标签
- 标签组展示为下拉菜单形式，节省标签栏空间
- 切换组内标签：`⌘⌥1` – `⌘⌥9`

#### 3.4.2 分屏布局

- 最多支持 4 个终端窗格（2×2 网格）
- 分屏操作入口：菜单栏 View → Split / 工具栏按钮 / 拖拽标签至分屏区域

| 布局 | 快捷键 |
|------|--------|
| 左右垂直分屏 | `⌘⌥→` |
| 上下水平分屏 | `⌘⌥↓` |
| 四格均分 | `⌘⌥G` |
| 关闭当前窗格（合并） | `⌘⌥W` |
| 聚焦到下一窗格 | `⌃⌥Tab` |

分屏比例：可拖拽分割线调整，双击分割线恢复等比例。

#### 3.4.3 全屏模式

- `⌘⌃F` 进入 macOS 原生全屏
- 全屏状态下侧边栏默认收起（鼠标移到屏幕左边缘时滑出）
- `⌘⌥F` 进入「专注模式」：隐藏标签栏 + 状态栏，仅显示终端内容

---

### 3.5 SFTP 文件管理器

#### 3.5.1 打开方式

- 建立 SSH 连接后，工具栏「SFTP」按钮（`⌘⇧S`）打开 SFTP 面板
- 参考 MobaXterm：首次连接后可配置「自动打开 SFTP 侧边栏」（Settings → SFTP → Auto-open on connect）
- SFTP 面板以右侧滑出抽屉形式呈现，宽度可调整（默认 300px，最小 200px）

#### 3.5.2 文件浏览器布局

```
┌─────────────────────────────────────┐
│ [←][→][↑] /home/ubuntu/services/   │  路径导航栏（可直接编辑路径）
├─────────────────────────────────────┤
│ 名称           大小     修改时间    │  列标题（可排序）
│ 📁 ..                               │
│ 📁 node_modules        Mar 10       │
│ 📁 src                 Mar 14       │
│ 📄 server.js    14.2K  Mar 14       │  当前选中（高亮）
│ 📄 package.json  2.1K  Mar 10       │
├─────────────────────────────────────┤
│ [上传] [下载] [新建目录] [刷新]     │  操作工具栏
└─────────────────────────────────────┘
```

#### 3.5.3 文件操作

| 操作 | 触发方式 |
|------|----------|
| 进入目录 | 双击 / 回车 |
| 返回上级 | 双击 `..` / `⌘↑` / 工具栏返回按钮 |
| 上传文件 | 从 Finder 拖入 / 工具栏「上传」按钮 / 右键 → 上传 |
| 下载文件 | 拖出到 Finder / 工具栏「下载」按钮 / 右键 → 下载到… |
| 删除 | `⌫` / 右键 → 删除 → 确认弹窗 |
| 重命名 | 双击文件名 / `F2` |
| 新建目录 | 右键 → 新建目录 / 工具栏按钮 |
| 修改权限（chmod） | 右键 → 属性 → 权限 → 图形化 chmod 界面 |
| 显示隐藏文件 | `⌘⇧.` 开关 |
| 多选 | `⌘` + 点击 / `⇧` + 点击 |

#### 3.5.4 传输队列

- 任意文件操作触发传输任务时，底部弹出传输队列面板
- 队列显示：文件名、传输方向（↑/↓）、大小、速度、进度条、剩余时间
- 支持：暂停单个任务、取消任务、查看已完成记录
- 多文件并发：最多 3 个并发传输任务（可配置 1–5）
- 断点续传：对支持 SFTP v3+ 的服务器自动断点续传
- 传输完成后：系统通知（含文件名和耗时）

#### 3.5.5 在终端中同步当前路径

- SFTP 面板与当前终端的工作目录双向同步（可配置）
- 在终端执行 `cd /some/path` 后，SFTP 浏览器自动跳转到对应路径
- SFTP 双击目录时，终端自动执行 `cd` 命令（可关闭）

---

### 3.6 端口转发与隧道管理

#### 3.6.1 功能概述

参考 Xshell 的「Instant Tunneling」和 MobaXterm 的「SSH Tunnels Manager」，提供图形化端口转发管理界面，规则持久化存储并与会话绑定。

#### 3.6.2 端口转发类型

| 类型 | 命令行等价 | 说明 |
|------|------------|------|
| 本地端口转发 | `ssh -L 本地端口:目标Host:目标端口 server` | 将本地端口流量转发至远端 |
| 远程端口转发 | `ssh -R 远端端口:本地Host:本地端口 server` | 将服务器端口流量转发至本机 |
| 动态端口转发（SOCKS） | `ssh -D 本地端口 server` | 本地 SOCKS5 代理 |

#### 3.6.3 隧道管理界面

入口：工具栏「隧道」按钮 / 菜单「Tools → Tunnel Manager」（`⌘⇧T`）

隧道列表显示列：

- 状态（运行中 / 已停止）
- 类型（本地 / 远程 / SOCKS）
- 本地地址:端口
- 远端目标地址:端口
- 绑定会话名称
- 操作（启动 / 停止 / 编辑 / 删除）

**新建 / 编辑隧道弹窗字段：**

```
类型：         [本地转发 ▼]
本地绑定地址：  [127.0.0.1]  端口：[8080]
远端目标主机：  [db.internal]  端口：[3306]
绑定会话：     [prod-web-01 ▼]
会话连接时自动启动：  [✅]
备注：         [访问生产数据库]
```

#### 3.6.4 即时隧道（Instant Tunnel）

- 在会话连接状态下，可无需重新配置直接临时新建隧道规则
- 通过「+」按钮快速创建，不影响已保存的规则配置
- 临时规则在会话断开后自动清除

---

### 3.7 快捷命令与效率工具

#### 3.7.1 快捷命令（Quick Commands）

参考 Xshell 的 Quick Commands 功能：

- 命令面板：可停靠于主窗口底部或右侧，以按钮组形式呈现
- 用户可创建、命名命令按钮，绑定命令字符串
- 支持命令集（Command Sets）：不同会话可绑定不同的命令集
- 命令变量占位符（v2.0 扩展）：

| 占位符 | 替换值 | 示例 |
|--------|--------|------|
| `{{HOST}}` | 当前会话主机名/IP | `192.168.1.1` |
| `{{USER}}` | 当前会话用户名 | `ubuntu` |
| `{{PORT}}` | 当前会话端口 | `22` |
| `{{SESSION_NAME}}` | 会话显示名称 | `prod-web-01` |
| `{{DATE}}` | 当前日期 `YYYY-MM-DD` | `2026-04-08` |
| `{{TIME}}` | 当前时间 `HH:MM:SS` | `14:30:00` |
| `{{INPUT:提示文字}}` | 执行前弹窗输入框 | `{{INPUT:请输入分支名}}` |

含 `{{INPUT:...}}` 的命令执行前弹出输入框，用户填写后才发送。
- 可将命令发送到：当前会话 / 当前分屏所有窗格 / 所有连接的会话

**快捷命令配置字段：**

| 字段 | 说明 |
|------|------|
| 名称 | 按钮显示文字（最多 20 字符） |
| 命令内容 | 支持多行，多行时按序发送 |
| 回车追加 | 是否在命令末尾自动追加换行 |
| 分组（Command Set） | 所属命令集 |
| 快捷键（可选） | 全局或会话内快捷键 |

#### 3.7.2 Compose Pane（命令编辑区）

参考 Xshell 的 Compose Pane：

- 可停靠于终端下方的多行文本输入区域
- 用途：在发送前编辑长命令或多行脚本，避免误触 Enter
- 发送方式：`⌃Return` 发送到当前终端 / 发送按钮
- 支持行号显示、语法高亮（bash/python/etc，基于文件扩展名提示）
- 多行内容粘贴模式：逐行发送（可配置行间延迟，单位 ms）

#### 3.7.3 同步输入（MultiExec / Synchronized Input）

参考 MobaXterm 的 MultiExec 功能：

- 入口：工具栏「同步」按钮 / 右键标签 → 同步输入
- 开启后：在当前终端的输入同步发送到所有已勾选的会话
- UI 提示：开启同步时，所有参与窗格边框显示橙色指示框
- 安全确认：开启同步输入时弹窗提示「即将同步发送到 N 个会话，请确认」

---

### 3.8 关键词高亮

#### 3.8.1 功能概述

参考 Xshell 的 Highlight Sets 和 SecureCRT 的关键词高亮，在终端输出中对匹配的关键词应用视觉样式。

#### 3.8.2 高亮规则配置

每条规则包含：

| 字段 | 说明 |
|------|------|
| 关键词 | 普通文本 或 正则表达式 |
| 匹配模式 | 大小写敏感 / 不敏感 / 正则 |
| 前景色 | 文字颜色 |
| 背景色 | 底色（可选） |
| 字体样式 | 加粗 / 斜体 / 下划线（可叠加） |
| 触发通知 | 开关，匹配时推送系统通知并使标签页标题闪烁 |
| 触发声音 | 开关，匹配时播放提示音 |

#### 3.8.3 内置规则集（预设）

开箱即用的预设规则组：

| 规则集名称 | 包含关键词（示例） |
|------------|--------------------|
| 系统错误 | `ERROR` `FATAL` `CRITICAL` `Exception` `Traceback` |
| 警告 | `WARN` `WARNING` `DEPRECATED` |
| 成功 | `OK` `SUCCESS` `PASSED` `Done` |
| 系统日志 | `sshd` `sudo` `kernel` `oom-killer` |
| 网络 | IP 地址正则、`timeout` `refused` `unreachable` |

#### 3.8.4 规则集管理

- 规则集可创建、命名、复制、删除
- 规则集可与会话绑定（单个会话可挂多个规则集）
- 全局规则集 vs 会话专属规则集
- 支持导入 / 导出规则集（JSON 格式），便于团队共享

---

### 3.9 日志与录制

#### 3.9.1 文件日志（磁盘持久化）

- 每个会话可独立配置日志：设置 → 会话配置 → 日志
- 日志内容：终端原始输出（含转义序列）/ 纯文本（过滤转义）
- 文件命名模板：`{session_name}_{date}_{time}.log`（可自定义）
- 日志轮转：按日期 / 按大小（达到上限时创建新文件）
- 触发开关：`terminal.loggingEnabled`（默认关闭，需用户在设置中启用）

#### 3.9.2 实时日志面板（LoggingPanel）

> **v2.0 新增** | 对应 Figma-Spec-v2 §14 §4

工具栏「日志」按钮打开面板（`max-w-[900px]`），展示当前会话的 in-memory 实时日志流（无需开启文件日志）：

**日志条目分类（LogType）：**

| 类型 | 颜色 | 触发场景 |
|------|------|----------|
| `.output`（输出） | 绿色 `#34c759` | SSH 服务端输出，按行切割，ANSI 转义码自动剥离 |
| `.input`（输入） | 蓝色 `#007aff` | 用户通过 Compose Pane 或快捷命令发送的命令 |
| `.error`（错误） | 红色 `#ff3b30` | AI 错误侦探检测到的错误片段 |
| `.system`（系统） | 橙色 `#ff9500` | 连接建立、正常断开、意外断开等连接事件 |

**过滤与搜索能力：**

- 类型过滤：全部 / 输入 / 输出 / 错误 / 系统（多选 Chip 样式）
- 关键词搜索：实时过滤内容与会话名
- 会话过滤：在"全部会话"视图中按会话名单独筛选
- 自动滚动：新条目自动滚到底部（可「暂停」固定浏览历史）

**操作：**

- 清空：清除当前会话（或全部会话）的内存日志
- 导出：将当前过滤结果保存为 `.txt` 文件（`NSSavePanel`）

**内存策略：** 保留最近 5000 条日志条目（FIFO 淘汰）；应用退出后清空，不持久化（持久化请启用文件日志）。

**验收用例（TC-LOG-001）：** 建立 SSH 连接后，执行 `ls -la` → 日志面板 `.output` 分类显示命令回显；在 Compose Pane 输入 `whoami` 并发送 → `.input` 分类显示 `whoami`；断开连接 → `.system` 分类显示「连接意外断开」。

#### 3.9.3 会话录制与回放

- 录制格式：`.cast`（兼容 asciinema 格式）
- 录制内容：终端输入输出 + 时间戳，可在应用内回放
- 回放控制：播放 / 暂停 / 倍速（0.5× / 1× / 2× / 4×） / 进度拖拽
- 录制操作：工具栏录制按钮（红点）/ 菜单 Tools → Start Recording
- 录制文件管理：Settings → Recordings，支持查看、导出、删除

---

### 3.10 安全与凭据管理

#### 3.10.1 Keychain 存储策略

- 所有密码和私钥 Passphrase 存储在 macOS Keychain（`kSecClassGenericPassword`）
- Keychain 条目以 `ShellMate_<UUID>` 为 Service 名称，与系统其他条目隔离
- 私钥文件内容以 `kSecClassKey` 加密存储，不以明文写入磁盘
- 应用访问 Keychain 时，系统自动要求用户授权（首次），后续在「始终允许」范围内免提示

#### 3.10.2 主密码（Master Password）

参考 Xshell 的 Master Password 功能：

- 可选启用：Settings → Security → Enable Master Password
- 启用后：应用启动时或超时锁定后，需输入主密码解锁
- 主密码用于加密本地会话配置文件中的敏感字段（备用保护层）
- 忘记主密码：只能重置（将清除加密字段，需重新输入凭据）

#### 3.10.3 自动锁定

- 可配置空闲超时自动锁定（默认：不锁定）
- 锁定后：终端内容模糊处理，需输入主密码（或 Touch ID）解锁
- 锁定不断开已有 SSH 连接，仅遮蔽 UI

#### 3.10.4 加密算法支持

| 类别 | 支持算法 |
|------|----------|
| 密钥交换 | curve25519-sha256、ecdh-sha2-nistp256/384/521、diffie-hellman-group14-sha256 |
| 主机密钥 | ssh-ed25519、ecdsa-sha2-nistp256/384/521、rsa-sha2-256/512 |
| 对称加密 | aes256-ctr、aes192-ctr、aes128-ctr、chacha20-poly1305 |
| MAC | hmac-sha2-256-etm、hmac-sha2-512-etm |

算法列表遵循 OpenSSH 8.x+ 的安全推荐，禁用 3DES、RC4、MD5、SHA1 等弱算法。

#### 3.10.5 已知主机管理

- 集中管理界面：Settings → Security → Known Hosts
- 列显示：主机名/IP、密钥类型、指纹、添加日期
- 支持单条删除、全部清除
- 当远端主机密钥变更时，高亮对应条目并提示核实

---

### 3.11 外观与个性化

#### 3.11.1 颜色方案

内置主题（可在「Settings → Appearance → Color Scheme」选择）：

| 主题名称 | 风格 |
|----------|------|
| ShellMate Dark（默认） | 深色，低对比度专业风 |
| ShellMate Light | 浅色，适合白天使用 |
| Solarized Dark | 经典 Solarized 暗色 |
| Solarized Light | 经典 Solarized 亮色 |
| Dracula | 紫色系流行主题 |
| One Dark | VS Code 同款暗色 |
| Nord | 北欧冷色调 |
| Gruvbox | 复古暖色调 |

支持自定义颜色方案：

- 可编辑所有 16 个 ANSI 颜色、背景色、前景色、光标色
- 支持导入 / 导出（v2.0 扩展）：

| 格式 | 导入 | 导出 | 说明 |
|------|------|------|------|
| `.shellmatetheme`（JSON） | ✅ | ✅ | ShellMate 原生格式，含字体+颜色+光标完整配置 |
| `.itermcolors`（Plist XML） | ✅ | ✅ | 与 iTerm2 互通 |
| JSON（Tabby / Hyper 格式） | ✅ | ❌ | 社区主题生态导入 |

- 内置主题市场入口（Settings → Appearance → 浏览主题）：展示精选社区主题，点击一键导入
- 主题支持 iCloud 同步（见 §3.12）

#### 3.11.2 字体配置

| 配置项 | 说明 |
|--------|------|
| 字体族 | 所有系统已安装等宽字体；内置 JetBrains Mono, Fira Code, Source Code Pro |
| 字号 | 8–32 px，步进 1；`⌘+` / `⌘-` 实时调整 |
| 行间距 | 0.8–2.0，步进 0.1 |
| 字间距 | -1 至 +2 px |
| 连字支持 | 开关（Fira Code、JetBrains Mono 等支持 ligature 的字体） |
| 非 ASCII 字体 | 单独指定（用于中文等宽字符） |

#### 3.11.3 光标配置

| 配置项 | 选项 |
|--------|------|
| 形状 | 块状（Block）/ 竖线（I-Beam）/ 下划线（Underline） |
| 颜色 | 自定义颜色 |
| 闪烁 | 开 / 关 |
| 空闲时闪烁 | 开 / 关（仅在空闲时闪烁） |

#### 3.11.4 窗口外观

- 背景透明度：0–100%（通过 Settings → Appearance → Transparency 调整）
- 背景模糊效果（Vibrancy）：开关
- 终端边距（Padding）：上下左右各自可调（0–32 px）

#### 3.11.5 设置中心结构（5-Tab）

> **v2.2 更新** | 对齐 MRD v2.0 §6.2.11，设置面板从 3-Tab 扩展为 **5-Tab**，新增"颜色"和"AI 助手"Tab。
> **Figma-Spec-v2 §07** | `sm:max-w-[600px]`，`grid-cols-5` Tab 切换。

| Tab | 主要配置项 |
|-----|-----------|
| 通用 | 启动行为（是否自动连接）、iCloud 同步开关、导入/导出配置、语言设置、Hotkey Window 快捷键配置、命令安全审计开关 |
| 外观 | 深色/浅色/跟随系统模式切换、窗口透明度、字体族与字号、行间距、连字支持 |
| 终端 | 滚动回溯行数、鼠标行为（右键/三指点击）、字符编码（UTF-8/GBK）、Bell 提示（音效/闪烁）、光标形状与闪烁 |
| 颜色 | 终端主题选择（8 款内置 + 社区主题）、自定义 16 色 ANSI 调色板、主题导入/导出（`.shellmatetheme` / `.itermcolors`）、关键词高亮规则管理 |
| AI 助手 | 模型服务商选择（Claude/OpenAI/自定义）、API Key 配置（存入 Keychain）、Base URL（自定义端点）、模型 ID、请求超时时长 |

---

### 3.12 iCloud 同步

#### 3.12.1 同步内容

| 内容 | 同步 | 不同步 | 备注 |
|------|------|--------|------|
| 会话列表与分组结构 | ✅ | | |
| 会话基本配置（主机/端口/用户名） | ✅ | | |
| 外观配置（颜色方案、字体） | ✅ | | 含自定义 `.shellmatetheme` |
| 快捷命令集 | ✅ | | |
| 高亮规则集 | ✅ | | |
| 端口转发规则 | ✅ | | |
| 自动化触发器规则 | ✅ | | v2.0 新增 |
| 密码 / 私钥 | | ❌ | Keychain 不跨设备同步 |
| AI API Key | | ❌ | 安全考虑，仅存本机 Keychain |
| 日志文件 | | ❌ | |
| 会话录制文件 | | ❌ | |

#### 3.12.2 同步机制

- 使用 CloudKit Private Database（数据仅属于用户本人 iCloud 账户）
- 冲突处理：以最新修改时间戳为准，合并冲突字段
- 离线状态下本地修改暂存，联网后自动同步
- 同步状态指示：状态栏右侧显示「已同步 ✓」/ 「同步中…」/ 「同步失败 ⚠」

#### 3.12.3 凭据的跨设备迁移

由于 Keychain 不跨设备同步，首次在新设备使用已同步会话时：

- 状态栏 / 会话条目旁显示「凭据未配置」警告图标
- 连接时提示输入认证信息，并询问是否保存到本设备 Keychain

---

### 3.13 AI 智能助手

> **P1 优先级** | 与 Warp AI 的差异：聚焦"SSH 连接中场景"，而非通用代码生成。

#### 3.13.1 错误诊断（Error Detective）

- **触发条件**：终端输出含关键词 `error`、`failed`、`denied`、`fatal`、`exception`（大小写不敏感，正则匹配）
- **UI 表现**：终端底部出现非阻塞内联提示条（`AIErrorDetectiveView`），显示「AI 已检测到错误，点击分析」
- **分析上下文**：自动截取错误所在行 ±10 行（约 500 字符），附加当前会话的主机系统信息
- **输出格式**：
  - 错误原因（1–2 句，中文）
  - 建议修复命令（代码块，含复制/一键执行按钮）
  - 相关文档链接（可选，AI 推断后附加）
- **隐私边界**：终端内容**默认不上传**；用户点击「分析」按钮才发送选中片段；按钮旁有「🔒 仅发送选中内容」提示
- **验收用例（TC-AI-001）**：SSH 会话中执行 `sudo apt install nonexistent`，底部出现 AI 提示条，点击后 3 秒内返回错误分析

#### 3.13.2 命令智能补全

- **唤起方式**：`⌘/`（与搜索会话 `⌘F` 隔离）
- **输入框**：浮层于终端上方，占位文字「描述你想执行的操作…」
- **上下文**：最近 50 行终端输出（约 2000 字符）+ 当前工作目录（由 Shell Integration 提供，若无则跳过）
- **输出**：1–3 条命令建议，含解释说明；可按 `Tab` 选择、`Enter` 直接发送到终端
- **验收用例（TC-AI-002）**：在已连接会话中按 `⌘/`，输入「查看占用 8080 端口的进程」，AI 返回 `lsof -i :8080` 并可一键执行

#### 3.13.3 会话摘要

- **唤起方式**：`⌘⇧M`（M = Memo）
- **生成内容**：基于当前会话的近 200 行终端历史，生成操作摘要（执行了哪些命令、关键输出结果）
- **格式**：Markdown，带时间戳；可复制到剪贴板 / 导出为 `.md` 文件
- **验收用例（TC-AI-003）**：执行系列部署命令后按 `⌘⇧M`，AI 返回含命令列表和结果摘要的 Markdown 报告

#### 3.13.4 多模型支持

| 配置项 | 说明 |
|--------|------|
| 模型提供商 | Claude（默认）/ OpenAI / 自定义（兼容 OpenAI API）|
| API Key | 存入本机 Keychain，**不**上传或同步 |
| Base URL | 支持自定义（私有部署、中转代理场景）|
| 模型 ID | 可手动输入（如 `claude-opus-4-6`、`gpt-4o`）|
| 请求超时 | 默认 30 秒（可调整）|

配置入口：Settings → 通用 → AI 助手

#### 3.13.5 隐私与安全边界

| 原则 | 实现 |
|------|------|
| **默认不发送** | 终端内容不自动上传，仅用户主动触发才发送 |
| **最小上下文** | 每次请求只发送必要片段，不包含历史会话全量数据 |
| **Key 本地化** | API Key 仅存本机 Keychain（`kSecAttrAccessibleWhenUnlockedThisDeviceOnly`）|
| **App Store 合规** | Privacy Manifest 声明 AI 数据使用目的；网络请求仅到达用户配置的 API 端点 |
| **用户可见** | 每次 AI 请求在「日志面板」记录发送字节数和响应时间（不记录内容）|

#### 3.13.6 命令安全审计（AI 危险命令拦截）

> **P1 优先级** | 防止误操作导致不可逆损失，对标企业级 SSH 工具的命令审计功能。

**拦截机制：**

用户通过 Compose Pane 发送命令或通过 AI 面板「插入终端」时，在实际发送前经 `CommandSafetyChecker` 进行静态分析（正则匹配，本地计算，无网络请求）：

| 风险等级 | 颜色 | 典型规则示例 |
|---------|------|------------|
| `.danger`（危险） | 红色 `#ff3b30` | `rm -rf /`、`sudo rm -rf`、`dd if=... of=/dev/`、`mkfs`、`DROP TABLE/DATABASE`、Fork Bomb |
| `.warning`（警告） | 橙色 `#ff9500` | `chmod 777 -R`、`chown -R root`、`find ... -delete`、`git push --force`、`kill -9` |

**用户确认流程：**

- **警告级**：弹出确认 Sheet（`CommandSafetyAlertView`），显示风险等级、匹配规则说明；用户点击「确认执行」后发送
- **危险级**：两步确认——首次点击显示「继续执行（风险操作）」，第二次才出现红色的「确认，我知道风险」按钮；可选点击「AI 分析风险」按钮触发 AI 流式说明最坏场景、替代方案

**安全设计原则：**

- 分析完全本地，不上传命令内容
- 不阻止用户最终执行（尊重用户意图）；仅提供摩擦 + 信息
- 可在 Settings → 通用 → 命令安全审计 中关闭该功能
- ANSI 转义码预处理（防绕过）

**验收用例（TC-AI-006）：** 在 Compose Pane 输入 `rm -rf /tmp/test`，点击发送时弹出危险警告弹窗；两步确认后命令才发送到终端；输入 `ls -la` 时无拦截，直接发送。

---

### 3.14 服务器实时监控

> **P1 优先级** | 对标 MobaXterm 状态栏实时指标，深化为可配置告警系统。

#### 3.14.1 监控指标与采集方式

连接建立后，在独立 SSH 通道（复用已有连接的新 Channel）后台轮询：

| 指标 | 采集命令 | 轮询间隔 |
|------|----------|----------|
| CPU 使用率 | `top -bn1 \| grep "Cpu(s)"` 或 `/proc/stat` 差分计算 | 2 秒 |
| 内存使用率 | `free -b` 或 `/proc/meminfo` | 2 秒 |
| 磁盘使用率 | `df -h /` | 10 秒 |
| 网络收发速率 | `/proc/net/dev` 差分计算（B/s） | 2 秒 |
| 系统负载 | `/proc/loadavg`（1/5/15 分钟平均）| 5 秒 |

数据解析失败时（非 Linux 系统如 BSD/macOS），优雅降级为显示 `--`。

#### 3.14.2 状态栏展示

参考 Figma-Spec-v2 §05 状态栏规范：

- 各指标以「图标 + 数值 + 迷你进度条」形式展示
- 颜色阈值：

| 指标 | 正常（绿） | 警告（黄） | 危险（红） |
|------|-----------|-----------|-----------|
| CPU | < 60% | 60–80% | > 80% |
| 内存 | < 70% | 70–85% | > 85% |
| 磁盘 | < 75% | 75–90% | > 90% |
| 负载 | < CPU 核数 | CPU 核数 ~ 2× | > 2× CPU 核数 |

- 鼠标悬停展示详细数值（工具提示）
- 断开连接后所有指标归零并显示 `--`，**不**保留上次缓存数据

#### 3.14.3 告警通知

- 指标首次超过告警阈值时，推送 macOS 系统通知（`UNUserNotificationCenter`）
- 同一会话同一指标，60 秒内不重复推送（防通知轰炸）
- 通知内容格式：「⚠️ {会话名称}：CPU 使用率 {value}%，已超过 80% 警戒线」
- 告警开关：每个指标独立可关闭（Settings → 监控 → 告警规则）
- **验收用例（TC-MON-001）**：SSH 连接后在服务器执行 `stress --cpu 4 --timeout 10`，ShellMate 状态栏 CPU 指标变红，10 秒内收到系统通知

---

### 3.15 Hotkey Window（全局浮动终端）

> **P1 优先级** | 对标 iTerm2 Hotkey Window，面向快速运维指令场景。

#### 3.15.1 功能概述

全局浮动终端窗口，无论当前焦点在哪个 App，均可通过快捷键瞬时唤起/隐藏。典型使用场景：浏览器查看文档时，快速执行一条命令后收起，不切换工作区。

#### 3.15.2 行为规范

| 场景 | 行为 |
|------|------|
| 按下快捷键（应用未处于前台） | Hotkey Window 从屏幕顶部滑出，获取焦点 |
| 按下快捷键（Hotkey Window 已可见） | Hotkey Window 收起（滑回顶部）|
| 鼠标点击 Hotkey Window 之外区域 | 若「失焦自动隐藏」开启，自动收起（默认开启）|
| 切换到其他 Space | 若「跟随 Space」关闭，Hotkey Window 在所有 Space 显示（`NSWindowCollectionBehavior.canJoinAllSpaces`）|

#### 3.15.3 窗口规格

| 属性 | 默认值 | 可调整范围 |
|------|--------|-----------|
| 宽度 | 屏幕宽度 80% | 40%–100% |
| 高度 | 屏幕高度 40% | 20%–60% |
| 位置 | 屏幕顶部居中 | 可选：顶部/底部/左侧/右侧 |
| 窗口层级 | `NSWindowLevel.floating + 1` | — |
| 背景 | 跟随应用外观（透明度可配置） | — |
| 动画 | 200ms 弹性滑动（与主窗口动效一致）| — |

#### 3.15.4 内容

- 无 SSH 会话时显示空状态引导界面（提示用户从侧边栏发起连接）
- 可切换到任意已连接 SSH 会话（标签栏与主窗口独立）
- 支持多标签（最多 5 个，避免面板臃肿）

#### 3.15.5 配置

入口：Settings → 通用 → Hotkey Window

| 配置项 | 默认值 |
|--------|--------|
| 全局快捷键 | `⌥Space` |
| 失焦自动隐藏 | 开 |
| 跟随所有 Space | 开 |
| 启动时不自动显示 | 开 |

- 快捷键冲突检测：注册前检查系统级快捷键，冲突时提示更换
- 实现方案：`NSEvent.addGlobalMonitorForEvents(matching: .keyDown, handler:)` 监听全局按键

#### 3.15.6 验收用例

| 用例编号 | 场景 | 预期结果 |
|----------|------|----------|
| TC-HK-001 | 焦点在 Safari，按 `⌥Space` | Hotkey Window 从顶部滑出，获取焦点 |
| TC-HK-002 | Hotkey Window 可见时再按 `⌥Space` | 窗口收起，Safari 重新获取焦点 |
| TC-HK-003 | 点击 Hotkey Window 外部区域 | 窗口自动收起 |

---

### 3.16 tmux 集成管理

> **P2 优先级** | 对标 iTerm2 tmux 集成，降低 tmux 使用门槛。

#### 3.16.1 自动检测

SSH 连接建立后，后台静默执行 `which tmux 2>/dev/null && tmux -V`：
- 检测到 tmux：在工具栏显示 tmux 图标（激活态），点击打开 tmux 管理器面板
- 未检测到：tmux 图标置灰，悬停提示「目标服务器未安装 tmux」

#### 3.16.2 tmux 管理器面板

面板宽度 `max-w-[900px]`，三 Tab 结构（对齐 Figma-Spec-v2 §10）：

**Tab 1：Sessions（会话列表）**
- 每个 tmux session 展示为卡片：会话 ID、名称、窗口数量、创建时间
- 已附加（Attached）的 session 卡片显示渐变高亮边框
- 操作：附加（Attach）/ 重命名 / 删除（确认弹窗）

**Tab 2：Windows（窗口列表）**
- 列出选中 session 下的所有窗口
- 每个窗口卡片：编号、名称、活跃/非活跃状态、包含的 Pane 数量
- 操作：切换到窗口 / 重命名 / 关闭

**Tab 3：Quick Actions（快捷操作）**
2×2 Grid 布局：

| 位置 | 操作 |
|------|------|
| 左上 | 新建 Session（输入名称 → 执行 `tmux new-session -s {name}`）|
| 右上 | 附加到已有 Session（选择列表 → 执行 `tmux attach -t {id}`）|
| 左下 | 新建 Window（在当前 session → 执行 `tmux new-window`）|
| 右下 | Kill Server（确认弹窗 → 执行 `tmux kill-server`）|

#### 3.16.3 验收用例

- **TC-TMUX-001**：SSH 连接到已安装 tmux 的服务器，工具栏 tmux 图标 2 秒内变为激活态
- **TC-TMUX-002**：在 tmux 管理器 Quick Actions 点击「新建 Session」，输入名称后服务器上创建新 tmux session，Session 列表刷新

---

### 3.17 FTUE 首次使用引导

> **P1 优先级** | 降低新用户上手门槛，提升 Day 1 留存。

#### 3.17.1 触发条件

`AppStorage("hasLaunchedBefore") == false`（已实现 `WelcomeScreenView`）

#### 3.17.2 三步 Onboarding 流程

**Step 1：欢迎（Welcome）**
- 英雄图标：ShellMate App Icon（96px）
- 主标题：「欢迎使用 ShellMate」
- 副标题：「专业开发者的 macOS 原生 SSH 工具」
- 三个差异化亮点卡片（横排）：
  - 🔒 安全本地化：凭据存于 Keychain，永不上云
  - 🤖 AI 错误诊断：终端报错自动分析，一键获取修复方案
  - ⚡ 极速管理：多标签并发、tmux 集成、Hotkey Window 随叫随到
- 操作按钮：「继续」（主按钮）

**Step 2：核心功能展示（Features）**
- 三张功能卡片（竖排列表或横排轮播）：
  - **AI 错误侦探**：终端报错 → AI 自动分析 → 一键执行修复命令（配截图或动效）
  - **Hotkey Window**：任意 App 内 `⌥Space` 唤起终端，运维指令触手可及
  - **tmux 集成**：图形化管理 tmux 会话/窗口，无需记忆繁琐命令
- 步骤指示器：3 个圆点，当前步为蓝色宽条（Figma-Spec-v2 §13）
- 操作按钮：「上一步」（次要）/ 「继续」（主按钮）

**Step 3：快捷开始（Get Started）**
- 标题：「选择你的起点」
- 三个行动卡片（3 列 Grid）：
  - 📥 导入配置：导入 `.ssh/config` 或 Xshell/Termius 配置文件 → 触发导入弹窗
  - ➕ 新建会话：创建第一个 SSH 会话 → 触发新建会话弹窗
  - 🖥️ 快速探索：直接进入应用主界面 → 关闭引导，从侧边栏浏览已有会话
- 跳过按钮（文字链）：右下角「跳过引导」

#### 3.17.3 完成标记

任意行动卡片点击后，或点击「跳过引导」时：
```swift
hasLaunchedBefore = true
```
不再展示引导（除非用户在 Settings 中手动重置）。

#### 3.17.4 验收用例

- **TC-FTUE-001**：全新安装，首次启动出现欢迎界面，三步流程可正常点击流转
- **TC-FTUE-002**：点击「新建会话」后，引导消失，新建会话弹窗弹出
- **TC-FTUE-003**：重启应用后，引导**不再**出现

---

### 3.18 自动化触发器

> **P2 优先级** | 对标 iTerm2 Triggers，面向运维场景自动化响应。

#### 3.18.1 触发器概述

当终端输出内容匹配指定条件时，自动执行预设动作。支持会话级别（仅对指定会话生效）和全局级别（对所有会话生效）。

#### 3.18.2 触发条件类型

| 类型 | 说明 | 示例 |
|------|------|------|
| 连接成功 | SSH 连接建立后立即触发 | 自动执行 `source ~/.profile` |
| 会话断开 | SSH 连接断开时触发 | 发送断连通知 |
| 输出匹配（正则） | 终端输出行匹配正则表达式 | `\[ERROR\].*OOM` → 触发内存告警 |
| 输出匹配（关键词） | 终端输出包含指定字符串 | `deployment complete` → 通知 |

#### 3.18.3 触发动作类型

| 类型 | 说明 | 参数 |
|------|------|------|
| 发送命令 | 向终端发送命令字符串 | 命令内容（支持 §3.7.1 变量） |
| 发送系统通知 | 推送 macOS 通知 | 标题、正文（支持 `\0` 捕获组引用）|
| 打开 URL | 在默认浏览器打开 URL | URL（支持变量替换） |
| 写入日志 | 追加一行到指定日志文件 | 文件路径、内容模板 |
| 运行本地脚本 | 在本机执行 Shell 脚本（Direct 版）| 脚本路径（App Store 版不支持）|
| 高亮行 | 对匹配行应用颜色高亮 | 前景色、背景色 |

#### 3.18.4 数据模型

```swift
struct AutomationTrigger {
    var id: UUID
    var name: String                        // 显示名称
    var isEnabled: Bool                     // 开关（不删除规则，只禁用）
    var scope: TriggerScope                 // .global / .session(UUID)
    var conditionType: TriggerCondition     // .onConnect / .onDisconnect / .outputRegex / .outputKeyword
    var pattern: String?                    // 正则或关键词（conditionType 为 output 时有效）
    var caseSensitive: Bool                 // 大小写敏感
    var actionType: TriggerAction           // .sendCommand / .notification / .openURL / .writeLog / .runScript / .highlightLine
    var actionPayload: String               // 动作参数（命令/URL/文件路径/通知文字）
    var cooldownSeconds: Int                // 冷却时间（同一触发器多少秒内不重复触发，0 = 不限制）
    var createdAt: Date
}
```

#### 3.18.5 UI 入口

- Settings → 自动化（设置面板第 6 个 Tab，扩展后完整结构：通用/外观/终端/颜色/AI 助手/自动化）
- 触发器列表：显示名称、条件摘要、动作摘要、开关、编辑/删除
- 新建/编辑触发器：Sheet 弹窗（`max-w-[560px]`）

#### 3.18.6 验收用例

- **TC-TRIG-001**：新建触发器「输出匹配 `\[ERROR\]` → 发送通知」，SSH 会话中执行 `echo "[ERROR] test"`, 收到 macOS 通知
- **TC-TRIG-002**：新建「连接成功 → 发送命令 `echo "Hi {{USER}}"` 」，连接后终端自动打印 `Hi ubuntu`

---

## 4. 全局交互规范

### 4.1 主窗口布局

```
┌─── 标题栏（macOS 原生，38px）──────────────────────────────────────┐
│  ● ● ●   ShellMate — prod-web-01                      [⇌][{}][⚙]  │
├─── 标签栏（36px）──────────────────────────────────────────────────┤
│  🟢 prod-web-01 ×    🟢 prod-db ×    🟡 dev-app ×    [+]          │
├─────────────────┬──────────────────────────────────┬───────────────┤
│                 │  工具栏（28px，可隐藏）           │               │
│   会话侧边栏    ├──────────────────────────────────┤  SFTP 面板    │
│   （220px）     │  ubuntu@prod-web-01:~/services/  │  （300px）    │
│                 │─────────────────────────────────  │  （可切换）   │
│  [搜索框]       │                                  │               │
│  ├ 生产环境     │   终端内容区                      │  路径导航     │
│  │ 🟢 web-01   │                                  │  文件列表     │
│  │ 🟢 web-02   │                                  │               │
│  │ 🟢 db       │                                  │  传输队列     │
│  ├ 开发环境     │                                  │               │
│  │ ⚪ dev-app  │                                  │               │
│  └ 跳板机       │                                  │               │
│  🟢 bastion    │                                  │               │
│                 ├──────────────────────────────────┤               │
│  [+ 新建会话]   │  快捷命令栏（可收起）             │               │
├─────────────────┴──────────────────────────────────┴───────────────┤
│  状态栏（24px）: 🟢已连接  SSH-2  ubuntu  UTF-8  22  ⌁11ms  120×38 │
└────────────────────────────────────────────────────────────────────┘
```

### 4.2 弹窗规范

| 弹窗类型 | 使用场景 | 规范 |
|----------|----------|------|
| Sheet（附属弹窗） | 会话配置、新建隧道 | 从主窗口顶部滑出，遮蔽主内容 |
| Alert（警告） | 删除确认、指纹警告 | 系统 NSAlert 样式 |
| Popover（气泡） | 快速信息显示 | 不遮蔽操作区域 |
| Panel（浮动面板） | 搜索、颜色选择器 | 可拖拽 |

### 4.3 侧边栏显隐

- 快捷键 `⌘⌥S` 收起/展开会话侧边栏
- 最小窗口宽度：侧边栏收起时 400px，展开时 620px
- 侧边栏宽度可拖拽调整（180–320px 范围），持久化记忆

### 4.4 状态栏信息

从左到右：

| 区域 | 内容 |
|------|------|
| 连接状态 | 🟢 已连接 / 🟡 重连中 / ⚪ 已断开 |
| 协议版本 | SSH-2.0 |
| 加密算法 | AES256-CTR（鼠标悬停展示完整算法套件） |
| 用户名 | ubuntu |
| 编码 | UTF-8 |
| 端口 | 22 |
| 连接时长 | 2h 14m（鼠标悬停展示精确时间） |
| （spacer） | — |
| 网络延迟 | ⌁ 11ms（每 30 秒更新） |
| 终端尺寸 | 120×38（列×行，调整窗口时实时更新） |
| iCloud 同步 | ✓ 已同步 / 🔄 / ⚠ |

---

## 5. 非功能性需求

### 5.1 性能指标

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 冷启动时间 | < 2 秒 | 从点击应用图标到主窗口可交互 |
| SSH 连接建立时间 | < 3 秒（网络正常时） | 从点击连接到终端 shell 就绪 |
| 终端渲染帧率 | ≥ 60 fps（大量输出时） | Instruments Core Animation |
| 键盘输入延迟（本地回显） | < 16 ms | 手动测量 |
| 空闲内存占用 | < 80 MB（3 个会话） | Activity Monitor |
| 500 个会话搜索响应 | < 100 ms | 代码内计时 |
| SFTP 100MB 文件传输开销 | < 5% 额外 CPU | Instruments Energy Log |

### 5.2 可靠性

- 单个 SSH 会话连续使用 8 小时不崩溃（MTBF ≥ 8h）
- 应用崩溃时自动恢复上次会话配置（不含活跃连接）
- 数据库（Core Data）写操作使用事务，确保断电不丢失已保存配置

### 5.3 兼容性

| 维度 | 要求 |
|------|------|
| macOS 版本 | macOS 13 Ventura 及以上 |
| 处理器 | Intel x86_64 + Apple Silicon（Universal Binary） |
| 屏幕 | Retina 与非 Retina 均正确渲染；支持外接多显示器 |
| 辅助功能 | VoiceOver 基础导航支持；Dynamic Type 字体缩放 |

### 5.4 安全性

- 不向任何外部服务器上报用户数据（除 CloudKit，属用户自有数据）
- SSH 通信全程通过 libssh2 加密，不走任何中间代理
- 应用沙盒（App Store 版）：仅申请必要权限
  - `com.apple.security.network.client`（SSH 出站）
  - `com.apple.security.files.user-selected.read-write`（私钥文件）
  - Keychain Access Groups

### 5.5 本地化

- 首发语言：简体中文（主要）、英文（同步）
- 字符串外部化（.strings 文件），便于后续扩展其他语言
- 日期 / 时间格式遵循系统区域设置

---

## 6. 数据模型

### 6.1 Session（会话）

```swift
struct Session {
    var id: UUID                          // 唯一标识
    var name: String                      // 显示名称
    var groupID: UUID?                    // 所属分组
    var host: String                      // 主机名或 IP
    var port: Int                         // 默认 22
    var username: String
    var authMethod: AuthMethod            // 认证方式枚举
    var keychainRef: String?              // Keychain 条目引用 UUID
    var jumpHosts: [JumpHost]             // 跳板机链路（有序数组）
    var terminalProfile: TerminalProfile  // 外观配置（或继承自分组）
    var tunnelRules: [TunnelRule]         // 关联端口转发规则
    var highlightSetIDs: [UUID]           // 关联高亮规则集
    var quickCommandSetID: UUID?          // 关联快捷命令集
    var logConfig: LogConfig              // 日志配置
    var keepAliveInterval: Int            // Keepalive 秒数
    var autoReconnect: Bool
    var encoding: String                  // UTF-8 / GBK 等
    var notes: String                     // 备注
    var tags: [String]                    // 标签
    var iconName: String?                 // 自定义图标
    var createdAt: Date
    var lastConnectedAt: Date?
}
```

### 6.2 SessionGroup（分组）

```swift
struct SessionGroup {
    var id: UUID
    var name: String
    var parentGroupID: UUID?              // 支持嵌套
    var color: String                     // 16 进制色值
    var sortOrder: Int
    var isExpanded: Bool                  // 折叠状态
    var inheritedProfile: TerminalProfile? // 子会话继承的属性
}
```

### 6.3 TunnelRule（端口转发规则）

```swift
struct TunnelRule {
    var id: UUID
    var type: TunnelType                  // localForward / remoteForward / dynamicSocks
    var localBindAddress: String          // 默认 127.0.0.1
    var localPort: Int
    var remoteHost: String
    var remotePort: Int
    var sessionID: UUID
    var autoStart: Bool
    var notes: String
}
```

### 6.4 HighlightRule / HighlightSet（高亮规则）

```swift
struct HighlightRule {
    var id: UUID
    var keyword: String
    var isRegex: Bool
    var caseSensitive: Bool
    var foregroundColor: String?
    var backgroundColor: String?
    var isBold: Bool
    var isItalic: Bool
    var isUnderline: Bool
    var triggerNotification: Bool
    var triggerSound: Bool
}

struct HighlightSet {
    var id: UUID
    var name: String
    var rules: [HighlightRule]
    var isGlobal: Bool
}
```

---

## 7. 错误处理规范

### 7.1 连接类错误

| 错误类型 | 用户提示 | 建议操作 |
|----------|----------|----------|
| 主机不可达 | 「无法连接到 {host}，主机不可达」 | 检查主机地址和网络 |
| 连接超时 | 「连接 {host} 超时（{timeout}s）」 | 增大超时时间或检查防火墙 |
| 端口拒绝 | 「端口 {port} 被拒绝，SSH 服务可能未启动」 | 检查服务器 sshd 状态 |
| 认证失败-密码 | 「密码认证失败，请检查用户名和密码」 | 重新输入密码 |
| 认证失败-密钥 | 「公钥认证失败，服务器未接受此密钥」 | 检查 authorized_keys |
| 主机密钥变更 | 「⚠️ 警告：{host} 的主机密钥已改变！」 | 核实后手动更新 Known Hosts |
| 握手失败 | 「SSH 握手失败：{detail}」 | 可能是协议版本不兼容 |

### 7.2 错误展示规则

- **P0 错误**（主机密钥变更、认证失败）：模态弹窗，强制用户确认后才可继续
- **P1 错误**（连接超时、断连）：状态栏内联显示 + 非模态提示条（toast），3 秒后自动消失，操作按钮保留
- **P2 错误**（SFTP 操作失败、日志写入失败）：状态栏短暂提示，不打断主操作流

---

## 8. 快捷键一览

### 8.1 全局快捷键

| 操作 | 快捷键 |
|------|--------|
| 新建会话（打开配置弹窗） | `⌘N` |
| 快速连接（直接输入地址） | `⌘⇧N` |
| 搜索会话 | `⌘F` |
| 打开设置 | `⌘,` |
| 收起/展开侧边栏 | `⌘⌥S` |
| 打开/关闭 SFTP 面板 | `⌘⇧S` |
| 打开隧道管理器 | `⌘⇧U` |
| 打开快捷命令管理器 | `⌘⇧K` |
| 唤起/隐藏 Hotkey Window | `⌥Space`（全局，可自定义） |
| 唤起 AI 命令补全 | `⌘/` |
| 生成会话摘要 | `⌘⇧M` |

### 8.2 标签页快捷键

| 操作 | 快捷键 |
|------|--------|
| 新建空标签 | `⌘T` |
| 重复当前会话标签 | `⌘⇧D` |
| 关闭当前标签 | `⌘W` |
| 下一个标签 | `⌘⇧]` |
| 上一个标签 | `⌘⇧[` |
| 跳转到第 N 个标签 | `⌘1` – `⌘9` |
| 左右分屏 | `⌘⌥→` |
| 上下分屏 | `⌘⌥↓` |
| 关闭当前窗格 | `⌘⌥W` |
| 切换到下一窗格 | `⌃⌥Tab` |

### 8.3 终端内快捷键

| 操作 | 快捷键 |
|------|--------|
| 终端内搜索 | `⌘F` |
| 放大字号 | `⌘+` |
| 缩小字号 | `⌘-` |
| 恢复默认字号 | `⌘0` |
| 清屏 | `⌘K` |
| 全选终端内容 | `⌘A` |
| 复制选中内容 | `⌘C` |
| 粘贴 | `⌘V` |
| 跳到缓冲区顶部 | `⌘Home` |
| 跳到缓冲区底部 | `⌘End` |
| 发送 Compose Pane 内容 | `⌃Return` |

---

## 9. 验收标准

### 9.1 P0 核心功能验收用例

| 用例编号 | 用例描述 | 预期结果 |
|----------|----------|----------|
| TC-001 | 密码认证 SSH 连接 | 终端显示 shell 提示符，状态栏「已连接」，延迟 < 3s |
| TC-002 | SSH Key 认证（Ed25519） | 无需输入密码成功连接 |
| TC-003 | 会话持久化 | 退出重启后所有会话配置和分组结构完整恢复 |
| TC-004 | 多标签并发（3 个会话） | 3 个独立标签页各自独立工作，相互不干扰 |
| TC-005 | 自动重连 | 断网后自动重试，网络恢复后自动重连 |
| TC-006 | SFTP 文件上传 | 文件成功上传，传输队列显示进度 100% |
| TC-007 | 本地端口转发 | 流量正确转发到远端目标 |
| TC-008 | 关键词高亮 | ERROR 关键词高亮显示为配置的颜色 |
| TC-009 | 主机密钥变更警告 | 弹出高优先级警告弹窗，阻止自动连接 |
| TC-010 | iCloud 同步 | Mac A 新建会话后，Mac B 在 30 秒内同步出现该会话 |

### 9.2 性能验收

| 测试项 | 通过标准 |
|--------|----------|
| 冷启动时间 | < 2 秒（M2 MacBook Air） |
| 500 会话搜索 | < 100ms 响应 |
| 10,000 行输出渲染 | 终端帧率维持 ≥ 60fps |
| 100MB SFTP 传输 | CPU 占用 < 40%，不影响终端交互 |
| 3 个并发连接内存 | < 150MB RSS |

---

## 10. 版本历史

| 版本 | 日期 | 变更摘要 |
|------|------|----------|
| v0.1 | 2026-03-15 | 初稿，功能模块框架 |
| v1.0 | 2026-03-18 | 完整 PRD，整合竞品研究，含数据模型、错误处理、快捷键表 |
| v1.1 | 2026-04-02 | 新增 §3.4.0 本地终端默认标签页（MobaXterm 行为对标） |
| v2.0 | 2026-04-08 | 竞品深度调研后全面升级：新增 §3.13 AI助手 / §3.14 监控 / §3.15 Hotkey Window / §3.16 tmux / §3.17 FTUE / §3.18 触发器；更新 §1.3 竞品分析（+Tabby/Royal TSX/iTerm2）、§2 架构图、§3.1.5 导入格式、§3.2.2 Touch ID、§3.3.6 通知、§3.7.1 参数化变量、§3.11 主题格式、§3.12 iCloud 同步范围 |
| v2.1 | 2026-04-12 | 补全实现对齐：§3.9 新增 §3.9.2「实时日志面板」（SessionLogStore 四分类 in-memory 日志、类型过滤、关键词搜索、导出）；§3.13 新增 §3.13.6「命令安全审计」（CommandSafetyChecker 本地正则分析、危险/警告两级、两步确认流程、AI 风险分析）；版本号升至 v2.1 |
| v2.2 | 2026-04-14 | 对齐 MRD v2.0：移除 §3.4.0 本地终端默认标签页（及 ⌘⇧L 快捷键、工具栏本地 Shell 按钮）；新增 §3.11.5 设置中心结构（5-Tab：通用/外观/终端/颜色/AI 助手，Figma-Spec-v2 §07）；更新 §3.15.4 Hotkey Window 内容描述；更新 §3.17.2 FTUE 引导内容（移除本地 Shell 相关描述，Step 2 功能展示替换为 tmux 集成，Step 3「直接开始」更新为「快速探索」）；更新 §3.18.5 自动化触发器 UI 入口为设置面板第 6 Tab；更新 §2 功能架构图 |

---

*本文档为产品研发内部文件。如有功能调整，请通过版本控制记录变更。*

*下一步：研发团队基于本 PRD 拆分 Sprint 任务，优先实现 P0 验收用例（TC-001 至 TC-005）。*
