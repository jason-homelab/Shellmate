# ShellMate for macOS — 技术方案文档

> **文档版本：** v4.0
> **创建日期：** 2026-03-18
> **最后更新：** 2026-04-14
> **文档类型：** 技术架构方案（Technical Architecture Document）
> **文档状态：** 已更新（对齐 PRD v2.2 + Figma-Spec-v2，2026-04-14）
> **基于：** ShellMate PRD v2.2 · 产品评审报告 v1.0 · **Figma-Spec-v2（2026-04-02）**
> **作者：** 解决方案架构师

---

## 目录

1. [技术选型与依据](#1-技术选型与依据)
2. [整体架构设计](#2-整体架构设计)
3. [模块详细设计](#3-模块详细设计)
   - 3.1 SSH 连接层
   - 3.2 终端渲染层（含四格分屏、AI 内联组件）
   - 3.3 数据持久化层
   - 3.4 SFTP 文件管理
   - 3.5 端口转发与隧道
   - 3.6 iCloud 同步层
   - 3.7 安全与 Keychain
   - 3.8 AI 服务层
   - 3.9 国际化策略
   - 3.10 系统性能监控模块（状态栏）
   - 3.11 欢迎引导模块（WelcomeScreen）
   - 3.12 脚本自动化模块（ScriptAutomationPanel）
   - 3.13 录制与回放模块（RecordingDialog）
   - 3.14 日志面板模块（LoggingPanel）
   - 3.15 导入/导出模块（SessionImportExportDialog）
   - 3.16 UI 设计令牌系统（Figma-Spec-v2）
   - 3.17 Hotkey Window 模块（全局浮动终端）
   - 3.18 tmux 集成管理模块
   - 3.19 自动化触发器引擎
4. [并发与线程模型](#4-并发与线程模型)
5. [数据流设计](#5-数据流设计)
6. [关键技术难点与解决方案](#6-关键技术难点与解决方案)
7. [性能设计](#7-性能设计)
8. [安全设计](#8-安全设计)
9. [测试策略](#9-测试策略)
10. [工程化规范](#10-工程化规范)
11. [分阶段交付计划](#11-分阶段交付计划)
12. [风险与应对](#12-风险与应对)

---

## 1. 技术选型与依据

### 1.1 核心技术栈

| 层次 | 技术选型 | 版本要求 | 选型依据 |
|------|---------|---------|---------|
| **开发语言** | Swift | 5.9+ | ABI 稳定性保证、原生性能、Apple 官方一等公民 |
| **UI 主框架** | SwiftUI | macOS 14+ | 声明式 UI、与 Combine 天然搭配、状态驱动渲染 |
| **UI 补充** | AppKit（NSView/NSWindow） | macOS 13+ | 复杂自定义控件（终端视图、拖拽、右键菜单）无法纯 SwiftUI 实现 |
| **SSH 协议层** | libssh2 | 1.11+ | MIT License、成熟稳定（15+ 年维护）、社区广泛验证 |
| **终端渲染** | SwiftTerm | Latest | Swift 原生、完整 VT100/xterm-256color/True Color 支持、MIT License |
| **数据持久化** | Core Data + CloudKit | — | Apple 官方方案、Sandbox 兼容、iCloud 同步原生支持 |
| **凭据存储** | Keychain Services | — | iOS/macOS 安全飞地级存储，无法被其他应用读取 |
| **云同步** | CloudKit（Private DB） | — | 数据不经过第三方，用户 iCloud 账户私有库 |
| **构建工具** | Xcode 15+ / Swift Package Manager | — | 原生工具链，SPM 管理第三方依赖 |
| **最低部署目标** | macOS 13 Ventura | — | 覆盖 98%+ 在用 Mac；SwiftUI 成熟度达标 |

### 1.2 关键第三方依赖

```
依赖关系（SPM Package.swift）:

dependencies: [
    // SSH 协议核心
    .package(url: "https://github.com/Kitura/BlueSocket", ...),
    // 或直接使用 libssh2 通过 XCFramework 集成

    // 终端渲染
    .package(url: "https://github.com/migueldeicaza/SwiftTerm", from: "1.1.0"),
]
```

> **注：** libssh2 以预编译 XCFramework（包含 arm64 + x86_64）形式集成，通过 Swift 桥接层暴露。不引入 Homebrew OpenSSL 的系统依赖，所有 libssh2 依赖的 openssl/libz 均静态链接至 framework 内，确保 App Store 兼容。

### 1.3 被排除的选项及理由

| 排除选项 | 排除原因 |
|---------|---------|
| Electron | 内存高（250MB+），无原生 Keychain 集成，与 HIG 违背 |
| NIO + Swift-NIO-SSH | 成熟度不足，SSH Edge Case 覆盖不如 libssh2 |
| Realm | 引入重型依赖，Core Data 已满足需求且原生支持 iCloud |
| SQLite.swift | 缺失 CloudKit 同步能力，需额外开发同步层 |

---

## 2. 整体架构设计

### 2.1 分层架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                       展示层（Presentation Layer）                   │
│  SwiftUI Views + AppKit NSView wrappers                             │
│  SessionSidebar │ TabBar │ TerminalView │ SFTPPanel │ AIAssistant   │
│  AIErrorDetective │ Settings │ WelcomeScreen │ ScriptAutoPanel   │
│  LoggingPanel │ RecordingDialog │ SessionImportExportDialog        │
│  HotkeyWindowManager │ TmuxManagerView │ AutomationTriggersView   │
└───────────────────────────────┬─────────────────────────────────────┘
                                │  @StateObject / @ObservedObject
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     应用层（Application Layer）                       │
│  ViewModels / Stores（ObservableObject）                            │
│  SessionStore │ TabBarStore │ GroupStore │ SFTPStore │ AIService    │
│  SystemMonitorStore │ WelcomeStore │ ScriptStore │ LogStore     │
│  TmuxSessionStore │ AutomationTriggerStore │ NetworkMonitor      │
│  CommandSafetyChecker（本地静态分析，无网络）                      │
└─────┬──────────────────┬─────────────────┬──────────────────────┬───┘
      │                  │                 │                      │
      ▼                  ▼                 ▼                      ▼
┌──────────┐   ┌──────────────────┐  ┌──────────┐   ┌────────────────┐
│ 会话管理  │   │  SSH 连接服务     │  │SFTP 服务 │   │  AI 服务        │
│ SessionMgr│  │  SSHConnectionSvc│  │SFTPSession│  │Claude/OpenAI/  │
│           │  │                  │  │(独立连接) │  │Ollama SSE      │
└──────┬───┘   └────────┬─────────┘  └──────┬───┘   └────────────────┘
       │                │                    │
       ▼                ▼                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     基础设施层（Infrastructure Layer）                │
│  ┌────────────┐  ┌──────────────┐  ┌──────────────────────────────┐│
│  │ Core Data  │  │  libssh2     │  │  CredentialVault             ││
│  │ Persistence│  │  Swift Bridge│  │  AES-256-GCM + Keychain MK  ││
│  └────────────┘  └──────────────┘  └──────────────────────────────┘│
│  ┌────────────┐  ┌──────────────┐  ┌──────────────────────────────┐│
│  │ SwiftTerm  │  │  CloudKit    │  │  iCloud 同步                  ││
│  │ Terminal   │  │  (Private DB)│  │  CloudKitSyncSvc             ││
│  └────────────┘  └──────────────┘  └──────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 关键设计原则

**原则 A：单向数据流（Unidirectional Data Flow）**

```
用户操作 → Action → Store.reduce() → State 更新 → UI 重渲染
               ↓
          副作用（Side Effects）：网络请求、Keychain 读写、磁盘 IO
```

Store 持有不可变 State 快照，所有突变通过 `send(_ action:)` 方法进入，避免散乱的状态修改。

**原则 B：Actor 隔离并发安全**

所有 Mutable Store 标注 `@MainActor`，SSH 工作线程通过 `async/await` 与主线程通信：

```swift
@MainActor
class SessionStore: ObservableObject {
    @Published var sessions: [Session] = []

    func connect(session: Session) async {
        // Kicks off non-mainthread work
        let conn = await SSHConnectionService.shared.connect(session)
        self.activeConnections[session.id] = conn
    }
}
```

**原则 C：依赖注入（Protocol-based DI）**

核心服务面向协议编程，便于 Mock 和测试：

```swift
protocol SSHConnectionServiceProtocol {
    func connect(_ session: Session) async throws -> SSHConnection
    func disconnect(_ connectionID: UUID) async
}

class SSHConnectionService: SSHConnectionServiceProtocol { ... }
class MockSSHConnectionService: SSHConnectionServiceProtocol { ... }
```

---

## 3. 模块详细设计

### 3.1 SSH 连接层

#### 3.1.1 架构概览

```
用户触发连接
     │
     ▼
SSHConnectionService.connect(session)
     │
     ├─→ 1. DNS 解析 & TCP 建连（DispatchQueue.global）
     │
     ├─→ 2. SSH 握手（libssh2_session_handshake）
     │
     ├─→ 3. 主机指纹校验
     │       ├─ Known? → 比对缓存指纹
     │       └─ Unknown? → 触发弹窗回调 → 等待用户确认
     │
     ├─→ 4. 认证（按配置顺序尝试）
     │       ├─ Auth/Password: libssh2_userauth_password
     │       ├─ Auth/PublicKey: libssh2_userauth_publickey_fromfile
     │       └─ Auth/Agent: libssh2_agent_userauth
     │
     ├─→ 5. 打开 PTY Channel
     │       libssh2_channel_open_session
     │       libssh2_channel_request_pty
     │       libssh2_channel_shell
     │
     └─→ 6. 返回 SSHConnection 对象，挂入 SwiftTerm
```

#### 3.1.2 SSHConnection 对象设计

```swift
actor SSHConnection {
    let id: UUID
    let sessionConfig: Session
    private var libssh2Session: OpaquePointer?
    private var channel: OpaquePointer?
    private var socket: Int32
    private(set) var state: ConnectionState = .idle

    // 非阻塞轮询：每帧调用，读取服务器数据
    func poll() async -> Data?

    // 写入终端输入
    func write(_ data: Data) async throws

    // 打开 SFTP 子系统（复用现有连接）
    func openSFTPSession() async throws -> SFTPSession

    // 添加端口转发规则（不需要重连）
    func addTunnel(_ rule: TunnelRule) async throws -> TunnelHandle
}
```

#### 3.1.3 nonblocking 模式与事件循环

libssh2 默认阻塞模式会锁死主线程。采用非阻塞模式 + BSD socket + DispatchSource：

```swift
// 设置非阻塞
libssh2_session_set_blocking(session, 0)

// 使用 DispatchSource 监听 socket 可读事件
let source = DispatchSource.makeReadSource(fileDescriptor: socketFD,
                                           queue: sshQueue)
source.setEventHandler {
    self.readAvailableData()
}
source.resume()
```

#### 3.1.4 跳板机（Jump Host）实现

多跳通过 libssh2 的 `direct-tcpip` channel 实现：

```
本机 → [SSH Channel 1 to Bastion] → [direct-tcpip to Target:22] → Target
```

```swift
// 跳板机实现伪代码
func connectViaJumpHosts(_ hops: [JumpHost], target: Session) async throws {
    var currentConn = try await connectDirect(hops[0])
    for hop in hops.dropFirst() {
        let forwardChannel = try await currentConn.openDirectTCPIP(
            host: hop.host, port: hop.port
        )
        currentConn = try await SSHConnection(overChannel: forwardChannel,
                                               credentials: hop.credentials)
    }
    // 最终连接目标
    let finalChannel = try await currentConn.openDirectTCPIP(
        host: target.host, port: target.port
    )
    return try await SSHConnection(overChannel: finalChannel,
                                   credentials: target.credentials)
}
```

#### 3.1.5 自动重连状态机

```
Idle ──connect()──→ Connecting ──success──→ Connected
                         │                       │
                      failure                 网络中断
                         │                       │
                    ConnectFailed          Reconnecting ──超过3次──→ Disconnected
                                                 │
                                              成功
                                                 │
                                           Connected（恢复 PTY 尺寸）
```

重连策略：指数退避，基准 2s，最大 30s，最多重试 3 次。

---

### 3.2 终端渲染层

#### 3.2.1 SwiftTerm 集成

SwiftTerm 提供 `LocalProcessTerminalView`（本地进程）和 `TerminalView`（数据流驱动）两种模式。ShellMate 使用数据流模式：

```swift
class ShellMateTerminalView: TerminalView, TerminalViewDelegate {

    var connection: SSHConnection?

    override func viewDidLoad() {
        terminalDelegate = self
        // 读取 SSH 数据投喂给终端
        Task {
            while let data = await connection?.poll() {
                self.feed(byteArray: [UInt8](data))
            }
        }
    }

    // 用户键盘输入 → SSH 写入
    func send(source: TerminalView, data: ArraySlice<UInt8>) {
        Task {
            try? await connection?.write(Data(data))
        }
    }

    // 终端窗口尺寸变化 → 通知服务器
    func sizeChanged(source: TerminalView, newCols: Int, newRows: Int) {
        Task {
            try? await connection?.resizePTY(cols: newCols, rows: newRows)
        }
    }
}
```

#### 3.2.2 关键词高亮实现

高亮规则采用 **ANSI SGR 转义序列注入** 方式，在数据流层面处理，不修改 SwiftTerm 核心渲染逻辑：

```swift
/// HighlightEngine — 在 SSH 数据流中注入 ANSI SGR 转义序列实现高亮
/// 处理流程：SSH Data → HighlightEngine.process() → 注入 SGR → SwiftTerm.feed()
class HighlightEngine {
    /// ANSI SGR 颜色映射（前景色）
    /// 红色→31, 绿色→32, 黄色→33, 蓝色→34, 洋红→35, 青色→36, 白色→37
    private static let ansiColorMap: [String: Int] = [
        "red": 31, "green": 32, "yellow": 33,
        "blue": 34, "magenta": 35, "cyan": 36, "white": 37
    ]

    var rules: [HighlightRule] = []

    /// 核心方法：处理原始 SSH 数据，注入高亮 SGR 序列后返回
    func process(_ data: Data) -> Data {
        guard !rules.isEmpty, let text = String(data: data, encoding: .utf8) else {
            return data
        }
        var result = text
        for rule in rules {
            // 从后向前替换，避免偏移漂移
            // 跳过已含 ESC(\u{1B}) 的片段，避免双重高亮损坏 ANSI 序列
            // 注入格式：\u{1B}[{ansiFGCode}m{matchedText}\u{1B}[0m
            result = applyRule(rule, to: result)
        }
        return Data(result.utf8)
    }
}
```

> **设计决策：** 选择 ANSI SGR 注入（而非 `NSMutableAttributedString` 后处理）的原因：
> 1. SwiftTerm 原生解析 ANSI 转义序列，无需额外渲染管线
> 2. 数据流层处理，兼容所有终端内容（包括 vi/tmux 等全屏程序）
> 3. 性能更优，避免在渲染线程进行正则匹配和属性字符串操作

#### 3.2.3 分屏窗格管理

基于 **Figma-Spec-v2 §1.5** 定义，支持四种分屏模式：

```swift
/// 分屏布局模式（Figma-Spec-v2：单屏/水平/垂直/四格）
enum SplitLayout {
    case none        // 单窗格（默认）
    case horizontal  // 上下分屏（VSplitView，border-b 分隔线）
    case vertical    // 左右分屏（HSplitView，border-r 分隔线）
    case grid        // 四格分屏（2×2，VSplitView 内嵌 HSplitView）
}

// ContentView 中的分屏渲染逻辑
@State private var splitLayout: SplitLayout = .none
@State private var splitSessionIds: [UUID] = []  // 最多 3 个附加会话

var body: some View {
    switch splitLayout {
    case .none:
        mainTerminalStack
    case .horizontal:
        VSplitView {
            mainTerminalStack.frame(minHeight: 200)
            TerminalView(sessionId: splitSessionIds[safe: 0]).frame(minHeight: 200)
        }
    case .vertical:
        HSplitView {
            mainTerminalStack.frame(minWidth: 300)
            TerminalView(sessionId: splitSessionIds[safe: 0]).frame(minWidth: 300)
        }
    case .grid:
        // 四格 2×2：上下各一行 HSplitView
        VSplitView {
            HSplitView {
                mainTerminalStack.frame(minWidth: 300, minHeight: 200)
                TerminalView(sessionId: splitSessionIds[safe: 0]).frame(minWidth: 300)
            }
            HSplitView {
                TerminalView(sessionId: splitSessionIds[safe: 1]).frame(minWidth: 300, minHeight: 200)
                TerminalView(sessionId: splitSessionIds[safe: 2]).frame(minWidth: 300)
            }
        }
    }
}
```

工具栏分屏下拉菜单（Figma-Spec-v2 §3.6）触发选择：

| 菜单项 | 对应 SplitLayout | 图标 |
|--------|-----------------|------|
| 单屏模式 | `.none` | `square` |
| 水平分屏 | `.horizontal` | `rectangle.split.1x2` |
| 垂直分屏 | `.vertical` | `rectangle.split.2x1` |
| 四格分屏 | `.grid` | `square.grid.2x2` |

> **设计决策：** 四格模式固定为 2×2 网格，不采用递归 PaneLayout。四格会话通过 `SplitSessionPickerView` 弹窗一次选择多会话（最多同时选 3 个附加会话）。

#### 3.2.4 AI 终端内联组件（Figma-Spec-v2 §4）

**Figma-Spec-v2** 在终端区域定义了两个 AI 驱动的内联 UI 组件，与现有的 `AIAssistantPanelView`（侧边栏面板）和 `AIErrorDetectiveView`（浮层）形成互补：

**① AI 命令说明 Tooltip（AICommandTooltip）**

触发：鼠标悬停在命令输入行的命令文字上（`hover` 0.5s 后）

```swift
/// AI 命令说明悬浮提示 — 显示当前 hover 命令的 AI 解释
struct AICommandTooltipView: View {
    let commandText: String
    @State private var explanation: String = ""
    @State private var isLoading: Bool = true

    var body: some View {
        VStack(alignment: .leading, spacing: 4) {
            HStack {
                Image(systemName: "info.circle")
                    .foregroundColor(Color("colorPrimary"))  // #007aff
                Text("Command Explanation")
                    .font(.caption).fontWeight(.semibold)
                    .foregroundColor(Color("colorPrimary"))
            }
            Text(explanation)
                .font(.caption)
                .foregroundColor(.white)
        }
        .padding(.horizontal, 12).padding(.vertical, 8)
        .background(Color(hex: "#1d1d1f"))
        .cornerRadius(8)
        .shadow(color: .black.opacity(0.3), radius: 10)
        .overlay(RoundedRectangle(cornerRadius: 8)
            .stroke(Color("colorPrimary").opacity(0.3), lineWidth: 1))
        .onAppear { fetchExplanation() }
    }

    private func fetchExplanation() {
        Task {
            let result = try? await AIService.shared.explainCommand(commandText)
            await MainActor.run {
                explanation = result ?? "Unable to fetch explanation."
                isLoading = false
            }
        }
    }
}
```

实现要点：
- 在 `TerminalView` 的命令行 overlay 中通过 `NSTrackingArea` 或 SwiftUI `onHover` 捕获悬停位置
- 缓存已查询命令（`[String: String]` 字典），避免重复 API 调用
- 悬停离开后 200ms 延迟关闭（`duration-fast` 令牌）

**② AI 错误建议条（AIErrorSuggestionBar）**

触发：命令执行返回非零 exit code 且 AIService 有分析结果时，在错误输出行下方内联显示

```swift
/// AI 错误建议条 — 错误输出后内联显示 AI 建议
struct AIErrorSuggestionBar: View {
    let suggestion: String

    var body: some View {
        HStack(alignment: .top, spacing: 8) {
            Image(systemName: "lightbulb.fill")
                .foregroundColor(Color("colorWarning"))  // #ff9500
                .frame(width: 16, height: 16)
            VStack(alignment: .leading, spacing: 2) {
                Text("AI Suggestion")
                    .font(.caption).fontWeight(.semibold)
                    .foregroundColor(Color("colorWarning"))
                Text(suggestion)
                    .font(.caption)
                    .foregroundColor(Color("terminalForeground"))
            }
        }
        .padding(.horizontal, 12).padding(.vertical, 8)
        .background(Color("colorWarning").opacity(0.1))
        .overlay(Rectangle()
            .frame(width: 4).foregroundColor(Color("colorWarning")),
            alignment: .leading)
        .cornerRadius(4)
        .padding(.vertical, 4)
    }
}
```

实现要点：
- `TerminalController` 错误检测（已有 16 种 `errorPatterns`）触发 `AIService.analyzeError()`
- 建议内容通过 `TerminalDataSource` 注入到终端输出流（插入 ANSI 序列后作为特殊标记行）
- 与现有 `AIErrorDetectiveView`（浮层徽章）并存：浮层用于主动触发详细分析，内联条用于轻量快捷展示

---

### 3.3 数据持久化层

#### 3.3.1 Core Data 模型设计

```
Entity: CDSession
  ├── id: UUID (indexed)
  ├── name: String
  ├── host: String
  ├── port: Int32 (default: 22)
  ├── username: String
  ├── authMethodRaw: Int16 (enum)
  ├── keychainRef: String? (UUID string)
  ├── sortOrder: Int32
  ├── createdAt: Date
  ├── lastConnectedAt: Date?
  ├── notesEncrypted: Data? (AES-GCM 加密存储)
  └── → group: CDSessionGroup (to-one)
  └── → tunnelRules: NSSet<CDTunnelRule> (to-many)

Entity: CDSessionGroup
  ├── id: UUID
  ├── name: String
  ├── colorHex: String
  ├── sortOrder: Int32
  ├── isExpanded: Bool
  └── → parent: CDSessionGroup? (to-one, 支持嵌套)
  └── → sessions: NSSet<CDSession> (to-many)

Entity: CDHighlightSet
  ├── id: UUID
  ├── name: String
  ├── isGlobal: Bool
  └── → rules: NSOrderedSet<CDHighlightRule> (to-many, ordered)
```

#### 3.3.2 Repository 模式封装

```swift
protocol SessionRepository {
    func fetchAll() async throws -> [Session]
    func save(_ session: Session) async throws
    func delete(id: UUID) async throws
    func move(sessionID: UUID, toGroupID: UUID?) async throws
}

class CoreDataSessionRepository: SessionRepository {
    private let context: NSManagedObjectContext

    init(container: NSPersistentCloudKitContainer) {
        self.context = container.newBackgroundContext()
        self.context.automaticallyMergesChangesFromParent = true
    }
    // ... 实现
}
```

#### 3.3.3 Core Data + CloudKit 同步配置

使用 `NSPersistentCloudKitContainer`（而非 `NSPersistentContainer`），启用 CloudKit 私有数据库同步：

```swift
lazy var persistentContainer: NSPersistentCloudKitContainer = {
    let container = NSPersistentCloudKitContainer(name: "ShellMate")

    let description = container.persistentStoreDescriptions.first!
    description.cloudKitContainerOptions =
        NSPersistentCloudKitContainerOptions(
            containerIdentifier: "iCloud.app.shellmate.sessions"
        )
    description.setOption(true as NSNumber,
                          forKey: NSPersistentHistoryTrackingKey)
    description.setOption(true as NSNumber,
                          forKey: NSPersistentStoreRemoteChangeNotificationPostOptionKey)

    container.loadPersistentStores { ... }
    container.viewContext.automaticallyMergesChangesFromParent = true
    container.viewContext.mergePolicy = NSMergeByPropertyObjectTrumpMergePolicy
    return container
}()
```

> **⚠️ 注意：** `密码` 和 `私钥 Passphrase` 不进入 Core Data / CloudKit，仅存于本设备 Keychain。新设备首次使用同步的会话时，提示用户重新输入凭据。

---

### 3.4 SFTP 文件管理

#### 3.4.1 SFTPSession 设计

SFTP 子系统使用 **独立的 SSH 连接**（不复用终端 Shell 连接），确保文件传输与终端交互互不干扰：

```swift
/// SFTP 会话 — 维护独立的 SSH 连接专用于 SFTP 文件操作
/// 与终端 Shell 连接互不干扰
actor SFTPSession {
    /// 独立的 libssh2 桥接实例（非共享终端连接）
    private let bridge = LibSSH2BridgeReal()
    /// 专用后台队列，所有 libssh2 调用在此队列执行
    private let queue = DispatchQueue(label: "app.shellmate.sftp", qos: .userInitiated)
    private var sftpHandle: OpaquePointer?

    /// 建立独立 SSH 连接并初始化 SFTP 子系统
    func connect(host: String, port: Int, username: String,
                 credential: SSHCredential) async throws

    // 列出目录
    func listDirectory(path: String) async throws -> [SFTPFileEntry]

    // 上传文件（支持断点续传）
    func upload(localURL: URL, remotePath: String,
                progress: @escaping (Double) -> Void) async throws

    // 下载文件
    func download(remotePath: String, localURL: URL,
                  progress: @escaping (Double) -> Void) async throws

    // 删除、重命名、新建目录、chmod
    func delete(path: String) async throws
    func rename(from: String, to: String) async throws
    func mkdir(path: String, permissions: UInt32) async throws
    func chmod(path: String, permissions: UInt32) async throws
}
```

> **设计决策：** 选择独立连接而非复用终端连接的原因：
> 1. SFTP 使用阻塞模式操作（简化文件 I/O 逻辑），终端使用非阻塞模式（低延迟交互）
> 2. 大文件传输不阻塞终端 PTY Channel 的数据吞吐
> 3. SFTP 会话可独立断开/重连，不影响终端 Shell 状态

#### 3.4.2 传输队列

```swift
@MainActor
class TransferQueue: ObservableObject {
    @Published var tasks: [TransferTask] = []

    private let concurrencyLimit = 3  // 最多 3 个并发
    private var activeTasks: [UUID: Task<Void, Error>] = [:]

    func enqueue(_ task: TransferTask) {
        tasks.append(task)
        drainQueue()
    }

    private func drainQueue() {
        let available = concurrencyLimit - activeTasks.count
        let pending = tasks.filter { $0.state == .pending }.prefix(available)
        for task in pending {
            start(task)
        }
    }
}
```

#### 3.4.3 断点续传实现

```swift
func resumeUpload(localURL: URL, remotePath: String,
                  resumeOffset: UInt64) async throws {
    // 1. 打开远端文件（LIBSSH2_FXF_WRITE | LIBSSH2_FXF_APPEND）
    let remoteHandle = libssh2_sftp_open(sftpHandle, remotePath,
                                         LIBSSH2_FXF_WRITE | LIBSSH2_FXF_APPEND,
                                         LIBSSH2_SFTP_S_IRUSR | LIBSSH2_SFTP_S_IWUSR)
    // 2. SFTP seek 到已上传偏移量
    libssh2_sftp_seek64(remoteHandle, resumeOffset)
    // 3. 从本地文件对应偏移量读取继续写入
}
```

---

### 3.5 端口转发与隧道

#### 3.5.1 本地端口转发（-L）

```
实现方案: 在本地监听 TCP Server Socket，
接受连接后通过 libssh2 开一个 direct-tcpip channel，
将流量桥接到远端目标。

本地 App [ListenerSocket:8080]
    → accept() 新连接
    → libssh2_channel_direct_tcpip(session, "db.internal", 3306)
    → 双向数据复制（读写循环）
```

```swift
class LocalForwardTunnel: TunnelHandle {
    private var listenerSocket: Int32
    private var bridgeTasks: [UUID: Task<Void, Never>] = [:]

    func start() throws {
        // bind + listen 本地端口
        // DispatchQueue 上异步 accept loop
        sshQueue.async { self.acceptLoop() }
    }

    private func acceptLoop() {
        while isRunning {
            let clientFD = accept(listenerSocket, nil, nil)
            guard clientFD >= 0 else { continue }
            Task { await self.bridge(clientFD: clientFD) }
        }
    }
}
```

#### 3.5.2 SOCKS5 动态代理（-D）

在本地实现 SOCKS5 握手协议，将代理请求转为 `direct-tcpip` channel：

```swift
class SOCKS5ProxyServer {
    // SOCKS5 握手状态机
    // → CONNECT 命令 → 提取目标 host:port
    // → libssh2_channel_direct_tcpip(session, targetHost, targetPort)
    // → 双向桥接
}
```

---

### 3.6 iCloud 同步层

#### 3.6.1 同步范围与策略

| 数据 | 同步方式 | 冲突策略 |
|------|---------|---------|
| Session、SessionGroup | NSPersistentCloudKitContainer 自动 | 最新修改时间戳胜出（`NSMergeByPropertyObjectTrumpMergePolicy`）|
| HighlightSet、TunnelRule、QuickCommandSet | 同上 | 同上 |
| TerminalProfile（外观） | 同上 | 同上 |
| 密码 / Passphrase | **不同步**，仅本设备 Keychain | — |
| 日志文件 | **不同步** | — |
| 录制文件 | **不同步** | — |

#### 3.6.2 同步状态监听

```swift
class CloudKitSyncMonitor: ObservableObject {
    @Published var syncState: SyncState = .synced

    init() {
        NotificationCenter.default.addObserver(
            self,
            selector: #selector(storeRemoteChange),
            name: .NSPersistentStoreRemoteChange,
            object: nil
        )
    }

    @objc private func storeRemoteChange(_ notification: Notification) {
        // 分析 NSPersistentHistoryTransaction
        // 更新 syncState → .syncing / .synced / .error
    }
}
```

#### 3.6.3 新设备首次使用处理

```swift
// 检测到 Session 有 keychainRef 但本设备 Keychain 无对应条目
func checkCredentialAvailability(session: Session) -> CredentialStatus {
    if let ref = session.keychainRef {
        let exists = KeychainService.shared.exists(ref: ref)
        return exists ? .available : .missingOnThisDevice
    }
    return .notConfigured
}

// 在 SessionRow 右侧显示 ⚠️ 图标
// 触发连接时弹窗提示输入凭据
```

---

### 3.7 安全与凭据金库（CredentialVault）

#### 3.7.1 架构概述

**设计决策：** 弃用 Keychain 逐条存储凭据的方案，改为内置凭据金库（CredentialVault）。

凭据金库采用 **Core Data 存储加密载荷 + 单枚主密钥（Master Key）存放于 Keychain** 的两层架构：

```
┌──────────────────────────────────────────────────────────────┐
│                   CredentialVault（应用层）                    │
│  CDCredential 实体（Core Data，不参与 CloudKit 同步）           │
│  encryptedPayload: AES-256-GCM 密文（nonce + ciphertext + tag）│
└──────────────────────────────────────────────────────────────┘
                         ↕ 读写加密载荷
┌──────────────────────────────────────────────────────────────┐
│                     CryptoKit（系统框架）                      │
│  AES.GCM.SealedBox — 256-bit 密钥，随机 nonce，认证标签        │
└──────────────────────────────────────────────────────────────┘
                         ↕ 取得/生成 Master Key
┌──────────────────────────────────────────────────────────────┐
│             macOS Keychain（单条条目）                         │
│  service: "app.shellmate.vault"  account: "master-key"       │
│  kSecAttrAccessibleWhenUnlocked（无权限弹窗）                  │
└──────────────────────────────────────────────────────────────┘
```

**优势对比：**

| 维度 | 旧方案（Keychain 逐条） | 新方案（CredentialVault） |
|------|----------------------|------------------------|
| 条目数量 | N 个会话 × 3 种凭据 = 大量条目 | 1 个 Master Key 条目 |
| 弹窗风险 | 每条都可能触发权限弹窗 | 零弹窗（Master Key 无 ACL）|
| 批量操作 | 无原子性，逐条操作 | Core Data 事务保证原子性 |
| 迁移能力 | 新设备无 Keychain，需重输 | CloudKit 同步配置，凭据本地加密单独存储 |
| 可审计性 | 散布在系统 Keychain | 统一 CDCredential 表，可查询统计 |

#### 3.7.2 CredentialVault 实现

```swift
/// 凭据金库 — 使用 AES-256-GCM 加密所有 SSH 凭据
/// Master Key 存于 Keychain 单条条目，凭据密文存于 Core Data CDCredential
actor CredentialVault {

    static let shared = CredentialVault()

    // MARK: - 写入凭据

    func save(_ secret: String, sessionId: UUID, type: CredentialType) async throws {
        let payload = try encrypt(Data(secret.utf8))
        // 写入 CDCredential，credentialTypeRaw = type.rawValue
        // 已存在则覆盖（delete + insert）
    }

    // MARK: - 读取凭据

    func load(sessionId: UUID, type: CredentialType) async throws -> String {
        // 查询 CDCredential where sessionId = ? AND credentialTypeRaw = ?
        let payload = /* CDCredential.encryptedPayload */
        let data = try decrypt(payload)
        guard let secret = String(data: data, encoding: .utf8) else {
            throw CredentialVaultError.decodingFailed
        }
        return secret
    }

    // MARK: - 删除

    func delete(sessionId: UUID, type: CredentialType) async throws { ... }
    func deleteAll(sessionId: UUID) async throws { ... }

    // MARK: - 加密（AES-256-GCM）

    private func encrypt(_ data: Data) throws -> Data {
        let key = try masterKey()
        let sealed = try AES.GCM.seal(data, using: key)
        return sealed.combined!   // nonce(12) + ciphertext + tag(16)
    }

    private func decrypt(_ data: Data) throws -> Data {
        let key = try masterKey()
        let box = try AES.GCM.SealedBox(combined: data)
        return try AES.GCM.open(box, using: key)
    }

    // MARK: - Master Key（懒加载，首次自动生成并写入 Keychain）

    private func masterKey() throws -> SymmetricKey {
        let service = "app.shellmate.vault"
        let account = "master-key"
        if let existing = try? readFromKeychain(service: service, account: account) {
            return SymmetricKey(data: existing)
        }
        // 首次：生成 256-bit 随机密钥并持久化
        let newKey = SymmetricKey(size: .bits256)
        let keyData = newKey.withUnsafeBytes { Data($0) }
        try writeToKeychain(keyData, service: service, account: account,
                            accessible: kSecAttrAccessibleWhenUnlocked)
        return newKey
    }
}
```

#### 3.7.3 旧版 Keychain 凭据迁移

首次启动时自动探测并迁移旧 Keychain 凭据：

```swift
func migrateFromLegacyKeychain(for sessions: [Session]) async {
    for session in sessions {
        // 尝试读取旧 Keychain 条目
        if let pwd = try? KeychainService.shared.getPassword(for: session.id, type: .password) {
            try? await CredentialVault.shared.save(pwd, sessionId: session.id, type: .password)
            try? KeychainService.shared.deletePassword(for: session.id, type: .password)
        }
        // passphrase / privateKey 同理
    }
}
```

迁移在 `PersistenceController.init()` 完成后、首个会话加载前执行，迁移成功后删除旧 Keychain 条目，确保幂等。

#### 3.7.4 已知主机（Known Hosts）管理

```swift
class KnownHostsManager {
    // 存储路径: ~/Library/Application Support/ShellMate/known_hosts
    // 格式: 兼容 OpenSSH known_hosts 格式

    func check(host: String, port: Int, fingerprint: String) -> KnownHostResult
    func add(host: String, port: Int, fingerprint: String, keyType: String)
    func remove(host: String, port: Int)
    func all() -> [KnownHostEntry]
}

enum KnownHostResult {
    case unknown       // 首次连接，触发 D02 弹窗
    case trusted       // 指纹匹配，直接连接
    case changed       // 指纹不匹配，触发 D03 P0 警告！
}
```

---

### 3.8 AI 服务层

#### 3.8.1 架构概述

AI 助手模块为 ShellMate 提供智能命令辅助和错误分析能力，支持三大 AI 提供商：

| 提供商 | 支持模型 | 认证方式 | 协议 |
|--------|---------|---------|------|
| **Anthropic Claude** | claude-sonnet-4-6（默认）、claude-haiku-4-5、claude-opus-4-6 | API Key | SSE Streaming |
| **OpenAI** | gpt-4o、gpt-4o-mini | API Key | SSE Streaming |
| **Ollama** | 本地模型（llama3 等） | 无需密钥 | HTTP Streaming |

#### 3.8.2 AIService 实现

```swift
/// AI 服务 — 统一三大提供商的流式对话接口
/// 基于 URLSession + SSE（Server-Sent Events）实现流式输出
class AIService: ObservableObject {
    @Published var isStreaming: Bool = false
    @Published var currentResponse: String = ""

    @AppStorage("ai.provider")  private var provider: String = "claude"
    @AppStorage("ai.model")     private var model: String = "claude-sonnet-4-6"
    @AppStorage("ai.apiKey")    private var apiKey: String = ""
    @AppStorage("ai.ollamaURL") private var ollamaURL: String = "http://localhost:11434"

    /// 流式发送消息，通过 AsyncStream 逐 token 回调
    func sendMessage(_ message: String, context: [ChatMessage]) async throws -> AsyncStream<String>

    /// 取消当前流式请求
    func cancelStream()
}
```

#### 3.8.3 UI 集成

AI 功能通过两个独立视图集成到终端界面：

```
┌─────────────────────────────────────────┐
│  TerminalView                           │
│  ┌───────────────────────────────────┐  │
│  │  终端内容区                        │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  AIAssistantPanelView（底部面板）   │  │  ← 用户主动打开，输入问题获取 AI 建议
│  └───────────────────────────────────┘  │
│  ┌─────────────────┐                    │
│  │AIErrorDetective │                    │  ← 检测到命令错误时自动弹出
│  │  View（浮层）    │                    │
│  └─────────────────┘                    │
└─────────────────────────────────────────┘
```

- **AIAssistantPanelView**：交互式对话面板，支持上下文感知（终端当前输出、工作目录等）
- **AIErrorDetectiveView**：命令执行失败时自动触发，分析错误原因并提供修复建议
- **AISettingsView**：设置面板中的 AI 配置页（提供商选择、API Key 输入、模型切换）

> **安全约束：** API Key 存入本机 Keychain（`kSecAttrAccessibleWhenUnlockedThisDeviceOnly`），**不**参与 iCloud 同步，不存入 UserDefaults。

#### 3.8.4 AI 命令安全审计（CommandSafetyChecker）

> **对应 PRD §3.13.6** | 在 Compose Pane 发送或 AI 面板「插入终端」前拦截高风险命令。

**设计原则：完全本地静态分析，零网络请求，不阻断用户意志（仅提供信息摩擦）。**

```swift
/// 命令安全检查器 — 正则规则引擎，本地执行，无网络
struct CommandSafetyChecker {

    // MARK: - 风险等级

    enum RiskLevel: Int, Comparable {
        case safe    = 0
        case warning = 1   // 橙色 #ff9500
        case danger  = 2   // 红色 #ff3b30

        var icon: String {
            switch self {
            case .safe:    return "checkmark.circle"
            case .warning: return "exclamationmark.triangle.fill"
            case .danger:  return "xmark.octagon.fill"
            }
        }
        static func < (lhs: Self, rhs: Self) -> Bool { lhs.rawValue < rhs.rawValue }
    }

    // MARK: - 规则表（v4.0，按风险等级排序）

    private static let dangerPatterns: [String] = [
        #"rm\s+-[rRf]{2,}\s+/"#,           // rm -rf /
        #"sudo\s+rm\s+-rf"#,               // sudo rm -rf
        #"dd\s+if=.+of=/dev/"#,            // dd 写入块设备
        #"mkfs\."#,                        // 格式化文件系统
        #"DROP\s+(TABLE|DATABASE)"#,        // SQL 删库
        #":\(\)\{.*\}.*:"#,                // Fork Bomb
    ]

    private static let warningPatterns: [String] = [
        #"chmod\s+-R\s+777"#,              // 全量可写
        #"chown\s+-R\s+root"#,             // 递归改主
        #"find\s+.+\s+-delete"#,           // find 批量删除
        #"git\s+push\s+--force"#,          // 强制推送
        #"kill\s+-9"#,                     // 强制杀进程
    ]

    // MARK: - 核心方法

    static func check(_ command: String) -> CommandRisk? {
        // 先预处理 ANSI 转义码（防绕过）
        let cleaned = stripANSI(command)

        for pattern in dangerPatterns {
            if cleaned.range(of: pattern, options: [.regularExpression, .caseInsensitive]) != nil {
                return CommandRisk(command: command, level: .danger,
                                   matchedPattern: pattern,
                                   reason: riskDescription(for: pattern))
            }
        }
        for pattern in warningPatterns {
            if cleaned.range(of: pattern, options: [.regularExpression, .caseInsensitive]) != nil {
                return CommandRisk(command: command, level: .warning,
                                   matchedPattern: pattern,
                                   reason: riskDescription(for: pattern))
            }
        }
        return nil   // 安全，直接发送
    }

    private static func stripANSI(_ s: String) -> String {
        s.replacingOccurrences(of: #"\x1B\[[0-9;]*[mGKHF]"#, with: "",
                               options: .regularExpression)
    }
}

/// 检测结果
struct CommandRisk {
    let command: String
    let level: CommandSafetyChecker.RiskLevel
    let matchedPattern: String
    let reason: String
}
```

**用户确认流程：**

| 风险级别 | 弹窗 | 操作路径 |
|---------|------|---------|
| `.warning` | `CommandSafetyAlertView`（Sheet） | 单步确认 → 执行 |
| `.danger`  | `CommandSafetyAlertView`（Sheet）| 两步确认（第 1 步 → 「继续…」；第 2 步 → 「确认，我知道风险」）；可选点击「AI 分析风险」触发流式说明 |

**集成点：**

```swift
// ComposePaneView / AIAssistantPanelView 中注入检查
func sendCommandToTerminal(_ cmd: String) {
    if let risk = CommandSafetyChecker.check(cmd) {
        // 弹出 CommandSafetyAlertView
        pendingRisk = risk
    } else {
        controller.sendToSSH(cmd)
    }
}
```

**开关入口：** Settings → 通用 → 命令安全审计（`@AppStorage("safety.enabled")`，默认开启）

---

### 3.9 国际化策略

#### 3.9.1 当前实现

应用通过 `@AppStorage("app.language")` 存储用户语言偏好，支持中文和英文切换：

```swift
// ContentView 中通过 .environment 注入 Locale
@AppStorage("app.language") private var appLanguage: String = "zh"

private var appLocale: Locale {
    appLanguage == "en" ? Locale(identifier: "en_US") : Locale(identifier: "zh_Hans")
}

var body: some View {
    ContentView()
        .environment(\.locale, appLocale)  // 仅影响 SwiftUI 视图树
}
```

#### 3.9.2 已知限制与待解决问题

| 问题 | 原因 | 解决方案（待实现） |
|------|------|------------------|
| 菜单栏始终中文 | `CommandMenu`/`CommandGroup` 不在 SwiftUI 视图树内，不继承 `.environment(\.locale)` | 使用 `NSLocalizedString` 或监听语言变更手动刷新 |
| Dock 菜单固定中文 | AppKit `NSMenuItem(title:)` 硬编码字符串 | 替换为 `NSLocalizedString` |
| 系统级文本无法切换 | 系统 Alert、File Dialog 等遵循系统语言 | 文档说明此为系统限制 |

---

### 3.10 系统性能监控模块（Figma-Spec-v2 §5）

#### 3.10.1 架构概述

状态栏底部实时展示 **CPU / 内存 / 磁盘 / 网络** 四项指标，通过 SSH 连接定期执行系统命令采集数据。

```
SSHConnection（已建立）
    └── SystemMonitorService（独立 SSH Channel）
            ├── 每 2s 轮询：top -bn1 | free -m | df -h | /proc/net/dev
            └── 解析结果 → @MainActor 更新 SystemMetrics
                    └── TerminalStatusBarView 订阅并渲染指标
```

#### 3.10.2 SystemMonitorService 设计

```swift
/// 系统性能监控服务 — 通过 SSH 定期采集远端服务器指标
@MainActor
class SystemMonitorService: ObservableObject {
    @Published var metrics: SystemMetrics = .zero

    private var monitorTask: Task<Void, Never>?
    private weak var connection: SSHConnection?

    struct SystemMetrics {
        var cpuPercent: Double      // 0.0 ~ 100.0
        var memUsedGB: Double       // 已用内存 GB
        var memTotalGB: Double      // 总内存 GB
        var diskUsedGB: Double      // 磁盘已用 GB
        var diskTotalGB: Double     // 磁盘总量 GB
        var netDownKBps: Double     // 下行速率 KB/s
        var netUpKBps: Double       // 上行速率 KB/s

        static let zero = SystemMetrics(
            cpuPercent: 0, memUsedGB: 0, memTotalGB: 0,
            diskUsedGB: 0, diskTotalGB: 0, netDownKBps: 0, netUpKBps: 0
        )
    }

    func startMonitoring(connection: SSHConnection) {
        self.connection = connection
        monitorTask = Task {
            while !Task.isCancelled {
                if let data = try? await collectMetrics() {
                    metrics = data
                }
                try? await Task.sleep(nanoseconds: 2_000_000_000)  // 2s
            }
        }
    }

    func stopMonitoring() {
        monitorTask?.cancel()
        metrics = .zero
    }

    private func collectMetrics() async throws -> SystemMetrics {
        // 组合命令，一次 SSH 请求返回所有数据
        let cmd = """
        echo "===CPU===" && top -bn1 | grep "Cpu(s)" | awk '{print $2}' | tr -d '%us,' && \
        echo "===MEM===" && free -m | awk '/Mem:/{print $2, $3}' && \
        echo "===DISK===" && df -h / | awk 'NR==2{print $2, $3}' && \
        echo "===NET===" && cat /proc/net/dev | awk '/eth0|ens|enp/{print $2, $10}'
        """
        let output = try await connection?.executeCommand(cmd) ?? ""
        return parseMetrics(output)
    }
}
```

#### 3.10.3 状态栏指标颜色阈值（Figma-Spec-v2 §5）

| 指标 | 颜色 < 50% | 颜色 50–80% | 颜色 > 80% |
|------|-----------|------------|-----------|
| CPU | `#34c759`（绿）| `#ff9500`（橙）| `#ff3b30`（红）|
| 内存 | `#34c759` | `#ff9500` | `#ff3b30` |
| 磁盘 | 固定 `#1d1d1f` | — | — |
| 网络下行 | 固定 `#34c759` | — | — |
| 网络上行 | 固定 `#007aff` | — | — |

CPU 迷你柱状图：8 根柱子，`w-0.5 h-3`，高度按 CPU% 等比缩放（SwiftUI `Rectangle` 配合 `frame(height:)`）。

> **注意：** Linux 命令因发行版不同存在差异（Alpine 无 `free`，macOS 用 `vm_stat`）。`SystemMonitorService` 需实现多命令回退策略，先检测可用命令再固定用法。

---

### 3.11 欢迎引导模块（Figma-Spec-v2 §13 / PRD §3.17）

> **v4.0 更新** | 对齐 PRD v2.2 §3.17，三步内容完整重定义；删除本地 Shell 相关引导步骤。

#### 3.11.1 触发逻辑

```swift
// ContentView 中通过 ZStack overlay 实现全屏覆层（z-50）
@AppStorage("hasLaunchedBefore") private var hasLaunchedBefore: Bool = false

var body: some View {
    MainContentView()
        .overlay {
            if !hasLaunchedBefore {
                WelcomeScreenView(
                    onDismiss: { hasLaunchedBefore = true },
                    onCreateSession: { hasLaunchedBefore = true; showNewSessionForm() },
                    onImportConfiguration: { hasLaunchedBefore = true; showImportDialog() }
                )
                .zIndex(100)
                .transition(.opacity.animation(.easeInOut(duration: 0.3)))
            }
        }
}
```

任意行动卡片点击或「跳过」按钮均写入 `hasLaunchedBefore = true`，确保幂等。Settings → 通用 → 重置引导 可再次触发。

#### 3.11.2 三步骤结构（对齐 PRD v2.2 §3.17.2）

| 步骤 | 标题 | 英雄图标 | 核心内容 | 操作 |
|------|------|--------|---------|------|
| Step 0（欢迎）| 欢迎使用 ShellMate | 👋 蓝紫渐变卡片 | 三个差异化亮点：🔒安全本地化 / 🤖AI错误诊断 / ⚡极速管理 | 「下一步」 / 「跳过」 |
| Step 1（特性）| 强大的功能，极致的体验 | 🚀 绿蓝渐变卡片 | 4 特性卡片（安全加密 / SFTP传输 / AI助手 / 脚本自动化） | 「下一步」 / 「跳过」 |
| Step 2（开始）| 准备好了，开始使用！ | ⚡ 橙红渐变卡片 | 3 行动卡片（创建会话 / 导入配置 / 直接进入） | 对应操作回调 |

步骤指示器：当前步 `w-8 h-2 rounded-full bg-[#007aff]`，其余 `w-2 h-2 rounded-full bg-[#d2d2d7]`（Figma-Spec-v2 §13.7）。

```swift
struct WelcomeScreenView: View {
    var onDismiss: (() -> Void)?
    var onCreateSession: (() -> Void)?
    var onImportConfiguration: (() -> Void)?

    @State private var currentStep: Int = 0

    var body: some View {
        ZStack {
            // bg-gradient-to-br from-[#f5f5f7] via-white to-[#e8e8ed]
            LinearGradient(colors: [Color(hex: "#f5f5f7"), .white, Color(hex: "#e8e8ed")],
                           startPoint: .topLeading, endPoint: .bottomTrailing)
                .ignoresSafeArea()

            VStack(spacing: 0) {
                // 关闭按钮（top-4 right-4）
                closeButton

                Spacer()
                heroSection          // emoji 渐变卡片 + 标题 + 副标题 + 步骤指示器
                if currentStep == 1 { featuresGrid }   // 4 特性卡片
                if currentStep == 2 { actionCards }    // 3 行动卡片
                if currentStep < 2  { navigationButtons }
                Spacer()
            }
            .frame(maxWidth: 900)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
```

---

### 3.12 脚本自动化模块（Figma-Spec-v2 §14.5）

#### 3.12.1 功能描述

全屏覆层脚本编辑器 + SSH 执行器，工具栏 `FileCode` 图标（⌘⇧S）触发。

> **Figma-Spec-v2 §14.5 更新（2026-04-14）**：Header 图标为 `FileCode`（非 `Code2`）；布局为**三栏**（左侧分类折叠侧边栏 w-64 + 中间代码编辑区 flex-1 + 右侧执行日志 w-96）；侧边栏无搜索框，按分类（Monitoring/Backup/Deployment/Maintenance/Security/Custom）折叠展开。

```swift
struct ScriptAutomationPanelView: View {
    @State private var scripts: [ScriptItem] = []
    @State private var selectedScript: ScriptItem?
    @State private var editorContent: String = ""
    @State private var executionLog: [String] = []
    @Environment(\.dismiss) private var dismiss

    var body: some View {
        // fixed inset-0 z-50 bg-black/30 backdrop-blur-sm：模态覆层
        ZStack {
            Color.black.opacity(0.3).ignoresSafeArea()
                .onTapGesture { dismiss() }
            HStack(spacing: 0) {
                // 左侧分类折叠脚本库（w-64）
                scriptCategoryPanel      // 分类：Monitoring/Backup/Deployment/Maintenance/Security/Custom
                Divider()
                // 中间代码编辑区（flex-1，bg #1e1e1e）
                scriptEditorPanel
                Divider()
                // 右侧执行日志区（w-96）
                executionLogPanel
            }
            .background(.regularMaterial)
            .cornerRadius(16)
            .padding(16)
            .shadow(radius: 25)
        }
    }
}
```

#### 3.12.2 ScriptItem 数据模型

```swift
struct ScriptItem: Identifiable, Codable {
    let id: UUID
    var name: String          // 脚本文件名（含 .sh）
    var content: String       // 脚本内容
    var createdAt: Date
    var lastRunAt: Date?
}
```

脚本持久化：存储于 `Core Data CDScript` 实体，参与 CloudKit 同步（脚本内容不含凭据，安全同步）。

执行方式：通过已建立的 `SSHConnection` 开启独立 channel 执行脚本内容，输出实时回显到编辑区下方日志面板。

---

### 3.13 录制与回放模块（Figma-Spec-v2 §14.6）

#### 3.13.1 录制格式

采用 **asciinema v2** 格式（JSON Lines），本地文件存储：

```
~/Library/Application Support/ShellMate/recordings/{sessionId}/{timestamp}.cast
```

```json
{"version": 2, "width": 220, "height": 50, "timestamp": 1712000000, "title": "prod-session"}
[0.0, "o", "ubuntu@prod:~$ "]
[0.5, "o", "ls -la\r\n"]
[1.2, "o", "total 48\r\ndrwxr-xr-x ..."]
```

#### 3.13.2 SessionRecorder 设计

```swift
/// 终端会话录制器 — 捕获 SSH 数据流并写入 asciinema 格式文件
actor SessionRecorder {
    private var fileHandle: FileHandle?
    private var startTime: Date = Date()
    private(set) var isRecording: Bool = false

    func startRecording(sessionId: UUID, cols: Int, rows: Int) throws { ... }
    func record(data: Data) async { ... }  // 写入 [elapsed, "o", text] 行
    func stopRecording() async throws -> URL { ... }
}
```

录制开关：状态栏或工具栏 `RecordingDialog` 弹窗控制，录制中显示 `bg-[#ff3b30] animate-pulse` 红色指示点。

---

### 3.14 日志面板模块（Figma-Spec-v2 §14.4）

#### 3.14.1 日志结构

```swift
struct SessionLogEntry: Identifiable {
    let id: UUID
    let timestamp: Date
    let type: LogType  // input / output / error
    let content: String

    enum LogType: String {
        case info, warning, error, command  // Figma-Spec-v2 §14.4 更新（2026-04-14）
    }
}
```

#### 3.14.2 LoggingPanelView 架构

工具栏 `FileText` 图标触发，弹窗规格 `max-w-[900px] max-h-[80vh]`。

```swift
@MainActor
class SessionLogStore: ObservableObject {
    @Published var entries: [SessionLogEntry] = []
    @Published var isPaused: Bool = false
    private let maxEntries: Int = 10_000

    func append(_ content: String, type: SessionLogEntry.LogType) {
        guard !isPaused else { return }
        let entry = SessionLogEntry(id: .init(), timestamp: Date(), type: type, content: content)
        entries.append(entry)
        if entries.count > maxEntries {
            entries.removeFirst(entries.count - maxEntries)
        }
    }

    func export() -> Data {
        // 导出为 CSV 或纯文本
        entries.map { "\($0.timestamp)\t\($0.type.rawValue)\t\($0.content)" }
               .joined(separator: "\n")
               .data(using: .utf8) ?? Data()
    }
}
```

过滤功能：类型 Select 下拉（`all / info / warning / error / command` 五选项）+ 关键词搜索框 + 会话过滤 Select（Figma-Spec-v2 §14.4，2026-04-14 更新）。日志行背景按类型区分：info=白/warning=orange-50/error=red-50/command=blue-50。

---

### 3.15 导入/导出模块（Figma-Spec-v2 §14.3）

#### 3.15.1 支持格式

| 格式 | 导入 | 导出 | 说明 |
|------|------|------|------|
| **JSON** | ✓ | ✓ | ShellMate 原生格式，包含分组/标签/高亮规则 |
| **CSV** | ✗ | ✓ | 简化格式（仅 Session 基本字段），便于外部工具处理 |
| **~/.ssh/config** | ✓ | ✗ | 已规划（W12.9），解析 Host/HostName/Port/User/IdentityFile/ProxyJump |
| **Xshell .xsh** | ✓ | ✗ | 已规划（W15.7），竞品迁移 |
| **SecureCRT .ini** | ✓ | ✗ | 已规划（W15.7） |

#### 3.15.2 JSON 导出格式

```json
{
  "version": 1,
  "exportedAt": "2026-04-02T10:00:00Z",
  "sessions": [
    {
      "id": "uuid",
      "name": "Production Server",
      "host": "192.168.1.100",
      "port": 22,
      "username": "admin",
      "authMethod": "password",
      "groupName": "Production",
      "tags": ["prod", "web"],
      "sortOrder": 0
    }
  ],
  "groups": [ ... ]
}
```

> **安全约束：** 导出文件**不含任何凭据**（密码/私钥/Passphrase），均存于本设备 Keychain / CredentialVault，不参与导出。

#### 3.15.3 SessionImportExportView 架构

工具栏 `Package` 图标触发，`max-w-[600px]` 弹窗，内含导出/导入两 Tab。导出 Tab：只读 Textarea 展示 JSON + Copy/Download 按钮；导入 Tab：可编辑 Textarea 粘贴 JSON + 文件上传按钮（非拖放区域）。（Figma-Spec-v2 §14.3，2026-04-14 更新）

---

### 3.16 UI 设计令牌系统（Figma-Spec-v2）

> **规范来源：** `Figma-Spec-v2/00-总纲与设计令牌.md`（2026-04-02）

#### 3.16.1 颜色令牌（Swift 实现）

```swift
/// DesignTokens — 基于 Figma-Spec-v2/00 完全同步
extension Color {
    static let colorPrimary        = Color(hex: "#007aff")  // Apple Blue
    static let colorPrimaryHover   = Color(hex: "#0051d5")
    static let colorSuccess        = Color(hex: "#34c759")
    static let colorWarning        = Color(hex: "#ff9500")
    static let colorError          = Color(hex: "#ff3b30")
    static let colorPurple         = Color(hex: "#5856d6")  // AI 品牌渐变
    static let colorTeal           = Color(hex: "#4ec9b0")  // VS Dark Prompt

    static let textPrimary         = Color(hex: "#1d1d1f")
    static let textSecondary       = Color(hex: "#86868b")
    static let textDisabled        = Color(hex: "#c7c7cc")
    static let bgPrimary           = Color(hex: "#ffffff")
    static let bgSecondary         = Color(hex: "#f5f5f7")
    static let bgTertiary          = Color(hex: "#e8e8ed")
    static let colorBorder         = Color(hex: "#d2d2d7")
}
```

#### 3.16.2 圆角令牌

| Token | 值 | SwiftUI |
|-------|-----|---------|
| `radius-sm` | 6px | `.cornerRadius(6)` |
| `radius-md` | 8px | `.cornerRadius(8)`（按钮、输入框） |
| `radius-lg` | 10px | `.cornerRadius(10)`（卡片） |
| `radius-xl` | 12px | `.cornerRadius(12)`（侧边栏行） |
| `radius-2xl` | 16px | `.cornerRadius(16)`（弹窗容器） |
| `radius-3xl` | 24px | `.cornerRadius(24)`（欢迎界面图标） |

#### 3.16.3 图标规范（SF Symbols 映射）

Figma-Spec-v2 使用 **Lucide React** 图标，macOS 原生实现使用 **SF Symbols** 对应：

| Lucide 图标 | SF Symbols 对应 | 用途 |
|------------|----------------|------|
| `Monitor` | `desktopcomputer` | 连接按钮 |
| `Sparkles` | `sparkles` | AI 助手 |
| `Code2` | `chevron.left.forwardslash.chevron.right` | 脚本自动化 |
| `FolderSync` | `folder.badge.gearshape` | SFTP 面板 |
| `SplitSquareVertical` | `rectangle.split.2x1` | 分屏 |
| `FileText` | `doc.text` | 日志面板 |
| `Zap` | `bolt` | 快捷命令 |
| `Network` | `network` | 隧道管理 |
| `Terminal` | `terminal` | tmux 管理 |
| `Package` | `shippingbox` | 导入/导出 |
| `Search` | `magnifyingglass` | 搜索 |
| `Settings` | `gearshape` | 设置 |
| `Cpu` | `cpu` | CPU 监控 |
| `MemoryStick` | `memorychip` | 内存监控 |
| `HardDrive` | `internaldrive` | 磁盘监控 |
| `Wifi` | `wifi` | 网络监控 |

#### 3.16.4 设置面板 Tab 结构（v4.0 更新：5 Tab + 自动化第 6 Tab）

> **v4.0 更新** | 对齐 PRD v2.2 §3.11.5（5 Tab）与 §3.18.5（自动化第 6 Tab）。Figma-Spec-v2 §07 为 5 Tab，代码实现以 PRD v2.2 为准扩展到 6 Tab。

| # | Tab 名称 | SF Symbol | 主要配置项 |
|---|---------|-----------|-----------|
| 1 | 通用（General）| `gearshape` | 语言/自动重连/iCloud 同步/Hotkey 快捷键/命令安全审计开关 |
| 2 | 外观（Appearance）| `paintpalette` | 深浅模式/窗口透明度/字体族与字号/行间距/连字 |
| 3 | 终端（Terminal）| `terminal` | 滚动回溯/鼠标行为/字符编码/响铃/光标形状 |
| 4 | 颜色（Colors）| `circle.hexagongrid.fill` | 终端主题 8 款内置/自定义 ANSI 调色板/主题导入导出/关键词高亮规则 |
| 5 | AI 助手（AI）| `sparkles` | 模型服务商/API Key（Keychain）/Base URL/模型 ID/请求超时 |
| 6 | 自动化（Automation）| `bolt` | 自动化触发器列表/新建编辑器（Sheet）/规则开关 |

```swift
enum SettingsTab: String, CaseIterable, Identifiable {
    case general     = "通用"
    case appearance  = "外观"
    case terminal    = "终端"
    case colors      = "颜色"
    case aiAssistant = "AI 助手"
    case automation  = "自动化"   // v4.0 新增，对应 AutomationTriggersView

    var id: String { rawValue }

    var iconName: String {
        switch self {
        case .general:     return "gearshape"
        case .appearance:  return "paintpalette"
        case .terminal:    return "terminal"
        case .colors:      return "circle.hexagongrid.fill"
        case .aiAssistant: return "sparkles"
        case .automation:  return "bolt"
        }
    }
}
```

**Tab 选择器规格（Figma-Spec-v2 §07）：** `grid-cols-5`（前 5 Tab）+ automation Tab 可追加为第 6 格；`bg-black/5 backdrop-blur-sm rounded-xl p-1`；激活态 `bg-white shadow-sm`。窗口宽度从 `600pt` 扩展为 `640pt` 以容纳 6 格。

---

### 3.17 Hotkey Window 模块（PRD §3.15）

> **v4.0 新增** | 全局浮动终端 NSPanel，对标 iTerm2 Hotkey Window。实现文件：`Features/HotkeyWindow/HotkeyWindowManager.swift`。

#### 3.17.1 技术方案核心

```
NSEvent.addGlobalMonitorForEvents（全局键盘监听）
    └── 匹配 ⌥Space（keyCode=49，modifier=.option）
            ├── 面板不可见 → show()：positionPanel + makeKeyAndOrderFront
            └── 面板可见   → hide()：orderOut(nil)
```

| 技术点 | 实现方案 |
|--------|---------|
| 全局快捷键 | `NSEvent.addGlobalMonitorForEvents(matching: .keyDown)`（需 Input Monitoring 权限，Sandbox 版降级为本地监听）|
| 窗口类型 | `NSPanel`（`isFloatingPanel = true`，`.nonactivatingPanel`，不强制抢夺焦点）|
| 窗口层级 | `NSWindowLevel(rawValue: CGWindowLevelForKey(.floatingWindow) + 1)` |
| 多 Space 跟随 | `collectionBehavior = [.canJoinAllSpaces, .auxiliary, .fullScreenAuxiliary]` |
| 失焦自动隐藏 | 订阅 `NSWindow.didResignKeyNotification`；受 `@AppStorage("hotkey.autoHideOnBlur")` 控制 |
| 动画 | `animationBehavior = .utilityWindow`（系统内置弹性动画）|

#### 3.17.2 HotkeyWindowManager 设计

```swift
/// 全局单例，生命周期与应用一致
final class HotkeyWindowManager {

    static let shared = HotkeyWindowManager()

    private var panel: NSPanel?
    private var globalMonitor: Any?
    private var localMonitor: Any?

    private let hotkeyCode: UInt16 = 49           // Space
    private let hotkeyModifiers: NSEvent.ModifierFlags = .option

    // MARK: - 公开 API

    func startMonitoring()   // applicationDidFinishLaunching 调用
    func stopMonitoring()    // applicationWillTerminate 调用
    func toggle()            // 手动切换（也可通过菜单栏调用）

    // MARK: - 私有

    private func buildPanel() -> NSPanel   // 懒构建，首次 show() 时创建
    private func positionPanel(_ p: NSPanel)
    private func panelFrame(for screen: NSScreen) -> NSRect   // 80%W × 40%H，上方 10% 偏移
    private func isHotkey(_ event: NSEvent) -> Bool
}
```

#### 3.17.3 配置持久化

```swift
// Settings → 通用 → Hotkey Window
UserDefaults.standard.register(defaults: [
    "hotkey.autoHideOnBlur": true,
    "hotkey.customShortcut": ""    // 预留自定义快捷键
])
```

#### 3.17.4 Sandbox 权限说明

| 版本 | 全局监听权限 | 降级行为 |
|------|-----------|---------|
| App Store（Sandbox）| 需要 `Input Monitoring` 隐私权限；首次使用弹系统请求对话框 | 权限拒绝时，仅注册 local monitor（应用前台时响应）|
| Direct 版 | 无沙盒限制，直接监听 | 无降级 |

#### 3.17.5 验收要点

- **TC-HK-001**：焦点在其他 App，按 ⌥Space，Panel 从屏幕顶部滑出，2s 内完成
- **TC-HK-002**：Panel 可见时再按 ⌥Space，窗口收起，前一个 App 重新获焦
- **TC-HK-003**：点击 Panel 外部，自动收起（autoHideOnBlur = true）

---

### 3.18 tmux 集成管理模块（PRD §3.16）

> **v4.0 新增** | 图形化 tmux 会话管理，通过已有 SSH 连接执行 tmux 命令，无独立进程。

#### 3.18.1 架构概述

```
TerminalController（已连接）
    └── TmuxSessionStore（ObservableObject）
            ├── detectTmux()  →  executeCommand("which tmux && tmux -V")
            ├── fetchSessions()  →  executeCommand("tmux list-sessions -F ...")
            ├── fetchWindows(session)  →  executeCommand("tmux list-windows -t {name} -F ...")
            └── performAction(TmuxAction)  →  executeCommand(...)
                    └── @MainActor 更新 @Published sessions / windows
```

#### 3.18.2 TmuxSessionStore 设计

```swift
@MainActor
final class TmuxSessionStore: ObservableObject {

    @Published var sessions: [TmuxSession] = []
    @Published var isDetected: Bool = false
    @Published var isVersionTooOld: Bool = false
    @Published var detectedVersion: String = ""

    private weak var controller: TerminalController?

    // MARK: - 版本检测（连接成功后 2s 内执行）

    func detectTmux() async {
        let out = await controller?.executeCommand("which tmux 2>/dev/null && tmux -V") ?? ""
        isDetected = out.contains("tmux")
        if let ver = parseVersion(out) {
            isVersionTooOld = ver < (2, 6)   // tmux 2.6+ 为最低可用版本
            detectedVersion = ver.display
        }
    }

    // MARK: - 数据刷新

    func fetchSessions() async {
        // tmux list-sessions -F "#{session_id}|#{session_name}|#{session_windows}|#{session_created}"
        let raw = await controller?.executeCommand(tmuxListCmd) ?? ""
        sessions = parseSessions(raw)
    }

    // MARK: - 操作

    func createSession(name: String, windowName: String = "main") {
        Task { await controller?.executeCommand("tmux new-session -d -s \(name) -n \(windowName)") }
        Task { await fetchSessions() }
    }

    func attachSession(_ session: TmuxSession) {
        controller?.sendToSSH("tmux attach -t \(session.id)\n")
    }

    func killSession(_ session: TmuxSession) {
        Task { await controller?.executeCommand("tmux kill-session -t \(session.id)") }
        Task { await fetchSessions() }
    }
}
```

#### 3.18.3 数据模型

```swift
struct TmuxSession: Identifiable, Equatable {
    let id: String           // tmux session_id（如 $1）
    let name: String
    let windowCount: Int
    let createdAt: Date
    var isAttached: Bool     // 当前 ShellMate 是否已附加到该 session
}

struct TmuxWindow: Identifiable, Equatable {
    let id: Int              // window_index
    let name: String
    let paneCount: Int
    let isActive: Bool
    let sessionName: String
}
```

#### 3.18.4 工具栏集成

```swift
// TerminalController 属性
@Published var tmuxStore: TmuxSessionStore?

// 连接成功后
func onConnected() {
    let store = TmuxSessionStore(controller: self)
    tmuxStore = store
    Task { await store.detectTmux() }
}
```

工具栏 tmux 图标：`isDetected = true` 时亮绿色激活态（`#34c759`），否则置灰 + Tooltip「目标服务器未安装 tmux」。

#### 3.18.5 验收要点

- **TC-TMUX-001**：SSH 连接到已安装 tmux 的服务器，工具栏图标 2s 内变激活
- **TC-TMUX-002**：Quick Actions「新建 Session」输入名称后，会话列表刷新

---

### 3.19 自动化触发器引擎（PRD §3.18）

> **v4.0 新增** | 终端输出匹配引擎，对标 iTerm2 Triggers，完全本地执行。

#### 3.19.1 架构概述

```
TerminalController.onOutputReceived(data)
    └── AutomationTriggerEngine.process(output, sessionId)
            ├── 遍历已启用的 AutomationTrigger 规则
            ├── 匹配条件（正则 / 关键词 / 连接事件）
            ├── 检查冷却时间（cooldownSeconds）
            └── 执行动作（Action Dispatcher）
                    ├── .sendCommand   → controller.sendToSSH(cmd)
                    ├── .notification  → UNUserNotificationCenter
                    ├── .openURL       → NSWorkspace.open(url)
                    ├── .writeLog      → FileHandle append
                    ├── .runScript     → Process（仅 Direct 版）
                    └── .highlightLine → ANSI SGR 注入（已有 HighlightEngine）
```

#### 3.19.2 AutomationTrigger 数据模型

```swift
/// 对应 PRD §3.18.4
struct AutomationTrigger: Identifiable, Codable {
    var id: UUID = .init()
    var name: String
    var isEnabled: Bool = true
    var scope: TriggerScope                  // .global / .session(UUID)
    var conditionType: TriggerConditionType
    var pattern: String?                     // 正则 / 关键词
    var caseSensitive: Bool = false
    var actionType: TriggerActionType
    var actionPayload: String                // 命令/URL/通知文字/文件路径
    var cooldownSeconds: Int = 60            // 0 = 不限制
    var createdAt: Date = .now

    enum TriggerScope: Codable {
        case global
        case session(UUID)
    }

    enum TriggerConditionType: String, Codable {
        case onConnect       // SSH 连接建立后
        case onDisconnect    // SSH 连接断开时
        case outputRegex     // 终端输出正则匹配
        case outputKeyword   // 终端输出关键词匹配
    }

    enum TriggerActionType: String, Codable {
        case sendCommand
        case notification
        case openURL
        case writeLog
        case runScript       // 仅 Direct 版
        case highlightLine
    }
}
```

#### 3.19.3 AutomationTriggerEngine 实现

```swift
/// 触发器引擎 — 线程安全，运行在 sshQueue 或 MainActor 均可
@MainActor
final class AutomationTriggerEngine {

    static let shared = AutomationTriggerEngine()

    // 上次触发时间戳（用于冷却时间判断）
    private var lastFiredAt: [UUID: Date] = [:]

    /// 终端输出到达时调用（由 TerminalController 注入）
    func process(output: String, sessionId: UUID, controller: TerminalController) {
        let triggers = AutomationTriggerStore.shared.activeTriggers(for: sessionId)
        for trigger in triggers {
            guard shouldFire(trigger) else { continue }
            if matches(trigger: trigger, output: output) {
                fire(trigger: trigger, output: output, controller: controller)
                lastFiredAt[trigger.id] = Date()
            }
        }
    }

    /// 连接事件（.onConnect / .onDisconnect）
    func processEvent(_ event: TriggerConditionType, sessionId: UUID, controller: TerminalController) {
        let triggers = AutomationTriggerStore.shared.activeTriggers(for: sessionId)
            .filter { $0.conditionType == event }
        for trigger in triggers where shouldFire(trigger) {
            fire(trigger: trigger, output: "", controller: controller)
            lastFiredAt[trigger.id] = Date()
        }
    }

    // MARK: - Private

    private func matches(trigger: AutomationTrigger, output: String) -> Bool {
        guard let pattern = trigger.pattern, !pattern.isEmpty else { return false }
        let options: String.CompareOptions = trigger.conditionType == .outputRegex
            ? (trigger.caseSensitive ? .regularExpression : [.regularExpression, .caseInsensitive])
            : (trigger.caseSensitive ? [] : .caseInsensitive)
        return output.range(of: pattern, options: options) != nil
    }

    private func shouldFire(_ trigger: AutomationTrigger) -> Bool {
        guard trigger.cooldownSeconds > 0,
              let last = lastFiredAt[trigger.id] else { return true }
        return Date().timeIntervalSince(last) >= Double(trigger.cooldownSeconds)
    }

    private func fire(trigger: AutomationTrigger, output: String, controller: TerminalController) {
        switch trigger.actionType {
        case .sendCommand:
            let cmd = substituteVariables(trigger.actionPayload, output: output)
            controller.sendToSSH(cmd + "\n")

        case .notification:
            let content = UNMutableNotificationContent()
            content.title = trigger.name
            content.body = substituteVariables(trigger.actionPayload, output: output)
            UNUserNotificationCenter.current().add(UNNotificationRequest(
                identifier: trigger.id.uuidString, content: content, trigger: nil))

        case .openURL:
            if let url = URL(string: substituteVariables(trigger.actionPayload, output: output)) {
                NSWorkspace.shared.open(url)
            }

        case .writeLog:
            // 追加到指定文件
            let text = substituteVariables(trigger.actionPayload, output: output) + "\n"
            try? (text as NSString).appendToFile(trigger.actionPayload, atomically: false,
                                                  encoding: .utf8)

        case .runScript:
            #if !APPSTORE
            let proc = Process(); proc.executableURL = URL(fileURLWithPath: "/bin/sh")
            proc.arguments = [trigger.actionPayload]; proc.launch()
            #endif

        case .highlightLine:
            // 通过 ANSI SGR 注入颜色（委托 HighlightEngine）
            HighlightEngine.shared.highlightLine(matching: trigger.pattern ?? "", in: controller)
        }
    }

    /// 变量替换：`{{USER}}` → session.username，`\0` → 正则捕获组 0
    private func substituteVariables(_ template: String, output: String) -> String {
        template  // TODO: 实现完整替换逻辑
    }
}
```

#### 3.19.4 持久化存储

```swift
/// Core Data 实体：CDAutomationTrigger（参与 CloudKit 同步）
/// 字段与 AutomationTrigger struct 一一对应，使用 NSValueTransformer 处理枚举
@MainActor
final class AutomationTriggerStore: ObservableObject {
    static let shared = AutomationTriggerStore()

    @Published var triggers: [AutomationTrigger] = []

    func activeTriggers(for sessionId: UUID) -> [AutomationTrigger] {
        triggers.filter { $0.isEnabled && ($0.scope == .global || $0.scope == .session(sessionId)) }
    }

    func add(_ trigger: AutomationTrigger) { /* Core Data insert + cloudKit */ }
    func update(_ trigger: AutomationTrigger) { /* Core Data update */ }
    func delete(_ trigger: AutomationTrigger) { /* Core Data delete */ }
}
```

#### 3.19.5 App Store vs Direct 差异

| 动作类型 | App Store | Direct |
|---------|-----------|--------|
| sendCommand / notification / openURL / writeLog | ✅ | ✅ |
| runScript（本机执行 sh 脚本）| ❌（沙盒禁止）| ✅ |
| highlightLine | ✅ | ✅ |

#### 3.19.6 验收要点

- **TC-TRIG-001**：触发器「输出匹配 `\[ERROR\]` → 系统通知」，会话中执行 `echo "[ERROR] test"`，10s 内收到通知
- **TC-TRIG-002**：「连接成功 → 发送命令 `echo "Hi {{USER}}"` 」，连接后终端自动打印 `Hi ubuntu`

---

## 4. 并发与线程模型

### 4.1 线程分配

```
MainThread（@MainActor）
  ├── SwiftUI / AppKit UI 渲染
  ├── Store 状态读写（@Published 更新）
  └── 用户交互事件处理

sshQueue（DispatchQueue.init("ssh", qos: .userInteractive, attributes: .concurrent)）
  ├── TCP Socket 建连
  ├── libssh2 握手 & 认证
  ├── PTY 数据轮询（DispatchSource read handler）
  └── SFTP 数据读写

ioQueue（DispatchQueue.init("io", qos: .utility)）
  ├── 文件读写（日志、录制文件）
  └── Core Data background context 操作

cloudKitQueue（系统托管，NSPersistentCloudKitContainer 内部）
  └── iCloud 同步、冲突解决
```

### 4.2 Swift Concurrency 使用原则

```swift
// 规则 1: SSH 工作通过 async/await 与主线程通信
Task.detached(priority: .userInitiated) {
    let data = try await connection.poll()
    await MainActor.run { viewModel.append(data) }
}

// 规则 2: actor 保护 SSHConnection 共享状态
actor SSHConnection {
    // 所有方法自动串行化，无需手动锁
}

// 规则 3: 传输任务使用 TaskGroup 实现并发限制
await withTaskGroup(of: Void.self) { group in
    for (i, task) in pendingTasks.enumerated() {
        if i >= concurrencyLimit { await group.next() }
        group.addTask { try await task.execute() }
    }
}
```

---

## 5. 数据流设计

### 5.1 建立 SSH 连接完整流程

```
[User: 双击 SessionRow]
         │
         ▼
SessionStore.connect(session)
         │
         ├── 1. 检查 Keychain 凭据 → KeychainService
         │        未找到 → 弹出密码输入框 → 等待用户输入
         │
         ├── 2. SSHConnectionService.connect(session)
         │        ├── TCP 建连（sshQueue）
         │        ├── SSH 握手（libssh2）
         │        ├── KnownHostsManager.check() ──────────┐
         │        │       unknown → @MainActor 弹 D02      │
         │        │       changed → @MainActor 弹 D03 (P0) │
         │        │       trusted → 继续                   │
         │        │                                        │
         │        │   ← 用户确认 ←─────────────────────────┘
         │        │
         │        ├── 认证（密码/Key/Agent/GSSAPI）
         │        └── 打开 PTY Channel
         │
         ├── 3. 创建 TabItem，加入 TabBar
         │
         ├── 4. 绑定 SSHConnection → TerminalView
         │        ShellMateTerminalView.bind(connection)
         │        启动 poll loop → feed(byteArray:)
         │
         └── 5. 启动关联 TunnelRules（autoStart = true）
                 TunnelStore.startAutoRules(for: session)
```

### 5.2 SFTP 上传数据流

```
[User: Finder 拖入 SFTPPanel]
         │
         ▼
SFTPStore.enqueueUpload(localURLs, remotePath)
         │
         ├── 1. 获取当前 Session 的 SFTPSession
         │        未初始化 → sshConnection.openSFTPSession()
         │
         ├── 2. 创建 TransferTask（state: .pending）
         │        加入 TransferQueue
         │
         ├── 3. TransferQueue.drainQueue()
         │        检查并发数（≤3）→ 启动 Task
         │
         ├── 4. SFTPSession.upload(localURL, remotePath, progress:)
         │        每 512KB 回调一次 progress
         │        → @MainActor 更新 TransferTask.progress
         │        → UI 进度条实时更新
         │
         └── 5. 完成
                 TransferTask.state = .completed
                 发送 UNUserNotificationCenter 系统通知
                 TransferQueue.drainQueue()（继续下一个任务）
```

---

## 6. 关键技术难点与解决方案

### 6.1 libssh2 非阻塞模式的 Swift 集成

**难点：** libssh2 是 C 库，非阻塞模式需要手动管理 `EAGAIN` 错误并重试；Swift 的 async/await 与 C 的 callback 模式不兼容。

**解决方案：**
```swift
// 使用 withCheckedThrowingContinuation 将 libssh2 回调桥接为 async
func readChannel() async throws -> Data {
    try await withCheckedThrowingContinuation { continuation in
        sshQueue.async {
            var buffer = [UInt8](repeating: 0, count: 65536)
            let rc = libssh2_channel_read(self.channel, &buffer, buffer.count)
            if rc == LIBSSH2_ERROR_EAGAIN {
                // 注册 DispatchSource，下次可读时重试
                self.scheduleRead(continuation: continuation)
            } else if rc > 0 {
                continuation.resume(returning: Data(buffer.prefix(rc)))
            } else {
                continuation.resume(throwing: SSHError.channelClosed)
            }
        }
    }
}
```

### 6.2 App Store Sandbox 下的 SSH Agent 访问

**难点：** SSH Agent 通过 UNIX Domain Socket（`$SSH_AUTH_SOCK`）通信，Sandbox 限制跨进程 socket 访问。

**解决方案（双版本策略）：**

| 版本 | 处理方式 |
|------|---------|
| App Store 版 | 不支持 SSH Agent（在 UI 中明确说明），提示用户改用私钥文件认证 |
| Direct 版（官网下载） | 无 Sandbox，正常访问 `SSH_AUTH_SOCK` |

代码判断：
```swift
#if SANDBOXED
    // 禁用 Agent 选项，显示说明文字
#else
    let agentSocketPath = ProcessInfo.processInfo.environment["SSH_AUTH_SOCK"]
#endif
```

### 6.3 SwiftTerm 与 libssh2 的数据竞争

**难点：** SwiftTerm 要求在主线程 feed 数据，libssh2 poll 在 sshQueue；高频输出（如 `cat large_file`）可能造成主线程积压。

**解决方案：** 引入背压（Backpressure）机制：

```swift
// 使用 AsyncStream 作为缓冲队列
let (stream, continuation) = AsyncStream<Data>.makeStream(
    bufferingPolicy: .bufferingNewest(100)  // 最多缓存 100 个数据块
)

// sshQueue 侧：生产数据
continuation.yield(data)

// MainActor 侧：消费数据（60fps 帧率驱动）
Task { @MainActor in
    for await data in stream {
        terminalView.feed(byteArray: [UInt8](data))
    }
}
```

### 6.4 Core Data + CloudKit 同步冲突

**难点：** 多设备同时修改同一 Session 配置，CloudKit 可能产生合并冲突。

**解决方案：**
- 使用 `NSMergeByPropertyObjectTrumpMergePolicy`（最新写入版本优先）
- 为关键字段添加 `modifiedAt: Date` 属性，自定义冲突仲裁逻辑
- Session 删除操作采用软删除（`isDeleted: Bool`），同步到所有设备后再彻底清除

### 6.5 终端 Unicode/CJK 字符正确渲染

**难点：** CJK 全角字符宽度是半角的 2 倍，与等宽字体的列计算不匹配，导致光标错位。

**解决方案：** SwiftTerm 已内置 Unicode 宽度表（基于 Unicode UCD），配合设置「非 ASCII 字体」（如冬青黑体）：
```swift
terminalView.font = NSFont(name: "JetBrainsMono-Regular", size: 13)!
terminalView.nativeFont = NSFont(name: "HiraginoSansGB-W3", size: 13)!
```

---

## 7. 性能设计

### 7.1 冷启动优化

**目标：** < 2 秒（PRD §5.1）

| 优化手段 | 说明 |
|---------|------|
| 懒加载 SessionStore | 首屏仅加载分组结构，会话详情按需加载 |
| Core Data Batch Fetch | 使用 `NSBatchFetchRequest` 减少行数，首屏最多加载 100 个会话 |
| 异步预热 | 启动时不建立任何 SSH 连接，等用户主动触发 |
| `@MainActor.run` 批量更新 | 合并多次 `@Published` 变更为一次 UI 更新 |

### 7.2 终端渲染性能

**目标：** ≥ 60fps，键盘延迟 < 16ms（PRD §5.1）

| 优化手段 | 说明 |
|---------|------|
| **TerminalDataCoalescer** | 16ms 时间窗口合并高频 SSH 数据包，减少 MainActor 微任务调度（详见下方） |
| Metal 渲染后端 | SwiftTerm 支持 Metal 加速文字渲染 |
| 滚动缓冲区分页 | 回滚缓冲区最多 10,000 行，使用虚拟列表渲染（仅渲染可见区域） |
| 高亮规则预编译 | 应用启动时预编译所有正则，避免每帧重新解析 |
| 缓冲区分级 | 读缓冲区 4KB→32KB→128KB 动态调整，适配不同吞吐场景 |

#### 7.2.1 TerminalDataCoalescer（数据合并器）

核心优化组件，解决高频 SSH 输出（如 `cat large_file`）导致的主线程微任务积压问题：

```swift
/// 终端数据合并器 — 将高频 SSH 数据包合并到 16ms 时间窗口
/// 对齐 60fps 刷新周期，每帧最多一次 SwiftTerm.feed() 调用
private actor TerminalDataCoalescer {
    private var buffer: [UInt8] = []
    private var hasPendingFlush = false

    /// 追加数据到缓冲区，返回是否需要触发 flush
    func append(_ bytes: [UInt8]) -> Bool {
        buffer.append(contentsOf: bytes)
        if hasPendingFlush { return false }  // 窗口内后续包只追加
        hasPendingFlush = true
        return true  // 第一个包触发 16ms flush 定时器
    }

    /// 取出并清空缓冲区
    func drain() -> [UInt8] {
        let data = buffer
        buffer.removeAll(keepingCapacity: true)
        hasPendingFlush = false
        return data
    }
}

// 使用方式（TerminalController 中）
if await coalescer.append(bytes) {
    Task { @MainActor in
        try? await Task.sleep(nanoseconds: 16_000_000)  // 16ms 窗口
        let merged = await coalescer.drain()
        terminalView.feed(byteArray: merged)  // 单次批量 feed
    }
}
```

> **效果：** 大文件输出场景下，MainActor Task 创建数量降低 10-50 倍，CPU 占用显著下降，终端渲染帧率稳定 60fps。

### 7.3 搜索性能

**目标：** 500 会话搜索 < 100ms（PRD §5.1）

```swift
// 内存中全量 sessions 数组，使用 Trie 实现模糊匹配
// 对于 500 个会话，线性扫描也远低于 100ms 阈值
// 搜索在 DispatchQueue.global(qos: .userInteractive) 执行
// 结果通过 MainActor 更新 filteredSessions

func search(_ query: String) {
    Task.detached(priority: .userInitiated) {
        let results = self.allSessions.filter {
            $0.name.fuzzyMatch(query) ||
            $0.host.contains(query) ||
            $0.username.contains(query)
        }
        await MainActor.run { self.filteredSessions = results }
    }
}
```

---

## 8. 安全设计

### 8.1 密钥存储安全边界

```
┌─────────────────────────────────────────────────────────────┐
│  应用内存（临时，仅连接生命周期内）                           │
│  · 解密后的密码（plaintext，复制后立即 overwrite）           │
│  · libssh2 session handle                                   │
└─────────────────────────────────────────────────────────────┘
         ↑ 启动时从 Keychain 读取，用后清除
┌─────────────────────────────────────────────────────────────┐
│  macOS Keychain（本设备安全存储）                            │
│  · SSH 密码（kSecClassGenericPassword）                     │
│  · 私钥 Passphrase（kSecClassGenericPassword）              │
│  · 主密码 PBKDF2 摘要（用于验证，不存明文）                  │
└─────────────────────────────────────────────────────────────┘
         ↑ 不参与 iCloud 同步
┌─────────────────────────────────────────────────────────────┐
│  Core Data + iCloud（跨设备同步）                            │
│  · Session 基本配置（host / port / username）               │
│  · keychainRef（UUID string，无凭据内容）                   │
│  · 外观配置、高亮规则、命令集                                │
└─────────────────────────────────────────────────────────────┘
          ↑ 不含任何敏感凭据
```

### 8.2 网络安全

- SSH 通信全程 libssh2 加密，禁用弱算法（见 PRD §3.10.4）
- 在 `SSHConnectionService` 中显式配置算法偏好列表：

```swift
libssh2_session_method_pref(session, LIBSSH2_METHOD_KEX,
    "curve25519-sha256,ecdh-sha2-nistp256")
libssh2_session_method_pref(session, LIBSSH2_METHOD_CRYPT_CS,
    "chacha20-poly1305@openssh.com,aes256-ctr")
// 明确禁用 3DES, RC4, MD5
```

### 8.3 指纹验证防 MITM

指纹变更检测是 P0 安全功能，实现上严格禁止绕过：

```swift
switch knownHostsResult {
case .changed:
    // 强制主线程显示 D03 弹窗
    await MainActor.run {
        windowManager.present(D03Alert(oldFingerprint: ..., newFingerprint: ...))
    }
    // 挂起当前连接 task，等待用户明确选择
    let userChoice = await D03Alert.userDecision()
    guard userChoice == .updateAndProceed else {
        throw SSHError.userRejectedKeyChange
    }
}
```

### 8.4 App Sandbox 权限申请（App Store 版）

```xml
<!-- ShellMate.entitlements -->
<key>com.apple.security.app-sandbox</key>
<true/>
<key>com.apple.security.network.client</key>  <!-- SSH 出站 -->
<true/>
<key>com.apple.security.files.user-selected.read-write</key>  <!-- 私钥文件 -->
<true/>
<key>com.apple.security.keychain-access-groups</key>
<array>
    <string>$(AppIdentifierPrefix)app.shellmate</string>
</array>
```

---

## 9. 测试策略

### 9.1 测试金字塔

```
          ┌───────────────┐
          │  E2E 测试      │  ← UI Test（XCUITest），少量
          │  真实 SSH 服务器│    TC-001 ~ TC-010（PRD 验收用例）
          └───────┬───────┘
         ┌────────┴────────┐
         │  集成测试        │  ← 本地 Docker sshd + Swift Test
         │  SSH + Core Data│    覆盖多跳/认证/断连/同步
         └───────┬─────────┘
      ┌──────────┴──────────┐
      │    单元测试（主力）   │  ← XCTest / Swift Testing Framework
      │ 业务逻辑、数据模型   │    Mock 外部依赖（SSH/Keychain/CloudKit）
      └─────────────────────┘
```

### 9.2 关键测试要点

```swift
// 示例：SSH 认证失败测试
@Test func testSSHAuthFailureShouldShowError() async throws {
    let mockSSH = MockSSHConnectionService()
    mockSSH.stubbedError = SSHError.authenticationFailed(.password)

    let store = SessionStore(sshService: mockSSH)
    await store.connect(session: .fixture)

    #expect(store.connectionError != nil)
    #expect(store.activeConnections.isEmpty)
}

// 示例：Known Hosts 指纹变更
@Test func testChangedFingerprintShouldTriggerAlert() async throws {
    let mockKH = MockKnownHostsManager()
    mockKH.stubbedResult = .changed
    // 验证 D03 Alert 被呈现
}
```

### 9.3 CI 集成

```yaml
# .github/workflows/ci.yml 或本地 Xcode Cloud
jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Run unit tests
        run: xcodebuild test -scheme ShellMate -destination 'platform=macOS'
      - name: Run integration tests (with Docker SSH)
        run: |
          docker run -d -p 2222:22 linuxserver/openssh-server
          xcodebuild test -scheme ShellMateIntegration
```

---

## 10. 工程化规范

### 10.1 项目目录结构

> **v4.0 更新** | 对齐实际代码仓库结构，补充 §3.17–§3.19 新模块路径，修正历史文档中的错误路径（`ScriptAutomation/` → `Scripts/`、`Monitor/` → 直接平铺、`Logging/` → `Logs/`、`ImportExport/` → 并入 `SessionForm/`）。

```
ShellMate/
├── App/                              ← 应用入口与全局配置
│   ├── ShellMateApp.swift            ← @main 入口、菜单栏定义、HotkeyWindowManager.startMonitoring()
│   ├── AppDelegate.swift             ← NSApplicationDelegate、Dock 菜单
│   ├── ContentView.swift             ← 主界面布局、四格分屏、语言/外观注入
│   └── ContentViewExtensions.swift   ← Toolbar、Lifecycle、Settings 拆分以规避编译器超时
│
├── Core/                             ← 核心业务逻辑与服务层
│   ├── Models/                       ← 数据模型（Session、Group 等）
│   │   ├── Session.swift
│   │   ├── SessionGroup.swift
│   │   ├── TerminalTab.swift
│   │   ├── TmuxModels.swift          ← TmuxSession / TmuxWindow（§3.18）
│   │   ├── TunnelModels.swift
│   │   ├── SFTPModels.swift
│   │   ├── ServerMetrics.swift       ← 性能监控指标快照（§3.10）
│   │   ├── QuickCommandModels.swift
│   │   └── Enums/                    ← ConnectionState、AuthMethod 等枚举
│   ├── Stores/                       ← 状态管理（ObservableObject / @MainActor）
│   │   ├── SessionStore.swift
│   │   ├── TabBarStore.swift
│   │   ├── GroupStore.swift
│   │   ├── QuickCommandStore.swift
│   │   ├── SessionLogStore.swift     ← 实时日志缓冲（§3.14）
│   │   ├── SyncInputStore.swift      ← 多标签同步输入（§3.2.3）
│   │   ├── TmuxSessionStore.swift    ← tmux 会话/窗口状态（§3.18）
│   │   └── TmuxConfigStore.swift     ← tmux 用户偏好持久化
│   ├── Services/                     ← 后端服务
│   │   ├── SSH/                      ← SSH 连接核心
│   │   │   ├── SSHConnection.swift
│   │   │   ├── SSH2Connection.swift
│   │   │   ├── SSHEventLoop.swift
│   │   │   ├── SSHAuthService.swift
│   │   │   ├── SSHChannelManager.swift
│   │   │   ├── SSHConfigParser.swift ← ~/.ssh/config 解析（R11）
│   │   │   ├── SSHProxyJump.swift
│   │   │   ├── SSHProcessBridge.swift
│   │   │   ├── SSHNetworkUtils.swift
│   │   │   ├── SSHError.swift
│   │   │   ├── SSHSessionConfig.swift
│   │   │   ├── LibSSH2Bridge.swift   ← C libssh2 桥接协议
│   │   │   ├── LibSSH2BridgeReal.swift
│   │   │   └── KnownHostsManager.swift
│   │   ├── SFTP/                     ← SFTP 文件管理（独立 SSH 连接，阻塞模式）
│   │   │   ├── SFTPSession.swift
│   │   │   └── SFTPTransferQueue.swift
│   │   ├── PortForwarding/           ← 端口转发与隧道（§3.5）
│   │   │   ├── LocalPortForwarder.swift
│   │   │   ├── RemotePortForwarder.swift
│   │   │   ├── Socks5Proxy.swift
│   │   │   └── TunnelManager.swift
│   │   ├── AI/                       ← AI 助手服务（§3.8）
│   │   │   └── AIService.swift
│   │   ├── Recording/                ← 录制服务（§3.13）
│   │   │   └── SessionRecorder.swift
│   │   ├── CommandSafetyChecker.swift  ← AI 命令安全审计（§3.8.4）
│   │   ├── CredentialVault.swift       ← AES-256-GCM 凭据保险库（§3.7）
│   │   ├── HighlightEngine.swift       ← 关键词高亮（ANSI SGR 注入）
│   │   ├── KeychainService.swift       ← Keychain 读写封装
│   │   ├── NetworkMonitor.swift        ← 网络可达性监控
│   │   ├── ServerMetricsMonitor.swift  ← 服务器性能采集（§3.10）
│   │   ├── SessionFormatParser.swift   ← JSON/CSV 导入导出解析（§3.15）
│   │   └── TerminalControllerRegistry.swift ← TC 注册表（标签→控制器映射）
│   └── Persistence/                  ← Core Data + CloudKit
│       ├── PersistenceController.swift
│       └── Repositories/
│           ├── SessionRepository.swift
│           └── GroupRepository.swift
│
├── Features/                         ← UI 功能模块（按特性划分）
│   ├── Terminal/                     ← 终端渲染与控制
│   │   ├── TerminalView.swift
│   │   ├── TerminalController.swift  ← TerminalDataCoalescer、日志/录制三点写入
│   │   ├── ShellMateTerminalView.swift ← SwiftTerm NSViewRepresentable 包装
│   │   ├── TerminalTextView.swift
│   │   ├── TerminalToolbarView.swift
│   │   ├── TerminalPlaceholderView.swift ← 空态视图（Figma §01 §04）
│   │   ├── ComposePaneView.swift     ← 命令组合面板（§3.2.3）
│   │   ├── SyncInputConfirmView.swift
│   │   ├── SplitSessionPickerView.swift
│   │   ├── LocalTerminalView.swift   ← 本地 Shell（Hotkey Window 复用）
│   │   ├── LocalTerminalController.swift
│   │   └── ConnectionErrorView.swift
│   ├── HotkeyWindow/                 ← 全局浮动终端面板（§3.17）
│   │   └── HotkeyWindowManager.swift ← NSPanel 单例 + ⌥Space 全局监听
│   ├── Tmux/                         ← tmux 图形化管理（§3.18）
│   │   ├── TmuxManagerView.swift
│   │   └── TmuxNewSessionSheet.swift
│   ├── AI/                           ← AI 助手 UI
│   │   ├── AIAssistantPanelView.swift
│   │   ├── AIErrorDetectiveView.swift
│   │   ├── AISummaryView.swift
│   │   └── CommandSafetyAlertView.swift ← 危险命令二次确认弹窗（§3.8.4）
│   ├── HostKey/                      ← 主机指纹安全弹窗（D02/D03）
│   │   ├── HostKeyConfirmationView.swift
│   │   └── HostKeyChangedWarningView.swift
│   ├── Sidebar/                      ← 侧边栏会话列表
│   │   ├── SessionSidebarView.swift
│   │   ├── SessionListView.swift
│   │   ├── SessionRowView.swift
│   │   ├── GroupHeaderView.swift
│   │   ├── GroupManagerView.swift
│   │   ├── SidebarFooterView.swift
│   │   └── SidebarSearchView.swift
│   ├── SessionForm/                  ← 会话创建/编辑表单 + 导入导出（§3.15 并入此处）
│   │   ├── SessionFormSheet.swift
│   │   ├── SessionBasicTab.swift
│   │   ├── SessionAuthTab.swift
│   │   ├── SessionAdvancedTab.swift
│   │   ├── SessionAppearanceTab.swift
│   │   ├── GroupFormSheet.swift
│   │   ├── SSHConfigImportView.swift
│   │   └── SessionImportExportView.swift
│   ├── TabBar/                       ← 标签页管理
│   │   ├── TerminalTabBarView.swift
│   │   └── TerminalTabView.swift
│   ├── StatusBar/                    ← 底部状态栏（含系统监控 §3.10）
│   │   ├── TerminalStatusBarView.swift
│   │   └── ServerMonitorPanelView.swift
│   ├── Settings/                     ← 设置面板（v4.0：6 Tab，见 §3.16.4）
│   │   ├── SettingsView.swift        ← Tab 容器：通用/外观/终端/颜色/AI助手/自动化
│   │   ├── GeneralSettingsView.swift
│   │   ├── AppearanceSettingsView.swift
│   │   ├── TerminalSettingsView.swift
│   │   ├── ColorsSettingsView.swift
│   │   ├── HighlightSettingsView.swift
│   │   ├── AISettingsView.swift
│   │   ├── SecuritySettingsView.swift
│   │   ├── CloudSyncSettingsView.swift
│   │   └── PasswordManagerView.swift
│   ├── Welcome/                      ← 欢迎引导界面（§3.11，3 步 Onboarding）
│   │   └── WelcomeScreenView.swift
│   ├── Scripts/                      ← 脚本自动化面板（§3.12）
│   │   ├── ScriptLibraryView.swift
│   │   ├── ScriptEditorSheet.swift
│   │   ├── ScriptModels.swift
│   │   └── ScriptStore.swift
│   ├── Recording/                    ← 录制与回放（§3.13）
│   │   └── RecordingDialogView.swift
│   ├── Logs/                         ← 日志面板（§3.14）
│   │   └── LogPanelView.swift
│   ├── SFTP/                         ← SFTP 面板 UI
│   │   └── SFTPPanelView.swift
│   ├── Tunnel/                       ← 隧道管理器 UI（§3.5）
│   │   └── TunnelManagerView.swift
│   └── QuickCommand/                 ← 快捷命令管理器
│       └── QuickCommandManagerView.swift
│
├── Shared/                           ← 共享组件与工具
│   ├── Components/                   ← 可复用 SwiftUI 组件
│   ├── Utilities/
│   │   └── DesignTokens.swift        ← 设计令牌（颜色/字体/间距/Glass效果 §3.16）
│   └── Extensions/                   ← Color(hex:)、Collection[safe:]、Notification 等
│
└── Tests/
    ├── UnitTests/
    └── IntegrationTests/
        └── SSHConnectionIntegrationTests.swift
```

> **v4.0 目录修正说明：**
> - `ScriptAutomation/` → `Scripts/`（含 ScriptStore、ScriptModels、ScriptEditorSheet）
> - `Monitor/` 子目录取消，`ServerMetricsMonitor.swift` 直接置于 `Core/Services/`
> - `Logging/` → `Logs/`（LogPanelView.swift）
> - `ImportExport/` 合并到 `SessionForm/`（SessionImportExportView.swift）
> - 新增 `Features/HotkeyWindow/`（§3.17）、`Features/Tmux/`（§3.18）
> - 新增 `Core/Services/CommandSafetyChecker.swift`（§3.8.4）、`Core/Stores/TmuxSessionStore.swift`（§3.18）
> - `Settings/` 从 3 Tab 扩展到 6 Tab（§3.16.4），待新增 `AutomationTriggersSettingsView.swift`

### 10.2 代码规范

- **SwiftLint** 集成，`.swiftlint.yml` 启用 `force_try`、`weak_delegate`、`class_delegate_protocol` 等规则
- **命名约定**：Store 用 `SessionStore`、Service 用 `SSHConnectionService`、Repository 用 `SessionRepository`、View 用 `SessionSidebarView`
- **注释**：所有 public 接口提供 `///` DocComment，复杂算法内联解释
- **SF Symbols**：所有图标通过 `Image(systemName:)` 使用，禁止内嵌自定义 PNG 图标（工具栏）

### 10.3 依赖管理

使用 Swift Package Manager，锁定具体版本（`.resolve` 文件提交到 git）：

```swift
// Package.resolved 示例
{
  "pins": [
    {
      "identity": "swiftterm",
      "kind": "remoteSourceControl",
      "location": "https://github.com/migueldeicaza/SwiftTerm",
      "state": { "revision": "...", "version": "1.1.0" }
    }
  ]
}
```

---

## 11. 分阶段交付计划

> **v4.0 更新** | 对齐 PRD v2.2，在原 17 周基础上追加第五阶段（W18–W24）以覆盖 Hotkey Window（§3.17）、tmux 集成（§3.18）、自动化触发器（§3.19）三个新模块。

### 第一阶段（第 1–5 周）：基础框架

| 周次 | 技术交付物 |
|------|----------|
| W1 | Xcode 工程结构搭建，SPM 依赖配置，libssh2 XCFramework 集成验证 |
| W2 | Core Data 模型定义，Repository 层实现，SessionStore 骨架 |
| W3 | 侧边栏 UI（SessionSidebarView），会话 CRUD，分组树渲染 |
| W4 | SharedComponents 基础组件库（Button、Input、StatusDot、Tag） |
| W5 | 阶段验收：SessionStore 持久化通过，侧边栏搜索/过滤 < 100ms |

### 第二阶段（第 6–9 周）：SSH 核心

| 周次 | 技术交付物 |
|------|----------|
| W6 | libssh2 Swift 桥接层，TCP 建连，SSH 握手，非阻塞 DispatchSource |
| W7 | 密码 & SSH Key 认证，KnownHostsManager，D02/D03 安全弹窗 |
| W8 | SwiftTerm 集成，PTY Channel，TerminalView 数据绑定，TabBar |
| W9 | Keychain 凭据存储，多标签并发稳定性测试，阶段验收 TC-001~003 |

### 第三阶段（第 10–13 周）：高级功能

| 周次 | 技术交付物 |
|------|----------|
| W10 | SFTP Panel，SFTPSession，文件列表，上传/下载（TransferQueue）|
| W11 | 端口转发（LocalForward + Remote + SOCKS），TunnelManager UI |
| W12 | 跳板机 ProxyJump 链路，iCloud CloudKit 同步集成，冲突解决 |
| W13 | 命令片段库，Compose Pane，HighlightEngine，阶段验收 TC-004~010 |

### 第四阶段（第 14–17 周）：打磨上架

| 周次 | 技术交付物 |
|------|----------|
| W14 | Sandbox 权限精细化，App Store 版 vs Direct 版差异处理 |
| W15 | 性能优化（冷启动/Terminal 60fps/内存），SwiftLint 整改 |
| W16 | 代码签名，公证（notarytool），App Store 元数据，TestFlight |
| W17 | P0 Bug 清零，App Store 正式提审，官网 DMG 发布 |

### 第五阶段（第 18–24 周）：PRD v2.2 新模块（v4.0 新增）

> 本阶段对应 PRD v2.2 §3.15–§3.19 新增需求，不影响已上架版本的稳定性。

| 周次 | 模块 | 技术交付物 |
|------|------|----------|
| W18 | **Hotkey Window（§3.17）** | `HotkeyWindowManager` NSPanel 骨架，`⌥Space` 全局事件监听（全局/本地双 monitor），LocalTerminalView 嵌入，失焦自动隐藏，NSPanel 定位逻辑（80%W × 40%H）|
| W19 | **Hotkey Window — 完善** | App Store Sandbox 降级兜底（Input Monitoring 权限检测），`AutoHideToggleButton` 偏好持久化，`⎋` 关闭支持（macOS 14+ `onKeyPress`），TC-HK-001/002 验收通过 |
| W20 | **tmux 集成（§3.18）** | `TmuxSessionStore`，SSH 命令执行层（`tmux list-sessions / new-session`），版本检测（≥2.6），TmuxManagerView 三 Tab 骨架 |
| W21 | **tmux 集成 — 完善** | `TmuxNewSessionSheet`，窗口列表（`tmux list-windows`），快捷操作（`attach/detach/kill`），工具栏集成（Popover），TC-TMUX-001/002 验收通过 |
| W22 | **AI 命令安全审计（§3.8.4）** | `CommandSafetyChecker` 静态正则分析（30 条 danger/warning 规则），`.danger` 二次确认弹窗（`CommandSafetyAlertView`），TC-AI-SEC-001/002 验收通过 |
| W23 | **自动化触发器（§3.19）** | `AutomationTriggerEngine` 骨架，`AutomationTrigger` Core Data Entity，输出流挂钩（`TerminalController.onOutput`），`matchTriggers` 正则引擎，冷却计时器 |
| W24 | **自动化触发器 — 完善** | 6 种 Action 派发（通知/命令/声音/脚本/断开/自定义），App Store 受限动作禁用，`AutomationTriggersSettingsView`（设置第 6 Tab），TC-TRIG-001/002 验收通过，全量回归测试 |

---

## 12. 风险与应对

> **v4.0 更新** | 在原 R01–R12 基础上追加 R13–R15，覆盖 §3.17–§3.19 新模块引入的风险。

| ID | 风险 | 可能性 | 影响 | 规避措施 |
|----|------|--------|------|---------|
| R01 | libssh2 在 macOS 新版本 API 变更 | 低 | 高 | 持续关注 macOS Beta；封装 thin wrapper 层隔离 C API |
| R02 | SwiftTerm 不支持特定终端转义序列 | 中 | 中 | 提前测试 vim/tmux/htop 等主流工具；必要时提交 PR 修复 |
| R03 | App Store 审核拒绝（网络权限说明不足）| 中 | 高 | 审核说明中详细描述 SSH 功能用途；准备 Direct 渠道保底 |
| R04 | CloudKit 同步在弱网下延迟过高 | 中 | 低 | 本地优先设计，离线完全可用；同步失败不影响基础功能 |
| R05 | libssh2 Sandbox 兼容性问题 | 低 | 高 | 阶段一结束前完成 Sandboxed 环境下的 PoC 验证 |
| R06 | SwiftUI 动画在 macOS 13 低版本卡顿 | 中 | 低 | 使用 `withAnimation` 而非 `.animation` 修饰符；测试 macOS 13 最低版本 |
| **R07** | **Known Hosts 安全流程缺失** | **高** | **高** | KnownHostsManager 已实现但未集成到连接流程；需在 W4 补全首次连接指纹确认弹窗（D02）和指纹变更警告（D03） |
| **R08** | **"有壳无魂"——核心差异功能未实现** | **高** | **高** | 当前仅完成基础连接，缺少 ~/.ssh/config 导入、Login Script、本地终端、Hotkey Window 等竞品标配功能；需按评审报告优先级在 M3/M4 阶段补齐 |
| R09 | CloudKit 容器未激活 | 中 | 高 | `NSPersistentCloudKitContainer` 已初始化但 iCloud 容器标识符未在 Apple Developer 激活；W12 前完成激活和真机验证 |
| R10 | QuickCommand 数据模型缺失 | 中 | 中 | PRD 定义的快捷命令功能尚无 Core Data Entity；需在 W5 完成 `CDQuickCommandSet` / `CDQuickCommand` 模型迁移 |
| R11 | ~/.ssh/config 导入解析器复杂度 | 中 | 中 | OpenSSH config 语法含 Match/Include/通配符，完整解析工作量大；建议分期实现（先 Host/HostName/Port/User 基础字段） |
| R12 | 菜单栏国际化不生效 | 低 | 低 | SwiftUI `CommandMenu` 不继承 `.environment(\.locale)`；需改用 `NSLocalizedString` 或 `Text(verbatim:)` + 手动监听语言切换 |
| **R13** | **Hotkey Window 全局监听在 App Store Sandbox 版被拒** | **高** | **高** | `NSEvent.addGlobalMonitorForEvents` 在 Sandbox 下需要 Input Monitoring 隐私权限（macOS 14+）。**规避：** ① 为 App Store 版实现降级路径——当 `globalMonitor` 返回 nil 时，在首次呼出后弹出权限引导弹窗（`AXIsProcessTrusted` 类似模式）；② Direct 版不受此限制，保持完整功能；③ 在 entitlements 中声明 `com.apple.security.temporary-exception.sbpl` 规则（参考 iTerm2 App Store 实现）；④ W19 完成 Sandbox PoC 验收，TC-HK-003 覆盖该场景。|
| **R14** | **tmux 命令输出格式在不同 Linux 发行版 / 版本不兼容** | **中** | **中** | `tmux list-sessions` 的默认格式在 tmux 2.x vs 3.x 之间有差异（字段顺序、时间戳格式）；Alpine/BusyBox 环境下命令路径可能异常。**规避：** ① `TmuxSessionStore` 始终使用 `-F` 格式化参数固定字段顺序（`#{session_name}:#{session_windows}:#{session_attached}`）；② 版本检测（§3.18.2）在 < 2.6 时显示降级提示而非静默失败；③ 集成测试覆盖 Ubuntu 22.04 / CentOS 7 / Alpine 3.18 三个真机环境（参见 CLAUDE.md §4.1 测试服务器）。|
| **R15** | **AutomationTrigger 正则引擎 ReDoS 攻击面** | **中** | **高** | 用户自定义触发器正则运行在主线程输出回调中（`TerminalController.onOutput`）。若写入回溯指数级复杂的正则（如 `(a+)+`），遇到特定服务器输出可造成 UI 冻结（ReDoS）。**规避：** ① `AutomationTriggerEngine.matchTriggers` 所有正则匹配在独立 `Task { await actor.match(...) }` 中执行，不阻塞 `@MainActor`；② 编译正则时捕获 `NSRegularExpression` 异常并在设置界面显示语法错误提示；③ 单次匹配设置 500ms 超时（`Task.sleep` + `withTaskCancellationHandler`）；④ 提供 `isValidRegex(_:)` 校验函数，在用户保存触发器前实时验证，并在 `AutomationTriggersSettingsView` 中展示复杂度警告。|

---

## 附录 A：关键 API 速查

| API | 来源 | 用途 |
|-----|------|------|
| `libssh2_session_init` | libssh2 | 初始化 SSH 会话 |
| `libssh2_session_handshake` | libssh2 | SSH 握手 |
| `libssh2_hostkey_hash` | libssh2 | 获取主机指纹 |
| `libssh2_userauth_password` | libssh2 | 密码认证 |
| `libssh2_userauth_publickey_fromfile` | libssh2 | 公钥认证 |
| `libssh2_channel_open_session` | libssh2 | 打开会话 channel |
| `libssh2_channel_request_pty` | libssh2 | 请求 PTY |
| `libssh2_channel_direct_tcpip` | libssh2 | 本地端口转发 |
| `libssh2_sftp_init` | libssh2 | 初始化 SFTP |
| `TerminalView.feed` | SwiftTerm | 向终端投喂数据 |
| `NSPersistentCloudKitContainer` | Apple | Core Data + CloudKit |
| `SecItemAdd / SecItemCopyMatching` | Apple | Keychain 读写 |
| `UNUserNotificationCenter` | Apple | 系统通知 |

---

## 附录 B：关键术语

| 术语 | 说明 |
|------|------|
| PTY | Pseudo Terminal，伪终端；数据通过 `libssh2_channel_request_pty` 申请 |
| EAGAIN | libssh2 非阻塞模式返回值，表示需要等待 socket 可读后重试 |
| Actor isolation | Swift 并发模型，`actor` 类型保证内部状态串行访问 |
| Backpressure | 生产者速率超过消费者时的背压机制，防止内存无限增长 |
| Repository pattern | 数据访问抽象层，隔离 Core Data 的具体实现 |
| XCFramework | Apple 多架构 framework 格式，同时包含 arm64 + x86_64 |
| SFTPv3 | libssh2 默认支持的 SFTP 版本，支持断点续传 seek 操作 |

---

---

## 版本历史

### v4.0（2026-04-14）— 对齐 PRD v2.2

**变更记录：**

| 章节 | 变更内容 |
|------|---------|
| 文档头 | 版本号 v3.0 → v4.0，基准文档更新为 PRD v2.2 + Figma-Spec-v2（2026-04-02）|
| 目录 | 新增 §3.17 Hotkey Window、§3.18 tmux 集成、§3.19 自动化触发器三个章节入口 |
| §2.1 | 架构图表现层新增 HotkeyWindowManager、TmuxManagerView；应用层新增 TmuxSessionStore、AutomationTriggerEngine |
| §3.8.4 | **新增** CommandSafetyChecker（AI 命令安全审计）：静态正则 30 条风险规则，`.danger` 二次确认弹窗，App Store 合规设计 |
| §3.11 | FTUE 三步内容完整重定义（对齐 PRD v2.2 §3.17），删除本地 Shell 引导，更新 WelcomeScreenView 签名 |
| §3.16.4 | 设置面板从 3 Tab 扩展到 6 Tab（通用/外观/终端/颜色/AI助手/自动化），`SettingsTab` 枚举补充 `.automation` |
| §3.17 | **新增** Hotkey Window 模块：NSPanel 单例、`⌥Space` 全局监听、Sandbox 权限降级策略、验收用例 |
| §3.18 | **新增** tmux 集成管理模块：TmuxSessionStore、SSH 命令执行层、版本检测（≥2.6）、TmuxManagerView 三 Tab |
| §3.19 | **新增** 自动化触发器引擎：AutomationTrigger 数据模型、6 种 Action 类型、输出流挂钩、Core Data 持久化、App Store vs Direct 能力差异 |
| §10.1 | 目录结构对齐实际代码仓库（修正 ScriptAutomation→Scripts、Logging→Logs、补充 HotkeyWindow/Tmux/HostKey/QuickCommand 等实际路径）|
| §11 | 交付计划追加第五阶段（W18–W24），覆盖三个新模块的全部开发里程碑 |
| §12 | 风险登记册新增 R13（Hotkey Window Input Monitoring 权限）、R14（tmux 跨发行版兼容性）、R15（AutomationTrigger ReDoS）|

---

### v3.0（2026-04-08）— 基于 PRD v2.0 + 系统模块扩充

| 章节 | 变更内容 |
|------|---------|
| §3.10 | 新增系统性能监控模块（状态栏实时指标）|
| §3.11 | 新增欢迎引导模块（WelcomeScreen 初版）|
| §3.12 | 新增脚本自动化面板（ScriptLibraryView）|
| §3.13 | 新增录制与回放模块（SessionRecorder + RecordingDialogView）|
| §3.14 | 新增日志面板模块（SessionLogStore + LogPanelView）|
| §3.15 | 新增导入/导出模块（SessionImportExportView）|
| §3.16 | 新增 UI 设计令牌系统（Figma-Spec-v2 §3.16）|

---

### v2.0（2026-04-01）— 基于产品评审报告修正

| 章节 | 变更内容 |
|------|---------|
| §2.1 | 架构图补充 AI 服务层、CredentialVault、独立 SFTP 连接标注 |
| §3.2.2 | 修正高亮实现：AttributedString → ANSI SGR 转义序列注入 |
| §3.2.3 | 修正分屏实现：递归 PaneLayout → 简洁 SplitLayout 三态枚举 |
| §3.4.1 | 修正 SFTP 架构：复用 SSH 连接 → 独立 SSH 连接（阻塞模式）|
| §3.8 | 新增 AI 服务层（Claude/OpenAI/Ollama SSE 流式对话）|
| §3.9 | 新增国际化策略及已知限制 |
| §7.2 | 补充 TerminalDataCoalescer 16ms 数据合并器、缓冲区分级优化 |
| §10.1 | 修正目录结构：Modules/Infrastructure → Core/Features/Shared 三层 |
| §12 | 补充 R07–R12 风险项（来源：产品评审报告）|

---

### v1.0（2026-03-18）— 初版

- 基于 PRD v1.0 的初始技术方案，覆盖 SSH 核心、SwiftTerm 渲染、Core Data + CloudKit、SFTP、端口转发、Keychain 安全层

---

*文档版本：v4.0 · 2026-04-14 · 作者：解决方案架构师*
