# ShellMate for macOS — 原型设计文档

> **文档版本：** v2.0
> **创建日期：** 2026-03-18
> **最后更新：** 2026-04-14
> **文档类型：** 原型规范（Wireframe Prototype Spec）
> **基于文档：** ShellMate PRD v2.2 / Figma-Spec-v2（fileKey: `upl5OBUkpLGnOe1u5aQRZ5`，2026-04-02）
> **文档状态：** 已批准

---

## 阅读说明

本文档是 PRD v2.2 的原型转化稿，以 Figma-Spec-v2 设计系统为唯一权威规范，面向视觉设计师和前端工程师。每个界面模块包含：

- **布局结构**：组件的位置关系与尺寸约束（对齐 Figma CSS 令牌）
- **元素清单**：界面内所有 UI 元素及其状态
- **交互说明**：用户操作→界面响应的映射关系
- **边界情况**：空状态、加载状态、错误状态的视觉处理

文档中的 ASCII 线框图为结构参考，不代表最终视觉风格。**所有颜色值以 Figma-Spec-v2 §00 设计令牌为准。**

---

## 目录

1. [应用整体框架](#1-应用整体框架)
2. [界面 P01：主窗口 — 侧边栏](#2-界面-p01主窗口--侧边栏)
3. [界面 P02：主窗口 — 终端区域](#3-界面-p02主窗口--终端区域)
4. [界面 P03：主窗口 — SFTP 面板](#4-界面-p03主窗口--sftp-面板)
5. [界面 P04：主窗口 — 状态栏（含系统监控）](#5-界面-p04主窗口--状态栏含系统监控)
6. [界面 P05：AI 助手面板](#6-界面-p05ai-助手面板)
7. [弹窗 D01：新建 / 编辑会话](#7-弹窗-d01新建--编辑会话)
8. [弹窗 D02：首次连接指纹确认](#8-弹窗-d02首次连接指纹确认)
9. [弹窗 D03：主机密钥变更警告](#9-弹窗-d03主机密钥变更警告)
10. [弹窗 D04：隧道管理器](#10-弹窗-d04隧道管理器)
11. [弹窗 D05：快捷命令管理器](#11-弹窗-d05快捷命令管理器)
12. [弹窗 D06：分组管理器](#12-弹窗-d06分组管理器)
13. [弹窗 D07：密码管理器](#13-弹窗-d07密码管理器)
14. [弹窗 D08：tmux 管理器](#14-弹窗-d08tmux-管理器)
15. [弹窗 D09：导入 / 导出](#15-弹窗-d09导入--导出)
16. [面板 S01：设置面板（6-Tab）](#16-面板-s01设置面板6-tab)
17. [面板 S02：日志面板](#17-面板-s02日志面板)
18. [覆层 O01：终端内搜索](#18-覆层-o01终端内搜索)
19. [覆层 O02：Compose Pane（命令编辑区）](#19-覆层-o02compose-pane命令编辑区)
20. [覆层 O03：同步输入模式](#20-覆层-o03同步输入模式)
21. [覆层 O04：AI 命令安全审计](#21-覆层-o04ai-命令安全审计)
22. [覆层 O05：FTUE 欢迎引导](#22-覆层-o05ftue-欢迎引导)
23. [覆层 O06：Hotkey Window](#23-覆层-o06hotkey-window)
24. [状态集合](#24-状态集合)
25. [组件库摘要](#25-组件库摘要)

---

## 1. 应用整体框架

### 1.1 窗口结构层次

> 对齐 Figma-Spec-v2 §01 整体布局规范

```
┌──────────────────────────────────────────────────────────────────────┐
│  NSWindow（最小尺寸 900×600，无最大限制）                              │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │  TitleBar（H: 40px，#f5f5f7/90 + backdrop-blur-2xl）          │    │
│  ├──────────────────────────────────────────────────────────────┤    │
│  │  ToolBar（H: 48px，#f5f5f7/80 + backdrop-blur-xl）            │    │
│  ├──────────────┬──────────────────────────────┬────────────────┤    │
│  │              │  TabBar（H: 40px）             │                │    │
│  │  Sidebar     ├──────────────────────────────┤  FileTransfer  │    │
│  │  （W: 256px  │                              │  Panel         │    │
│  │   固定，     │  TerminalView                │  （W: 500px，  │    │
│  │   不可拖拽） │  （flex-1）                   │   可关闭）     │    │
│  │              │                              │                │    │
│  │              │  [AI 助手面板可插入此处左侧]   │                │    │
│  │              │   W: 400px，可关闭            │                │    │
│  ├──────────────┴──────────────────────────────┴────────────────┤    │
│  │  StatusBar（H: 32px，#f5f5f7/90 + backdrop-blur-2xl）         │    │
│  └──────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────┘
```

### 1.2 布局规则

| 区域 | 尺寸约束 | 背景 | 可见性控制 |
|------|----------|------|-----------|
| TitleBar | H: 40px，固定 | `#f5f5f7/90` + blur(24px) | 始终可见 |
| ToolBar | H: 48px，固定 | `#f5f5f7/80` + blur(24px) | 可在 View 菜单隐藏 |
| Sidebar | W: 256px，**不可拖拽调整** | `#f5f5f7/95` + blur(16px) | `⌘⌥S` 收起/展开 |
| TabBar | H: 40px，固定 | `#f5f5f7/80` + blur(16px) | 始终可见 |
| TerminalView | flex-1（占满剩余空间） | `white/50` + blur(sm) | 始终可见 |
| AI 助手面板 | W: 400px，固定 | `white/90` + blur(16px) | 工具栏按钮切换 |
| FileTransfer 面板 | W: 500px，固定 | `white/90` + blur(16px) | `⌘⇧S` 打开/关闭 |
| StatusBar | H: 32px，固定 | `#f5f5f7/90` + blur(24px) | 始终可见 |

### 1.3 设计令牌（Figma-Spec-v2 §00）

> ⚠️ v2.0 起完全对齐 Figma-Spec-v2 令牌系统，弃用旧版自定义色值

#### 品牌色 / 功能色

| 令牌 | HEX | 用途 |
|------|-----|------|
| `color-primary` | `#007aff` | 主操作按钮、选中态、链接（Apple Blue） |
| `color-primary-hover` | `#0051d5` | 主按钮悬停 |
| `color-success` | `#34c759` | 已连接状态、上传成功 |
| `color-warning` | `#ff9500` | 告警、AI 建议高亮、同步输入边框 |
| `color-error` | `#ff3b30` | 错误、高 CPU/内存、危险命令 |
| `color-purple` | `#5856d6` | AI 功能渐变终止色 |

#### 中性色（语义化）

| 令牌 | HEX | 用途 |
|------|-----|------|
| `color-text-primary` | `#1d1d1f` | 主要文字 |
| `color-text-secondary` | `#86868b` | 次要文字、描述、标签 |
| `color-text-disabled` | `#c7c7cc` | 禁用文字 |
| `color-bg-primary` | `#ffffff` | 白色卡片/面板 |
| `color-bg-secondary` | `#f5f5f7` | 应用背景、侧边栏 |
| `color-bg-tertiary` | `#e8e8ed` | 渐变背景末端 |
| `color-border` | `#d2d2d7` | 分隔线（通常用 50% 透明度：`rgba(210,210,215,0.5)`） |
| `color-overlay-modal` | `rgba(0,0,0,0.30)` | 模态蒙层 |

#### 终端主题色板

| 主题 ID | 背景 | 前景 | Prompt 绿 |
|---------|------|------|-----------|
| `vs-dark`（默认）| `#1e1e1e` | `#cccccc` | `#4ec9b0` |
| `monokai` | `#272822` | `#f8f8f2` | `#a6e22e` |
| `dracula` | `#282a36` | `#f8f8f2` | `#50fa7b` |
| `solarized-dark` | `#002b36` | `#839496` | `#859900` |
| `nord` | `#2e3440` | `#d8dee9` | `#a3be8c` |
| `gruvbox` | `#282828` | `#ebdbb2` | `#98971a` |
| `ocean` | `#1b2b34` | `#c0c5ce` | `#99c794` |

#### 排版令牌

| 令牌 | 字号 | 字重 | 用途 |
|------|------|------|------|
| `text-hero` | 48px | 700 | 欢迎界面大标题 |
| `text-xl` | 20px | 500 | 欢迎副标题、空状态标题 |
| `text-title` | 18px | 500 | 弹窗标题 |
| `text-body-lg` | 16px | 400 | 正文、按钮 |
| `text-body` | 14px | 500 | 侧边栏会话名 |
| `text-sm` | 13px | 400 | 会话描述、弹窗描述 |
| `text-xs` | 12px | 500 | 标签页标签、工具栏按钮标签 |
| `text-caption` | 11px | 400 | 文件尺寸、时间戳、状态栏文字 |
| `text-terminal` | 14px | 400 | 终端等宽字体 |

#### 圆角令牌

| 令牌 | 值 | 用途 |
|------|----|----|
| `radius-sm` | 6px | 图标背景徽章、Tooltip |
| `radius-md` | 8px | 按钮、输入框 |
| `radius-lg` | 10px | 卡片、面板区块 |
| `radius-xl` | 12px | 侧边栏会话行、标签触发器 |
| `radius-2xl` | 16px | 弹窗容器（Dialog） |
| `radius-3xl` | 24px | 欢迎界面英雄图标容器 |
| `radius-full` | 9999px | 胶囊形徽章、步骤指示器 |

---

## 2. 界面 P01：主窗口 — 侧边栏

> 对齐 Figma-Spec-v2 §02 侧边栏规范

### 2.1 线框结构

```
┌──────────────────────────────────┐  W: 256px，固定
│  搜索框区域（H: 40px，px-3）     │  ← [🔍 搜索图标] [搜索会话… 占位] [清除×]
├──────────────────────────────────┤
│  顶部工具行（H: 40px，px-3）     │  ← [＋新建] [📁分组] [🔑密码] [⚙设置]
├──────────────────────────────────┤
│  ─── 生产环境  ─────────── [+]  │  ← 分组标题行：[展开箭头▶] [📁图标] [分组名] [+按钮]
│    🟢 prod-web-01                │  ← 会话行（已连接）
│       192.168.1.100              │
│    🟢 prod-db                    │
│    ⚪ prod-cache                 │  ← 会话行（未连接）
│                                  │
│  ─── 开发环境  ─────────── [+]  │
│    🟡 dev-app                    │  ← 连接中（黄色脉冲）
│    ⚪ dev-db                     │
│                                  │
│  （空白区域，可拖入会话）         │
│                                  │
├──────────────────────────────────┤
│  [＋ 新建会话]（底部固定按钮）   │
└──────────────────────────────────┘
```

### 2.2 元素规格

#### 搜索框

| 属性 | 规格 |
|------|------|
| 高度 | 30px |
| 外边距 | 上下 5px，左右 8px |
| 背景 | `rgba(0,0,0,0.05)` |
| 边框 | `1px rgba(210,210,215,0.5)`，聚焦时 `#007aff` |
| 圆角 | `radius-md`（8px） |
| 图标 | SF Symbol `magnifyingglass`，11px，`#86868b` |
| 清除按钮 | 仅有内容时显示，点击清空 |

#### 顶部工具行

四个图标按钮（等宽排列）：

| 按钮 | 图标 | 功能 |
|------|------|------|
| 新建 | `plus` | 打开新建会话弹窗 D01 |
| 分组管理 | `folder` | 打开分组管理弹窗 D06 |
| 密码管理 | `key` | 打开密码管理弹窗 D07 |
| 设置 | `gear` | 打开设置面板 S01 |

按钮样式：`h-8 w-8 rounded-lg ghost`，悬停 `bg-black/5`

#### 分组行

| 属性 | 规格 |
|------|------|
| 高度 | 28px |
| 左内边距 | 8px |
| 分组图标 | `p-1 rounded-md bg-[#007aff]/10`，`folder h-3 w-3 text-[#007aff]` |
| 分组名 | `text-xs font-medium text-[#86868b]` |
| 展开箭头 | `chevron-right`（折叠）/ `chevron-down`（展开），动画 200ms |
| 新建按钮 | `+`，悬停时显示（默认隐藏） |

#### 会话行

| 状态 | 状态点 | 背景 | 文字颜色 |
|------|--------|------|---------|
| 未连接 | `w-2 h-2 rounded-full bg-[#c7c7cc]` | 透明 | `#86868b` |
| 连接中 | `w-2 h-2 rounded-full bg-[#ff9500] animate-pulse` | 透明 | `#1d1d1f` |
| 已连接 | `w-2 h-2 rounded-full bg-[#34c759]` | 透明 | `#1d1d1f` |
| 选中激活 | 同上 | `rgba(0,122,255,0.08)` | `#007aff` |
| 悬停 | 同上 | `rgba(0,0,0,0.05)` | `#1d1d1f` |

会话行高度：**52px**（含 padding）

会话行内容（从左到右）：
1. 状态点（6px 圆，左对齐，固定宽 16px）
2. 会话名（`text-body`=14px，最大宽 flex，超出 ellipsis）
3. 主机地址（`text-caption`=11px，`#86868b`，另起一行）

同步输入激活标签（⚡ bolt.fill，橙色 9px）：显示在会话名右侧

右键菜单项：连接 / 在新标签打开 / 编辑 / 重命名 / 复制 / 删除

#### 底部新建按钮

| 属性 | 规格 |
|------|------|
| 高度 | 36px |
| 外边距 | 8px |
| 样式 | 虚线边框 `border-dashed border-[#d2d2d7]`，`#007aff` 文字，圆角 `radius-lg` |
| 悬停 | 背景 `rgba(0,122,255,0.08)` |
| 图标 | `plus` 12px，与文字间距 6px |

### 2.3 交互说明

| 操作 | 触发 | 响应 |
|------|------|------|
| 双击会话行 | — | 新标签页建立 SSH 连接 |
| 单击会话行 | — | 高亮选中（不连接），已有对应标签则激活 |
| 拖拽会话行 | 长按 200ms | 拖入另一分组时高亮目标分组 |
| 点击分组箭头 | — | 折叠/展开，动画 200ms，持久化状态 |
| `⌘F` | 全局 | 聚焦搜索框，选中全部内容 |
| 搜索输入 | 实时 | 过滤列表，匹配字符加粗高亮 |

### 2.4 边界情况

**空状态（无会话）：**
```
┌──────────────────────────────────┐
│  [搜索框]                        │
│  [顶部工具行]                    │
├──────────────────────────────────┤
│                                  │
│         [SSH 图标 32px]          │
│      还没有保存的会话             │
│    点击下方按钮创建第一个          │
│                                  │
│  [＋ 新建会话]                   │
└──────────────────────────────────┘
```

**搜索无结果：**
```
│                                  │
│   未找到 "xxx" 相关会话           │
│   检查拼写或尝试 IP 搜索          │
│                                  │
```

---

## 3. 界面 P02：主窗口 — 终端区域

> 对齐 Figma-Spec-v2 §04 终端区域规范

### 3.1 标签栏线框

```
┌──────────────────────────────────────────────────────────────┐  H: 40px
│  [🟢 prod-web-01 ×]  [🟢 prod-db ×]  [🟡 dev-app ×]  [+]    │
│  ↑ 活跃标签                          ↑ 非活跃             ↑新建│
│  bg-white/90 shadow-sm                bg-transparent            │
└──────────────────────────────────────────────────────────────┘
```

#### 标签元素规格

| 元素 | 规格 |
|------|------|
| 标签栏高度 | 40px（`h-10`） |
| 标签内边距 | 左右各 12px（`px-3`） |
| 标签最小宽度 | 80px（`min-w-[80px]`） |
| 标签最大宽度 | 160px（`max-w-[160px]`），超出 ellipsis |
| 状态点 | 5px，颜色与侧边栏一致 |
| 标签名字号 | `text-xs`（12px），`font-medium` |
| **活跃标签** | `bg-white/90 shadow-sm`，文字 `#1d1d1f` |
| **非活跃标签** | 透明背景，文字 `#86868b`，悬停 `rgba(0,0,0,0.04)` |
| 关闭按钮 | `xmark` 9px，默认透明，悬停/选中时显示，`20×20px bg-black/10 rounded` |
| 标签栏背景 | `#f5f5f7/80 backdrop-blur-xl border-b border-[#d2d2d7]/50` |
| 右侧分隔线 | 非活跃标签右侧 `1px rgba(210,210,215,0.5)` |

新建标签按钮（`+`）：`28×28px`，`ghost`，悬停 `bg-black/5`

**同步输入激活标签（⚡）：**  
显示在标签名右侧，`bolt.fill 9px orange`，见覆层 O03

**Tooltip**（悬停 2 秒后）：
```
┌─────────────────────────────────┐
│  ubuntu@192.168.1.100           │
│  端口：22  连接时长：2h 14m     │
└─────────────────────────────────┘
```

### 3.2 终端内容区

#### 终端字体与渲染

| 属性 | 默认值 | 可配置范围 |
|------|--------|-----------|
| 字体 | Menlo / Monaco | 所有系统等宽字体 |
| 字号 | 14px（`text-terminal`） | 8–32px |
| 行高 | 1.6 | 1.2–2.0 |
| 背景 | `#1e1e1e`（VS Dark 默认） | 见 §1.3 终端主题色板 |
| 前景 | `#cccccc` | ANSI 256 色 |
| 光标 | 块状，白色，闪烁 | 3 种形状 |
| 内边距 | 12px 四周 | 0–32px 各边独立 |

#### 终端内容区线框

```
┌────────────────────────────────────────────────────────────┐
│  ubuntu@prod-web-01:~$ sudo systemctl status nginx          │
│  ● nginx.service - A high performance web server            │
│     Active: active (running) since Mon 2026-03-14           │  ← 绿色高亮
│                                                             │
│  [ERROR] connection refused to db:5432                      │  ← 红色高亮（规则命中）
│  ubuntu@prod-web-01:~$ █                                    │  ← 光标（闪烁）
│                                                             │
│                         ↑ AI 错误建议条（内联，底部浮现）    │
└────────────────────────────────────────────────────────────┘
```

#### AI 错误建议条（内联，非阻塞）

当终端输出匹配 error/failed/denied/fatal 时，底部出现：

```
┌─────────────────────────────────────────────────────────────┐
│  ✨ AI 已检测到错误   [分析原因]   [忽略]   🔒 仅发送选中内容│
└─────────────────────────────────────────────────────────────┘
```

样式：`bg-[#007aff]/5 border-t border-[#007aff]/20 px-4 py-2 text-xs`

### 3.3 空状态（无活跃 SSH 会话）

> 对齐 Figma-Spec-v2 §01 §4 空态规范

```
┌──────────────────────────────────────────┐
│                                          │
│           ┌──────────┐                  │
│           │    🖥️    │  96×96px         │
│           │ 渐变圆角 │  from-[#007aff]/10│
│           └──────────┘  to-[#5856d6]/10 │
│                                          │
│        暂无活跃会话                       │
│   text-xl semibold text-[#1d1d1f]        │
│                                          │
│    在侧边栏选择或新建一个会话开始          │
│   text-sm text-[#86868b]                │
│                                          │
│        [ + 新建会话 ]                    │
│   bg-[#007aff] text-white rounded-xl    │
│                                          │
└──────────────────────────────────────────┘
```

### 3.4 分屏布局

**左右分屏（vertical）：**
```
┌─────────────────┬─────────────────┐
│  Terminal 1     │  Terminal 2     │
│   flex-1        │   flex-1        │
└─────────────────┴─────────────────┘
                border-r border-[#d2d2d7]/50
```

**上下分屏（horizontal）：**
```
┌───────────────────────────────────┐
│  Terminal 1    flex-1             │
├───────────────────────────────────┤  border-b border-[#d2d2d7]/50
│  Terminal 2    flex-1             │
└───────────────────────────────────┘
```

**四格分屏（grid）：**
```
┌─────────────────┬─────────────────┐
│  Terminal 1     │  Terminal 2     │  上半 flex-1
├─────────────────┼─────────────────┤  border-b + border-r
│  Terminal 3     │  Terminal 4     │  下半 flex-1
└─────────────────┴─────────────────┘
```

分割线：`border-[#d2d2d7]/50`，无可拖拽分割条（固定等比例）

### 3.5 快捷命令栏（可收起，底部停靠）

```
┌────────────────────────────────────────────────────────────┐  H: 40px
│  [服务重启] [查看日志] [磁盘空间] [CPU状态]  [···]  [编辑]  │
└────────────────────────────────────────────────────────────┘
```

| 元素 | 规格 |
|------|------|
| 高度 | 40px |
| 命令按钮 | `rounded-lg border border-[#d2d2d7]/50 text-xs px-3 py-1.5` |
| 命令按钮悬停 | `bg-[#007aff]/5 border-[#007aff]/20 text-[#007aff]` |
| 收起按钮 | 右侧，点击折叠为细条 |
| 编辑按钮 | 最右侧，打开命令管理器 D05 |

---

## 4. 界面 P03：主窗口 — SFTP 面板

> 对齐 Figma-Spec-v2 §08 文件传输面板规范

### 4.1 线框结构

```
┌──────────────────────────────────────┐  W: 500px，H: 100%（side panel）
│  文件传输  prod-web-01           [×]  │  ← 面板标题（28px）
├──────────────────────────────────────┤
│  [←][→][↑] /home/ubuntu/            │  ← 路径导航栏（32px）
│                                [刷新]│
├──────────────────────────────────────┤
│  名称                大小  修改时间   │  ← 列标题
├──────────────────────────────────────┤
│  📁 ..                               │
│  📁 node_modules    —    Mar 10      │
│  📁 src             —    Mar 14      │
│  ► 📄 server.js     14K  Mar 14      │  ← 选中行
│  📄 package.json    2K   Mar 10      │
│  📄 .env            0.5K Feb 28      │  ← 隐藏文件（灰色斜体）
├──────────────────────────────────────┤
│  [↑上传] [↓下载] [📁新建目录] [⋯]   │  ← 操作工具栏（36px）
├──────────────────────────────────────┤
│  传输队列（折叠态，点击展开）         │
└──────────────────────────────────────┘
```

### 4.2 文件列表规格

| 属性 | 规格 |
|------|------|
| 行高 | 36px（`h-9`） |
| 选中行 | `bg-[#007aff]/8 text-[#1d1d1f]` |
| 悬停行 | `bg-black/5` |
| 隐藏文件 | `text-[#c7c7cc]`，斜体（`⌘⇧.` 切换显示） |
| 目录图标 | 蓝色 `folder`，14px |
| 文件图标 | 按扩展名区分颜色（代码橙，图片绿，压缩黄，其余灰） |

### 4.3 传输队列（展开态）

```
┌──────────────────────────────────────┐
│  传输队列（2 进行中，1 已完成）       │  ← 折叠/展开标题
├──────────────────────────────────────┤
│  ↑ server.js           [▮▮▮▮▮▮──]   │
│    14.2 KB/s  剩余 3s      [暂停]    │
├──────────────────────────────────────┤
│  ↓ backup.tar.gz       [▮▮▮▮────]   │
│    2.1 MB/s  剩余 28s      [取消]    │
├──────────────────────────────────────┤
│  ✓ config.yml    完成  0.5KB  3s     │
└──────────────────────────────────────┘
```

| 元素 | 规格 |
|------|------|
| 进度条 | `h-1.5 rounded-full bg-[#007aff]`，背景 `bg-[#e8e8ed]` |
| 速度/剩余 | `text-caption` `text-[#86868b]` |
| 完成行 | `✓ text-[#34c759]` |

---

## 5. 界面 P04：主窗口 — 状态栏（含系统监控）

> 对齐 Figma-Spec-v2 §05 状态栏规范。H: 32px（`h-8`）

### 5.1 连接状态下线框

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐  H: 32px
│ ● Production Server • ubuntu@192.168.1.1  |  🖥 CPU 32.1%  |  🧠 6.2/16 GB  |  💾 245/500 GB  |  📶 ↓45 ↑12 KB/s  │
└─────────────────────────────────────────────────────────────────────────────────────────┘
  bg-[#f5f5f7]/90 backdrop-blur-2xl border-t border-[#d2d2d7]/50 text-xs px-4
```

### 5.2 未连接状态

```
┌─────────────────────────────────────────────────────────────────────────┐
│  ⊗  Not connected                                                       │
└─────────────────────────────────────────────────────────────────────────┘
  WifiOff h-3 w-3 text-[#86868b]
```

### 5.3 各区段规格

| 区段 | 内容 | 颜色/样式 |
|------|------|-----------|
| 状态点 | `w-2 h-2 rounded-full bg-[#34c759] animate-pulse` | 绿色脉冲 |
| 会话名 | `text-[#1d1d1f] font-medium` | 主文字 |
| 分隔点 | `•`，`text-[#86868b]` | 次要文字 |
| 用户@主机 | `text-[#86868b]` | 次要文字 |
| 分隔线 | `h-4 w-px bg-[#d2d2d7]/50` | — |
| CPU 指示器 | 图标 `Cpu h-3 w-3 text-[#007aff]` + 数值 | 见颜色阈值表 |
| 内存指示器 | 图标 `MemoryStick h-3 w-3 text-[#5856d6]` + 已用/总量 + 进度条 | 见颜色阈值表 |
| 磁盘指示器 | 图标 `HardDrive h-3 w-3 text-[#34c759]` + 已用/总量 | 见颜色阈值表 |
| 网络指示器 | 图标 `Wifi h-3 w-3 text-[#ff9500]` + ↓下行 ↑上行 | 无阈值，灰色 |

#### 颜色阈值表（CPU / 内存 / 磁盘）

| 指标 | 正常（绿 `#34c759`） | 警告（橙 `#ff9500`） | 危险（红 `#ff3b30`） |
|------|---------------------|---------------------|---------------------|
| CPU | < 60% | 60–80% | > 80% |
| 内存 | < 70% | 70–85% | > 85% |
| 磁盘 | < 75% | 75–90% | > 90% |

> ⚠️ CPU 仅显示「图标 + 标签 + 百分比数值」，**不显示迷你柱状图**（Figma-Spec-v2 §05 §5 更新）  
> 内存显示进度条（`h-1 rounded-full`）

### 5.4 状态栏交互 Tooltip

**加密算法悬停：**
```
┌──────────────────────────────────┐
│  密钥交换：curve25519-sha256     │
│  主机密钥：ssh-ed25519           │
│  对称加密：aes256-ctr            │
│  MAC：hmac-sha2-256-etm         │
└──────────────────────────────────┘
```

**系统指标悬停（内存示例）：**
```
┌──────────────────────────────────┐
│  内存使用率：38.75%              │
│  已用：6.2 GB / 总计：16 GB     │
│  采集：/proc/meminfo  2s 刷新   │
└──────────────────────────────────┘
```

---

## 6. 界面 P05：AI 助手面板

> 对齐 Figma-Spec-v2 §09 AI 助手面板规范。W: 400px（左侧附加面板）

### 6.1 整体线框

```
┌──────────────────────────────────────────────────────┐  W: 400px
│  ┌────┐  AI Assistant                                │  Header，H: 64px
│  │ ✨ │  Helping with Production Server              │  p-4 border-b
│  └────┘                                              │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌────┐  Hello! I'm your AI terminal...             │  AI 消息（左对齐）
│  │ ✨ │  How can I help you today?                  │
│  └────┘                                              │
│                                                      │
│              [How do I find large files?]  [U]       │  用户消息（右对齐）
│                                                      │
│  ┌────┐                                              │  AI 回复
│  │ ✨ │  To find large files, use:                  │
│  └────┘  ┌─────────────────────────────────────┐    │  命令块
│           │  find . -type f -size +100M  [📋]  │    │  代码字体，[📋]=复制
│           └─────────────────────────────────────┘    │
│                                                      │
│  ┌────┐                                              │  Typing 指示器
│  │ ✨ │  ●  ●  ●  （三点动画）                      │
│  └────┘                                              │
│                                                      │
│  💡  Quick suggestions（仅首条消息时显示）            │
│  [How do I check disk usage?] [Monitor CPU] [···]    │
├──────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────┐    │  输入区
│  │  Ask me anything...                  [发送]  │    │
│  └──────────────────────────────────────────────┘    │
│  按 Enter 发送，Shift+Enter 换行                      │
└──────────────────────────────────────────────────────┘
  bg-white/90 backdrop-blur-xl border-r border-[#d2d2d7]/50
```

### 6.2 Header 规格

| 元素 | 规格 |
|------|------|
| 容器 | `p-4 border-b border-[#d2d2d7]/50 bg-white/60 backdrop-blur-xl` |
| AI 图标（大） | `p-2 rounded-xl bg-gradient-to-br from-[#007aff] to-[#5856d6] shadow-lg` |
| 图标 | `Sparkles h-5 w-5 text-white` |
| 标题 | `font-semibold text-[#1d1d1f]`，"AI Assistant" |
| 副标题 | `text-xs text-[#86868b]`，"Helping with {会话名}" |

### 6.3 消息规格

**AI 消息（左对齐）：**
- 外层 `flex gap-3 justify-start`
- 头像：`w-8 h-8 rounded-full bg-gradient-to-br from-[#007aff] to-[#5856d6]`，内含 `Sparkles h-4 w-4 text-white`
- 气泡：`max-w-[85%] rounded-2xl px-4 py-3 bg-white/80 backdrop-blur-sm text-[#1d1d1f]`

**用户消息（右对齐）：**
- 外层 `flex gap-3 justify-end`
- 气泡：`max-w-[85%] rounded-2xl px-4 py-3 bg-[#007aff] text-white`
- 头像（U）：`w-8 h-8 rounded-full bg-[#e8e8ed] flex items-center justify-center text-xs font-medium text-[#86868b]`

**命令块：**
```css
bg-[#1e1e1e] text-[#cccccc] rounded-xl p-3 text-xs font-mono
flex items-center justify-between
/* 复制按钮 */
Copy h-3.5 w-3.5 text-[#86868b] hover:text-white
```

**Typing 指示器：**
- 三个圆点，`w-2 h-2 rounded-full bg-[#86868b]`，`animate-bounce`，交错延迟

---

## 7. 弹窗 D01：新建 / 编辑会话

> 对齐 Figma-Spec-v2 §06 新建会话弹窗规范

### 7.1 弹窗规格

| 属性 | 值 |
|------|---|
| 类型 | Sheet（从主窗口顶部滑出） |
| 最大宽度 | `sm:max-w-[500px]` |
| 高度 | 自适应 |
| 背景 | `bg-white/95 backdrop-blur-2xl rounded-2xl border border-[#d2d2d7]/50 shadow-2xl` |

### 7.2 弹窗结构（单页表单）

```
┌──────────────────────────────────────────────────────────┐
│  新建 SSH 会话                                       [×]  │  ← 标题行
│  Create a new SSH connection                             │  ← 副标题 text-sm text-[#86868b]
├──────────────────────────────────────────────────────────┤
│                                                          │
│  会话名称（可选）                                        │
│  ┌────────────────────────────────────────────────────┐  │
│  │ 生产服务器                                         │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  主机地址 *                               端口 *         │
│  ┌───────────────────────────────┐  ┌───────────────┐   │
│  │ 192.168.1.100                 │  │ 22            │   │
│  └───────────────────────────────┘  └───────────────┘   │
│                                                          │
│  用户名 *                              分组              │
│  ┌───────────────────────────────┐  ┌───────────────┐   │
│  │ ubuntu                        │  │ 生产环境    ▼ │   │
│  └───────────────────────────────┘  └───────────────┘   │
│                                                          │
│  认证方式                                                │
│  ┌──────────────────────────────────────────────────┐   │
│  │ [🔑 SSH 密钥]  [🔒 密码]  [🤝 SSH Agent]         │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  私钥文件路径                                            │
│  ┌──────────────────────────────────┐  [选择文件]       │
│  │ ~/.ssh/id_ed25519                │                   │
│  └──────────────────────────────────┘                   │
│                                                          │
│  备注                                                    │
│  ┌────────────────────────────────────────────────────┐  │
│  │ 主要 Web 服务器                                    │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
├──────────────────────────────────────────────────────────┤
│  [取消]              [测试连接]         [保存并连接]      │
└──────────────────────────────────────────────────────────┘
```

### 7.3 表单验证状态

| 字段 | 必填 | 空值提示 | 格式错误提示 |
|------|------|---------|------------|
| 主机地址 | ✅ | 「请输入主机地址」 | 「无效的 IP 或域名格式」 |
| 端口 | ✅ | 「请输入端口」 | 「端口范围为 1–65535」 |
| 用户名 | ✅ | 「请输入用户名」 | — |

**「测试连接」按钮状态：**
- 点击后 → Loading（旋转圆圈），文字「连接中…」
- 成功 → `✓ 连接成功` 绿色，2s 后恢复
- 失败 → `✗ 连接失败：{原因}` 红色

---

## 8. 弹窗 D02：首次连接指纹确认

### 8.1 弹窗规格

| 属性 | 值 |
|------|---|
| 类型 | Alert（模态） |
| 宽度 | 420px |
| 触发时机 | 首次连接未知主机 |

### 8.2 线框结构

```
┌──────────────────────────────────────────────┐
│  ⚠  验证服务器身份                            │
├──────────────────────────────────────────────┤
│                                              │
│  您正在连接到：prod-web-01 (10.0.1.12:22)   │
│                                              │
│  服务器指纹（ED25519 / SHA256）：            │
│  ┌─────────────────────────────────────┐    │
│  │ SHA256:xU7mDPD3K29BPLs1Tc…ABCD      │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  ⚠ 请在继续之前向服务器管理员核实此指纹      │
│                                              │
├──────────────────────────────────────────────┤
│  [拒绝]    [仅本次接受]    [接受并记住]       │
└──────────────────────────────────────────────┘
```

| 按钮 | 行为 |
|------|------|
| 拒绝 | 中止连接，关闭弹窗 |
| 仅本次接受 | 继续连接，不写入 Known Hosts |
| 接受并记住 | 继续连接，写入 Known Hosts |

---

## 9. 弹窗 D03：主机密钥变更警告

### 9.1 弹窗规格

| 属性 | 值 |
|------|---|
| 类型 | Alert（强制模态，无法 Esc 关闭） |
| 边框 | `2px solid #ff3b30` |
| 优先级 | P0，必须用户主动确认 |

### 9.2 线框结构

```
┌──────────────────────────────────────────────┐
│  🔴  安全警告：服务器指纹已更改              │
├──────────────────────────────────────────────┤
│                                              │
│  prod-web-01 (10.0.1.12) 的主机密钥          │
│  与之前记录的不匹配！                         │
│                                              │
│  这可能意味着：                               │
│  · 服务器重新安装了操作系统                   │
│  · 有人正在进行中间人攻击（MITM）             │
│                                              │
│  已记录指纹：SHA256:oldFingerprint…ABCD      │
│  当前指纹：  SHA256:newFingerprint…EFGH      │
│                                              │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│  建议：请通过其他渠道向管理员确认密钥变更。    │
│                                              │
├──────────────────────────────────────────────┤
│  [取消连接]              [更新并继续连接]     │
└──────────────────────────────────────────────┘
```

---

## 10. 弹窗 D04：隧道管理器

> 对齐 Figma-Spec-v2 §11 隧道管理器规范。`max-w-[700px]`

### 10.1 线框结构

```
┌────────────────────────────────────────────────────────────────┐
│  隧道管理器                                               [×]  │
│  管理 SSH 端口转发与 SOCKS5 代理隧道                           │
├────────────────────────────────────────────────────────────────┤
│  [+ 新建隧道规则]                    [全部启动]  [全部停止]    │
├───┬──────┬───────────────┬──────────────┬──────────┬──────────┤
│状态│类型  │本地            │远端目标       │绑定会话  │  操作    │
├───┼──────┼───────────────┼──────────────┼──────────┼──────────┤
│🟢 │本地  │127.0.0.1:8080 │db.int:3306   │prod-web  │[⏸][🗑] │
│⚪ │远程  │server:9000    │localhost:3000│dev-app   │[▶][🗑] │
│🟢 │SOCKS │127.0.0.1:1080 │—             │bastion   │[⏸][🗑] │
└───┴──────┴───────────────┴──────────────┴──────────┴──────────┘
│                                                                │
│  选中规则后展示详情面板（可编辑）：                             │
│  类型：    [本地转发 ▼]                                        │
│  本地：    [127.0.0.1]  端口：[8080]                           │
│  远端：    [db.internal]  端口：[3306]                         │
│  绑定会话：[prod-web-01 ▼]                                     │
│  ☑ 会话连接时自动启动                                          │
│  备注：    [访问生产数据库]                                     │
│                                        [取消]  [保存]          │
└────────────────────────────────────────────────────────────────┘
```

---

## 11. 弹窗 D05：快捷命令管理器

> 对齐 Figma-Spec-v2 §12 快捷命令管理器规范。`max-w-[700px]`

### 11.1 线框结构

```
┌────────────────────────────────────────────────────────────┐
│  快捷命令管理器                                       [×]  │
├────────────────────────────────────────────────────────────┤
│  命令集：[默认命令集 ▼]        [新建命令集]  [删除命令集]  │
├─────────────────────────────────┬──────────────────────────┤
│  命令列表                        │  编辑面板               │
│  ─────────────────────────────  │  ─────────────────────  │
│  [📋 查看系统状态]    [▶][✏][🗑]│  名称                   │
│  [📋 重启 Nginx]      [▶][✏][🗑]│  ┌────────────────────┐ │
│  [📋 清理日志]        [▶][✏][🗑]│  │ 查看系统状态       │ │
│  [──────────────────────────]   │  └────────────────────┘ │
│  [+ 新建命令]                   │  命令内容（支持多行）    │
│                                 │  ┌────────────────────┐ │
│                                 │  │ df -h              │ │
│                                 │  │ free -m            │ │
│                                 │  │ uptime             │ │
│                                 │  └────────────────────┘ │
│                                 │  ☑ 末尾追加回车         │
│                                 │                [保存]   │
└─────────────────────────────────┴──────────────────────────┘
```

---

## 12. 弹窗 D06：分组管理器

> 对齐 Figma-Spec-v2 §14 §1 分组管理规范。`sm:max-w-[400px]`

### 12.1 线框结构

```
┌──────────────────────────────────────────┐
│  分组管理                           [×]  │
├──────────────────────────────────────────┤
│                                          │
│  [📁] 生产环境                     [🗑] │
│  [📁] 开发环境                     [🗑] │
│  [📁] 跳板机                       [🗑] │
│                                          │
│  ─────────────────────────────────────  │
│  新建分组                                │
│  ┌──────────────────────────────┐  [+]  │
│  │ 分组名称…                    │       │
│  └──────────────────────────────┘       │
│                                          │
└──────────────────────────────────────────┘
```

### 12.2 分组行样式

```css
/* 分组行 */
flex items-center justify-between py-2 border-b border-[#d2d2d7]/50

/* 文件夹图标 */
p-1.5 rounded-md bg-[#007aff]/10
Folder h-3.5 w-3.5 text-[#007aff]

/* 删除按钮 */
h-7 w-7 ghost hover:bg-red-50 hover:text-red-600
```

删除后：该分组内所有会话归入「无分组」

---

## 13. 弹窗 D07：密码管理器

> 对齐 Figma-Spec-v2 §14 §2 密码管理规范。`sm:max-w-[500px]`

### 13.1 线框结构

```
┌────────────────────────────────────────────────────────────────┐
│  密码管理                                                 [×]  │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  [🔑] prod-web-01                          [🗑]         │   │
│  │       192.168.1.100  ••••••••                          │   │
│  └────────────────────────────────────────────────────────┘   │
│  ┌────────────────────────────────────────────────────────┐   │
│  │  [🔑] dev-app                              [🗑]         │   │
│  │       10.0.0.5       ••••••••                          │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                │
│  ℹ 密码已加密存储于本机 macOS Keychain，不会上传至任何服务器  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### 13.2 密码行样式

```css
bg-white/80 backdrop-blur-sm rounded-xl border border-[#d2d2d7]/50 p-3
/* 图标 */
p-1.5 rounded-md bg-[#ff9500]/10
KeyRound h-3.5 w-3.5 text-[#ff9500]
/* 密码遮蔽 */
font-mono text-xs text-[#86868b]  "••••••••"
```

---

## 14. 弹窗 D08：tmux 管理器

> 对齐 Figma-Spec-v2 §10 tmux 管理器规范。`sm:max-w-[900px] max-h-[85vh]`

### 14.1 整体线框

```
┌──────────────────────────────────────────────────────────────────┐
│  ■ Tmux Session Manager                                          │  标题
│  Manage and monitor tmux sessions across your servers           │  副标题
├──────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐ ┌─────────────────┐ ┌────────────────────┐ │
│  │    Sessions     │ │    Windows      │ │   Quick Actions    │ │  Tab 切换
│  └─────────────────┘ └─────────────────┘ └────────────────────┘ │
├──────────────────────────────────────────────────────────────────┤
│  [🔄 刷新]  3 sessions                    [+ New Session]       │
│                                                                  │
│  ■ prod.example.com                                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  production              [Attached]        [▶][🗑]       │   │  绿色渐变 bg-[#34c759]/5
│  │  3 windows · Created 10:24:15 AM                        │   │  border-[#34c759]/20
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  monitoring                               [■][🗑]        │   │
│  │  4 windows · Created Yesterday                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 14.2 Tab 内容说明

**Tab 1：Sessions（会话列表）**

| 元素 | 规格 |
|------|------|
| 会话卡片（已附加） | `bg-gradient-to-br from-[#34c759]/5 to-[#30d158]/5 border-[#34c759]/20` |
| 会话卡片（未附加） | `bg-white/60 border-[#d2d2d7]/50` |
| 操作按钮 | Attach（▶）/ Kill（🗑），`rounded-lg text-xs` |
| [Attached] 徽章 | `bg-[#34c759]/10 text-[#34c759] rounded-full text-xs px-2` |

**Tab 2：Windows（窗口列表）**

列出选中 Session 下的所有窗口卡片：窗口编号 + 名称 + 活跃/非活跃 + Pane 数量

**Tab 3：Quick Actions（快捷操作）**

```
┌───────────────────────┬───────────────────────┐
│  + New Session        │  → Attach Session     │  2×2 Grid
│    新建 tmux 会话      │    附加到已有会话       │  rounded-xl border
├───────────────────────┼───────────────────────┤
│  ⊞ New Window         │  💀 Kill Server       │
│    在当前会话新建窗口   │    终止 tmux 服务器    │
└───────────────────────┴───────────────────────┘
  Kill Server 操作需弹出二次确认 Alert
```

---

## 15. 弹窗 D09：导入 / 导出

> 对齐 Figma-Spec-v2 §14 §3 导入/导出规范。`sm:max-w-[500px]`

### 15.1 线框结构

```
┌──────────────────────────────────────────────────────────┐
│  导入 / 导出会话配置                                 [×]  │
├──────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────────┐  │
│  │         导出          │  │          导入             │  │  grid-cols-2 Tab
│  └──────────────────────┘  └──────────────────────────┘  │
├──────────────────────────────────────────────────────────┤
│  导出 Tab 内容：                                          │
│  ☑ prod-web-01    192.168.1.100    生产环境               │
│  ☑ prod-db        192.168.1.101    生产环境               │
│  ☐ dev-app        10.0.0.5         开发环境               │
│                                                          │
│  格式：  (●) JSON   ( ) CSV                              │
│                                                          │
│  [↓ 导出选中会话（2）]                                   │
├──────────────────────────────────────────────────────────┤
│  导入 Tab 内容：                                          │
│  ┌────────────────────────────────────────────────┐     │
│  │  ↑  将文件拖拽到此处，或点击选择               │     │
│  │     支持 .json（ShellMate）/ .ssh/config       │     │
│  └────────────────────────────────────────────────┘     │
│  或粘贴 JSON 内容：                                       │
│  ┌────────────────────────────────────────────────┐     │
│  │ [{"name":"...","host":"...",...}]              │     │
│  └────────────────────────────────────────────────┘     │
│                                                          │
│  [↑ 导入]                                                │
└──────────────────────────────────────────────────────────┘
```

---

## 16. 面板 S01：设置面板（6-Tab）

> 对齐 Figma-Spec-v2 §07 设置面板规范 + PRD v2.2 §3.11.5 + §3.18.5
> `sm:max-w-[600px] max-h-[80vh]`，`bg-white/95 backdrop-blur-2xl rounded-2xl border border-[#d2d2d7]/50 shadow-2xl`

### 16.1 Tab 结构

```
┌────────────────────────────────────────────────────────────────┐
│  设置                                                          │
│  Customize your ShellMate experience.                         │
├────────────────────────────────────────────────────────────────┤
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐  │
│  │ 通用 │ │ 外观 │ │ 终端 │ │ 颜色 │ │AI助手│ │  自动化  │  │  6 Tabs
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────────┘  │
│  TabsList: grid-cols-6 bg-black/5 rounded-xl p-1             │
├────────────────────────────────────────────────────────────────┤
│  [各 Tab 内容]                                                 │
└────────────────────────────────────────────────────────────────┘
```

### 16.2 通用（General）Tab

```
│  语言                                                         │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ 简体中文                                          ▼  │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                              │
│  连接时自动重连              [Switch 默认关]                  │
│  关闭标签前二次确认           [Switch 默认开]                  │
│  保存会话日志到磁盘            [Switch 默认关]                  │
│  默认协议：[SSH ▼]                                           │
```

### 16.3 外观（Appearance）Tab

```
│  主题：[跟随系统 ▼]（Light / Dark / System）                  │
│  字体大小：[14px ▼]（12/13/14/16/18）                        │
│  显示侧边栏                   [Switch 默认开]                  │
```

### 16.4 终端（Terminal）Tab

```
│  配色方案：[VS Dark ▼]（7 种主题）                            │
│  终端字体：[Menlo ▼]                                         │
│  滚动回溯：[───────● 5000] 行                                │
│  字符编码：[UTF-8 ▼]                                         │
│  Bell 提示：☐ 音效  ☐ 闪烁                                   │
│  光标形状：(●) 块状  ( ) I 形  ( ) 下划线                    │
│  ☑ 光标闪烁                                                  │
```

### 16.5 颜色（Colors）Tab

```
│  主题选择（8 款 + 社区）                                      │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │
│  │ vs-dk│ │Mono  │ │Dracu │ │Sol.D │ │ Nord │ │+自定义│    │
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │
│  （64×48px 终端预览缩略图，选中态蓝色边框）                   │
│                                                              │
│  [导入 .itermcolors]  [导出主题]                             │
│                                                              │
│  关键词高亮规则集                                             │
│  ● 系统错误（全局）   ERROR FATAL CRITICAL   [编辑][删除]    │
│  ● 自定义规则集       nginx upstream         [编辑][删除]    │
│  [+ 新建规则集]                                              │
```

### 16.6 AI 助手（AI）Tab

```
│  模型服务商                                                   │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Claude（Anthropic）                              ▼  │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                              │
│  API Key（存入本机 Keychain）                                │
│  ┌──────────────────────────────┐  [保存]                   │
│  │ sk-ant-•••••••••••••••••••  │                           │
│  └──────────────────────────────┘                           │
│                                                              │
│  模型 ID                                                     │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ claude-opus-4-6                                      │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                              │
│  Base URL（可选，私有部署）                                  │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ https://api.anthropic.com                            │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                              │
│  请求超时：[30] 秒                                           │
│                                                              │
│  命令安全审计                                                 │
│  开启命令安全审计拦截              [Switch 默认开]            │
```

### 16.7 自动化（Automation）Tab

```
│  触发器列表                                        [+ 新建]  │
│  ─────────────────────────────────────────────────────────   │
│  [●] 错误通知    输出匹配 \[ERROR\]  →  发送系统通知  [✏][🗑]│
│  [●] 连接初始化  连接成功           →  发送命令 source...  [✏][🗑]│
│  [○] OOM 监控   输出匹配 oom_kill   →  写入日志（已禁用）[✏][🗑]│
│                                                              │
│  选中触发器 → 底部展示编辑 Sheet 弹窗（max-w-[560px]）       │
```

---

## 17. 面板 S02：日志面板

> 对齐 Figma-Spec-v2 §14 §4 日志面板规范。`sm:max-w-[900px] max-h-[80vh]`

### 17.1 线框结构

```
┌──────────────────────────────────────────────────────────────────────┐
│  日志面板                                                       [×]  │
├──────────────────────────────────────────────────────────────────────┤
│  [全部] [输入] [输出] [错误] [系统]      [🔍 关键词…]    [清空][导出]│  过滤行
├──────────────────────────────────────────────────────────────────────┤
│  09:12:45  [SYSTEM]   SSH 连接已建立 — prod-web-01                   │  橙色
│  09:12:46  [INPUT]    ls -la                                         │  蓝色
│  09:12:46  [OUTPUT]   total 48 drwxr-xr-x 5 ubuntu ubuntu           │  绿色
│  09:15:22  [ERROR]    connection refused to db:5432                  │  红色
│  09:15:23  [INPUT]    systemctl status nginx                         │  蓝色
│  ...                                                                 │
│                               （自动滚动到底部，点击「暂停」固定）  │
├──────────────────────────────────────────────────────────────────────┤
│  共 128 条  |  [▶ 自动滚动]                                         │  底部状态栏
└──────────────────────────────────────────────────────────────────────┘
```

### 17.2 日志条目规格

| 类型 | 颜色 Token | 前缀标签 | 触发条件 |
|------|-----------|---------|---------|
| `.system` | `#ff9500` | `[SYSTEM]` | 连接/断开事件 |
| `.input` | `#007aff` | `[INPUT]` | Compose Pane / 快捷命令发送 |
| `.output` | `#34c759` | `[OUTPUT]` | SSH 服务端输出 |
| `.error` | `#ff3b30` | `[ERROR]` | AI 侦探检测到错误片段 |

内存策略：保留最近 5000 条（FIFO 淘汰），应用退出后清空

---

## 18. 覆层 O01：终端内搜索

### 18.1 规格

| 属性 | 值 |
|------|---|
| 位置 | 终端区域右上角，浮层 |
| 宽度 | 320px |
| 高度 | 40px |
| 触发 | `⌘F`（终端聚焦时），`Esc` 关闭 |
| 动画 | 从右上角淡入下滑，150ms |

### 18.2 线框

```
                              ┌──────────────────────────────────────────────┐
                              │ [🔍] [nginx error___] [Aa] [.*] [↑] [↓] [×] │
                              │                          0 / 0 匹配           │
                              └──────────────────────────────────────────────┘
```

| 元素 | 功能 |
|------|------|
| 输入框 | 实时搜索，高亮匹配项（`bg-[#ff9500]/40`） |
| `Aa` | 大小写敏感开关，激活时 `text-[#007aff]` |
| `.*` | 正则模式开关 |
| `↑` / `↓` | 跳转上/下一个匹配 |
| `×` | 关闭，清除高亮 |
| 匹配计数 | `当前/总数`，无匹配时红色 `0 / 0` |

---

## 19. 覆层 O02：Compose Pane（命令编辑区）

### 19.1 规格

| 属性 | 值 |
|------|---|
| 位置 | 终端区域底部，可停靠 |
| 高度 | 80–200px（可拖拽调整） |
| 触发 | View → Compose Pane / 工具栏按钮 |

### 19.2 线框

```
┌──────────────────────────────────────────────────────────┐  ← 4px 拖拽调整线
│  1  sudo systemctl restart nginx                         │
│  2  echo "Restart complete at $(date)"                   │
│  3  systemctl status nginx | head -5                     │
│                                                          │
│  [发送 ⌃↵]  [逐行模式]  延迟 [50]ms  [🔠 bash ▼]        │
└──────────────────────────────────────────────────────────┘
```

| 元素 | 功能 |
|------|------|
| 编辑区 | 多行文本，等宽字体，行号，语法高亮 |
| 发送按钮 | `⌃Return` 快捷键，整体一次发送 |
| 逐行模式 | 每行间隔 `延迟` ms 分别发送 |
| 语言下拉 | `bash` / `python` / `sh`，影响语法高亮 |

---

## 20. 覆层 O03：同步输入模式

### 20.1 触发与关闭

- 开启：工具栏「同步」按钮 → 勾选目标会话 → 确认
- 关闭：再次点击「同步」按钮 / 关闭所有参与标签

### 20.2 开启确认弹窗

```
┌──────────────────────────────────────────────────┐
│  启用同步输入                                     │
├──────────────────────────────────────────────────┤
│  以下会话将同步接收您的输入：                     │
│                                                  │
│  ☑  🟢 prod-web-01                              │
│  ☑  🟢 prod-web-02                              │
│  ☐  ⚪ dev-app（未连接，不可用）                  │
│                                                  │
│  ⚠  此操作将在多台服务器上同时执行命令，请谨慎。 │
│                                                  │
│              [取消]   [确认开启同步]              │
└──────────────────────────────────────────────────┘
```

### 20.3 同步模式视觉提示

- 所有参与窗格/标签边框：**2px 橙色连续边框** `border-[#ff9500]`
- 标签名前追加 `⚡ bolt.fill orange 9px`
- 工具栏「同步」按钮保持激活态（`bg-[#ff9500]/10 text-[#ff9500]`）
- 状态栏左侧追加 `⚡ 同步输入 (2)` 提示

---

## 21. 覆层 O04：AI 命令安全审计

> 对齐 PRD v2.2 §3.13.6 命令安全审计规范

### 21.1 触发时机

用户通过 Compose Pane 或 AI 面板「插入终端」发送命令时，`CommandSafetyChecker` 本地静态分析（正则匹配，无网络请求）：

| 风险等级 | 颜色 | 典型规则 |
|---------|------|---------|
| `.warning`（警告） | `#ff9500` | `chmod 777 -R`、`git push --force`、`kill -9` |
| `.danger`（危险） | `#ff3b30` | `rm -rf /`、`DROP DATABASE`、Fork Bomb |

### 21.2 警告级弹窗

```
┌───────────────────────────────────────────────────────────┐
│  ⚠  命令安全提示                                          │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  以下命令可能存在风险：                                    │
│  ┌───────────────────────────────────────────────────┐   │
│  │  git push --force                                 │   │
│  └───────────────────────────────────────────────────┘   │
│                                                           │
│  风险说明：强制推送可能覆盖远端仓库历史，操作不可逆       │
│                                                           │
│  [取消]                            [确认执行（警告操作）]  │
└───────────────────────────────────────────────────────────┘
```

### 21.3 危险级弹窗（两步确认）

**第一步：**
```
┌───────────────────────────────────────────────────────────┐
│  🔴  危险操作警告                                         │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  以下命令属于高风险操作：                                  │
│  ┌───────────────────────────────────────────────────┐   │
│  │  rm -rf /tmp/test                                 │   │
│  └───────────────────────────────────────────────────┘   │
│                                                           │
│  风险说明：递归删除目录，操作不可逆                       │
│                                                           │
│  [✨ AI 分析风险]   [取消]   [继续执行（风险操作）→]      │
└───────────────────────────────────────────────────────────┘
```

**第二步（点击「继续执行」后）：**
```
│  [取消]           [确认，我知道风险]（红色主按钮）          │
```

---

## 22. 覆层 O05：FTUE 欢迎引导

> 对齐 Figma-Spec-v2 §13 欢迎界面规范。`fixed inset-0 z-50`

### 22.1 整体规格

```css
fixed inset-0
bg-gradient-to-br from-[#f5f5f7] via-white to-[#e8e8ed]
flex items-center justify-center
z-50 p-4
```

关闭按钮：`absolute top-4 right-4 rounded-full ghost hover:bg-black/5`

### 22.2 三步 Onboarding 布局

**Step 0：欢迎（Welcome）**

```
┌─────────────────────────────────────────────────────────────────┐
│                                                         [×]     │
│                                                                 │
│                    ┌──────────────┐                             │
│                    │      👋      │  128×128px                  │
│                    │  rounded-[2.5rem]                          │
│                    │  from-blue-500/10 to-purple-500/10         │
│                    └──────────────┘                             │
│                                                                 │
│                 欢迎使用 ShellMate                               │  text-5xl font-bold #1d1d1f
│        专业开发者的 macOS 原生 SSH 工具                          │  text-xl #86868b
│                                                                 │
│                  ●  ○  ○   （步骤指示器）                       │  当前步：w-8 h-2 rounded-full #007aff
│                                                                 │
│               三个差异化亮点卡片（横排 3 列）：                  │
│    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│    │  🔒 安全本地 │ │  🤖 AI 诊断  │ │  ⚡ 极速管理  │          │
│    │  凭据存 Keychain │错误自动分析  │  tmux/Hotkey  │          │
│    └──────────────┘ └──────────────┘ └──────────────┘          │
│                                                                 │
│                        [继续  →]                               │  bg-[#007aff] text-white
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**Step 1：核心功能展示（Features）**

```
│                    ┌──────────────┐                             │
│                    │      🚀      │  from-green-500/10 to-blue-500/10  │
│                    └──────────────┘                             │
│               探索 ShellMate 的核心能力                         │
│               ○  ●  ○                                          │
│                                                                 │
│    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│    │  🤖 AI 错误  │ │  ⌥Space HK  │ │  tmux 集成   │          │
│    │  侦探自动分析  │ │  任意App唤起 │ │  图形化管理  │          │
│    └──────────────┘ └──────────────┘ └──────────────┘          │
│                                                                 │
│           [← 上一步]                    [继续  →]              │
```

**Step 2：快捷开始（Get Started）**

```
│                    ┌──────────────┐                             │
│                    │      ⚡      │  from-orange-500/10 to-red-500/10 │
│                    └──────────────┘                             │
│               You're Ready to Get Started!                      │
│                    ○  ○  ●                                      │
│                                                                 │
│    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐          │
│    │  ➕           │ │  📥          │ │  🖥️           │          │
│    │ Create First │ │   Import     │ │   Explore    │          │
│    │ Connection   │ │Configuration │ │   the App    │          │
│    └──────────────┘ └──────────────┘ └──────────────┘          │
│                                               [跳过引导 →]      │
│           [← 上一步]                                            │
```

三个行动卡片行为：
- **新建会话**：关闭引导 → 弹出新建会话弹窗 D01
- **导入配置**：关闭引导 → 弹出导入/导出弹窗 D09
- **快速探索**：关闭引导 → 进入应用主界面

### 22.3 步骤指示器规格

```css
/* 激活点 */
w-8 h-2 rounded-full bg-[#007aff] transition-all duration-300

/* 未激活点 */
w-2 h-2 rounded-full bg-[#d2d2d7] hover:bg-[#86868b] transition-all duration-300
```

点击可跳转到对应步骤。

---

## 23. 覆层 O06：Hotkey Window

> 对齐 PRD v2.2 §3.15 Hotkey Window 规范。`NSPanel`，浮动，全局快捷键 `⌥Space`

### 23.1 窗口规格

| 属性 | 默认值 |
|------|--------|
| 宽度 | 屏幕宽度 80% |
| 高度 | 屏幕高度 40% |
| 位置 | 屏幕顶部居中（距顶 10% 屏高） |
| 窗口层级 | `NSWindowLevel.floating + 1`（覆盖所有 App） |
| 动画 | 200ms 弹性滑动（从顶部滑出/收回） |
| 集合行为 | `canJoinAllSpaces`（在所有 Space 显示） |

### 23.2 窗口线框

```
┌──────────────────────────────────────────────────────────────────┐  屏幕宽 80%
│  ■ Hotkey Terminal  ⌥Space  [░░ 失焦隐藏]              [×]     │  标题条（40px）
│  terminal.fill 图标  快捷键徽章  自动隐藏切换按钮    关闭按钮    │  bg-[#0d0d0f/0.85]
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   （终端内容区 / SSH 会话显示 / 空状态引导）                       │  flex-1，黑色背景
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 23.3 标题条规格

| 元素 | 规格 |
|------|------|
| 图标 | `terminal.fill` 11px，`white/55` |
| 标题 | "Hotkey Terminal"，12px medium，`white/75` |
| 快捷键徽章 | "⌥Space"，10px mono，`white/35`，`rounded-4px bg-white/10 px-1.5 py-0.5` |
| 自动隐藏按钮 | 开：`pin.slash + 失焦隐藏`；关：`pin.fill + 保持显示`，`rounded-4px` |
| 关闭按钮 | `xmark` 10px，`white/55`，`20×20px bg-white/10 rounded-full` |
| 背景 | `rgb(20,20,26)/0.85 + 顶部渐变 white/5` |
| 下边框 | `0.5px white/8` |

### 23.4 行为规范

| 场景 | 行为 |
|------|------|
| 按 `⌥Space`（任意 App） | 面板从顶部滑出，获取焦点 |
| 再按 `⌥Space` | 面板收回顶部 |
| 按 `Esc` | 同收回 |
| 点击面板外区域 | 若「失焦隐藏」开，自动收回 |

---

## 24. 状态集合

### 24.1 SSH 会话连接状态

| 状态 | 状态点（侧边栏/标签） | 标签页背景 | 侧边栏文字 | 可执行操作 |
|------|---------------------|-----------|-----------|-----------|
| 未连接 | `bg-[#c7c7cc]` 6px | 透明 | `#86868b` | 双击连接、编辑、删除 |
| 连接中 | `bg-[#ff9500] animate-pulse` 6px | 透明 | `#1d1d1f` | 取消连接 |
| 已连接 | `bg-[#34c759]` 6px | `bg-white/90`（活跃标签） | `#1d1d1f` | 断开、分屏、SFTP 等 |
| 重连中 | `bg-[#ff9500] animate-spin` 6px | 透明 | `#1d1d1f` | 取消重连 |
| 连接失败 | `bg-[#ff3b30]` 6px | 透明 | `#ff3b30` | 重试、查看错误 |
| 已断开 | `bg-[#c7c7cc]` 6px | 透明 | `#86868b` | 重连、编辑 |

### 24.2 SFTP 传输状态

| 状态 | 进度条视觉 | 操作 |
|------|-----------|------|
| 等待中 | 灰色空心 | 取消 |
| 传输中 | `bg-[#007aff] animate` | 暂停、取消 |
| 已暂停 | `bg-[#007aff]/50` 静止 | 恢复、取消 |
| 已完成 | `bg-[#34c759]` 100%，`✓` | 关闭记录 |
| 失败 | `bg-[#ff3b30]`，`✗` | 重试、关闭 |

### 24.3 iCloud 同步状态

| 状态 | 状态栏图标 | 颜色 | 悬停说明 |
|------|-----------|------|---------|
| 已同步 | `✓` | `#34c759` | 「上次同步：刚刚」 |
| 同步中 | 旋转圆圈 🔄 | `#007aff` | 「正在同步…」 |
| 等待同步 | ⏳ | `#86868b` | 「队列中，等待网络」 |
| 同步失败 | ⚠ | `#ff3b30` | 点击查看错误详情 |

---

## 25. 组件库摘要

### 25.1 基础按钮变体

| 变体 | CSS 类 | 用途 |
|------|--------|------|
| Primary | `bg-[#007aff] text-white rounded-xl hover:bg-[#0051d5]` | 主要操作（保存、连接、新建） |
| Secondary | `border border-[#d2d2d7] bg-white/80 text-[#1d1d1f] rounded-xl` | 次要操作（取消、返回） |
| Danger | `bg-red-50 text-red-600 hover:bg-red-100 rounded-xl` | 破坏性操作（删除） |
| Ghost | `bg-transparent hover:bg-black/5 rounded-lg` | 工具栏图标按钮 |
| Outline | `border border-[#d2d2d7] rounded-lg hover:bg-black/5` | 次级操作 |

### 25.2 输入框变体

| 变体 | CSS 类 |
|------|--------|
| 默认 | `border border-[#d2d2d7]/50 bg-white/80 rounded-xl focus:border-[#007aff] focus:ring-1 focus:ring-[#007aff]/30` |
| 错误 | `border-[#ff3b30] bg-[#ff3b30]/5 focus:ring-[#ff3b30]/30` + 右侧红色叹号 |
| 禁用 | `bg-[#e8e8ed] text-[#c7c7cc] cursor-not-allowed` |

### 25.3 状态点（GlowingStatusDot）规格

```
直径：5–6px（标签栏 5px，侧边栏 6px）
已连接：bg-[#34c759]，box-shadow 0 0 4px rgba(52,199,89,0.6)
连接中：bg-[#ff9500] animate-pulse
未连接：bg-[#c7c7cc]，无光晕
失败：  bg-[#ff3b30]
```

### 25.4 Tooltip 规格

```
背景：bg-white/95 backdrop-blur-xl
边框：1px border-[#d2d2d7]/50
圆角：radius-lg（10px）
内边距：6px 12px
字号：text-caption（11px），text-[#1d1d1f]
阴影：shadow-lg
出现延迟：悬停 500ms 后显示
最大宽度：280px
```

### 25.5 弹窗容器统一规格（Figma-Spec-v2 通用）

```css
bg-white/95 backdrop-blur-2xl
border border-[#d2d2d7]/50
shadow-2xl rounded-2xl
overflow-hidden
```

### 25.6 Switch 组件

```css
/* 激活态 */
bg-[#34c759]  /* success green */

/* 非激活态 */
bg-[#e8e8ed]
```

### 25.7 弹窗标准尺寸速查表

| 弹窗/面板 | 最大宽度 |
|----------|---------|
| 新建会话 | `sm:max-w-[500px]` |
| 设置面板 | `sm:max-w-[600px]` |
| 分组管理 | `sm:max-w-[400px]` |
| 密码管理 | `sm:max-w-[500px]` |
| 导入/导出 | `sm:max-w-[500px]` |
| 快捷命令 | `sm:max-w-[700px]` |
| 隧道管理 | `sm:max-w-[700px]` |
| 日志面板 | `sm:max-w-[900px]` |
| tmux 管理器 | `sm:max-w-[900px]` |
| AI 助手面板 | W: 400px（固定侧拉） |
| 文件传输面板 | W: 500px（固定侧拉） |

---

## 附录 A：界面跳转关系图

```
覆层 O05（FTUE 欢迎引导）← 首次启动自动触发
    │
    ├── 新建会话 ──────────────────────→ 弹窗 D01（新建会话）
    ├── 导入配置 ──────────────────────→ 弹窗 D09（导入/导出）
    └── 快速探索 ──────────────────────→ 进入主窗口

主窗口（侧边栏 + 标签栏 + 终端 + SFTP 面板 + AI 助手面板）
    │
    ├── 双击会话 ─────────────────────→ 新标签建立连接
    │       └── 首次连接 ──────────────→ 弹窗 D02（指纹确认）
    │           └── 密钥变更 ──────────→ 弹窗 D03（密钥警告）
    │
    ├── 侧边栏工具行 [+新建] ──────────→ 弹窗 D01（新建会话）
    ├── 侧边栏工具行 [📁分组] ─────────→ 弹窗 D06（分组管理）
    ├── 侧边栏工具行 [🔑密码] ─────────→ 弹窗 D07（密码管理）
    ├── 侧边栏工具行 [⚙设置] ─────────→ 面板 S01（设置面板）
    │
    ├── 工具栏 [AI 助手] ──────────────→ 界面 P05（AI 助手面板，左侧滑入）
    ├── 工具栏 [tmux] ──────────────────→ 弹窗 D08（tmux 管理器）
    ├── 工具栏 [日志] ──────────────────→ 面板 S02（日志面板）
    ├── 工具栏 [隧道 ⌘⇧U] ─────────────→ 弹窗 D04（隧道管理器）
    ├── 工具栏 [命令 ⌘⇧K] ─────────────→ 弹窗 D05（快捷命令管理器）
    ├── 工具栏 [SFTP ⌘⇧S] ─────────────→ 界面 P03（SFTP 面板，右侧滑入）
    ├── 工具栏 [导入/导出] ─────────────→ 弹窗 D09（导入/导出）
    │
    ├── [⌘F]（终端聚焦时） ─────────────→ 覆层 O01（终端内搜索）
    ├── View → Compose Pane ─────────────→ 覆层 O02（命令编辑区）
    ├── 工具栏「同步」────────────────────→ 覆层 O03（同步输入确认）
    ├── Compose Pane 发送危险命令 ──────────→ 覆层 O04（AI 命令安全审计）
    │
    └── [⌥Space]（全局）──────────────────→ 覆层 O06（Hotkey Window，任意 App 内）
```

---

## 附录 B：文档修订历史

| 版本 | 日期 | 修订人 | 说明 |
|------|------|--------|------|
| v1.0-draft | 2026-03-18 | 产品团队 | 初版原型草稿，基于 PRD v1.0 |
| v2.0 | 2026-04-14 | 产品团队 | 全面对齐 Figma-Spec-v2（fileKey: `upl5OBUkpLGnOe1u5aQRZ5`）与 PRD v2.2：更新设计令牌系统（Apple Blue `#007aff`，语义色完整映射）；更新整体布局尺寸（标题栏 40px / 工具栏 48px / 侧边栏 256px 固定 / 标签栏 40px / 状态栏 32px）；移除本地 Shell 标签页相关所有内容，新增空状态引导界面；状态栏新增系统实时监控（CPU/内存/磁盘/网络，颜色阈值）；新增界面 P05 AI 助手面板（400px 左侧面板）；新增弹窗 D06 分组管理、D07 密码管理、D08 tmux 管理器、D09 导入/导出；设置面板更新为 6-Tab（通用/外观/终端/颜色/AI助手/自动化）；新增面板 S02 日志面板；新增覆层 O04 AI 命令安全审计、O05 FTUE 欢迎引导、O06 Hotkey Window；组件库更新至 v2 令牌体系 |

---

*本文档为产品研发内部文件。ASCII 线框图为结构参考，不代表最终视觉风格。*
*所有尺寸单位为逻辑像素（@1x），视网膜屏按 @2x 输出资源。*
*终稿以 Figma-Spec-v2 视觉稿为准，本文档与 Figma 存在冲突时以 Figma 为准。*
