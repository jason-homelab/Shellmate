# ShellMate for macOS — 原型设计文档

> **文档版本：** v1.0-draft
> **创建日期：** 2026-03-18
> **文档类型：** 草稿原型（Wireframe Prototype Spec）
> **基于文档：** ShellMate PRD v1.0
> **文档状态：** 草稿，待视觉设计师确认

---

## 阅读说明

本文档是 PRD 的原型转化稿，面向视觉设计师和前端工程师。每个界面模块包含：

- **布局结构**：组件的位置关系与尺寸约束
- **元素清单**：界面内所有 UI 元素及其状态
- **交互说明**：用户操作→界面响应的映射关系
- **边界情况**：空状态、加载状态、错误状态的视觉处理

文档中的 ASCII 线框图为结构参考，不代表最终视觉风格。

---

## 目录

1. [应用整体框架](#1-应用整体框架)
2. [界面 P01：主窗口 — 侧边栏（会话列表）](#2-界面-p01主窗口--侧边栏会话列表)
3. [界面 P02：主窗口 — 终端区域](#3-界面-p02主窗口--终端区域)
4. [界面 P03：主窗口 — SFTP 面板](#4-界面-p03主窗口--sftp-面板)
5. [界面 P04：主窗口 — 状态栏](#5-界面-p04主窗口--状态栏)
6. [弹窗 D01：新建 / 编辑会话](#6-弹窗-d01新建--编辑会话)
7. [弹窗 D02：首次连接指纹确认](#7-弹窗-d02首次连接指纹确认)
8. [弹窗 D03：主机密钥变更警告](#8-弹窗-d03主机密钥变更警告)
9. [弹窗 D04：端口转发管理器](#9-弹窗-d04端口转发管理器)
10. [弹窗 D05：快捷命令管理器](#10-弹窗-d05快捷命令管理器)
11. [面板 S01：关键词高亮设置](#11-面板-s01关键词高亮设置)
12. [面板 S02：外观与个性化设置](#12-面板-s02外观与个性化设置)
13. [面板 S03：安全与凭据管理](#13-面板-s03安全与凭据管理)
14. [覆层 O01：终端内搜索](#14-覆层-o01终端内搜索)
15. [覆层 O02：Compose Pane（命令编辑区）](#15-覆层-o02compose-pane命令编辑区)
16. [覆层 O03：同步输入模式](#16-覆层-o03同步输入模式)
17. [状态集合](#17-状态集合)
18. [组件库摘要](#18-组件库摘要)

---

## 1. 应用整体框架

### 1.1 窗口结构层次

```
┌──────────────────────────────────────────────────────────────────┐
│  NSWindow（最小尺寸 620×480，无最大限制）                         │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  TitleBar（38px 高，macOS 原生）                            │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │  TabBar（36px 高）                                          │  │
│  ├──────────────┬──────────────────────────┬──────────────────┤  │
│  │              │  ToolBar（28px，可隐藏）   │                  │  │
│  │  Sidebar     ├──────────────────────────┤  SFTPPanel       │  │
│  │  （220px，   │  PaneBar（28px/窗格）     │  （300px，       │  │
│  │   可调整     ├──────────────────────────┤   可调整，        │  │
│  │   180–320px）│                          │   可隐藏）        │  │
│  │              │  TerminalView            │                  │  │
│  │              │  （flex-fill）           │                  │  │
│  │              ├──────────────────────────┤                  │  │
│  │              │  QuickCommandBar（可收起）│                  │  │
│  ├──────────────┴──────────────────────────┴──────────────────┤  │
│  │  StatusBar（24px 高）                                       │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### 1.2 布局规则

| 区域 | 尺寸约束 | 可见性控制 |
|------|----------|-----------|
| TitleBar | 高度 38px，固定 | 始终可见 |
| TabBar | 高度 36px，固定 | 始终可见 |
| Sidebar | 宽度 180–320px，默认 220px | `⌘⌥S` 收起/展开 |
| ToolBar | 高度 28px | 可在 View 菜单隐藏 |
| PaneBar（每窗格顶部） | 高度 28px | 始终可见 |
| TerminalView | flex-fill（占满剩余空间） | 始终可见 |
| SFTPPanel | 宽度 200–500px，默认 300px | `⌘⇧S` 打开/关闭 |
| QuickCommandBar | 高度 48px（1 行）或 96px（2 行） | 可收起 |
| StatusBar | 高度 24px，固定 | 始终可见 |

### 1.3 主题色板（设计令牌）

| 令牌名 | 深色模式值 | 浅色模式值 | 用途 |
|--------|-----------|-----------|------|
| `bg-primary` | `#0d0d0f` | `#ffffff` | 终端背景 |
| `bg-secondary` | `#111113` | `#f5f5f5` | 侧边栏背景 |
| `bg-tertiary` | `#1c1c1f` | `#ebebeb` | 标签栏、工具栏背景 |
| `bg-elevated` | `#222226` | `#ffffff` | 弹窗背景 |
| `accent` | `#3d8ef0` | `#1d6dd4` | 强调色，活跃态 |
| `status-green` | `#2ec27e` | `#1e9a5e` | 已连接状态 |
| `status-amber` | `#e5a50a` | `#c2860a` | 连接中/警告 |
| `status-red` | `#e01b24` | `#c00c14` | 错误/断连 |
| `text-primary` | `#f0eff4` | `#1a1a1a` | 主要文本 |
| `text-secondary` | `#a8a7b0` | `#5c5c5c` | 辅助文本 |
| `text-muted` | `#6b6a74` | `#8a8a8a` | 占位符/禁用 |
| `border` | `rgba(255,255,255,0.08)` | `rgba(0,0,0,0.12)` | 分割线 |

---

## 2. 界面 P01：主窗口 — 侧边栏（会话列表）

### 2.1 线框结构

```
┌─────────────────────────┐
│  搜索框区域（40px）      │  ← [搜索图标] [输入框 placeholder="搜索会话…"] [清除×]
├─────────────────────────┤
│  工具栏（28px）          │  ← [折叠全部] [展开全部] [flex-spacer] [导入] [导出]
├─────────────────────────┤
│                         │
│  ─── 生产环境  ──── [+] │  ← 分组行：[展开箭头▶] [分组名] [会话数徽章] [新建按钮+]
│    ● prod-web-01   PROD │  ← 会话行（已连接）
│    ● prod-web-02   PROD │
│    ○ prod-cache    PROD │  ← 会话行（未连接）
│                         │
│  ─── 开发环境  ──── [+] │
│    ◐ dev-app        DEV │  ← 会话行（连接中，黄色脉冲动画）
│    ○ dev-db         DEV │
│                         │
│  ─── 跳板机    ──── [+] │
│    ● bastion-us    JUMP │
│                         │
│  （空白区域，可拖入会话）│
│                         │
├─────────────────────────┤
│  [＋ 新建会话]           │  ← 底部固定按钮
└─────────────────────────┘
```

### 2.2 元素规格

#### 搜索框

| 属性 | 规格 |
|------|------|
| 高度 | 30px |
| 外边距 | 上下 6px，左右 8px |
| 背景 | `bg-tertiary` |
| 边框 | 1px `border`，聚焦时 `accent` |
| 圆角 | 6px |
| 图标 | 放大镜，12×12px，`text-muted` |
| 清除按钮 | 仅有内容时显示，点击清空 |

#### 分组行

| 属性 | 规格 |
|------|------|
| 高度 | 28px |
| 左内边距 | 12px |
| 展开箭头 | `▶`（折叠）/ `▼`（展开），8px，过渡动画 150ms |
| 分组名 | 11px，全大写，`text-muted`，字间距 0.06em |
| 会话数徽章 | 圆角矩形，10px 字体，`bg-tertiary`，`text-muted` |
| 新建按钮 | `+`，14px，悬停时显示（默认隐藏），点击新建会话到该分组 |
| 右键菜单 | 新建会话、重命名、删除分组、展开/折叠子项 |

#### 会话行

| 状态 | 状态点样式 | 背景 | 右侧标签 |
|------|-----------|------|---------|
| 未连接 | 6px 灰色实心圆 | 透明 | 彩色分组标签（可选） |
| 连接中 | 6px 黄色脉冲动画 | 透明 | 同上 |
| 已连接 | 6px 绿色实心圆 + 2px 外发光 | 透明 | 同上 |
| 选中（活跃标签对应） | 同上 + 右侧 2px `accent` 竖条 | `accent`×8% 透明 | 同上 |
| 悬停 | 同上 | `bg-tertiary` | 同上 |

会话行高度：**44px**（含上下 padding 各 6px，内容区 32px）

会话行元素（从左到右）：
1. 状态点（6px 圆，固定宽 16px）
2. 会话名（12px，`text-primary`，最大宽度 flex，超出 ellipsis）
3. 主机地址（10px，`text-muted`，仅在会话行展开时显示，另起一行）
4. 分组标签徽章（可选，9px，圆角 3px，各分组对应颜色）

右键菜单项：连接 / 在新窗口打开 / 编辑 / 复制 / 重命名 / 删除

#### 底部新建按钮

| 属性 | 规格 |
|------|------|
| 高度 | 34px |
| 外边距 | 8px |
| 样式 | 虚线边框 `border`，`accent` 文字，圆角 6px |
| 悬停 | 背景变 `accent`×15%，边框变 `accent` |
| 图标 | `+` 12px，与文字间距 6px |

### 2.3 交互说明

| 操作 | 触发 | 响应 |
|------|------|------|
| 双击会话行 | — | 在新标签页建立 SSH 连接，标签页激活 |
| 单击会话行 | — | 高亮选中（不连接），如已有对应标签则激活该标签 |
| 拖拽会话行 | 长按 200ms 后可拖拽 | 拖入另一分组时高亮目标分组；拖入空白区自动新建分组 |
| 点击分组箭头 | — | 折叠/展开，箭头旋转动画 150ms，持久化状态 |
| `⌘F` | 全局 | 聚焦搜索框，选中全部内容 |
| 搜索输入 | 实时 | 过滤会话列表，匹配字符加粗高亮，无匹配显示空状态 |

### 2.4 边界情况

**空状态（无会话）：**
```
┌─────────────────────────┐
│  [搜索框]                │
├─────────────────────────┤
│                         │
│      [SSH 图标 32px]     │
│   还没有保存的会话        │
│   点击下方按钮创建第一个  │
│                         │
│  [＋ 新建会话]           │
└─────────────────────────┘
```

**搜索无结果：**
```
│                         │
│   未找到 "xxx" 相关会话  │
│   检查拼写或尝试 IP 搜索  │
│                         │
```

---

## 3. 界面 P02：主窗口 — 终端区域

### 3.1 标签栏线框

```
┌──────────────────────────────────────────────────────────────┐
│  [●prod-web-01 ×]  [●prod-db ×]  [◐dev-app ×]  [+新建]      │
└──────────────────────────────────────────────────────────────┘
```

#### 标签元素规格

| 元素 | 规格 |
|------|------|
| 标签高度 | 28px（在 36px 标签栏内底部对齐） |
| 内边距 | 左右各 10px |
| 状态点 | 5px，颜色同侧边栏状态点 |
| 标签名 | 12px，`text-secondary`（非活跃）/ `text-primary`（活跃） |
| 关闭按钮 | `×`，10px，默认隐藏，悬停标签时出现；点击关闭 |
| 活跃标签 | 底部 2px `accent` 下划线，背景 `bg-primary` |
| 非活跃标签 | 背景 `bg-tertiary`，悬停变 `bg-secondary` |
| 颜色标记（可选） | 标签名左侧 3px 宽竖条，对应会话分组颜色 |
| 最大宽度 | 160px，超出 ellipsis |
| 最小宽度 | 80px |

新建标签按钮：`+`，28×28px，圆角，悬停变 `bg-tertiary`

**Tooltip**（悬停 2 秒后）：
```
┌─────────────────────────────┐
│  ubuntu@10.0.1.12           │
│  端口：22  连接时长：2h 14m │
└─────────────────────────────┘
```

### 3.2 窗格工具栏（PaneBar）线框

```
┌────────────────────────────────────────────────────────────┐
│  ubuntu@prod-web-01:~/services  [flex-spacer]  [⊟][⊞][🔍][⊗]│
└────────────────────────────────────────────────────────────┘
```

| 元素 | 规格 |
|------|------|
| 路径文本 | 11px 等宽字体，`text-secondary`；用户名 `accent` 色，路径 `#e5a50a` |
| 图标按钮 | 20×20px，圆角 3px，悬停 `bg-tertiary` |
| `⊟` 上下分屏 | 点击在下方新增窗格 |
| `⊞` 左右分屏 | 点击在右侧新增窗格 |
| `🔍` 搜索 | 点击唤出终端内搜索覆层 |
| `⊗` 关闭窗格 | 仅多窗格时显示 |

### 3.3 终端内容区线框

```
┌────────────────────────────────────────────────────────────┐
│  ubuntu@prod-web-01:~$ sudo systemctl status nginx          │
│  ● nginx.service - A high performance web server            │
│     Loaded: loaded (/lib/systemd/system/nginx.service)      │
│     Active: active (running) since Mon 2026-03-14           │  ← 绿色高亮
│                                                             │
│  ubuntu@prod-web-01:~$ tail -f /var/log/app.log             │
│  [ERROR] connection refused to db:5432                      │  ← 红色高亮（规则命中）
│  [INFO] retry attempt 1/3                                   │
│  ubuntu@prod-web-01:~$ █                                    │  ← 光标（闪烁）
│                                                             │
│                                         ↑滚动条（4px 宽）   │
└────────────────────────────────────────────────────────────┘
```

#### 终端字体与渲染

| 属性 | 默认值 | 可配置范围 |
|------|--------|-----------|
| 字体 | JetBrains Mono | 所有系统等宽字体 |
| 字号 | 13px | 8–32px |
| 行高 | 1.6 | 1.2–2.0 |
| 背景 | `bg-primary` | 任意颜色 |
| 前景 | `#e5e5e5` | ANSI 256色 |
| 光标 | 块状，白色，1.2s 闪烁 | 3 种形状 |
| 内边距 | 12px 上下，14px 左右 | 0–32px 各边独立 |

### 3.4 分屏布局示意

**左右分屏（2 窗格）：**
```
┌─────────────────┬─────────────────┐
│  PaneBar        │  PaneBar        │
├─────────────────┤─────────────────┤
│                 │                 │
│   Terminal 1    │ ← 4px 分割线 →  │  Terminal 2
│                 │   （可拖拽）     │
└─────────────────┴─────────────────┘
```

**四格分屏（4 窗格）：**
```
┌─────────────────┬─────────────────┐
│  PaneBar 1      │  PaneBar 2      │
├─────────────────┤─────────────────┤
│  Terminal 1     │  Terminal 2     │
├─────────────────┼─────────────────┤
│  PaneBar 3      │  PaneBar 4      │
├─────────────────┤─────────────────┤
│  Terminal 3     │  Terminal 4     │
└─────────────────┴─────────────────┘
```

分割线：4px 宽，`bg-tertiary`，鼠标悬停变 `accent`×30%，拖拽时高亮

### 3.5 快捷命令栏线框（可收起）

```
┌────────────────────────────────────────────────────────────┐
│  [服务重启] [查看日志] [磁盘空间] [CPU状态] [─────] [编辑]  │
└────────────────────────────────────────────────────────────┘
```

| 元素 | 规格 |
|------|------|
| 高度 | 40px |
| 命令按钮 | 圆角 4px，内边距 6px 12px，12px 字体，`bg-tertiary` |
| 命令按钮悬停 | `accent`×15%，边框 `accent` |
| 收起箭头 | 右侧，点击折叠为 4px 高度条 |
| 编辑按钮 | 最右侧，打开命令管理器 |

---

## 4. 界面 P03：主窗口 — SFTP 面板

### 4.1 线框结构

```
┌─────────────────────────────────┐
│  SFTP — prod-web-01        [×]  │  ← 面板标题栏（28px）
├─────────────────────────────────┤
│  [←][→][↑] /home/ubuntu/       │  ← 路径导航栏（32px）
│                           [刷新]│
├─────────────────────────────────┤
│  名称              大小  修改时间│  ← 列标题（可点击排序）
├─────────────────────────────────┤
│  📁 ..                          │
│  📁 node_modules  —    Mar 10   │
│  📁 src           —    Mar 14   │
│  ▶📄 server.js    14K  Mar 14   │  ← 选中行（蓝色背景）
│  📄 package.json  2K   Mar 10   │
│  📄 .env          0.5K Feb 28   │  ← 隐藏文件（灰色斜体，需开启显示）
│                                 │
│  （文件列表，可滚动）            │
├─────────────────────────────────┤
│  [↑上传] [↓下载] [📁新建] [⋯]  │  ← 操作工具栏（36px）
└─────────────────────────────────┘
│  传输队列（折叠态，点击展开）    │
└─────────────────────────────────┘
```

### 4.2 路径导航栏

| 元素 | 规格 |
|------|------|
| 返回按钮 `←` | 10px 箭头图标，无历史时灰色禁用 |
| 前进按钮 `→` | 同上 |
| 上级按钮 `↑` | 在根目录时禁用 |
| 路径文本 | 11px 等宽字体，可点击各级路径段（面包屑样式）|
| 直接编辑 | 单击路径文本变为可编辑输入框，`Enter` 跳转，`Esc` 取消 |
| 刷新按钮 | 右侧，旋转动画表示加载中 |

### 4.3 文件列表

#### 列布局

| 列 | 宽度 | 对齐 | 排序 |
|----|------|------|------|
| 名称 | flex-fill | 左对齐 | 默认，点击反转 |
| 大小 | 60px | 右对齐 | 点击切换 |
| 修改时间 | 80px | 右对齐 | 点击切换 |

#### 行高与图标

- 行高：32px
- 目录图标：📁 蓝色，12px
- 文件图标：按扩展名区分颜色（代码文件橙色，图片绿色，压缩包黄色，其余灰色）
- 选中行：背景 `accent`×12%，文字 `text-primary`
- 悬停行：背景 `bg-tertiary`
- 隐藏文件（以 `.` 开头）：`text-muted`，斜体（默认隐藏，`⌘⇧.` 切换显示）

#### 文件选择

| 操作 | 行为 |
|------|------|
| 单击 | 选中单行 |
| `⌘` + 单击 | 多选/取消选 |
| `⇧` + 单击 | 范围选择 |
| `⌘A` | 全选 |
| `Esc` | 清除选择 |

### 4.4 传输队列（展开态）

```
┌─────────────────────────────────┐
│  传输队列（2 进行中，1 已完成）  │  ← 折叠/展开标题行
├─────────────────────────────────┤
│  ↑ server.js       [========─]  │
│    14.2 KB/s  剩余 3s   [暂停]  │
├─────────────────────────────────┤
│  ↓ backup.tar.gz   [====────]   │
│    2.1 MB/s  剩余 28s   [取消]  │
├─────────────────────────────────┤
│  ✓ config.yml  完成  0.5KB  3s  │  ← 已完成行（可关闭）
└─────────────────────────────────┘
```

| 元素 | 规格 |
|------|------|
| 进度条 | 高 4px，圆角 2px，`accent` 前景，`bg-tertiary` 背景 |
| 文件名 | 12px，最多 140px，超出 ellipsis |
| 速度/剩余 | 10px，`text-muted` |
| 暂停/取消 | 10px 文字按钮，`text-secondary` |
| 完成行 | ✓ 绿色图标，`text-muted` 字体 |

---

## 5. 界面 P04：主窗口 — 状态栏

### 5.1 线框结构

```
┌──────────────────────────────────────────────────────────────────┐
│  🟢 已连接  │  SSH-2.0  │  AES256-CTR  │  ubuntu  │  UTF-8  │  22  │  2h 14m  │  ⌁ 11ms  │  120×38  │  ✓  │
└──────────────────────────────────────────────────────────────────┘
```

### 5.2 状态栏各区段

| 区段 | 宽度 | 内容 | 可交互 |
|------|------|------|--------|
| 连接状态点+文字 | 80px | `🟢 已连接` / `🟡 重连中` / `⚪ 已断开` | 点击尝试重连（断连时） |
| 分隔线 | 1px `border` | — | — |
| 协议版本 | 56px | `SSH-2.0` | — |
| 加密算法 | 88px | `AES256-CTR`（缩写） | 悬停展示完整套件 Tooltip |
| 用户名 | 72px | `ubuntu` | — |
| 编码 | 48px | `UTF-8` | 点击展示下拉，切换会话编码 |
| 端口 | 32px | `22` | — |
| 连接时长 | 56px | `2h 14m` | 悬停展示精确时间戳 |
| flex-spacer | — | — | — |
| 网络延迟 | 56px | `⌁ 11ms` | 每 30 秒更新；悬停展示历史曲线 Tooltip |
| 终端尺寸 | 56px | `120×38` | 调整窗口时实时更新 |
| iCloud 状态 | 20px | `✓` / `🔄` / `⚠` | 悬停展示同步详情；`⚠` 时点击查看错误 |

### 5.3 状态栏交互 Tooltip 示例

**加密算法 Tooltip：**
```
┌─────────────────────────────────┐
│  密钥交换：curve25519-sha256    │
│  主机密钥：ssh-ed25519          │
│  对称加密：aes256-ctr           │
│  MAC：hmac-sha2-256-etm        │
└─────────────────────────────────┘
```

**iCloud 同步错误 Tooltip：**
```
┌─────────────────────────────────┐
│  ⚠  上次同步失败                │
│  原因：iCloud 配额不足           │
│  上次成功：10 分钟前             │
│  [重试同步]                      │
└─────────────────────────────────┘
```

---

## 6. 弹窗 D01：新建 / 编辑会话

### 6.1 弹窗规格

| 属性 | 值 |
|------|---|
| 类型 | Sheet（从主窗口顶部滑出） |
| 宽度 | 540px |
| 高度 | 自适应（最小 480px） |
| 圆角 | 12px |

### 6.2 弹窗结构（分 Tab 标签页）

```
┌─────────────────────────────────────────────────────┐
│  新建 SSH 会话                                  [×]  │  ← 标题行
├─────────────────────────────────────────────────────┤
│  [基本连接]  [认证]  [高级]  [外观]                  │  ← Tab 导航（4个）
├─────────────────────────────────────────────────────┤
│                                                     │
│  会话名称（可选）                                   │
│  ┌─────────────────────────────────────────────┐   │
│  │ prod-web-01                                 │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  主机地址 *                          端口 *         │
│  ┌───────────────────────────┐  ┌─────────────┐   │
│  │ 192.168.1.100             │  │ 22          │   │
│  └───────────────────────────┘  └─────────────┘   │
│                                                     │
│  用户名 *                           分组            │
│  ┌───────────────────────────┐  ┌─────────────┐   │
│  │ ubuntu                    │  │ 生产环境 ▼  │   │
│  └───────────────────────────┘  └─────────────┘   │
│                                                     │
│  备注                                               │
│  ┌─────────────────────────────────────────────┐   │
│  │ 主要 Web 服务器                              │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
├─────────────────────────────────────────────────────┤
│  [取消]        [测试连接]        [保存并连接]        │
└─────────────────────────────────────────────────────┘
```

### 6.3 Tab — 认证页

```
├─────────────────────────────────────────────────────┤
│                                                     │
│  认证方式                                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │  🔑      │  │  🔒      │  │  🤝      │         │
│  │ SSH Key  │  │  密码    │  │  Agent   │         │
│  └──────────┘  └──────────┘  └──────────┘         │
│     ▲（选中，蓝色边框）                             │
│                                                     │
│  私钥文件路径                                       │
│  ┌─────────────────────────────┐  [选择文件]       │
│  │ ~/.ssh/id_ed25519            │                   │
│  └─────────────────────────────┘                   │
│                                                     │
│  ☐ 私钥有 Passphrase（存入 Keychain）              │
│                                                     │
│  [生成新密钥对…]                                    │
│                                                     │
│  跳板机（可选）                          [+ 添加跳板机]
│  ┌─────────────────────────────────────────────┐   │
│  │  本机  →  bastion-us  →  prod-web-01        │   │
│  │          [编辑]  [删除]                      │   │
│  └─────────────────────────────────────────────┘   │
```

### 6.4 Tab — 高级页

```
├─────────────────────────────────────────────────────┤
│                                                     │
│  连接                                               │
│  连接超时    [15] 秒                                 │
│  Keepalive   [30] 秒（0 = 关闭）                   │
│  自动重连    [✅] 最多 [3] 次                       │
│  字符编码    [UTF-8 ▼]                              │
│                                                     │
│  代理                                               │
│  ☐ 使用代理    [SOCKS5 ▼]                          │
│  代理地址   [127.0.0.1]  端口  [1080]               │
│                                                     │
│  端口转发（关联规则）                               │
│  [+ 添加转发规则]                                   │
│  本地 8080 → db.internal:3306   [✅自动启动] [删除] │
```

### 6.5 表单验证状态

| 字段 | 必填 | 空值提示 | 格式错误提示 |
|------|------|---------|------------|
| 主机地址 | ✅ | 「请输入主机地址」 | 「无效的 IP 或域名格式」 |
| 端口 | ✅ | 「请输入端口」 | 「端口范围为 1–65535」 |
| 用户名 | ✅ | 「请输入用户名」 | — |

**「测试连接」按钮状态：**
- 点击后按钮变 Loading 态（圆形旋转），文字「连接中…」
- 成功：绿色勾 `✓ 连接成功`，停留 2s 后恢复
- 失败：红色叹号 `✗ 连接失败：{原因}`，旁边显示错误详情

---

## 7. 弹窗 D02：首次连接指纹确认

### 7.1 弹窗规格

| 属性 | 值 |
|------|---|
| 类型 | Alert（模态） |
| 宽度 | 420px |
| 触发时机 | 首次连接未知主机，或 Known Hosts 中无记录 |

### 7.2 线框结构

```
┌────────────────────────────────────────────┐
│  ⚠  验证服务器身份                         │
├────────────────────────────────────────────┤
│                                            │
│  您正在连接到：                             │
│  prod-web-01 (10.0.1.12:22)               │
│                                            │
│  服务器指纹（ED25519 / SHA256）：           │
│  ┌────────────────────────────────────┐   │
│  │ SHA256:xU7mDPD3K29BPLs1Tc…ABCD     │   │
│  └────────────────────────────────────┘   │
│                                            │
│  指纹可视化：                               │
│  ┌──────┐                                  │
│  │ 随机  │  每台服务器的指纹艺术图唯一       │
│  │ 艺术  │  可与管理员核对此图案            │
│  │ 图案  │                                  │
│  └──────┘                                  │
│                                            │
│  ⚠ 请在继续之前向服务器管理员核实此指纹     │
│                                            │
├────────────────────────────────────────────┤
│  [拒绝]   [仅本次接受]   [接受并记住]       │
└────────────────────────────────────────────┘
```

### 7.3 按钮行为

| 按钮 | 行为 |
|------|------|
| 拒绝 | 中止连接，关闭弹窗，返回会话列表 |
| 仅本次接受 | 继续连接，本次会话有效，不写入 Known Hosts |
| 接受并记住 | 继续连接，写入 Known Hosts，下次免确认 |

---

## 8. 弹窗 D03：主机密钥变更警告

### 8.1 弹窗规格

| 属性 | 值 |
|------|---|
| 类型 | Alert（强制模态，无法通过 Esc 关闭） |
| 边框 | 2px `status-red` 高亮边框 |
| 优先级 | P0，必须用户主动确认 |

### 8.2 线框结构

```
┌────────────────────────────────────────────┐
│  🔴  安全警告：服务器指纹已更改            │
├────────────────────────────────────────────┤
│                                            │
│  prod-web-01 (10.0.1.12) 的主机密钥        │
│  与之前记录的不匹配！                       │
│                                            │
│  这可能意味着：                             │
│  · 服务器重新安装了操作系统                  │
│  · 有人正在进行中间人攻击（MITM）           │
│                                            │
│  已记录的指纹：                             │
│  SHA256:oldFingerprint…ABCD                │
│                                            │
│  当前服务器指纹：                           │
│  SHA256:newFingerprint…EFGH                │
│                                            │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━    │
│  建议：请先通过其他渠道（电话/邮件）向      │
│  管理员确认服务器密钥是否已更换。            │
│                                            │
├────────────────────────────────────────────┤
│  [取消连接]              [更新并继续连接]   │
└────────────────────────────────────────────┘
```

---

## 9. 弹窗 D04：端口转发管理器

### 9.1 弹窗规格

| 属性 | 值 |
|------|---|
| 类型 | Panel（浮动，可拖拽） |
| 宽度 | 640px |
| 高度 | 480px |
| 快捷键 | `⌘⇧U` |

### 9.2 线框结构

```
┌──────────────────────────────────────────────────────────┐
│  隧道管理器                                          [×]  │
├──────────────────────────────────────────────────────────┤
│  [+ 新建规则]              [全部启动]  [全部停止]         │
├────┬──────┬──────────────┬──────────────┬──────────┬────┤
│状态│类型  │本地            │远端目标       │绑定会话   │操作│
├────┼──────┼──────────────┼──────────────┼──────────┼────┤
│ 🟢 │本地  │127.0.0.1:8080│db.int:3306   │prod-web-01│⏸🗑│
│ ⚪ │远程  │server:9000   │localhost:3000│dev-app    │▶🗑│
│ 🟢 │SOCKS │127.0.0.1:1080│—             │bastion-us │⏸🗑│
└────┴──────┴──────────────┴──────────────┴──────────┴────┘
│                                                          │
│  选中规则后，下方展示详情（可编辑）：                     │
│                                                          │
│  类型：  [本地转发 ▼]                                   │
│  本地：  [127.0.0.1]  端口：[8080]                      │
│  远端：  [db.internal]  端口：[3306]                    │
│  绑定会话：[prod-web-01 ▼]                              │
│  ☑ 会话连接时自动启动                                   │
│  备注：  [访问生产数据库]                                │
│                                    [取消]  [保存]        │
└──────────────────────────────────────────────────────────┘
```

---

## 10. 弹窗 D05：快捷命令管理器

### 10.1 弹窗规格

| 属性 | 值 |
|------|---|
| 类型 | Panel（浮动） |
| 宽度 | 560px |
| 高度 | 460px |
| 快捷键 | `⌘⇧K` |

### 10.2 线框结构

```
┌──────────────────────────────────────────────────────┐
│  快捷命令管理器                                  [×]  │
├──────────────────────────────────────────────────────┤
│  命令集：[默认命令集 ▼]       [新建命令集] [删除命令集]│
├──────────────────────────────────────────────────────┤
│  命令列表（左）              │  编辑面板（右）         │
│  ─────────────────────────  │  ─────────────────     │
│  [查看系统状态]              │  名称                  │
│  [重启 Nginx]               │  ┌────────────────┐    │
│  [清理日志]                  │  │查看系统状态     │    │
│  [检查端口]                  │  └────────────────┘    │
│  [─────────]                │                        │
│  [+ 新建命令]                │  命令内容（支持多行）   │
│                              │  ┌────────────────┐    │
│                              │  │df -h           │    │
│                              │  │free -m         │    │
│                              │  │uptime          │    │
│                              │  └────────────────┘    │
│                              │                        │
│                              │  ☑ 末尾追加回车         │
│                              │                        │
│                              │  可选快捷键             │
│                              │  ┌────────────────┐    │
│                              │  │ ⌘⇧1           │    │
│                              │  └────────────────┘    │
│                              │            [保存]      │
└──────────────────────────────────────────────────────┘
```

---

## 11. 面板 S01：关键词高亮设置

### 11.1 面板位置

系统偏好 → Settings → Highlight（或通过会话右键 → 高亮规则进入）

### 11.2 线框结构

```
┌──────────────────────────────────────────────────────────┐
│  ← 返回   关键词高亮                                     │
├──────────────────────────────────────────────────────────┤
│  规则集：                                                 │
│  ● 系统错误（全局）               [编辑] [复制] [删除]   │
│    ERROR  FATAL  CRITICAL ···                            │
│  ● 自定义规则集                   [编辑] [复制] [删除]   │
│    nginx  upstream ···                                   │
│                                                          │
│  [+ 新建规则集]  [导入]  [导出]                          │
├──────────────────────────────────────────────────────────┤
│  规则集详情：系统错误                                     │
├──────────────────────────────────────────────────────────┤
│  关键词        匹配模式    前景色   背景色   样式  通知   │
│  ─────────────────────────────────────────────────────   │
│  ERROR         不敏感      🔴白    🔴红背   B     ✅     │
│  FATAL         不敏感      🔴白    🔴红背   B     ✅     │
│  WARNING       不敏感      🟡黑    🟡黄背   —     ☐     │
│  ^Traceback    正则        🔴白    透明     BI    ✅     │
│                                                          │
│  [+ 添加规则]                                            │
└──────────────────────────────────────────────────────────┘
```

### 11.3 添加规则行（内联展开）

```
  ┌──────────────────────────────────────────────────────┐
  │  关键词：[_______________]  ☐ 正则  ☐ 大小写敏感     │
  │  前景色：[████ 选色]  背景色：[████ 无 ▼]           │
  │  样式：  ☐ 加粗  ☐ 斜体  ☐ 下划线                  │
  │  ☐ 匹配时推送通知  ☐ 匹配时播放声音                  │
  │                                       [取消] [添加]  │
  └──────────────────────────────────────────────────────┘
```

---

## 12. 面板 S02：外观与个性化设置

### 12.1 线框结构

```
┌──────────────────────────────────────────────────────────┐
│  ← 返回   外观                                           │
├──────────────────────────────────────────────────────────┤
│  颜色主题                                                 │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐│
│  │深色▶ │ │浅色  │ │Sol.D │ │Drcu  │ │Nord  │ │+自定义││
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘│
│  （每个卡片为 64×48px 终端预览缩略图）                   │
│                                                          │
│  [导入 .itermcolors]  [导出主题]                         │
├──────────────────────────────────────────────────────────┤
│  字体                                                     │
│  字体族：  [JetBrains Mono ▼]                            │
│  字号：    ━━━━●━━━━━  13px    [A+] [A-]                │
│  行间距：  ━●━━━━━━━━  1.4                              │
│  ☑ 启用字体连字                                          │
│  非 ASCII 字体：[冬青黑体 ▼]（中文等宽）                │
├──────────────────────────────────────────────────────────┤
│  光标                                                     │
│  形状：  (●) 块状  ( ) I 形  ( ) 下划线                 │
│  ☑ 光标闪烁  ☐ 仅空闲时闪烁                             │
│  光标颜色：[████ 选色]                                   │
├──────────────────────────────────────────────────────────┤
│  窗口                                                     │
│  背景透明度：━━━━━●━━━━  0%      ━━━━━━━━━━  100%       │
│  ☐ 背景模糊（Vibrancy）                                  │
│  内边距：[12] 上  [14] 右  [12] 下  [14] 左              │
└──────────────────────────────────────────────────────────┘
```

### 12.2 终端预览区

在设置面板右侧或底部，实时预览终端效果：

```
┌────────────────────────────────────┐
│  实时预览                          │
│  ───────────────────────────────  │
│  ubuntu@server:~$ ls -la           │
│  total 48                          │
│  drwxr-xr-x  5 ubuntu ubuntu ...  │
│  -rw-r--r--  1 ubuntu ubuntu ...  │
│  ubuntu@server:~$ █                │
└────────────────────────────────────┘
```

---

## 13. 面板 S03：安全与凭据管理

### 13.1 线框结构

```
┌──────────────────────────────────────────────────────────┐
│  ← 返回   安全                                           │
├──────────────────────────────────────────────────────────┤
│  主密码                                                   │
│  ☐ 启用主密码（应用启动时要求验证）                       │
│  自动锁定：[不锁定 ▼]                                    │
│  [修改主密码…]                                           │
├──────────────────────────────────────────────────────────┤
│  Known Hosts（已信任的服务器指纹）                        │
│  ─────────────────────────────────────────────────────   │
│  主机名/IP          密钥类型    添加日期        操作       │
│  prod-web-01        ED25519     2026-03-10      [删除]    │
│  10.0.1.12          RSA         2026-01-05      [删除]    │
│  bastion-us         ED25519     2026-02-20  ⚠  [更新][删除]│
│                                                          │
│  ⚠ 表示远端指纹与已记录不符，请核实                      │
│  [全部清除]                                              │
├──────────────────────────────────────────────────────────┤
│  SSH 密钥管理                                             │
│  ─────────────────────────────────────────────────────   │
│  已存储私钥：                                            │
│  id_ed25519         Ed25519   用于 3 个会话   [查看][删除]│
│  id_rsa_prod        RSA-4096  用于 1 个会话   [查看][删除]│
│                                                          │
│  [生成新密钥对…]  [导入私钥…]                            │
└──────────────────────────────────────────────────────────┘
```

---

## 14. 覆层 O01：终端内搜索

### 14.1 规格

| 属性 | 值 |
|------|---|
| 位置 | 终端区域右上角，浮层 |
| 宽度 | 320px |
| 高度 | 40px |
| 触发 | `⌘F`，`Esc` 关闭 |
| 动画 | 从右上角淡入下滑，150ms |

### 14.2 线框

```
                                          ┌──────────────────────────────────────────┐
                                          │  [🔍] [nginx error___] [Aa] [.*] [↑][↓] [×] │
                                          │                        0 / 0 匹配         │
                                          └──────────────────────────────────────────┘
```

| 元素 | 功能 |
|------|------|
| 输入框 | 实时搜索，高亮所有匹配项（黄色背景） |
| `Aa` 按钮 | 大小写敏感开关，激活时 `accent` 填色 |
| `.*` 按钮 | 正则模式开关 |
| `↑` / `↓` | 跳转上/下一个匹配，当前匹配项橙色高亮 |
| `×` | 关闭，清除高亮 |
| 匹配计数 | `当前/总数`，无匹配时红色 `0 / 0` |

---

## 15. 覆层 O02：Compose Pane（命令编辑区）

### 15.1 规格

| 属性 | 值 |
|------|---|
| 位置 | 终端区域底部，可停靠 |
| 高度 | 80–200px（可拖拽调整） |
| 触发 | View → Compose Pane / 工具栏按钮 |

### 15.2 线框

```
┌──────────────────────────────────────────────────────────┐ ← 4px 拖拽调整线
│  1  sudo systemctl restart nginx                         │
│  2  echo "Restart complete at $(date)"                   │
│  3  systemctl status nginx | head -5                     │
│                                                          │
│  [发送 ⌃↵]  [逐行模式]  延迟 [50]ms  [🔠 bash ▼]        │
└──────────────────────────────────────────────────────────┘
```

| 元素 | 功能 |
|------|------|
| 编辑区 | 多行文本，等宽字体，行号显示，语法高亮 |
| 发送按钮 | `⌃Return` 快捷键，整体一次发送 |
| 逐行模式 | 开启后多行内容逐行发送，每行间隔 `延迟` ms |
| 语言下拉 | `bash` / `python` / `sh`，影响语法高亮 |

---

## 16. 覆层 O03：同步输入模式

### 16.1 触发与关闭

- 开启：工具栏「同步」按钮 → 勾选目标会话 → 确认
- 关闭：再次点击「同步」按钮 / 关闭所有参与标签

### 16.2 开启确认弹窗

```
┌─────────────────────────────────────────────┐
│  启用同步输入                                │
├─────────────────────────────────────────────┤
│  以下会话将同步接收您的输入：               │
│                                             │
│  ☑  🟢 prod-web-01                         │
│  ☑  🟢 prod-web-02                         │
│  ☐  ⚪ dev-app（未连接，不可用）            │
│                                             │
│  ⚠  此操作将在多台服务器上同时执行命令，    │
│     请谨慎操作。                            │
│                                             │
│             [取消]  [确认开启同步]           │
└─────────────────────────────────────────────┘
```

### 16.3 同步模式视觉提示

- 所有参与窗格/标签的边框：**2px 橙色连续边框**（`#e5a50a`）
- 标签栏标签名前添加 `⚡` 图标
- 工具栏「同步」按钮保持激活态（`accent` 填色）
- 状态栏左侧追加 `⚡ 同步输入 (2)` 提示

---

## 17. 状态集合

### 17.1 会话连接状态

| 状态 | 状态点 | 标签页指示 | 侧边栏文字颜色 | 可执行操作 |
|------|--------|-----------|-------------|-----------|
| 未连接 | ⚪ 灰色 6px | ⚪ | `text-muted` | 双击连接、编辑、删除 |
| 连接中 | 🟡 黄色脉冲 | 🟡 | `text-secondary` | 取消连接 |
| 已连接 | 🟢 绿色 + 外发光 | 🟢 | `text-primary` | 断开、分屏、SFTP 等 |
| 重连中 | 🟡 黄色旋转 | 🟡 | `text-secondary` | 取消重连 |
| 连接失败 | 🔴 红色 | 🔴 | `status-red` | 重试、查看错误 |
| 已断开 | ⚪ 灰色 | ⚪ | `text-secondary` | 重连、编辑 |

### 17.2 SFTP 传输状态

| 状态 | 视觉 | 操作 |
|------|------|------|
| 等待中 | 进度条灰色空心 | 取消 |
| 传输中 | 进度条动画（`accent`） | 暂停、取消 |
| 已暂停 | 进度条静止（`accent`×50%） | 恢复、取消 |
| 已完成 | ✓ 绿色，进度条 100% | 关闭记录 |
| 失败 | ✗ 红色，进度条 `status-red` | 重试、关闭 |

### 17.3 iCloud 同步状态

| 状态 | 状态栏图标 | 颜色 | 悬停说明 |
|------|-----------|------|---------|
| 已同步 | ✓ | `status-green` | `上次同步：刚刚` |
| 同步中 | 旋转圆圈 🔄 | `accent` | `正在同步…` |
| 等待同步 | ⏳ | `text-muted` | `队列中，等待网络` |
| 同步失败 | ⚠ | `status-red` | 点击查看错误详情 |
| 已关闭 | — | — | 不显示 |

---

## 18. 组件库摘要

### 18.1 基础按钮变体

| 变体 | 外观描述 | 用途 |
|------|---------|------|
| Primary | `accent` 背景，白色文字，圆角 6px | 主要操作（保存、连接） |
| Secondary | `bg-tertiary` 背景，`border` 边框，`text-primary` | 次要操作（取消、返回） |
| Danger | `status-red`×15% 背景，`status-red` 文字 | 破坏性操作（删除） |
| Ghost | 透明背景，悬停 `bg-tertiary` | 工具栏图标按钮 |
| Link | 无背景，`accent` 文字，悬停下划线 | 文字链接 |

### 18.2 输入框变体

| 变体 | 描述 |
|------|------|
| 默认 | `bg-tertiary` 背景，`border` 边框，聚焦时 `accent` 边框 |
| 错误 | `status-red`×10% 背景，`status-red` 边框，右侧红色叹号图标 |
| 禁用 | `bg-secondary` 背景，`text-muted` 文字，不可点击 |
| 只读 | 同禁用，但允许文字选择 |

### 18.3 状态点（StatusDot）规格

```
直径：6px
已连接：fill #2ec27e，box-shadow 0 0 4px #2ec27e（发光效果）
连接中：fill #e5a50a，CSS animation pulse 1s infinite
未连接：fill #6b6a74（text-muted），无光晕
失败：fill #e01b24
```

### 18.4 分隔线

```
水平分隔线：border-top: 1px solid var(--border)，margin: 8px 0
垂直分隔线（状态栏）：border-left: 1px solid var(--border)，height: 14px，margin: 0 8px
```

### 18.5 Tooltip 规格

```
背景：bg-elevated，边框：1px border，圆角：6px，内边距：6px 10px
字号：11px，text-primary
出现延迟：悬停 500ms 后显示
消失：鼠标移出立即消失
最大宽度：280px，超出自动换行
```

---

## 附录 A：界面跳转关系图

```
主窗口（侧边栏 + 终端 + SFTP）
    │
    ├── 双击会话 ──────────────────→ 建立连接，终端就绪
    │
    ├── [+ 新建会话] ──────────────→ 弹窗 D01（新建/编辑会话）
    │       └── 测试连接 ──────────→ 弹窗 D02（首次指纹） 或 弹窗 D03（密钥变更）
    │
    ├── [⌘⇧T] / 工具栏隧道 ────────→ 弹窗 D04（端口转发管理器）
    ├── [⌘⇧K] / 工具栏命令 ────────→ 弹窗 D05（快捷命令管理器）
    │
    ├── [⌘,] 设置 ─────────────────→ 设置面板
    │       ├── 安全 ───────────────→ 面板 S03（安全与凭据）
    │       ├── 高亮 ───────────────→ 面板 S01（关键词高亮）
    │       └── 外观 ───────────────→ 面板 S02（外观设置）
    │
    ├── [⌘F]（终端聚焦时） ─────────→ 覆层 O01（终端内搜索）
    ├── View → Compose Pane ────────→ 覆层 O02（命令编辑区）
    └── 工具栏「同步」──────────────→ 覆层 O03（同步输入确认）
```

---

## 附录 B：文档修订历史

| 版本 | 日期 | 修订人 | 说明 |
|------|------|--------|------|
| v1.0-draft | 2026-03-18 | 产品团队 | 初版原型草稿，基于 PRD v1.0 生成 |

---

*本文档为原型草稿，供视觉设计师进行视觉稿设计，供前端工程师评估实现复杂度。*
*所有尺寸单位为逻辑像素（@1x），视网膜屏按 @2x 输出资源。*
*终稿前需与设计师确认所有 ASCII 线框的布局意图。*
